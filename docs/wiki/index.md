<!-- docs/wiki/index.md — auto-generated by build-wiki-index.py. Do not hand-edit. -->

# soul-protocol — Wiki Index

_328 articles · indexed at `ba7a807cd7dc` · generated 2026-04-24T03:18:33+00:00_

Auto-generated from each article's YAML frontmatter. See [README.md](./README.md) for the update workflow.

## By Category (308 categories)

### `API contract` — 1 article(s)

- [Test Suite: JSON Schema Generation from Pydantic Models](./test-suite-json-schema-generation-from-pydantic-models.md) — Verifies that every Soul Protocol Pydantic model can produce a valid, JSON-serializable schema, that a sample `SoulConfig` dict validates against the generated schema, and that the `generate_schemas.py` script produces the expected on-disk files. This acts as a contract test for external consumers who depend on the published JSON schemas.

### `API surface` — 1 article(s)

- [DNA Subpackage Public API (Re-export Module)](./dna-subpackage-public-api-re-export-module.md) — The `dna` subpackage `__init__.py` re-exports the two public functions from `dna/prompt.py` — `dna_to_system_prompt` and `dna_to_markdown` — providing a clean, stable import surface for consumers. This thin shim was fixed during a runtime restructure to use absolute import paths rooted at `soul_protocol.runtime`.

### `CLI` — 10 article(s)

- [CLI Test Suite: Agent Platform Integration Setup (`soul setup`)](./cli-test-suite-agent-platform-integration-setup-soul-setup.md) — Test suite for `soul setup`, which wires a Soul Protocol MCP server into AI coding platforms (Cursor, VS Code, Claude Code, Continue, Windsurf). Covers platform auto-detection, MCP config file writing and merging, instruction file injection, `.gitignore` updates, and `uvx` path resolution.
- [CLI Test Suite: Core Commands (birth, inspect, status, remember, recall, init)](./cli-test-suite-core-commands-birth-inspect-status-remember-recall-init.md) — Comprehensive test suite for the Soul Protocol CLI covering the full lifecycle of a soul: creation, inspection, memory storage across tiers, multi-format output, and init options. Tests use click.testing.CliRunner to invoke commands in isolation and assert on exit codes, output text, and ZIP archive contents.
- [CLI Test Suite: Full Onboarding, Multi-User Lifecycle, and Destroy Workflow](./cli-test-suite-full-onboarding-multi-user-lifecycle-and-destroy-workflow.md) — End-to-end test suite for the `soul init` onboarding wizard covering user scope creation, fleet provisioning, status reporting, soul deletion, and the destroy command that archives then wipes all data. Also validates journal append-only privacy constraints and users-dir precedence rules.
- [CLI Test Suite: Health, Cleanup, and Repair Commands](./cli-test-suite-health-cleanup-and-repair-commands.md) — Test suite for three soul maintenance CLI commands: `soul health` (diagnostic reporting), `soul cleanup` (duplicate/low-importance memory removal), and `soul repair` (energy reset). Covers dry-run safety guards, auto-mode mutations, and persistence verification.
- [CLI Test Suite: Org Initialization (`soul org init`)](./cli-test-suite-org-initialization-soul-org-init.md) — Test suite for `soul org init`, which bootstraps an organization by creating a root soul, a journal, and cryptographic keys. Covers happy-path artifact creation, idempotency guards, force-overwrite behavior, key file permissions, journal privacy, and round-trip loadability.
- [CLI Test Suite: Soul Context Injection into AI IDE Config Files](./cli-test-suite-soul-context-injection-into-ai-ide-config-files.md) — Test suite for `soul inject`, which writes a soul's identity and recent memories into AI IDE configuration files (Claude Code, Cursor, VS Code, Windsurf, Cline, Continue). Covers target path resolution, idempotent section replacement, context block formatting, memory truncation, and auto-discovery of soul directories.
- [Interactive CLI Agent Demonstrating the Full Soul Lifecycle](./interactive-cli-agent-demonstrating-the-full-soul-lifecycle.md) — A runnable conversational agent that exercises the complete Soul Protocol lifecycle — birth, observe, recall, reflect, and save — with pluggable LLM backends (Claude, OpenAI, Ollama, and a no-LLM heuristic mode). It uses a dual-model architecture where a cheaper model handles soul cognition and a stronger model handles user-facing chat, demonstrating the cost-efficiency pattern Soul Protocol enables.
- [Research Package Entry Point (`python -m research`)](./research-package-entry-point-python-m-research.md) — A minimal `__main__.py` that enables the research package to be invoked directly with `python -m research`, delegating immediately to `research.run.main()`. This is the standard Python idiom for making a package executable without requiring the user to know the internal module structure.
- [Test Suite for Eternal Storage CLI Commands](./test-suite-for-eternal-storage-cli-commands.md) — Tests the soul-protocol CLI commands for eternal storage operations: archive (with optional tier selection), eternal-status (tier health check), and recover (with failure path handling). Uses Click's CliRunner for isolated, in-process command invocation.
- [Test Suite: Soul Configuration — OCEAN, Communication, Biorhythms, Persona, and Config Files](./test-suite-soul-configuration-ocean-communication-biorhythms-persona-and-config-.md) — Validates the flexible birth configuration system for `Soul`, covering direct-kwarg birth with OCEAN personality traits, communication style, biorhythms, and persona text, as well as file-based birth from YAML and JSON configs via `Soul.birth_from_config()`. The suite also covers CLI `--config` and `--openness/--neuroticism` flags and verifies that all settings survive export → awaken round-trips.

### `DID` — 1 article(s)

- [Identity Subpackage: DID Generation Interface](./identity-subpackage-did-generation-interface.md) — The `identity/__init__.py` init exposes `generate_did` as the single public entry point for the identity subsystem, making Decentralized Identifier (DID) creation available via `from soul_protocol.runtime.identity import generate_did` without exposing the underlying DID implementation details.

### `DNA` — 1 article(s)

- [Evolution Manager: DNA Mutation Lifecycle](./evolution-manager-dna-mutation-lifecycle.md) — `EvolutionManager` orchestrates the full lifecycle of soul personality mutations — proposal, approval/rejection, and application to DNA — across three modes: disabled, supervised, and autonomous. It is the core mechanism by which souls evolve over time based on interaction performance.

### `DSPy` — 4 article(s)

- [DSPy Cognitive Modules: Optimizable Memory Pipeline Components](./dspy-cognitive-modules-optimizable-memory-pipeline-components.md) — This module defines three DSPy-native modules — `SignificanceGate`, `QueryExpander`, and `FactExtractor` — that replace hand-written heuristics in soul-protocol's memory pipeline with learnable, optimizable LLM programs. All DSPy imports are lazy so the module loads safely in environments where DSPy is not installed.
- [DSPy Training Data Generation Subpackage](./dspy-training-data-generation-subpackage.md) — The `dspy_training/` subpackage provides training data generation for optimizing Soul Protocol's DSPy modules — the LLM-powered components used for memory extraction, significance scoring, and entity recognition. Created under the `feat/dspy-integration` branch, it is a nascent subpackage with the actual generation logic in sibling modules.
- [DSPyCognitiveProcessor: Learnable Memory Cognition via DSPy](./dspycognitiveprocessor-learnable-memory-cognition-via-dspy.md) — DSPyCognitiveProcessor is a drop-in replacement for `CognitiveProcessor` that routes significance assessment, query expansion, and fact extraction through optimizable DSPy modules instead of hand-written prompts. It bridges DSPy's synchronous execution model to soul-protocol's async architecture via a thread pool executor.
- [SoulOptimizer: MIPROv2 Training Harness for Cognitive Modules](./souloptimizer-miprov2-training-harness-for-cognitive-modules.md) — `SoulOptimizer` is an offline training harness that uses DSPy's MIPROv2 optimizer to tune `SignificanceGate` and `QueryExpander` against labeled interaction data derived from ablation scenarios or production logs. Optimized module weights are serialized to disk and loaded by `DSPyCognitiveProcessor` at runtime.

### `GDPR` — 3 article(s)

- [ProceduralStore: How-To Memory Storage and GDPR Deletion](./proceduralstore-how-to-memory-storage-and-gdpr-deletion.md) — Provides an in-memory store specifically for procedural memories — step-by-step how-tos and learned processes. Includes token-overlap relevance search and GDPR-targeted deletion operations added in v0.3.
- [SemanticStore: Fact-Based Memory with Confidence and Eviction](./semanticstore-fact-based-memory-with-confidence-and-eviction.md) — Stores semantic memories — extracted facts and general knowledge about the world and the user — with importance scoring, confidence tracking, supersession logic for updated facts, and a bounded capacity that evicts least-important entries when full. GDPR deletion APIs are provided for targeted and time-based removal.
- [Test Suite for GDPR-Compliant Memory Deletion](./test-suite-for-gdpr-compliant-memory-deletion.md) — Comprehensive tests for Soul Protocol's right-to-erasure implementation, covering query-based, entity-based, and time-based deletion across all memory tiers, with full audit trail verification. Ensures deleted memories become invisible to recall and that audit records never expose the deleted content itself.

### `IDE integration` — 1 article(s)

- [CLI Test Suite: Soul Context Injection into AI IDE Config Files](./cli-test-suite-soul-context-injection-into-ai-ide-config-files.md) — Test suite for `soul inject`, which writes a soul's identity and recent memories into AI IDE configuration files (Claude Code, Cursor, VS Code, Windsurf, Cline, Continue). Covers target path resolution, idempotent section replacement, context block formatting, memory truncation, and auto-discovery of soul directories.

### `JSON` — 1 article(s)

- [JSON Soul Configuration Parser (parse_soul_json)](./json-soul-configuration-parser-parsesouljson.md) — A single-function module that parses a JSON string into a validated SoulConfig using Pydantic's model_validate_json, combining deserialization and schema validation in one atomic step.

### `LCM` — 9 article(s)

- [Context Package Exports: LCMContext and SQLiteContextStore](./context-package-exports-lcmcontext-and-sqlitecontextstore.md) — The `runtime/context` package's `__init__.py` provides a clean public surface for the Lossless Context Management (LCM) subsystem, re-exporting `LCMContext` (the reference `ContextEngine` implementation) and `SQLiteContextStore` (the persistence layer) so consumers import from a single stable path rather than internal submodules.
- [Context Subpackage Interface: Lossless Context Management Spec Layer](./context-subpackage-interface-lossless-context-management-spec-layer.md) — The `spec/context/__init__.py` re-exports all Lossless Context Management (LCM) primitives — the `ContextEngine` protocol and seven Pydantic models — as the public interface for context window management. This subpackage is spec-only: no opinionated implementations, no runtime imports.
- [LCM Compaction Prompt Templates: SUMMARY and BULLETS](./lcm-compaction-prompt-templates-summary-and-bullets.md) — This module provides the two LLM prompt templates used by `ThreeLevelCompactor` when a `CognitiveEngine` is available: `SUMMARY_PROMPT` for Level 1 prose summarization and `BULLETS_PROMPT` for Level 2 bullet-point compression. Both follow the `[TASK:xxx]` routing convention for compatibility with `HeuristicEngine`.
- [LCM Data Models: Context Messages, DAG Nodes, and Compaction Levels](./lcm-data-models-context-messages-dag-nodes-and-compaction-levels.md) — The LCM data models define the atomic units of Lossless Context Management: `ContextMessage` (immutable ingested messages), `ContextNode` (compacted DAG nodes), `CompactionLevel` (verbatim to truncated), and result types for assembly, search, expansion, and metadata queries.
- [LCM Retrieval Tools: grep, expand, and describe](./lcm-retrieval-tools-grep-expand-and-describe.md) — This module provides three retrieval functions — `grep`, `expand`, and `describe` — that expose different views into the immutable LCM message store. `grep` searches by regex, `expand` recursively reconstructs original messages from compacted DAG nodes, and `describe` returns a metadata snapshot of the entire store.
- [LCMContext: Reference Lossless Context Management Engine](./lcmcontext-reference-lossless-context-management-engine.md) — `LCMContext` is soul-protocol's reference implementation of the `ContextEngine` protocol. It ingests conversation messages into an append-only SQLite store, assembles token-bounded context windows with automatic three-level compaction, and exposes grep/expand/describe retrieval tools — all without requiring a `Soul` instance.
- [SQLiteContextStore: Immutable Message Store and Compaction DAG](./sqlitecontextstore-immutable-message-store-and-compaction-dag.md) — `SQLiteContextStore` is the persistence layer for LCM, implementing an append-only SQLite database with three tables: immutable conversation messages, compaction DAG nodes, and DAG edges. It uses `asyncio.to_thread()` to make synchronous SQLite calls safe in async code without adding any external dependencies.
- [Test Suite for LCMContext (Long-Context Memory)](./test-suite-for-lcmcontext-long-context-memory.md) — Integration tests for `LCMContext`, the context engine that ingests conversation messages, assembles token-budgeted context windows, and auto-compacts history using a `CognitiveEngine`. Covers protocol compliance, initialization guards, token estimation, compaction triggers, grep search, and lifecycle close/reopen semantics.
- [ThreeLevelCompactor: Escalating Context Window Management](./threelevelcompactor-escalating-context-window-management.md) — `ThreeLevelCompactor` keeps the active context window within a token budget using three escalating strategies: LLM-generated prose summaries, LLM-generated bullet-point compressions, and deterministic head truncation. The compactor always applies the least aggressive level that achieves the budget, and guarantees convergence even without an LLM.

### `LLM` — 1 article(s)

- [Smart Memory Reranking via LLM (Prompt-Injection-Hardened)](./smart-memory-reranking-via-llm-prompt-injection-hardened.md) — Provides an optional second-pass reranking stage that uses a lightweight LLM call to select the most contextually relevant memories from a heuristically-ranked candidate pool. The implementation includes robust prompt injection defenses because memory content is untrusted user input that could manipulate the ranking decision.

### `LLM adapters` — 1 article(s)

- [Test Suite: Cognitive Adapter Layer -- CallableEngine, Auto-Detection, and Provider Integration](./test-suite-cognitive-adapter-layer-callableengine-auto-detection-and-provider-in.md) — Tests for the adapter layer that bridges Soul Protocol's `CognitiveEngine` protocol with real LLM providers (Anthropic, OpenAI, Ollama) and user-supplied callables. Covers callable wrapping, environment-variable-driven auto-detection, Soul factory integration, import safety for optional dependencies, and HTTP-level mocking for Ollama.

### `LLM integration` — 4 article(s)

- [DNA to System Prompt and Markdown Renderer](./dna-to-system-prompt-and-markdown-renderer.md) — Converts a soul's identity, DNA (personality blueprint), core memory, and current state into either an LLM system prompt or a human-readable markdown document. The system prompt renderer is the primary mechanism by which a soul's personality becomes an active influence on its LLM-driven behavior.
- [Test Suite: Cognitive Engine, Heuristic Routing, and LLM Fallback](./test-suite-cognitive-engine-heuristic-routing-and-llm-fallback.md) — Comprehensive test suite for the Soul Protocol cognitive processing layer, covering HeuristicEngine task routing, CognitiveProcessor LLM integration, JSON parsing helpers, and graceful fallback when LLMs fail or return garbage. Also includes regression tests for the heuristic-only mode bug.
- [Test Suite: Cognitive Prompt Template Formatting and JSON Parsability](./test-suite-cognitive-prompt-template-formatting-and-json-parsability.md) — Tests that every cognitive prompt template in Soul Protocol formats without error and produces output parseable by the JSON extraction helper. Validates both the template variables contract and the round-trip between typical LLM responses and the `_parse_json` function.
- [Test Suite: LLM-Powered Memory Reranking and Smart Recall](./test-suite-llm-powered-memory-reranking-and-smart-recall.md) — Covers `rerank_memories()`, the `_parse_indices()` helper, and `Soul.smart_recall()` — the optional LLM-driven reranking layer on top of heuristic memory retrieval. The suite validates correct ordering, fallback behavior on engine failure or timeout, opt-in flag semantics, and prompt injection defenses.

### `LLM token budget` — 1 article(s)

- [Test Suite: Context Compaction -- Three-Level Token Budget Enforcement](./test-suite-context-compaction-three-level-token-budget-enforcement.md) — Tests for Soul Protocol's context compaction system, which keeps conversation history within a token budget through three escalating strategies: LLM-generated summaries, bullet compression, and hard truncation. Covers zero-cost paths, summary/bullet/truncation tiers, idempotency, and guaranteed convergence.

### `MCP` — 5 article(s)

- [Soul Agent: Claude Agent SDK + Soul Protocol with 15 MCP Tools](./soul-agent-claude-agent-sdk-soul-protocol-with-15-mcp-tools.md) — Implements a full-featured conversational agent that combines the Claude Agent SDK's tool execution and streaming loop with Soul Protocol's v0.2.3 feature set, exposing 15 MCP tools covering memory, personality, emotion, bond tracking, skills, GDPR deletion, evolution proposals, core memory editing, reincarnation, and encrypted export. The module-level `_soul` singleton pattern mirrors the MCP server architecture for consistency.
- [Test Package Initializer for MCP Server Integration Tests](./test-package-initializer-for-mcp-server-integration-tests.md) — The `tests/test_mcp/__init__.py` file marks the `test_mcp` directory as a Python package, enabling pytest to discover and organize MCP server integration tests as a cohesive module. It contains no executable code — its presence is structural.
- [Test Suite for MCP Psychology Tools (Skills, Evaluate, Learn, Evolve)](./test-suite-for-mcp-psychology-tools-skills-evaluate-learn-evolve.md) — Validates the higher-order MCP tools that expose soul psychology — skill tracking, rubric-based evaluation, learning events, and the propose/approve/reject evolution workflow. Tests use FastMCP's in-process client to exercise the full MCP tool call path without a network round-trip.
- [Test Suite for MCP Sampling Engine (LLM Think with Heuristic Fallback)](./test-suite-for-mcp-sampling-engine-llm-think-with-heuristic-fallback.md) — Validates the MCPSamplingEngine, which wraps the MCP context's sampling capability to give souls LLM-powered reflection, with graceful fallback to the HeuristicEngine when the MCP context is unavailable or raises. Tests cover the success path, three failure scenarios, the no-context path, and the server's engine wiring lifecycle.
- [Test Suite for MCP Server Core Tools (Birth, Observe, Memory, State, Lifecycle)](./test-suite-for-mcp-server-core-tools-birth-observe-memory-state-lifecycle.md) — Integration tests for the core soul_protocol MCP server tools, covering soul lifecycle (birth, save, export), memory operations (remember, recall), emotional state (feel), observation, reflection, and multi-soul management (list, switch). Uses a registry reset helper and in-process FastMCP client to achieve full isolation between tests.

### `MCP integration` — 2 article(s)

- [CLI Test Suite: Agent Platform Integration Setup (`soul setup`)](./cli-test-suite-agent-platform-integration-setup-soul-setup.md) — Test suite for `soul setup`, which wires a Soul Protocol MCP server into AI coding platforms (Cursor, VS Code, Claude Code, Continue, Windsurf). Covers platform auto-detection, MCP config file writing and merging, instruction file injection, `.gitignore` updates, and `uvx` path resolution.
- [MCPSamplingEngine: Host-LLM Cognitive Routing via MCP](./mcpsamplingengine-host-llm-cognitive-routing-via-mcp.md) — MCPSamplingEngine routes cognitive prompts to the host LLM (Claude, GPT-4, etc.) through the Model Context Protocol sampling API, so soul-protocol tools embedded in Claude Code or Claude Desktop can leverage the host model without needing a separate API key. It falls back to HeuristicEngine whenever the MCP client does not support sampling or when called outside an MCP context.

### `ML-optimization` — 1 article(s)

- [DSPy Training Data Generation Subpackage](./dspy-training-data-generation-subpackage.md) — The `dspy_training/` subpackage provides training data generation for optimizing Soul Protocol's DSPy modules — the LLM-powered components used for memory extraction, significance scoring, and entity recognition. Created under the `feat/dspy-integration` branch, it is a nascent subpackage with the actual generation logic in sibling modules.

### `NLP` — 2 article(s)

- [Heuristic Sentiment Detection for Somatic Memory Markers](./heuristic-sentiment-detection-for-somatic-memory-markers.md) — Provides zero-dependency emotion detection using curated word lists and a valence-arousal coordinate model, returning a SomaticMarker that the memory pipeline attaches to entries for emotional recall prioritization. Implements Damasio's Somatic Marker Hypothesis without requiring an LLM call.
- [TF-IDF Embedder: Vocabulary-Fitted Semantic Embeddings](./tf-idf-embedder-vocabulary-fitted-semantic-embeddings.md) — The `TFIDFEmbedder` builds corpus-fitted TF-IDF vectors using only Python's standard library — no external dependencies. It produces semantically meaningful similarity (unlike `HashEmbedder`) but requires a `fit()` call on a corpus before embedding, making it useful for lightweight production use cases where a known document set exists.

### `OCEAN` — 1 article(s)

- [Personality-Modulated Memory Retrieval (OCEAN Trait Scoring)](./personality-modulated-memory-retrieval-ocean-trait-scoring.md) — Maps each of the five OCEAN personality traits to a memory-scoring signal so that a soul's recall results reflect its character rather than pure relevance rank. The module injects per-trait weight deltas into the activation pipeline, remaining fully backwards-compatible when all traits sit at the default 0.5 midpoint.

### `OpenAI` — 2 article(s)

- [OpenAI Embedding Provider with Retry Backoff](./openai-embedding-provider-with-retry-backoff.md) — The `OpenAIEmbeddingProvider` produces high-quality semantic embeddings via the OpenAI API, with built-in exponential backoff for rate limits and transient server errors. It lazily initializes the OpenAI client, pre-knows dimensions for standard models to avoid a probe call, and guarantees input ordering even when the API returns results out of order.
- [OpenAIEngine: OpenAI and OpenAI-Compatible Cognitive Adapter](./openaiengine-openai-and-openai-compatible-cognitive-adapter.md) — OpenAIEngine connects soul-protocol's cognitive pipeline to OpenAI's chat completions API, or any OpenAI-compatible endpoint such as a local Ollama server in OpenAI mode, vLLM, or LM Studio. The adapter is an optional extra that requires `pip install soul-protocol[openai]` and performs a soft import to avoid breaking the core package for users who don't need it.

### `PocketPaw` — 1 article(s)

- [Test Suite for SoulProvider PocketPaw Integration](./test-suite-for-soulprovider-pocketpaw-integration.md) — Covers the `SoulProvider` reference integration class with 14 async pytest tests verifying system prompt generation, memory recall injection, interaction tracking, auto-save behavior, factory constructors, and low-energy behavioral adaptation. Tests are isolated using fresh Soul instances per fixture to prevent cross-test memory contamination.

### `Pydantic` — 1 article(s)

- [Test Suite: Context Spec Models -- Pydantic Schemas for Conversation Context](./test-suite-context-spec-models-pydantic-schemas-for-conversation-context.md) — Tests for the Pydantic models that define Soul Protocol's conversation context data layer, including `CompactionLevel` enum, `ContextMessage`, `ContextNode`, and the result types (`AssembleResult`, `GrepResult`, `ExpandResult`, `DescribeResult`). Validates defaults, serialization round-trips, unique ID generation, and string-enum comparability.

### `SQLite` — 1 article(s)

- [SQLiteContextStore: Immutable Message Store and Compaction DAG](./sqlitecontextstore-immutable-message-store-and-compaction-dag.md) — `SQLiteContextStore` is the persistence layer for LCM, implementing an append-only SQLite database with three tables: immutable conversation messages, compaction DAG nodes, and DAG edges. It uses `asyncio.to_thread()` to make synchronous SQLite calls safe in async code without adding any external dependencies.

### `SQLite persistence` — 1 article(s)

- [Test Suite: Context Store -- SQLite-Backed Conversation History](./test-suite-context-store-sqlite-backed-conversation-history.md) — Tests for Soul Protocol's SQLite-backed context store, which persists conversation messages and compaction nodes with sequential ordering, full-text grep, date-range filtering, and cross-reconnect durability. Covers both in-memory (test) and file-backed (persistence) store configurations.

### `SoulSpec` — 1 article(s)

- [Test Suite for SoulSpec Import and Export](./test-suite-for-soulspec-import-and-export.md) — Tests for the SoulSpec importer, which converts a human-authored directory of markdown files and soul.json into a Soul Protocol `SoulConfig`, and the corresponding exporter that writes those files back out. Covers OCEAN trait mapping, memory tier assignments, round-trip fidelity, and error handling.

### `TavernAI` — 1 article(s)

- [Test Suite for TavernAI Character Card Import and Export](./test-suite-for-tavernai-character-card-import-and-export.md) — Tests for the TavernAI importer, which converts Character Card V2 JSON (and PNG-embedded cards) into Soul Protocol `SoulConfig`, and the exporter that writes cards back. Covers field mapping to memory tiers, validation errors, and PNG tEXt chunk embedding.

### `YAML` — 1 article(s)

- [YAML Soul Configuration Parser (parse_soul_yaml)](./yaml-soul-configuration-parser-parsesoulyaml.md) — Parses a YAML soul configuration string into a SoulConfig using yaml.safe_load for deserialization and Pydantic model_validate for schema enforcement, with a two-step approach that separates parsing errors from validation errors.

### `a2a` — 1 article(s)

- [Test Suite for the A2A Agent Card Bridge](./test-suite-for-the-a2a-agent-card-bridge.md) — This test suite validates bidirectional conversion between Soul Protocol souls and Google A2A Agent Cards via `A2AAgentCardBridge`. It covers spec model serialization, full round-trip identity preservation (name, DID, OCEAN traits, skills), immutability guards on enrichment, and file-based CLI round-trips.

### `ablation` — 3 article(s)

- [Five Experimental Memory Conditions for Soul Protocol Ablation Study](./five-experimental-memory-conditions-for-soul-protocol-ablation-study.md) — Implements the five independent-variable conditions for the Soul Protocol validation study, ranging from a completely stateless baseline to the full psychology-informed stack. Each condition is a class that wraps a Soul (or no Soul) and exposes a uniform `observe / recall / get_state / reset` interface, allowing the experiment runner to swap conditions without changing any other code.
- [Quick Ablation Runner: Heuristic-Only Long-Horizon Scenario Executor](./quick-ablation-runner-heuristic-only-long-horizon-scenario-executor.md) — A minimal, API-key-free script that runs all long-horizon scenarios through the `LongHorizonRunner` and prints a structured ablation table comparing recall precision, memory count, and bond strength across conditions. It is the fastest way to verify the heuristic pipeline without incurring LLM costs.
- [Scale Ablation Runner: 1000-Turn Memory Efficiency Study](./scale-ablation-runner-1000-turn-memory-efficiency-study.md) — A dedicated runner for the 1000-turn marathon scenario that benchmarks Soul Protocol's selective storage against RAG (store-everything) and two null conditions at scale. At 1000 turns, the soul achieves 85% recall with 175 memories (4.9x more efficient than RAG's 1000), and the study breaks down recall by memory age to identify decay patterns.

### `ablation-study` — 6 article(s)

- [Long-Horizon Ablation Results Analyzer](./long-horizon-ablation-results-analyzer.md) — The `LongHorizonAnalyzer` takes `LongHorizonResults` produced by the runner and computes summary statistics, pairwise Cohen's d effect sizes, and per-scenario breakdowns, then renders a structured markdown report. It is the statistical layer that transforms raw recall hits and memory counts into publishable evidence.
- [Long-Horizon Ablation Runner — Four-Condition Infrastructure Benchmark](./long-horizon-ablation-runner-four-condition-infrastructure-benchmark.md) — The `LongHorizonRunner` executes 100+ turn synthetic conversation scenarios through four ablation conditions (Full Soul, RAG Only, Personality Only, Bare Baseline) and collects infrastructure metrics — recall precision, memory count, memory efficiency, bond trajectory — without requiring an LLM for response generation. It optionally integrates DSPy query expansion and significance gating.
- [Long-Horizon Ablation Study Package](./long-horizon-ablation-study-package.md) — The `research/long_horizon` package bundles the scenario generators, ablation runner, and statistical analyzer for Soul Protocol's long-horizon study, which proves that the psychology stack (significance gating, activation decay, somatic markers) produces measurable advantages at 100+ turn conversation scales. The `__init__.py` re-exports the three key classes and five scenario generator functions as the public API.
- [Long-Horizon Conversation Scenario Generators — Three Ablation Test Cases](./long-horizon-conversation-scenario-generators-three-ablation-test-cases.md) — Defines the `LongHorizonScenario` and `TestPoint` data structures and generates three distinct 100-160 turn synthetic conversation scenarios designed to stress different aspects of Soul Protocol's psychology stack: long-range recall, emotional continuity under extreme valence swings, and recall precision under adversarial fact burial.
- [Marathon Scale Scenario — 1000-Turn Conversation for Scale Ablation](./marathon-scale-scenario-1000-turn-conversation-for-scale-ablation.md) — Generates a fully deterministic 1000-turn synthetic conversation (`marathon_1000`) with 35 strategically planted facts and 40 recall test points designed to reveal the point at which Soul Protocol's selective memory corpus outperforms a naive RAG store. Facts are seeded across the full 1000-turn span — including deeply buried and callback facts — to stress-test long-range recall.
- [Multi-Condition Response Generator — Isolating Soul Protocol Components](./multi-condition-response-generator-isolating-soul-protocol-components.md) — Defines four experimental conditions (`FULL_SOUL`, `RAG_ONLY`, `PROMPT_PERSONALITY`, `BARE_BASELINE`) and the `MultiConditionResponder` class, which generates responses under each condition against the same underlying soul state to isolate the contribution of personality, memory, and emotional scaffolding to response quality.

### `access control` — 2 article(s)

- [Scope Matching — Bidirectional Hierarchical RBAC/ABAC Scope Evaluation](./scope-matching-bidirectional-hierarchical-rbacabac-scope-evaluation.md) — This module provides `match_scope`, `match_scope_strict`, and `normalise_scopes` — pure-stdlib functions for evaluating hierarchical scope tags used across all Soul Protocol retrieval paths. The `match_scope` function uses bidirectional containment after a bug was found where agents with concrete scopes (`org:sales:leads`) could not see memories tagged with parent glob scopes (`org:sales:*`) from bundled archetypes.
- [Test Suite: Scope Tags and Hierarchical Memory Access Control](./test-suite-scope-tags-and-hierarchical-memory-access-control.md) — Tests the `match_scope` and `match_scope_strict` helpers that implement hierarchical RBAC/ABAC filtering for memory recall. The suite covers bidirectional glob containment, strict one-way matching, scope normalization, export/awaken round-trips for scoped memories, and `Soul.recall` scope-filter integration.

### `access-control` — 1 article(s)

- [Test Suite: Memory Visibility Tiers](./test-suite-memory-visibility-tiers.md) — Tests for the three-tier memory visibility system (PUBLIC, BONDED, PRIVATE) that controls which memories are accessible to external requesters based on bond strength, preventing private memories from leaking while maintaining full system access for the soul itself.

### `activation` — 1 article(s)

- [Test Suite for Memory Activation Functions (ACT-R Base-Level, Spreading, Emotional Boost)](./test-suite-for-memory-activation-functions-act-r-base-level-spreading-emotional-.md) — Validates the three activation components that determine how retrievable a memory is at query time: the ACT-R base-level activation formula (recency and frequency), spreading activation (query-content token overlap), and emotional boost (somatic marker arousal and valence). Together these tests pin the mathematical contracts that underlie memory retrieval scoring.

### `adapters` — 9 article(s)

- [AnthropicEngine: Claude API Cognitive Adapter](./anthropicengine-claude-api-cognitive-adapter.md) — Implements `CognitiveEngine` using Anthropic's Claude API, defaulting to `claude-haiku-4-5-20251001` for fast, cost-effective cognitive processing. Uses a soft import pattern so the module loads without errors even when the `anthropic` package is not installed.
- [CallableEngine: Wrap Any Function as a Cognitive Engine](./callableengine-wrap-any-function-as-a-cognitive-engine.md) — Wraps any sync or async callable as a `CognitiveEngine`, letting developers pass a simple lambda or function to `Soul.birth()` without implementing the full protocol. Handles both sync and async callables transparently using `asyncio.iscoroutinefunction` and `run_in_executor`.
- [Cognitive Adapters Package: Provider Adapter Exports](./cognitive-adapters-package-provider-adapter-exports.md) — Package init for the cognitive adapters subpackage, re-exporting all provider-specific engine implementations and the `engine_from_env()` auto-detection helper. All adapters use soft imports that raise only at instantiation time, not on module load.
- [Cognitive Engine Package: Public API and Adapter Exports](./cognitive-engine-package-public-api-and-adapter-exports.md) — Package init for the cognitive engine subsystem, exporting the base `CognitiveEngine` and `HeuristicEngine` protocols alongside all provider-specific adapters (Anthropic, OpenAI, Ollama, LiteLLM, Callable) and the `engine_from_env()` auto-detection helper.
- [engine_from_env: Auto-Detect Best Available Cognitive Engine](./enginefromenv-auto-detect-best-available-cognitive-engine.md) — Provides `engine_from_env()`, a zero-configuration helper that inspects environment variables to select the most capable available cognitive engine. Priority order is Anthropic, OpenAI, Ollama, then HeuristicEngine as the always-available fallback.
- [LiteLLMEngine: Universal 100+ Provider Cognitive Adapter](./litellmengine-universal-100-provider-cognitive-adapter.md) — Implements `CognitiveEngine` using LiteLLM, providing a single adapter that routes cognitive calls to any of 100+ LLM providers including Anthropic, OpenAI, Ollama, Bedrock, Vertex, and Cohere. Provider is selected by model string format (e.g., `anthropic/claude-haiku-4-5-20251001`).
- [MCPSamplingEngine: Host-LLM Cognitive Routing via MCP](./mcpsamplingengine-host-llm-cognitive-routing-via-mcp.md) — MCPSamplingEngine routes cognitive prompts to the host LLM (Claude, GPT-4, etc.) through the Model Context Protocol sampling API, so soul-protocol tools embedded in Claude Code or Claude Desktop can leverage the host model without needing a separate API key. It falls back to HeuristicEngine whenever the MCP client does not support sampling or when called outside an MCP context.
- [OllamaEngine: Local Model Cognitive Adapter](./ollamaengine-local-model-cognitive-adapter.md) — OllamaEngine connects soul-protocol's cognitive pipeline to a locally running Ollama server, using the existing `httpx` dependency so no additional packages are required. It posts prompts to Ollama's REST API and returns the model response, enabling fully offline AI companion operation.
- [OpenAIEngine: OpenAI and OpenAI-Compatible Cognitive Adapter](./openaiengine-openai-and-openai-compatible-cognitive-adapter.md) — OpenAIEngine connects soul-protocol's cognitive pipeline to OpenAI's chat completions API, or any OpenAI-compatible endpoint such as a local Ollama server in OpenAI mode, vLLM, or LM Studio. The adapter is an optional extra that requires `pip install soul-protocol[openai]` and performs a soft import to avoid breaking the core package for users who don't need it.

### `agent` — 2 article(s)

- [Interactive CLI Agent Demonstrating the Full Soul Lifecycle](./interactive-cli-agent-demonstrating-the-full-soul-lifecycle.md) — A runnable conversational agent that exercises the complete Soul Protocol lifecycle — birth, observe, recall, reflect, and save — with pluggable LLM backends (Claude, OpenAI, Ollama, and a no-LLM heuristic mode). It uses a dual-model architecture where a cheaper model handles soul cognition and a stronger model handles user-facing chat, demonstrating the cost-efficiency pattern Soul Protocol enables.
- [Soul Agent: Claude Agent SDK + Soul Protocol with 15 MCP Tools](./soul-agent-claude-agent-sdk-soul-protocol-with-15-mcp-tools.md) — Implements a full-featured conversational agent that combines the Claude Agent SDK's tool execution and streaming loop with Soul Protocol's v0.2.3 feature set, exposing 15 MCP tools covering memory, personality, emotion, bond tracking, skills, GDPR deletion, evolution proposals, core memory editing, reincarnation, and encrypted export. The module-level `_soul` singleton pattern mirrors the MCP server architecture for consistency.

### `agent learning` — 1 article(s)

- [Decision Trace Models: Structured Agent Proposal and Human Correction Events](./decision-trace-models-structured-agent-proposal-and-human-correction-events.md) — `spec/decisions.py` defines the payload types and helpers for the Org Journal's decision trace workstream — structuring every agent proposal, human correction, and pattern graduation as auditable, linkable `EventEntry` pairs. The module enables agents to learn from correction patterns without requiring ad-hoc feedback loops.

### `agent protocol` — 1 article(s)

- [A2A Agent Card Models: Soul Protocol Interop with Google's Agent-to-Agent Protocol](./a2a-agent-card-models-soul-protocol-interop-with-googles-agent-to-agent-protocol.md) — `spec/a2a.py` provides Pydantic models that map Google's A2A Agent Card specification to Soul Protocol primitives. `A2AAgentCard` is the agent's public identity, `A2ASkill` advertises capabilities, and `SoulExtension` embeds a soul's DID and OCEAN personality into the card's extension block.

### `agent-loop` — 1 article(s)

- [SoulProvider: PocketPaw Integration Bridge for Soul Protocol](./soulprovider-pocketpaw-integration-bridge-for-soul-protocol.md) — Defines `SoulProvider`, a reference integration class that replaces PocketPaw's `DefaultBootstrapProvider` with a soul-driven system prompt generator, interaction tracker, and auto-save mechanism. It bridges soul-protocol's memory and identity system into any agent loop that calls a `get_system_prompt()` / `on_interaction()` pattern, with zero dependency on PocketPaw internals.

### `analysis` — 1 article(s)

- [Statistical Analysis Engine for Soul Protocol Research Results](./statistical-analysis-engine-for-soul-protocol-research-results.md) — Provides `ResultsAnalyzer`, which takes flat metric rows from experiment runs and produces statistical summary tables, pairwise condition comparisons with Cohen's d effect sizes and Mann-Whitney U p-values, ablation chains, per-use-case breakdowns, and a full markdown report. All analysis uses stdlib-only dependencies to maximize portability.

### `anthropic` — 1 article(s)

- [AnthropicEngine: Claude API Cognitive Adapter](./anthropicengine-claude-api-cognitive-adapter.md) — Implements `CognitiveEngine` using Anthropic's Claude API, defaulting to `claude-haiku-4-5-20251001` for fast, cost-effective cognitive processing. Uses a soft import pattern so the module loads without errors even when the `anthropic` package is not installed.

### `api` — 6 article(s)

- [Cognitive Adapters Package: Provider Adapter Exports](./cognitive-adapters-package-provider-adapter-exports.md) — Package init for the cognitive adapters subpackage, re-exporting all provider-specific engine implementations and the `engine_from_env()` auto-detection helper. All adapters use soft imports that raise only at instantiation time, not on module load.
- [Cognitive Engine Package: Public API and Adapter Exports](./cognitive-engine-package-public-api-and-adapter-exports.md) — Package init for the cognitive engine subsystem, exporting the base `CognitiveEngine` and `HeuristicEngine` protocols alongside all provider-specific adapters (Anthropic, OpenAI, Ollama, LiteLLM, Callable) and the `engine_from_env()` auto-detection helper.
- [Journal Engine Public API — `engine.journal` Package](./journal-engine-public-api-enginejournal-package.md) — Re-exports the complete public surface of the journal engine: the `Journal` class, `JournalBackend` protocol, `SQLiteJournalBackend` implementation, the full exception hierarchy, and scope-matching utilities. Callers import from this package rather than individual submodules.
- [MCP Package Public Entry Points](./mcp-package-public-entry-points.md) — Thin package init that re-exports `create_server` and `run_server` from the MCP server module, providing a stable public surface for consumers who integrate soul-protocol's FastMCP server. Keeps the import path short (`from soul_protocol.mcp import create_server`) regardless of internal module organization.
- [Runtime Package: Reference Implementation Public API](./runtime-package-reference-implementation-public-api.md) — The runtime package `__init__.py` defines the public API surface for soul-protocol's reference implementation, currently exporting the Lossless Context Management (LCM) and evaluation subsystems. It serves as the 'batteries-included' layer built on top of the lower-level spec primitives.
- [soul-protocol Public API — Package Root](./soul-protocol-public-api-package-root.md) — The top-level `__init__.py` is the single import surface for all public soul-protocol types, runtime objects, and exceptions. It bridges the two-layer architecture (spec primitives and runtime engine) into a stable, versioned API.

### `architecture` — 14 article(s)

- [EmbeddingProvider Protocol Re-export (Runtime Shim)](./embeddingprovider-protocol-re-export-runtime-shim.md) — This file is a one-line shim that re-exports `EmbeddingProvider` from its canonical location in `soul_protocol.spec.embeddings.protocol` into the runtime embeddings namespace. It exists to provide a stable import path for runtime code while keeping the authoritative protocol definition in the spec layer.
- [Engine Package Namespace — `soul_protocol.engine`](./engine-package-namespace-soulprotocolengine.md) — An intentionally minimal package initialiser that marks `soul_protocol.engine` as a Python package and documents its role as the runtime implementation layer behind the protocol's `spec/` models.
- [Eternal Storage Protocol Re-Export Layer](./eternal-storage-protocol-re-export-layer.md) — A thin compatibility shim that re-exports the three canonical eternal storage types — `EternalStorageProvider`, `ArchiveResult`, and `RecoverySource` — from the spec layer into the runtime package. It exists so callers already importing from `soul_protocol.runtime.eternal.protocol` continue to work after those definitions were moved to `soul_protocol.spec.eternal.protocol`.
- [Evolution Subpackage Public Interface](./evolution-subpackage-public-interface.md) — The `evolution/__init__.py` init re-exports `EvolutionManager` as the sole public entry point for the evolution subsystem. Callers import from `soul_protocol.runtime.evolution` rather than navigating to the `manager` submodule directly.
- [Identity Subpackage: DID Generation Interface](./identity-subpackage-did-generation-interface.md) — The `identity/__init__.py` init exposes `generate_did` as the single public entry point for the identity subsystem, making Decentralized Identifier (DID) creation available via `from soul_protocol.runtime.identity import generate_did` without exposing the underlying DID implementation details.
- [Journal Backend Protocol — Storage Contract for Event Journals](./journal-backend-protocol-storage-contract-for-event-journals.md) — Defines `JournalBackend` as a `runtime_checkable` Protocol (structural interface) that any storage backend must satisfy. Specifies four operations — append, query, replay, and close — with precise filter semantics for the query method.
- [Journal Engine Public API — `engine.journal` Package](./journal-engine-public-api-enginejournal-package.md) — Re-exports the complete public surface of the journal engine: the `Journal` class, `JournalBackend` protocol, `SQLiteJournalBackend` implementation, the full exception hierarchy, and scope-matching utilities. Callers import from this package rather than individual submodules.
- [Journal High-Level Class — Invariant Enforcement and Hash Chaining](./journal-high-level-class-invariant-enforcement-and-hash-chaining.md) — The `Journal` class wraps a `JournalBackend` to enforce append invariants (timezone-aware timestamps, monotonic ordering) and maintain an opportunistic SHA-256 hash chain linking each event to the previous one. `open_journal()` is the factory for production use.
- [Parsers Subpackage: Soul Configuration File Loader Registry](./parsers-subpackage-soul-configuration-file-loader-registry.md) — The parsers subpackage is the namespace for soul configuration format readers — soul.md, soul.yaml, and soul.json parsers live here. The __init__.py is intentionally empty so each parser module is imported directly, avoiding circular dependency risks.
- [SearchStrategy Protocol: Pluggable Memory Retrieval Scoring](./searchstrategy-protocol-pluggable-memory-retrieval-scoring.md) — Defines a Protocol interface for memory retrieval scoring, enabling consumers to replace the default BM25 scorer with embeddings, vector databases, or any custom implementation. Ships with two built-in strategies: a zero-dependency token-overlap fallback and a BM25-backed default.
- [Soul Protocol CLI — Primary Command Surface (`main.py`)](./soul-protocol-cli-primary-command-surface-mainpy.md) — The main Click application that powers the `soul` command-line tool, exposing 40+ subcommands covering the full soul lifecycle: initialisation, memory management, eternal storage, platform injection, format conversion, org management, and health tooling.
- [soul-protocol Public API — Package Root](./soul-protocol-public-api-package-root.md) — The top-level `__init__.py` is the single import surface for all public soul-protocol types, runtime objects, and exceptions. It bridges the two-layer architecture (spec primitives and runtime engine) into a stable, versioned API.
- [Soul: The Central Digital Identity and Lifecycle Manager](./soul-the-central-digital-identity-and-lifecycle-manager.md) — The Soul class is the top-level API object in Soul Protocol — it unifies identity, memory, personality, skills, evolution, dreaming, bonding, and eternal storage into a single coherent interface. It is the primary object that consuming applications instantiate and interact with.
- [Spike Namespace: In-Progress Design Experiments](./spike-namespace-in-progress-design-experiments.md) — The `spike/` package is a dedicated quarantine zone for experimental code that is under active design validation but not yet ready for the public API. It exists to allow risky ideas to be tested inside the full suite without any risk of accidental consumer imports.

### `archival` — 5 article(s)

- [Archival Memory Store for Conversation History](./archival-memory-store-for-conversation-history.md) — Provides long-term, compressed storage for complete conversation sessions via `ArchivalMemoryStore` and `ConversationArchive`. Supports keyword search across summaries and key moments, and date-range queries, without consuming active context.
- [Soul File I/O — Pack and Unpack .soul Zip Archives](./soul-file-io-pack-and-unpack-soul-zip-archives.md) — This module provides `pack_soul`, `unpack_soul`, and `unpack_to_container` — the three functions that serialize and deserialize a soul's identity and memories to and from the `.soul` zip archive format. It depends only on stdlib (`zipfile`, `json`, `io`) and the spec-layer models, keeping the archival format fully portable.
- [Soul Manifest — Archive Metadata Header for .soul Files](./soul-manifest-archive-metadata-header-for-soul-files.md) — The `Manifest` model defines the `manifest.json` file stored at the root of every `.soul` zip archive. It captures format version, soul identity, creation timestamp, an integrity checksum placeholder, and an open `stats` dict for runtime-specific metadata like memory counts and layer names.
- [Test Suite for Archival Memory Integration: Compression Pipeline and Persistence](./test-suite-for-archival-memory-integration-compression-pipeline-and-persistence.md) — This integration test suite validates the full archival pipeline in `MemoryManager`: how old episodic entries are compressed into `ConversationArchive` objects, how high-importance entries become key moments, how archived entries are excluded from recall, and how archives survive serialization round-trips through `to_dict()` / `from_dict()`.
- [Test Suite for the Archival Memory Store](./test-suite-for-the-archival-memory-store.md) — This test suite validates `ArchivalMemoryStore` — the fifth memory tier that stores compressed conversation summaries as `ConversationArchive` objects. It covers creation, keyword search with overlap-based ranking, date-range overlap queries, count tracking, and get-by-id retrieval.

### `arweave` — 1 article(s)

- [Mock Arweave Eternal Storage Provider](./mock-arweave-eternal-storage-provider.md) — `MockArweaveProvider` simulates Arweave's permanent, pay-once storage model using an in-memory dict and deterministic transaction IDs — no gateway required. It lets test suites and local development exercise the full Arweave archive/retrieve/verify cycle without real AR tokens or network access.

### `attention` — 2 article(s)

- [LIDA Significance Gate for Episodic Memory Storage](./lida-significance-gate-for-episodic-memory-storage.md) — Implements a LIDA-inspired attention gate that decides whether an interaction is significant enough to store as an episodic memory. Four dimensions — novelty, emotional intensity, goal relevance, and content richness — are blended into a single score, filtered by a tunable threshold.
- [Test Suite for Memory Attention and Interaction Significance Scoring](./test-suite-for-memory-attention-and-interaction-significance-scoring.md) — Validates the attention system that decides whether an interaction is significant enough to trigger memory storage. Tests cover novelty scoring, emotional intensity detection, goal-relevance matching against core values, and the composite SignificanceScore type.

### `audit` — 1 article(s)

- [Test Suite: Decision Trace Spec — Agent Proposals, Human Corrections, and Graduation](./test-suite-decision-trace-spec-agent-proposals-human-corrections-and-graduation.md) — Validates the decision-trace spec from Workstream D of the Org Architecture RFC, covering the three payload types (`AgentProposal`, `HumanCorrection`, `DecisionGraduation`), their JSON round-trips, field validation, and the builder helpers that emit correctly shaped `EventEntry` records to the org journal.

### `audit log` — 1 article(s)

- [Org Journal — Append-Only Event Log with Actor Attribution, Scoped Writes, and Root Protection](./org-journal-append-only-event-log-with-actor-attribution-scoped-writes-and-root-.md) — The org journal is the immutable, UTC-stamped, scope-tagged event store that serves as a single source of truth for all mutations in an org instance. `EventEntry`, `Actor`, and `DataRef` are the core spec models; `check_root_undeletable` is an advisory safeguard that prevents any journal event from retiring or deleting the org's root soul.

### `audit trail` — 1 article(s)

- [Test Suite: Retrieval Trace — Observability Receipt for Memory Recall](./test-suite-retrieval-trace-observability-receipt-for-memory-recall.md) — Locks the shape and runtime behavior of `RetrievalTrace`, the observability receipt emitted by `Soul.recall()` and `Soul.smart_recall()` after every memory lookup. The suite verifies that traces are populated even on empty results, are not serialized into `.soul` files, and correctly record actor, query, candidates, and rerank metadata.

### `auditing` — 1 article(s)

- [Decision Trace Models: Structured Agent Proposal and Human Correction Events](./decision-trace-models-structured-agent-proposal-and-human-correction-events.md) — `spec/decisions.py` defines the payload types and helpers for the Org Journal's decision trace workstream — structuring every agent proposal, human correction, and pattern graduation as auditable, linkable `EventEntry` pairs. The module enables agents to learn from correction patterns without requiring ad-hoc feedback loops.

### `authorization` — 1 article(s)

- [Journal Scope Matching: Pattern Grammar and Overlap Checks](./journal-scope-matching-pattern-grammar-and-overlap-checks.md) — Provides two public functions for colon-delimited scope pattern matching used throughout the journal engine, retrieval router, and credential broker. The module was extracted from the SQLite backend to make the shared logic accessible as a proper public API.

### `auto-detection` — 1 article(s)

- [engine_from_env: Auto-Detect Best Available Cognitive Engine](./enginefromenv-auto-detect-best-available-cognitive-engine.md) — Provides `engine_from_env()`, a zero-configuration helper that inspects environment variables to select the most capable available cognitive engine. Priority order is Anthropic, OpenAI, Ollama, then HeuristicEngine as the always-available fallback.

### `backward-compatibility` — 1 article(s)

- [Test Suite: Multi-Participant Interactions and Multi-Bond Identity](./test-suite-multi-participant-interactions-and-multi-bond-identity.md) — Tests for the multi-participant Interaction model (issue #95) and multi-bond Identity (issue #94), including full backward compatibility with the legacy user_input/agent_output API, serialization round-trips, and coverage of the spec-layer equivalents.

### `batch operations` — 1 article(s)

- [Test Suite: Soul Templates and Batch Spawning via SoulFactory](./test-suite-soul-templates-and-batch-spawning-via-soulfactory.md) — Validates `SoulTemplate` (reusable soul blueprints with personality profiles, memories, and skills) and `SoulFactory` (the factory that instantiates souls from templates, including batch spawning with controlled personality variance). The suite covers model validation, override semantics, variance clamping, reproducible seeding, and public API exports.

### `benchmarking` — 3 article(s)

- [Enhanced Quality Validation Runner — N-Variation Factorial Benchmark with Checkpointing](./enhanced-quality-validation-runner-n-variation-factorial-benchmark-with-checkpoi.md) — Orchestrates a full factorial quality benchmark: N scenario variations × 3 test types × M judge models × 4 ablation conditions, accumulating statistically meaningful results with 95% confidence intervals. Checkpoint/resume logic survives process crashes, and a formatted results table with win rates is printed on completion.
- [Mem0 Comparison Benchmark — Soul Protocol vs. Vector Memory vs. Stateless Baseline](./mem0-comparison-benchmark-soul-protocol-vs-vector-memory-vs-stateless-baseline.md) — Runs a three-way quality comparison between Soul Protocol, Mem0 (embedding-based vector memory), and a stateless baseline across four test types, producing a scorecard that quantifies what the soul stack adds beyond raw memory storage. The benchmark is designed to appear in the Soul Protocol research paper as evidence that personality, emotional tracking, and bond are not incidental features.
- [Whitepaper Chart Generator for Soul Protocol Research](./whitepaper-chart-generator-for-soul-protocol-research.md) — Generates five publication-quality PNG charts that visualize Soul Protocol's research validation results, covering multi-judge quality tests, component ablation, competitive comparisons (vs. Mem0), judge agreement heatmaps, and a waterfall plot of cumulative score improvements. Each chart is designed to standalone as a figure in the Soul Protocol whitepaper with accessible color coding and clean academic styling.

### `benchmarks` — 1 article(s)

- [Test Suite for JournalBackedMemoryStore Benchmark Harness](./test-suite-for-journalbackedmemorystore-benchmark-harness.md) — This benchmark harness compares `JournalBackedMemoryStore` against the baseline `DictMemoryStore` across four dimensions: recall quality (recall@5), write latency, search latency, and delete correctness. It was created to produce a data-driven decision on whether the append-only journal architecture is worth adopting, with pass criteria derived from the spike design document.

### `bi-temporal` — 1 article(s)

- [Test Suite for Bi-Temporal Memory Fields (ingested_at and superseded)](./test-suite-for-bi-temporal-memory-fields-ingestedat-and-superseded.md) — Validates bi-temporal tracking on MemoryEntry — the `ingested_at` timestamp (when a fact entered this soul's memory vs. when it was created) and the `superseded` flag (marking outdated facts replaced by newer information). Tests cover defaults, explicit values, manager behavior, serialization round-trips, and backward compatibility with soul files that predate these fields.

### `binary-formats` — 1 article(s)

- [TavernAI Character Card V2 Importer and Exporter](./tavernai-character-card-v2-importer-and-exporter.md) — Supports full round-trip conversion between TavernAI Character Card V2 (JSON and PNG-embedded) and DSP Soul objects. Handles PNG binary parsing — extracting and embedding character JSON in `tEXt`/`iTXt` chunks — without any external image library dependency.

### `blockchain` — 1 article(s)

- [Mock Blockchain Soul Registry Provider](./mock-blockchain-soul-registry-provider.md) — `MockBlockchainProvider` simulates an on-chain soul registry where each soul is minted as a unique NFT-like token with a hash-based token ID. It enables testing of blockchain-based soul identity ownership without a real wallet, chain, or gas fees.

### `bond` — 2 article(s)

- [Test Suite for the Human-Soul Bond Model](./test-suite-for-the-human-soul-bond-model.md) — This test suite validates the `Bond` model that tracks the relationship strength between a soul and its bonded human, covering construction defaults, logarithmic growth on `strengthen()`, floor/ceiling clamping on `weaken()`, and Pydantic validation of out-of-range `bond_strength` values.
- [Test Suite: Phase 1 Ablation Fixes](./test-suite-phase-1-ablation-fixes.md) — Tests for five Phase 1 ablation fixes: the significance gate short-message penalty, mundane-interaction rejection, select_top_k attention selection, BM25Index scoring and the BM25SearchStrategy protocol, and logarithmic bond growth with diminishing returns.

### `bond-mechanics` — 1 article(s)

- [End-to-End New Soul Primitives Test — Bond, Skills, Retirement, and Reincarnation](./end-to-end-new-soul-primitives-test-bond-skills-retirement-and-reincarnation.md) — End-to-end test suite for newer soul-protocol primitives introduced post-v1: bond mechanics, skill registry, lifecycle state transitions (active → retired → reincarnated), and `.soul` archive integrity. Tests validate the logarithmic bond growth curve and the presence of `dna.md` in exported archives.

### `bridges` — 2 article(s)

- [A2A Agent Card Bridge: Soul Protocol and Google A2A Interoperability](./a2a-agent-card-bridge-soul-protocol-and-google-a2a-interoperability.md) — Provides bidirectional conversion between Soul Protocol souls and Google's A2A Agent Card JSON format, mapping soul identity, OCEAN personality traits, and skills in both directions. A stateless utility class with three static methods covering export, import, and non-destructive enrichment.
- [Protocol Bridges Package: A2A Interoperability](./protocol-bridges-package-a2a-interoperability.md) — Package init for the runtime bridges subsystem, currently exposing the `A2AAgentCardBridge` for bidirectional conversion between Soul Protocol and Google's A2A Agent Card format. Bridges enable soul-powered agents to participate in multi-agent ecosystems that use other protocols.

### `bug-regression` — 1 article(s)

- [Regression Tests for Bug #15: Core Memory Replace vs. Append](./regression-tests-for-bug-15-core-memory-replace-vs-append.md) — This regression suite locks the fix for Bug #15, where `edit_core_memory()` was appending new values to existing persona and human fields instead of replacing them. The tests verify replace semantics, partial-field isolation, and the explicit absence of the old append behavior.

### `cli` — 9 article(s)

- [CLI Package Namespace — `soul_protocol.cli`](./cli-package-namespace-soulprotocolcli.md) — An intentionally empty package initialiser that establishes `soul_protocol.cli` as a Python package namespace. Its sole function is structural: making the directory importable so that `cli.main`, `cli.inject`, `cli.org`, and `cli.setup` are discoverable.
- [Org Management CLI — `soul org` and `soul user`](./org-management-cli-soul-org-and-soul-user.md) — Implements the `soul org init`, `soul org status`, `soul org destroy`, and `soul user invite` commands for managing multi-soul organisational deployments. Creates a governance root soul, writes journal events for every lifecycle action, and enforces layered protections against root-soul deletion.
- [Platform Auto-Detection and MCP Wiring — `setup.py`](./platform-auto-detection-and-mcp-wiring-setuppy.md) — Detects installed AI coding platforms (Claude Code, Cursor, VS Code, Windsurf, and 8 others), generates platform-appropriate MCP server config, and writes soul memory instructions into each platform's instruction file. Designed to be run once during `soul init --setup-targets`.
- [Quality Validation Runner: CLI Orchestrator for Soul Protocol Test Suite](./quality-validation-runner-cli-orchestrator-for-soul-protocol-test-suite.md) — The quality runner provides a command-line interface to execute one or more of the four fixed validation tests (response quality, personality consistency, hard recall, emotional continuity) and outputs both a live scorecard and saved JSON/Markdown reports. It uses concurrent `HaikuCognitiveEngine` instances for agent and judge roles to keep cost tracking separate.
- [Research Simulation CLI: Entry Point for Full-Scale Ablation Experiments](./research-simulation-cli-entry-point-for-full-scale-ablation-experiments.md) — The `run.py` module is the primary CLI entry point for the Soul Protocol research simulation framework, translating command-line arguments into an `ExperimentConfig` and orchestrating the full simulation, analysis, and report pipeline. It supports configuring agent count, random seed, memory conditions, use cases, output directory, batch size, and a quick mode for rapid iteration.
- [Soul Context Injection — CLI Inject Module](./soul-context-injection-cli-inject-module.md) — Provides functions to write or replace a soul's identity, state, and recent memories into agent platform configuration files (CLAUDE.md, .cursorrules, etc.) using idempotent HTML comment markers. Supports six platforms and runs without MCP.
- [Soul Health Score Evaluation CLI Entry Point](./soul-health-score-evaluation-cli-entry-point.md) — Thin `__main__.py` that enables `python -m research.eval.suite` as a runnable command by delegating immediately to `suite.main()`. Provides zero additional logic, keeping the entry point surface minimal.
- [Soul Protocol CLI — Primary Command Surface (`main.py`)](./soul-protocol-cli-primary-command-surface-mainpy.md) — The main Click application that powers the `soul` command-line tool, exposing 40+ subcommands covering the full soul lifecycle: initialisation, memory management, eternal storage, platform injection, format conversion, org management, and health tooling.
- [Test Suite Package Initializer for CLI Tests](./test-suite-package-initializer-for-cli-tests.md) — The `tests/test_cli/__init__.py` file marks the `tests/test_cli/` directory as a Python package, enabling pytest to discover and correctly namespace the CLI test modules it contains. It contains no executable code.

### `cognitive` — 7 article(s)

- [AnthropicEngine: Claude API Cognitive Adapter](./anthropicengine-claude-api-cognitive-adapter.md) — Implements `CognitiveEngine` using Anthropic's Claude API, defaulting to `claude-haiku-4-5-20251001` for fast, cost-effective cognitive processing. Uses a soft import pattern so the module loads without errors even when the `anthropic` package is not installed.
- [CallableEngine: Wrap Any Function as a Cognitive Engine](./callableengine-wrap-any-function-as-a-cognitive-engine.md) — Wraps any sync or async callable as a `CognitiveEngine`, letting developers pass a simple lambda or function to `Soul.birth()` without implementing the full protocol. Handles both sync and async callables transparently using `asyncio.iscoroutinefunction` and `run_in_executor`.
- [Cognitive Adapters Package: Provider Adapter Exports](./cognitive-adapters-package-provider-adapter-exports.md) — Package init for the cognitive adapters subpackage, re-exporting all provider-specific engine implementations and the `engine_from_env()` auto-detection helper. All adapters use soft imports that raise only at instantiation time, not on module load.
- [Cognitive Engine Package: Public API and Adapter Exports](./cognitive-engine-package-public-api-and-adapter-exports.md) — Package init for the cognitive engine subsystem, exporting the base `CognitiveEngine` and `HeuristicEngine` protocols alongside all provider-specific adapters (Anthropic, OpenAI, Ollama, LiteLLM, Callable) and the `engine_from_env()` auto-detection helper.
- [engine_from_env: Auto-Detect Best Available Cognitive Engine](./enginefromenv-auto-detect-best-available-cognitive-engine.md) — Provides `engine_from_env()`, a zero-configuration helper that inspects environment variables to select the most capable available cognitive engine. Priority order is Anthropic, OpenAI, Ollama, then HeuristicEngine as the always-available fallback.
- [FastMCP Server: 28-Tool Soul Protocol Agent Integration](./fastmcp-server-28-tool-soul-protocol-agent-integration.md) — Implements the full soul-protocol MCP server using FastMCP, exposing 28 tools, 3 resources, and 2 prompts for AI agent integration. Manages a registry of multiple souls, handles auto-reload from disk, lazy cognitive engine wiring, and graceful shutdown with auto-save.
- [LiteLLMEngine: Universal 100+ Provider Cognitive Adapter](./litellmengine-universal-100-provider-cognitive-adapter.md) — Implements `CognitiveEngine` using LiteLLM, providing a single adapter that routes cognitive calls to any of 100+ LLM providers including Anthropic, OpenAI, Ollama, Bedrock, Vertex, and Cohere. Provider is selected by model string format (e.g., `anthropic/claude-haiku-4-5-20251001`).

### `cognitive engine` — 8 article(s)

- [Cognitive Prompt Templates for LLM-Powered Memory Tasks](./cognitive-prompt-templates-for-llm-powered-memory-tasks.md) — This module contains all LLM prompt templates used by `CognitiveProcessor` for sentiment analysis, significance gating, fact extraction, entity extraction, and self-reflection. Each template embeds a `[TASK:xxx]` routing marker so that `HeuristicEngine` can dispatch to its regex fallbacks without parsing the full prompt.
- [CognitiveEngine Protocol, HeuristicEngine, and CognitiveProcessor](./cognitiveengine-protocol-heuristicengine-and-cognitiveprocessor.md) — This module defines the core cognitive abstraction layer for soul-protocol: the `CognitiveEngine` protocol (the one interface consumers implement), `HeuristicEngine` (the zero-dependency regex fallback), and `CognitiveProcessor` (the internal orchestrator that turns raw LLM responses into typed memory objects). Together they decouple memory pipeline logic from any specific LLM provider.
- [DSPy Cognitive Modules: Optimizable Memory Pipeline Components](./dspy-cognitive-modules-optimizable-memory-pipeline-components.md) — This module defines three DSPy-native modules — `SignificanceGate`, `QueryExpander`, and `FactExtractor` — that replace hand-written heuristics in soul-protocol's memory pipeline with learnable, optimizable LLM programs. All DSPy imports are lazy so the module loads safely in environments where DSPy is not installed.
- [DSPyCognitiveProcessor: Learnable Memory Cognition via DSPy](./dspycognitiveprocessor-learnable-memory-cognition-via-dspy.md) — DSPyCognitiveProcessor is a drop-in replacement for `CognitiveProcessor` that routes significance assessment, query expansion, and fact extraction through optimizable DSPy modules instead of hand-written prompts. It bridges DSPy's synchronous execution model to soul-protocol's async architecture via a thread pool executor.
- [MCPSamplingEngine: Host-LLM Cognitive Routing via MCP](./mcpsamplingengine-host-llm-cognitive-routing-via-mcp.md) — MCPSamplingEngine routes cognitive prompts to the host LLM (Claude, GPT-4, etc.) through the Model Context Protocol sampling API, so soul-protocol tools embedded in Claude Code or Claude Desktop can leverage the host model without needing a separate API key. It falls back to HeuristicEngine whenever the MCP client does not support sampling or when called outside an MCP context.
- [OllamaEngine: Local Model Cognitive Adapter](./ollamaengine-local-model-cognitive-adapter.md) — OllamaEngine connects soul-protocol's cognitive pipeline to a locally running Ollama server, using the existing `httpx` dependency so no additional packages are required. It posts prompts to Ollama's REST API and returns the model response, enabling fully offline AI companion operation.
- [OpenAIEngine: OpenAI and OpenAI-Compatible Cognitive Adapter](./openaiengine-openai-and-openai-compatible-cognitive-adapter.md) — OpenAIEngine connects soul-protocol's cognitive pipeline to OpenAI's chat completions API, or any OpenAI-compatible endpoint such as a local Ollama server in OpenAI mode, vLLM, or LM Studio. The adapter is an optional extra that requires `pip install soul-protocol[openai]` and performs a soft import to avoid breaking the core package for users who don't need it.
- [SoulOptimizer: MIPROv2 Training Harness for Cognitive Modules](./souloptimizer-miprov2-training-harness-for-cognitive-modules.md) — `SoulOptimizer` is an offline training harness that uses DSPy's MIPROv2 optimizer to tune `SignificanceGate` and `QueryExpander` against labeled interaction data derived from ablation scenarios or production logs. Optimized module weights are serialized to disk and loaded by `DSPyCognitiveProcessor` at runtime.

### `cognitive processing` — 3 article(s)

- [Test Suite: Cognitive Adapter Layer -- CallableEngine, Auto-Detection, and Provider Integration](./test-suite-cognitive-adapter-layer-callableengine-auto-detection-and-provider-in.md) — Tests for the adapter layer that bridges Soul Protocol's `CognitiveEngine` protocol with real LLM providers (Anthropic, OpenAI, Ollama) and user-supplied callables. Covers callable wrapping, environment-variable-driven auto-detection, Soul factory integration, import safety for optional dependencies, and HTTP-level mocking for Ollama.
- [Test Suite: Cognitive Engine, Heuristic Routing, and LLM Fallback](./test-suite-cognitive-engine-heuristic-routing-and-llm-fallback.md) — Comprehensive test suite for the Soul Protocol cognitive processing layer, covering HeuristicEngine task routing, CognitiveProcessor LLM integration, JSON parsing helpers, and graceful fallback when LLMs fail or return garbage. Also includes regression tests for the heuristic-only mode bug.
- [Test Suite: Cognitive Prompt Template Formatting and JSON Parsability](./test-suite-cognitive-prompt-template-formatting-and-json-parsability.md) — Tests that every cognitive prompt template in Soul Protocol formats without error and produces output parseable by the JSON extraction helper. Validates both the template variables contract and the round-trip between typical LLM responses and the `_parse_json` function.

### `cognitive-architecture` — 3 article(s)

- [ACT-R Activation-Based Memory Scoring](./act-r-activation-based-memory-scoring.md) — Implements Anderson's ACT-R cognitive model for memory recall scoring. Combines power-law recency/frequency decay, query-spreading activation, emotional boost from somatic markers, and personality-modulated ranking to produce a single activation score per memory candidate.
- [LIDA Significance Gate for Episodic Memory Storage](./lida-significance-gate-for-episodic-memory-storage.md) — Implements a LIDA-inspired attention gate that decides whether an interaction is significant enough to store as an episodic memory. Four dimensions — novelty, emotional intensity, goal relevance, and content richness — are blended into a single score, filtered by a tunable threshold.
- [Semantic Contradiction Detection for Memory Pipeline](./semantic-contradiction-detection-for-memory-pipeline.md) — Detects when a new memory contradicts an existing one using heuristic pattern matching (negation pairs, entity-attribute assertions, verb-based fact patterns) and optional LLM delegation. When a contradiction is detected, the superseded memory is marked rather than deleted.

### `cognitive-processing` — 1 article(s)

- [DSPy Integration Test Suite — Cognitive Processor, Adapter, and Optimizer](./dspy-integration-test-suite-cognitive-processor-adapter-and-optimizer.md) — Test suite for the DSPy-powered cognitive layer in soul-protocol, covering module instantiation, the `DSPyCognitiveProcessor` adapter bridge, training data generation, graceful fallback when DSPy is absent, and the `SoulOptimizer`. All LLM calls are mocked — no real API calls are made.

### `compaction` — 3 article(s)

- [LCM Compaction Prompt Templates: SUMMARY and BULLETS](./lcm-compaction-prompt-templates-summary-and-bullets.md) — This module provides the two LLM prompt templates used by `ThreeLevelCompactor` when a `CognitiveEngine` is available: `SUMMARY_PROMPT` for Level 1 prose summarization and `BULLETS_PROMPT` for Level 2 bullet-point compression. Both follow the `[TASK:xxx]` routing convention for compatibility with `HeuristicEngine`.
- [Test Suite: Context Compaction -- Three-Level Token Budget Enforcement](./test-suite-context-compaction-three-level-token-budget-enforcement.md) — Tests for Soul Protocol's context compaction system, which keeps conversation history within a token budget through three escalating strategies: LLM-generated summaries, bullet compression, and hard truncation. Covers zero-cost paths, summary/bullet/truncation tiers, idempotency, and guaranteed convergence.
- [ThreeLevelCompactor: Escalating Context Window Management](./threelevelcompactor-escalating-context-window-management.md) — `ThreeLevelCompactor` keeps the active context window within a token budget using three escalating strategies: LLM-generated prose summaries, LLM-generated bullet-point compressions, and deterministic head truncation. The compactor always applies the least aggressive level that achieves the budget, and guarantees convergence even without an LLM.

### `compatibility` — 1 article(s)

- [Eternal Storage Protocol Re-Export Layer](./eternal-storage-protocol-re-export-layer.md) — A thin compatibility shim that re-exports the three canonical eternal storage types — `EternalStorageProvider`, `ArchiveResult`, and `RecoverySource` — from the spec layer into the runtime package. It exists so callers already importing from `soul_protocol.runtime.eternal.protocol` continue to work after those definitions were moved to `soul_protocol.spec.eternal.protocol`.

### `compression` — 2 article(s)

- [Rule-Based Memory Compressor](./rule-based-memory-compressor.md) — Provides `MemoryCompressor`, a SimpleMem-inspired compression pipeline that reduces memory footprint through deduplication, importance-based pruning, and grouped summarization — entirely without LLM calls.
- [Test Suite: Memory Compression -- Summarization, Deduplication, and Importance Pruning](./test-suite-memory-compression-summarization-deduplication-and-importance-pruning.md) — Tests for `MemoryCompressor`, which reduces the size of a soul's long-term memory store through four strategies: text summarization, near-duplicate removal, importance-based pruning, and export splitting. Covers edge cases like empty inputs, threshold tuning, and the rule that high-importance memories are never pruned regardless of age.

### `concurrency` — 1 article(s)

- [SQLite WAL-Mode Journal Backend](./sqlite-wal-mode-journal-backend.md) — Implements `JournalBackend` using SQLite with WAL mode, providing append-only event storage with strict sequential ordering and monotonic timestamp enforcement. A single threading lock serializes all writes while allowing concurrent readers through WAL isolation.

### `configuration` — 8 article(s)

- [Bundled Soul Templates: Role Archetype Registry](./bundled-soul-templates-role-archetype-registry.md) — The `soul_protocol.templates` package ships four pre-built YAML role archetypes — Arrow (sales), Flash (content), Cyborg (recruiting), and Analyst (research) — alongside a small discovery API. It gives consumers a zero-configuration starting point while keeping custom templates fully supported via path-based loading.
- [engine_from_env: Auto-Detect Best Available Cognitive Engine](./enginefromenv-auto-detect-best-available-cognitive-engine.md) — Provides `engine_from_env()`, a zero-configuration helper that inspects environment variables to select the most capable available cognitive engine. Priority order is Anthropic, OpenAI, Ollama, then HeuristicEngine as the always-available fallback.
- [Experiment Configuration and Enum Definitions for Soul Protocol Research](./experiment-configuration-and-enum-definitions-for-soul-protocol-research.md) — Defines the `ExperimentConfig` dataclass and supporting enums (`MemoryCondition`, `UseCase`) that control every parameter of the Soul Protocol validation study. All experiment parameters live in one place to guarantee reproducibility — any change to scale, thresholds, or conditions is a single-file edit.
- [JSON Soul Configuration Parser (parse_soul_json)](./json-soul-configuration-parser-parsesouljson.md) — A single-function module that parses a JSON string into a validated SoulConfig using Pydantic's model_validate_json, combining deserialization and schema validation in one atomic step.
- [Logging Configuration Guide for Soul Protocol](./logging-configuration-guide-for-soul-protocol.md) — Provides three ready-to-use logging configurations for the soul-protocol runtime — a human-readable development setup, a structured JSON production setup, and a dict-based config compatible with YAML/JSON config files. All configurations target the `soul_protocol.runtime` logger namespace to give operators precise control over verbosity without silencing third-party libraries.
- [Markdown Soul Parser: soul.md to SoulConfig Conversion](./markdown-soul-parser-soulmd-to-soulconfig-conversion.md) — Parses human-authored soul.md files into SoulConfig, supporting optional YAML frontmatter for structured metadata alongside free-form markdown sections for identity, personality, core memory, and DNA. Automatically generates a DID if one is not provided.
- [Parsers Subpackage: Soul Configuration File Loader Registry](./parsers-subpackage-soul-configuration-file-loader-registry.md) — The parsers subpackage is the namespace for soul configuration format readers — soul.md, soul.yaml, and soul.json parsers live here. The __init__.py is intentionally empty so each parser module is imported directly, avoiding circular dependency risks.
- [YAML Soul Configuration Parser (parse_soul_yaml)](./yaml-soul-configuration-parser-parsesoulyaml.md) — Parses a YAML soul configuration string into a SoulConfig using yaml.safe_load for deserialization and Pydantic model_validate for schema enforcement, with a two-step approach that separates parsing errors from validation errors.

### `consistency` — 1 article(s)

- [Semantic Contradiction Detection for Memory Pipeline](./semantic-contradiction-detection-for-memory-pipeline.md) — Detects when a new memory contradicts an existing one using heuristic pattern matching (negation pairs, entity-attribute assertions, verb-based fact patterns) and optional LLM delegation. When a contradiction is detected, the superseded memory is marked rather than deleted.

### `consolidation` — 1 article(s)

- [Test Suite for Memory Consolidation, Reflection, and Fact Supersession](./test-suite-for-memory-consolidation-reflection-and-fact-supersession.md) — Validates the consolidation pipeline that transforms raw episodic memories into structured semantic knowledge — converting summaries into semantic memories, grouping episodes under general-event themes, storing self-insight and emotional patterns, and superseding outdated facts. Also tests the reflect() entry point and the apply=False dry-run mode.

### `context management` — 13 article(s)

- [Context Package Exports: LCMContext and SQLiteContextStore](./context-package-exports-lcmcontext-and-sqlitecontextstore.md) — The `runtime/context` package's `__init__.py` provides a clean public surface for the Lossless Context Management (LCM) subsystem, re-exporting `LCMContext` (the reference `ContextEngine` implementation) and `SQLiteContextStore` (the persistence layer) so consumers import from a single stable path rather than internal submodules.
- [Context Subpackage Interface: Lossless Context Management Spec Layer](./context-subpackage-interface-lossless-context-management-spec-layer.md) — The `spec/context/__init__.py` re-exports all Lossless Context Management (LCM) primitives — the `ContextEngine` protocol and seven Pydantic models — as the public interface for context window management. This subpackage is spec-only: no opinionated implementations, no runtime imports.
- [ContextEngine Protocol: Interface for Lossless Context Window Management](./contextengine-protocol-interface-for-lossless-context-window-management.md) — `ContextEngine` is a `@runtime_checkable` Python `Protocol` that defines the six operations any context management implementation must provide: `ingest`, `assemble`, `grep`, `expand`, `describe`, and `compact`. It decouples the soul runtime from any specific context engine implementation.
- [LCM Compaction Prompt Templates: SUMMARY and BULLETS](./lcm-compaction-prompt-templates-summary-and-bullets.md) — This module provides the two LLM prompt templates used by `ThreeLevelCompactor` when a `CognitiveEngine` is available: `SUMMARY_PROMPT` for Level 1 prose summarization and `BULLETS_PROMPT` for Level 2 bullet-point compression. Both follow the `[TASK:xxx]` routing convention for compatibility with `HeuristicEngine`.
- [LCM Data Models: Context Messages, DAG Nodes, and Compaction Levels](./lcm-data-models-context-messages-dag-nodes-and-compaction-levels.md) — The LCM data models define the atomic units of Lossless Context Management: `ContextMessage` (immutable ingested messages), `ContextNode` (compacted DAG nodes), `CompactionLevel` (verbatim to truncated), and result types for assembly, search, expansion, and metadata queries.
- [LCM Retrieval Tools: grep, expand, and describe](./lcm-retrieval-tools-grep-expand-and-describe.md) — This module provides three retrieval functions — `grep`, `expand`, and `describe` — that expose different views into the immutable LCM message store. `grep` searches by regex, `expand` recursively reconstructs original messages from compacted DAG nodes, and `describe` returns a metadata snapshot of the entire store.
- [LCMContext: Reference Lossless Context Management Engine](./lcmcontext-reference-lossless-context-management-engine.md) — `LCMContext` is soul-protocol's reference implementation of the `ContextEngine` protocol. It ingests conversation messages into an append-only SQLite store, assembles token-bounded context windows with automatic three-level compaction, and exposes grep/expand/describe retrieval tools — all without requiring a `Soul` instance.
- [SQLiteContextStore: Immutable Message Store and Compaction DAG](./sqlitecontextstore-immutable-message-store-and-compaction-dag.md) — `SQLiteContextStore` is the persistence layer for LCM, implementing an append-only SQLite database with three tables: immutable conversation messages, compaction DAG nodes, and DAG edges. It uses `asyncio.to_thread()` to make synchronous SQLite calls safe in async code without adding any external dependencies.
- [Test Suite: Context Compaction -- Three-Level Token Budget Enforcement](./test-suite-context-compaction-three-level-token-budget-enforcement.md) — Tests for Soul Protocol's context compaction system, which keeps conversation history within a token budget through three escalating strategies: LLM-generated summaries, bullet compression, and hard truncation. Covers zero-cost paths, summary/bullet/truncation tiers, idempotency, and guaranteed convergence.
- [Test Suite: Context Spec Models -- Pydantic Schemas for Conversation Context](./test-suite-context-spec-models-pydantic-schemas-for-conversation-context.md) — Tests for the Pydantic models that define Soul Protocol's conversation context data layer, including `CompactionLevel` enum, `ContextMessage`, `ContextNode`, and the result types (`AssembleResult`, `GrepResult`, `ExpandResult`, `DescribeResult`). Validates defaults, serialization round-trips, unique ID generation, and string-enum comparability.
- [Test Suite: Context Store Retrieval — Grep, Expand, and Describe](./test-suite-context-store-retrieval-grep-expand-and-describe.md) — Validates the three retrieval operations on Soul Protocol's conversation context store: regex-based message search (`grep`), DAG node expansion to recover original messages (`expand`), and metadata snapshot generation (`describe`). These operations power agent tools that let an LLM inspect its own conversation history without re-loading the full context window.
- [Test Suite: Context Store -- SQLite-Backed Conversation History](./test-suite-context-store-sqlite-backed-conversation-history.md) — Tests for Soul Protocol's SQLite-backed context store, which persists conversation messages and compaction nodes with sequential ordering, full-text grep, date-range filtering, and cross-reconnect durability. Covers both in-memory (test) and file-backed (persistence) store configurations.
- [ThreeLevelCompactor: Escalating Context Window Management](./threelevelcompactor-escalating-context-window-management.md) — `ThreeLevelCompactor` keeps the active context window within a token budget using three escalating strategies: LLM-generated prose summaries, LLM-generated bullet-point compressions, and deterministic head truncation. The compactor always applies the least aggressive level that achieves the budget, and guarantees convergence even without an LLM.

### `context store` — 1 article(s)

- [Test Suite: Context Store -- SQLite-Backed Conversation History](./test-suite-context-store-sqlite-backed-conversation-history.md) — Tests for Soul Protocol's SQLite-backed context store, which persists conversation messages and compaction nodes with sequential ordering, full-text grep, date-range filtering, and cross-reconnect durability. Covers both in-memory (test) and file-backed (persistence) store configurations.

### `context-management` — 2 article(s)

- [Runtime Package: Reference Implementation Public API](./runtime-package-reference-implementation-public-api.md) — The runtime package `__init__.py` defines the public API surface for soul-protocol's reference implementation, currently exporting the Lossless Context Management (LCM) and evaluation subsystems. It serves as the 'batteries-included' layer built on top of the lower-level spec primitives.
- [Test Suite for LCMContext (Long-Context Memory)](./test-suite-for-lcmcontext-long-context-memory.md) — Integration tests for `LCMContext`, the context engine that ingests conversation messages, assembles token-budgeted context windows, and auto-compacts history using a `CognitiveEngine`. Covers protocol compliance, initialization guards, token estimation, compaction triggers, grep search, and lifecycle close/reopen semantics.

### `contradiction` — 1 article(s)

- [Test Suite for Memory Contradiction Detection (Heuristic and LLM Modes)](./test-suite-for-memory-contradiction-detection-heuristic-and-llm-modes.md) — Validates the contradiction detector that identifies when a new memory conflicts with existing ones — using both fast heuristic rules (negation patterns, entity-attribute conflicts) and LLM-powered analysis. Tests cover true-positive detection, false-positive prevention, superseded-memory exclusion, similarity thresholds, and graceful fallback when the LLM errors.

### `contradiction detection` — 1 article(s)

- [Test Suite: Cross-Session Contradiction Detection in the Observe Pipeline](./test-suite-cross-session-contradiction-detection-in-the-observe-pipeline.md) — End-to-end regression tests for Soul Protocol's contradiction detection, targeting a specific bug where heuristic contradiction detection fired in isolation but silently failed during `observe()` when a user updated a fact across sessions (e.g., location or employer change). Tests verify that new facts correctly supersede old ones.

### `conversation history` — 1 article(s)

- [Test Suite: Context Store Retrieval — Grep, Expand, and Describe](./test-suite-context-store-retrieval-grep-expand-and-describe.md) — Validates the three retrieval operations on Soul Protocol's conversation context store: regex-based message search (`grep`), DAG node expansion to recover original messages (`expand`), and metadata snapshot generation (`describe`). These operations power agent tools that let an LLM inspect its own conversation history without re-loading the full context window.

### `core API` — 1 article(s)

- [Test Suite: Core Soul API — Birth, Memory, Emotion, Export, and Psychology Pipeline](./test-suite-core-soul-api-birth-memory-emotion-export-and-psychology-pipeline.md) — The primary integration test suite for the `Soul` class, covering the full API surface from construction through the psychology pipeline. It validates that soul birth, memory operations, emotional state, import/export round-trips, system prompt generation, and the attention gate all work correctly together.

### `core architecture` — 2 article(s)

- [CognitiveEngine Protocol, HeuristicEngine, and CognitiveProcessor](./cognitiveengine-protocol-heuristicengine-and-cognitiveprocessor.md) — This module defines the core cognitive abstraction layer for soul-protocol: the `CognitiveEngine` protocol (the one interface consumers implement), `HeuristicEngine` (the zero-dependency regex fallback), and `CognitiveProcessor` (the internal orchestrator that turns raw LLM responses into typed memory objects). Together they decouple memory pipeline logic from any specific LLM provider.
- [LCMContext: Reference Lossless Context Management Engine](./lcmcontext-reference-lossless-context-management-engine.md) — `LCMContext` is soul-protocol's reference implementation of the `ContextEngine` protocol. It ingests conversation messages into an append-only SQLite store, assembles token-bounded context windows with automatic three-level compaction, and exposes grep/expand/describe retrieval tools — all without requiring a `Soul` instance.

### `core primitives` — 1 article(s)

- [SoulContainer: The Schema-Free Portable Soul Primitive](./soulcontainer-the-schema-free-portable-soul-primitive.md) — `SoulContainer` is the core-layer primitive introduced in v0.4.0 — a schema-free portable AI identity that holds an `Identity` and a `MemoryStore` with no opinions about personality models, cognitive engines, or evolution systems. It supports a `create`/`open`/`save` lifecycle for `.soul` file archives.

### `core-memory` — 1 article(s)

- [Regression Tests for Bug #15: Core Memory Replace vs. Append](./regression-tests-for-bug-15-core-memory-replace-vs-append.md) — This regression suite locks the fix for Bug #15, where `edit_core_memory()` was appending new values to existing persona and human fields instead of replacing them. The tests verify replace semantics, partial-field isolation, and the explicit absence of the old append behavior.

### `crypto` — 1 article(s)

- [Symmetric Encryption for Soul Files (Fernet + PBKDF2)](./symmetric-encryption-for-soul-files-fernet-pbkdf2.md) — Provides passphrase-based encryption and decryption for soul file data using Fernet symmetric encryption with PBKDF2-HMAC-SHA256 key derivation. The salt is embedded in the ciphertext so only the passphrase is needed for decryption — no separate salt management required.

### `cryptography` — 4 article(s)

- [AES-256-GCM Encryption for Soul Archive Files](./aes-256-gcm-encryption-for-soul-archive-files.md) — `crypto.py` provides password-based encryption and decryption for `.soul` file contents using AES-256-GCM authenticated encryption with scrypt key derivation. Each call to `encrypt_blob` generates fresh randomness, ensuring that the same password encrypts the same content to a different ciphertext every time.
- [Crypto Package Exports: Soul File Encryption Helpers](./crypto-package-exports-soul-file-encryption-helpers.md) — The `runtime/crypto` package's `__init__.py` re-exports `derive_key`, `encrypt_data`, and `decrypt_data` from the internal `encrypt` module, providing a clean single-import path for the symmetric encryption primitives used to protect `.soul` archive files.
- [Cryptographic Encryption Test Suite (Fernet + PBKDF2)](./cryptographic-encryption-test-suite-fernet-pbkdf2.md) — Test suite validating the `soul_protocol.runtime.crypto.encrypt` module, which provides passphrase-based symmetric encryption for soul data. Covers the full encrypt/decrypt roundtrip, wrong-passphrase rejection, and deterministic key derivation.
- [DID Generation for Digital Souls](./did-generation-for-digital-souls.md) — Generates decentralized identifiers (DIDs) for soul instances using the `did:soul:` method. Each DID is deterministically random — human-readable yet collision-resistant — by combining the soul's name with a UUID4-seeded SHA-256 hash.

### `data lifecycle` — 1 article(s)

- [CLI Test Suite: Full Onboarding, Multi-User Lifecycle, and Destroy Workflow](./cli-test-suite-full-onboarding-multi-user-lifecycle-and-destroy-workflow.md) — End-to-end test suite for the `soul init` onboarding wizard covering user scope creation, fleet provisioning, status reporting, soul deletion, and the destroy command that archives then wipes all data. Also validates journal append-only privacy constraints and users-dir precedence rules.

### `data models` — 3 article(s)

- [LCM Data Models: Context Messages, DAG Nodes, and Compaction Levels](./lcm-data-models-context-messages-dag-nodes-and-compaction-levels.md) — The LCM data models define the atomic units of Lossless Context Management: `ContextMessage` (immutable ingested messages), `ContextNode` (compacted DAG nodes), `CompactionLevel` (verbatim to truncated), and result types for assembly, search, expansion, and metadata queries.
- [Runtime Types: The Complete Soul Protocol Pydantic Model Library](./runtime-types-the-complete-soul-protocol-pydantic-model-library.md) — `types.py` is the canonical data model layer for Soul Protocol — over 30 Pydantic models covering identity, personality (OCEAN), psychology-informed memory, emotional state, evolution, and interaction. It is the schema backbone that all other runtime modules depend on.
- [Test Suite: Context Spec Models -- Pydantic Schemas for Conversation Context](./test-suite-context-spec-models-pydantic-schemas-for-conversation-context.md) — Tests for the Pydantic models that define Soul Protocol's conversation context data layer, including `CompactionLevel` enum, `ContextMessage`, `ContextNode`, and the result types (`AssembleResult`, `GrepResult`, `ExpandResult`, `DescribeResult`). Validates defaults, serialization round-trips, unique ID generation, and string-enum comparability.

### `data-flow` — 1 article(s)

- [Memory Categories and Salience Scoring Example](./memory-categories-and-salience-scoring-example.md) — Demonstrates Soul Protocol's v0.2.3 memory extraction taxonomy by walking through the observe pipeline with five realistic interactions, then inspecting how the system assigns categories, salience scores, and memory types to extracted facts. The example also shows the deduplication pipeline and progressive content loading (L0/L1/L2) through targeted recall queries.

### `data-models` — 2 article(s)

- [Test Suite: Core Pydantic Data Models](./test-suite-core-pydantic-data-models.md) — Validates the foundational Pydantic models in soul_protocol.types — Identity defaults, Personality trait bounds, SoulConfig JSON serialization round-trip, MemoryEntry defaults, Mood enum completeness, and EvolutionMode enum values.
- [Test Suite: Multi-Participant Interactions and Multi-Bond Identity](./test-suite-multi-participant-interactions-and-multi-bond-identity.md) — Tests for the multi-participant Interaction model (issue #95) and multi-bond Identity (issue #94), including full backward compatibility with the legacy user_input/agent_output API, serialization round-trips, and coverage of the spec-layer equivalents.

### `database` — 2 article(s)

- [Journal SQLite Schema and Migration Helper](./journal-sqlite-schema-and-migration-helper.md) — Defines the SQLite schema for the soul-protocol journal engine and provides an idempotent migration function. Ensures the on-disk database is always brought up to the current schema version before use, preventing schema drift between engine upgrades.
- [SQLite WAL-Mode Journal Backend](./sqlite-wal-mode-journal-backend.md) — Implements `JournalBackend` using SQLite with WAL mode, providing append-only event storage with strict sequential ordering and monotonic timestamp enforcement. A single threading lock serializes all writes while allowing concurrent readers through WAL isolation.

### `decentralized storage` — 1 article(s)

- [Eternal Storage Spec Package — Public Interface for Permanent Soul Archival](./eternal-storage-spec-package-public-interface-for-permanent-soul-archival.md) — The `spec/eternal` package exposes the three types that define Soul Protocol's permanent archival tier: `EternalStorageProvider` (the backend protocol), `ArchiveResult` (what an archive operation returns), and `RecoverySource` (what a soul can be restored from). All implementation details live in the engine layer.

### `decision tracing` — 2 article(s)

- [Decision Trace Models: Structured Agent Proposal and Human Correction Events](./decision-trace-models-structured-agent-proposal-and-human-correction-events.md) — `spec/decisions.py` defines the payload types and helpers for the Org Journal's decision trace workstream — structuring every agent proposal, human correction, and pattern graduation as auditable, linkable `EventEntry` pairs. The module enables agents to learn from correction patterns without requiring ad-hoc feedback loops.
- [Test Suite: Decision Trace Spec — Agent Proposals, Human Corrections, and Graduation](./test-suite-decision-trace-spec-agent-proposals-human-corrections-and-graduation.md) — Validates the decision-trace spec from Workstream D of the Org Architecture RFC, covering the three payload types (`AgentProposal`, `HumanCorrection`, `DecisionGraduation`), their JSON round-trips, field validation, and the builder helpers that emit correctly shaped `EventEntry` records to the org journal.

### `deduplication` — 3 article(s)

- [Semantic Memory Deduplication and Fact Reconciliation](./semantic-memory-deduplication-and-fact-reconciliation.md) — Implements `reconcile_fact()`, the gating function that decides whether a new semantic fact should be created, merged into an existing entry, or skipped as a near-duplicate. Uses an enhanced Jaccard similarity with a containment coefficient to handle enriched/superset facts correctly.
- [Test Suite for Memory Deduplication (Jaccard Similarity, Short-Token False Positives, and Containment Merge)](./test-suite-for-memory-deduplication-jaccard-similarity-short-token-false-positiv.md) — Validates the deduplication system that prevents near-identical facts from being stored multiple times, covering the tokenizer, Jaccard similarity computation, short-token false-positive prevention (go vs. js, ai vs. ml), fact reconciliation decisions (CREATE/SKIP/MERGE), and the containment-merge logic for enriched facts.
- [Test Suite: Phase 2 Memory Architecture (Categories, Salience, Dedup)](./test-suite-phase-2-memory-architecture-categories-salience-dedup.md) — Tests for the Phase 2 memory architecture improvements: the MemoryCategory enum, generated abstracts, salience scoring, the deduplication reconcile_fact pipeline, TemporalEdge metadata in the knowledge graph, and the EpisodicStore entry-update API.

### `demo` — 1 article(s)

- [Interactive Rich TUI Demo — `demo.py`](./interactive-rich-tui-demo-demopy.md) — A 5-act interactive terminal walkthrough of soul-protocol that demonstrates memory storage, emotional state, OCEAN personality, and cognitive processing without requiring an LLM or API key. Designed for GIF recording and first-run discovery.

### `dependencies` — 1 article(s)

- [Test Suite for Base Install Invariants (Issue #157)](./test-suite-for-base-install-invariants-issue-157.md) — Regression tests that verify a bare `pip install soul-protocol` produces a working CLI, by checking that click, rich, pyyaml, and cryptography are declared as base dependencies, the deprecated `[engine]` extra still exists for backwards compatibility, and the CLI module imports cleanly without optional extras.

### `deserialization` — 1 article(s)

- [Soul Archive Unpacker: Loading .soul ZIP Archives](./soul-archive-unpacker-loading-soul-zip-archives.md) — `unpack_soul` deserializes a `.soul` ZIP archive back into a `(SoulConfig, memory_data)` tuple, handling both encrypted and unencrypted archives with clear error separation between missing-password and wrong-password scenarios. It is the read-side counterpart to `pack_soul` and is called on every `awaken()` operation.

### `developer-experience` — 1 article(s)

- [Developer Onboarding Demo Smoke Test](./developer-onboarding-demo-smoke-test.md) — A minimal async smoke test that runs the soul-protocol onboarding demo end-to-end in a non-interactive mode. It uses `monkeypatch` to set `SOUL_DEMO_NO_PAUSE=1`, preventing the demo from blocking on user input during CI.

### `developer-tools` — 3 article(s)

- [Interactive Rich TUI Demo — `demo.py`](./interactive-rich-tui-demo-demopy.md) — A 5-act interactive terminal walkthrough of soul-protocol that demonstrates memory storage, emotional state, OCEAN personality, and cognitive processing without requiring an LLM or API key. Designed for GIF recording and first-run discovery.
- [Memory Evolution Simulation Script](./memory-evolution-simulation-script.md) — A standalone simulation that exercises 100 interactions to demonstrate memory compression, archival storage growth, and temporal knowledge-graph evolution in soul-protocol. It serves as a developer-facing proof that the memory pipeline works end-to-end without a live LLM.
- [Module Entry Point — `python -m soul_protocol`](./module-entry-point-python-m-soulprotocol.md) — A minimal `__main__.py` that enables running soul-protocol as a module with `python -m soul_protocol`, delegating immediately to the interactive demo. The `__name__` guard prevents accidental execution on import.

### `development-process` — 1 article(s)

- [Spike Namespace: In-Progress Design Experiments](./spike-namespace-in-progress-design-experiments.md) — The `spike/` package is a dedicated quarantine zone for experimental code that is under active design validation but not yet ready for the public API. It exists to allow risky ideas to be tested inside the full suite without any risk of accidental consumer imports.

### `disaster-recovery` — 1 article(s)

- [Test Suite for JournalBackedMemoryStore: Unit, Smoke, E2E, and Real-World Scenarios](./test-suite-for-journalbackedmemorystore-unit-smoke-e2e-and-real-world-scenarios.md) — This comprehensive test suite for `JournalBackedMemoryStore` follows a four-tier discipline — unit, smoke, E2E, and real-world simulations — covering every public method and the failure modes that motivated the journal architecture. Special attention is given to disaster recovery (the April 2026 cleanup incident), GDPR-compliant tombstone deletion, idempotent rebuild, and latency budgets.

### `dream-engine` — 1 article(s)

- [Dream Engine Test Suite — Memory Consolidation and Behavioral Analysis](./dream-engine-test-suite-memory-consolidation-and-behavioral-analysis.md) — Comprehensive test suite for the soul-protocol `dream` subsystem, which performs offline memory consolidation: clustering topics, detecting recurring procedures, pruning duplicates, and suggesting personality evolution. Tests span unit-level phase functions through full integration scenarios with before/after memory state comparisons.

### `dspy` — 1 article(s)

- [DSPy Integration Test Suite — Cognitive Processor, Adapter, and Optimizer](./dspy-integration-test-suite-cognitive-processor-adapter-and-optimizer.md) — Test suite for the DSPy-powered cognitive layer in soul-protocol, covering module instantiation, the `DSPyCognitiveProcessor` adapter bridge, training data generation, graceful fallback when DSPy is absent, and the `SoulOptimizer`. All LLM calls are mocked — no real API calls are made.

### `dspy-optimization` — 2 article(s)

- [DSPy Optimization Pipeline: MIPROv2-Based Significance Gate and Query Expansion Tuning](./dspy-optimization-pipeline-miprov2-based-significance-gate-and-query-expansion-t.md) — This pipeline runs a four-phase experiment: baseline heuristic ablation, DSPy MIPROv2 optimization of the significance gate and query expander, post-optimization ablation with the trained modules active, and saved reports comparing before/after metrics. It exists to replace hand-tuned heuristics with learned prompts that generalize across diverse interaction types.
- [DSPy Training Dataset Generator](./dspy-training-dataset-generator.md) — Generates labeled training data for DSPy-optimized memory significance and recall query expansion. Produces two dataset types—significance classification and query expansion—split into train/val sets for fine-tuning the soul's memory filtering models.

### `embeddings` — 23 article(s)

- [Embedding Provider Factory Test Suite](./embedding-provider-factory-test-suite.md) — Tests for `get_embedding_provider()`, the factory function that instantiates embedding backends by name. Covers all built-in providers (hash, TF-IDF), external providers with lazy imports (sentence-transformers, OpenAI, Ollama), kwargs passthrough, unknown provider errors, and import failure handling.
- [Embedding Provider Protocol Compliance Test Suite](./embedding-provider-protocol-compliance-test-suite.md) — Tests that verify `HashEmbedder` and `TFIDFEmbedder` correctly satisfy the `EmbeddingProvider` structural protocol at runtime. Also validates that partial or non-compliant implementations fail the `isinstance` check, ensuring the protocol is genuinely enforced rather than nominal.
- [EmbeddingProvider Protocol — Pluggable Embedding Backend Interface](./embeddingprovider-protocol-pluggable-embedding-backend-interface.md) — `EmbeddingProvider` is a runtime-checkable `Protocol` class that defines the three-method contract any embedding backend must satisfy. It decouples Soul Protocol's retrieval and search features from any specific ML framework by requiring only `dimensions`, `embed`, and `embed_batch`.
- [EmbeddingProvider Protocol Re-export (Runtime Shim)](./embeddingprovider-protocol-re-export-runtime-shim.md) — This file is a one-line shim that re-exports `EmbeddingProvider` from its canonical location in `soul_protocol.spec.embeddings.protocol` into the runtime embeddings namespace. It exists to provide a stable import path for runtime code while keeping the authoritative protocol definition in the spec layer.
- [Embeddings Spec Package — Public API Surface](./embeddings-spec-package-public-api-surface.md) — The `spec/embeddings` package defines the protocol-level public interface for all embedding operations in Soul Protocol. It re-exports the `EmbeddingProvider` protocol and three vector math primitives so consumers import from a single stable namespace.
- [Embeddings Subsystem: Factory and Public API](./embeddings-subsystem-factory-and-public-api.md) — The embeddings `__init__.py` provides the `get_embedding_provider()` factory function and re-exports all public types from the embedding subsystem. The factory uses lazy imports for optional providers (sentence-transformers, OpenAI, Ollama) so that users without those dependencies installed do not pay an import cost or receive confusing `ImportError` on module load.
- [Embeddings Test Package Initializer](./embeddings-test-package-initializer.md) — Package initializer for the `tests/test_embeddings/` test subpackage, created to group all embedding-related test modules under a common namespace. The file contains no executable code — its presence makes the directory a Python package discoverable by pytest.
- [End-to-End Vector Search Test Suite — TF-IDF Semantic Memory Retrieval](./end-to-end-vector-search-test-suite-tf-idf-semantic-memory-retrieval.md) — End-to-end tests for soul-protocol's semantic memory search pipeline using `TFIDFEmbedder` and `VectorSearchStrategy`. Tests create memory entries on distinct topics, fit the embedder, and verify that topical queries return relevant results in the correct order.
- [Hash Embedder: Deterministic Pseudo-Embeddings for Testing](./hash-embedder-deterministic-pseudo-embeddings-for-testing.md) — The `HashEmbedder` produces deterministic fixed-length vectors from text using character n-gram hashing — no external dependencies, no model downloads, same input always yields the same output. It is intentionally non-semantic (similar texts do not produce similar vectors) and exists solely to enable embedding pipeline tests without requiring real embedding backends.
- [Hash Embedder Test Suite — Determinism, Normalization, and Batch Embedding](./hash-embedder-test-suite-determinism-normalization-and-batch-embedding.md) — Test suite for `HashEmbedder`, soul-protocol's zero-dependency embedding backend that converts text to L2-normalized vectors via hashing. Covers dimensionality configuration, deterministic output, L2 normalization, edge cases (empty strings, whitespace), and batch embedding consistency.
- [Ollama Embedding Provider: Local Semantic Embeddings](./ollama-embedding-provider-local-semantic-embeddings.md) — The `OllamaEmbeddingProvider` generates real semantic embeddings by calling a locally-running Ollama server, requiring no API key and keeping data on-device. It lazily initializes the Ollama client on first use and auto-detects vector dimensionality from the first embedding call.
- [Ollama Embedding Provider Test Suite — Local Model Integration with Lazy Loading](./ollama-embedding-provider-test-suite-local-model-integration-with-lazy-loading.md) — Test suite for `OllamaEmbeddingProvider`, which connects to a locally-running Ollama server for embedding generation. Tests use a mocked `ollama` library to avoid requiring a real server in CI, covering protocol compliance, lazy client initialization, batch embedding, custom model and base URL configuration, and graceful import error handling.
- [OpenAI Embedding Provider Test Suite — API Key Validation, Retry Logic, and Batch Embedding](./openai-embedding-provider-test-suite-api-key-validation-retry-logic-and-batch-em.md) — Test suite for `OpenAIEmbeddingProvider`, which calls the OpenAI embeddings API for high-quality vector representations. Tests use a mocked `openai` library and cover protocol compliance, API key validation, exponential backoff retry logic for rate limits, model dimension detection, and graceful import error handling.
- [OpenAI Embedding Provider with Retry Backoff](./openai-embedding-provider-with-retry-backoff.md) — The `OpenAIEmbeddingProvider` produces high-quality semantic embeddings via the OpenAI API, with built-in exponential backoff for rate limits and transient server errors. It lazily initializes the OpenAI client, pre-knows dimensions for standard models to avoid a probe call, and guarantees input ordering even when the API returns results out of order.
- [Sentence Transformer Embedding Provider Test Suite — Lazy Loading and Mock-Based Validation](./sentence-transformer-embedding-provider-test-suite-lazy-loading-and-mock-based-v.md) — Test suite for `SentenceTransformerProvider`, which wraps the `sentence-transformers` library for high-quality local embedding generation. Tests mock the heavy ML dependency to run in CI, covering protocol compliance, lazy model loading, batch embedding, custom model and device configuration, and import error handling.
- [SentenceTransformer Embedding Provider: Local Semantic Embeddings](./sentencetransformer-embedding-provider-local-semantic-embeddings.md) — The `SentenceTransformerProvider` generates real semantic embeddings using the `sentence-transformers` library, with lazy model loading to defer the heavy import until first use. The default model `all-MiniLM-L6-v2` balances quality and speed with 384-dimensional normalized vectors, and supports GPU acceleration via configurable device selection.
- [Similarity Functions Re-export (Runtime Shim)](./similarity-functions-re-export-runtime-shim.md) — This shim re-exports the three vector similarity functions (`cosine_similarity`, `dot_product`, `euclidean_distance`) from their canonical spec-layer location into the runtime embeddings namespace. It provides a stable import path for runtime code without creating a direct dependency on the spec layer's internal structure.
- [Test Suite for TF-IDF Embedder](./test-suite-for-tf-idf-embedder.md) — Validates the TFIDFEmbedder class, which converts text into fixed-length float vectors using term-frequency/inverse-document-frequency weighting. Tests cover the fit/embed lifecycle, semantic similarity behavior, edge cases for unfitted state, and batch consistency.
- [Test Suite for Vector Similarity Functions](./test-suite-for-vector-similarity-functions.md) — Covers the three core similarity primitives — cosine similarity, Euclidean distance, and dot product — used by soul-protocol's embedding layer. Tests enforce correct mathematical behavior, zero-vector safety, empty-vector edge cases, and dimension-mismatch guards.
- [Test Suite for VectorSearchStrategy](./test-suite-for-vectorsearchstrategy.md) — Tests the VectorSearchStrategy class, which wraps an embedder to perform similarity-based memory search with configurable threshold filtering and pre-built index support. Covers both the deterministic HashEmbedder path and the semantic TFIDFEmbedder path.
- [TF-IDF Embedder: Vocabulary-Fitted Semantic Embeddings](./tf-idf-embedder-vocabulary-fitted-semantic-embeddings.md) — The `TFIDFEmbedder` builds corpus-fitted TF-IDF vectors using only Python's standard library — no external dependencies. It produces semantically meaningful similarity (unlike `HashEmbedder`) but requires a `fit()` call on a corpus before embedding, making it useful for lightweight production use cases where a known document set exists.
- [Vector Similarity Functions — Pure-Python Cosine, Euclidean, and Dot Product](./vector-similarity-functions-pure-python-cosine-euclidean-and-dot-product.md) — This module provides three zero-dependency vector similarity functions (`cosine_similarity`, `euclidean_distance`, `dot_product`) using only the Python standard library. All three include mismatch guards added in 2026-03-06 to catch incompatible vectors from different embedding backends before producing silent garbage results.
- [VectorSearchStrategy: Embedding-Based Semantic Memory Search](./vectorsearchstrategy-embedding-based-semantic-memory-search.md) — The `VectorSearchStrategy` wraps any `EmbeddingProvider` to implement semantic similarity search over memory entries. It maintains a pre-built index for performance, falls back to on-the-fly embedding for un-indexed candidates, and uses cosine similarity with a configurable threshold to rank and filter results.

### `emotion` — 4 article(s)

- [D2 Emotional Intelligence Evaluator](./d2-emotional-intelligence-evaluator.md) — Evaluates four aspects of emotional intelligence: sentiment classification accuracy, significance gate calibration (emotional vs. neutral filtering), mood state responsiveness, and multi-phase emotional arc coherence. Contributes 20% of the Soul Health Score.
- [Heuristic Sentiment Detection for Somatic Memory Markers](./heuristic-sentiment-detection-for-somatic-memory-markers.md) — Provides zero-dependency emotion detection using curated word lists and a valence-arousal coordinate model, returning a SomaticMarker that the memory pipeline attaches to entries for emotional recall prioritization. Implements Damasio's Somatic Marker Hypothesis without requiring an LLM call.
- [Test Suite: Heuristic Sentiment Detection (Somatic Markers)](./test-suite-heuristic-sentiment-detection-somatic-markers.md) — Tests for the heuristic sentiment detector that produces Damasio-style somatic markers (valence, arousal, label) from raw text, covering neutral text, positive/negative words, intensifiers, negation, mixed emotions, and label accuracy.
- [Test Suite: Known Sentiment Detector Gaps](./test-suite-known-sentiment-detector-gaps.md) — A bug-documenting test suite that captures known failure modes in the heuristic sentiment detector — missing vocabulary, joy/excitement confusion, sadness/frustration confusion, gratitude misclassification, and neutral false positives — intended to go green once the corresponding fixes land in sentiment.py.

### `emotional-state` — 1 article(s)

- [Test Suite for StateManager: Mood Inertia, Label Mapping, Energy, and Auto-Regen](./test-suite-for-statemanager-mood-inertia-label-mapping-energy-and-auto-regen.md) — This test suite covers the `StateManager` component responsible for tracking a soul's emotional and physical state across interactions. It validates EMA-based mood inertia, label-to-mood mapping, the TIRED energy override, and the fully configurable `Biorhythms` parameter set including time-based auto-regeneration — with a particular focus on preventing mood instability and ensuring always-on agent deployments work correctly.

### `encryption` — 2 article(s)

- [AES-256-GCM Encryption for Soul Archive Files](./aes-256-gcm-encryption-for-soul-archive-files.md) — `crypto.py` provides password-based encryption and decryption for `.soul` file contents using AES-256-GCM authenticated encryption with scrypt key derivation. Each call to `encrypt_blob` generates fresh randomness, ensuring that the same password encrypts the same content to a different ciphertext every time.
- [Test Suite for Soul File Encryption](./test-suite-for-soul-file-encryption.md) — Validates the full encryption stack for .soul files, from low-level blob crypto primitives through pack/unpack pipeline integration to the high-level Soul.export() and Soul.awaken() API. Covers round-trips, wrong-password errors, backward compatibility with unencrypted files, and manifest readability without a password.

### `end-to-end` — 1 article(s)

- [End-to-End Test Suite for Eternal Storage Lifecycle](./end-to-end-test-suite-for-eternal-storage-lifecycle.md) — End-to-end tests covering the full eternal storage lifecycle: birth a soul, archive to multiple tiers, verify archives exist, recover from each tier individually, and confirm recovered data can be awakened into a valid Soul. Also tests multi-tier fallback recovery when primary sources fail.

### `engine` — 1 article(s)

- [Cognitive Engine Package: Public API and Adapter Exports](./cognitive-engine-package-public-api-and-adapter-exports.md) — Package init for the cognitive engine subsystem, exporting the base `CognitiveEngine` and `HeuristicEngine` protocols alongside all provider-specific adapters (Anthropic, OpenAI, Ollama, LiteLLM, Callable) and the `engine_from_env()` auto-detection helper.

### `episodic` — 1 article(s)

- [Episodic Memory Store — Autobiographical Interaction History](./episodic-memory-store-autobiographical-interaction-history.md) — Provides `EpisodicStore`, the in-memory repository for timestamped interaction records. Supports both basic and psychology-enriched storage paths, significance-based eviction, GDPR-compliant deletion, and filtering of archived entries from search results.

### `error handling` — 1 article(s)

- [Test Suite for Soul Error Handling](./test-suite-for-soul-error-handling.md) — Verifies that Soul's three main lifecycle operations — awaken(), export(), and retire() — raise specific, descriptive exception types rather than raw Python exceptions. Also tests exception class structure to ensure callers can extract actionable information like file paths and failure reasons.

### `error-handling` — 2 article(s)

- [Journal Engine Error Hierarchy — `exceptions.py`](./journal-engine-error-hierarchy-exceptionspy.md) — Defines the four-class exception hierarchy for the journal engine, with `JournalError` as the catchable base class and three specialisations covering schema incompatibility, invariant violations, and missing records.
- [Soul Protocol Custom Exception Hierarchy](./soul-protocol-custom-exception-hierarchy.md) — `exceptions.py` defines the typed exception hierarchy for soul-protocol operations, covering file-not-found, corruption, export failures, encryption errors, and protected role violations. Using typed exceptions instead of bare `Exception` lets callers write precise `except` clauses and present actionable error messages to users.

### `eternal storage` — 11 article(s)

- [End-to-End Test Suite for Eternal Storage Lifecycle](./end-to-end-test-suite-for-eternal-storage-lifecycle.md) — End-to-end tests covering the full eternal storage lifecycle: birth a soul, archive to multiple tiers, verify archives exist, recover from each tier individually, and confirm recovered data can be awakened into a valid Soul. Also tests multi-tier fallback recovery when primary sources fail.
- [Eternal Storage Protocol — Archive, Retrieve, and Verify Interface for Permanent Soul Backends](./eternal-storage-protocol-archive-retrieve-and-verify-interface-for-permanent-sou.md) — This module defines the canonical protocol and result models for Soul Protocol's eternal (permanent) storage tier. `EternalStorageProvider` is a runtime-checkable async `Protocol` that any backend (IPFS, Arweave, local) must satisfy, while `ArchiveResult` and `RecoverySource` capture what an archival operation produces and how a soul can later be restored.
- [Eternal Storage Spec Package — Public Interface for Permanent Soul Archival](./eternal-storage-spec-package-public-interface-for-permanent-soul-archival.md) — The `spec/eternal` package exposes the three types that define Soul Protocol's permanent archival tier: `EternalStorageProvider` (the backend protocol), `ArchiveResult` (what an archive operation returns), and `RecoverySource` (what a soul can be restored from). All implementation details live in the engine layer.
- [Eternal Storage Subsystem Public API](./eternal-storage-subsystem-public-api.md) — The `eternal` subpackage `__init__.py` re-exports the four public symbols from the eternal storage subsystem: `EternalStorageProvider`, `EternalStorageManager`, `ArchiveResult`, and `RecoverySource`. This clean facade hides the internal module layout and provides a single import target for callers persisting soul data to permanent storage.
- [Eternal Storage Test Package Initializer](./eternal-storage-test-package-initializer.md) — Marks `tests/test_eternal/` as a Python package, enabling pytest discovery of the eternal storage subsystem's test modules. Created when the eternal storage subsystem was introduced on 2026-03-06.
- [EternalStorageManager: Multi-Tier Eternal Storage Orchestrator](./eternalstoragemanager-multi-tier-eternal-storage-orchestrator.md) — The `EternalStorageManager` coordinates multiple eternal storage backends (Arweave, IPFS, etc.) for archiving and recovering soul data. It registers providers by tier name, fans out archive operations across requested tiers, and attempts ordered recovery from tracked sources — failing gracefully if individual tiers are unavailable.
- [Test Suite for Eternal Storage CLI Commands](./test-suite-for-eternal-storage-cli-commands.md) — Tests the soul-protocol CLI commands for eternal storage operations: archive (with optional tier selection), eternal-status (tier health check), and recover (with failure path handling). Uses Click's CliRunner for isolated, in-process command invocation.
- [Test Suite for Eternal Storage Protocol Compliance](./test-suite-for-eternal-storage-protocol-compliance.md) — Verifies that all eternal storage providers satisfy the EternalStorageProvider protocol at runtime, and validates the Pydantic models ArchiveResult and RecoverySource for field defaults, full construction, and serialization round-trips.
- [Test Suite for Eternal Storage Providers](./test-suite-for-eternal-storage-providers.md) — Per-provider tests for the four eternal storage backends: MockIPFSProvider, MockArweaveProvider, MockBlockchainProvider, and LocalStorageProvider. Each provider is tested for archive, retrieve, verify, content addressing, unique identifiers, and error behavior on missing references.
- [Test Suite for EternalStorageManager](./test-suite-for-eternalstoragemanager.md) — Unit tests for EternalStorageManager — the orchestration layer that routes archive, recover, and verify operations across multiple storage providers. Covers provider registration/unregistration, selective tier archiving, fallback recovery chains, unavailable-provider skipping, and verification status.
- [Test Suite for Soul Eternal Storage Integration](./test-suite-for-soul-eternal-storage-integration.md) — Tests the integration between the Soul class and EternalStorageManager, covering Soul.archive(), the with_mocks() factory, the eternal= parameter on Soul.birth() and Soul.awaken(), and the export(archive=True) convenience flag.

### `eternal-storage` — 6 article(s)

- [Eternal Storage Protocol Re-Export Layer](./eternal-storage-protocol-re-export-layer.md) — A thin compatibility shim that re-exports the three canonical eternal storage types — `EternalStorageProvider`, `ArchiveResult`, and `RecoverySource` — from the spec layer into the runtime package. It exists so callers already importing from `soul_protocol.runtime.eternal.protocol` continue to work after those definitions were moved to `soul_protocol.spec.eternal.protocol`.
- [Eternal Storage Providers Package Init](./eternal-storage-providers-package-init.md) — The `eternal/providers/` package init collects all four storage provider implementations — `LocalStorageProvider`, `MockIPFSProvider`, `MockArweaveProvider`, and `MockBlockchainProvider` — and exposes them through a single import point. This makes provider selection ergonomic for callers who want to pick a backend without navigating nested modules.
- [Local Filesystem Eternal Storage Provider](./local-filesystem-eternal-storage-provider.md) — `LocalStorageProvider` archives soul data as files on the local filesystem using a hash-based naming scheme to avoid collisions. It serves as the most basic, always-available eternal storage tier — no network, no external dependency — making it suitable for development, offline use, and as a fast local cache alongside remote backends.
- [Mock Arweave Eternal Storage Provider](./mock-arweave-eternal-storage-provider.md) — `MockArweaveProvider` simulates Arweave's permanent, pay-once storage model using an in-memory dict and deterministic transaction IDs — no gateway required. It lets test suites and local development exercise the full Arweave archive/retrieve/verify cycle without real AR tokens or network access.
- [Mock Blockchain Soul Registry Provider](./mock-blockchain-soul-registry-provider.md) — `MockBlockchainProvider` simulates an on-chain soul registry where each soul is minted as a unique NFT-like token with a hash-based token ID. It enables testing of blockchain-based soul identity ownership without a real wallet, chain, or gas fees.
- [Mock IPFS Content-Addressed Storage Provider](./mock-ipfs-content-addressed-storage-provider.md) — `MockIPFSProvider` simulates IPFS's content-addressed storage model by generating deterministic CID-like references from the SHA-256 hash of the archived data and storing it in memory. It faithfully reproduces the key property of IPFS — the same content always produces the same address — without requiring a running IPFS node.

### `evaluation` — 20 article(s)

- [D1 Memory Recall Evaluator](./d1-memory-recall-evaluator.md) — Evaluates a soul's ability to recall planted facts across long conversations, measuring recall precision, memory storage efficiency, and resistance to adversarial "burial" noise. Contributes 20% of the composite Soul Health Score.
- [D2 Emotional Intelligence Evaluator](./d2-emotional-intelligence-evaluator.md) — Evaluates four aspects of emotional intelligence: sentiment classification accuracy, significance gate calibration (emotional vs. neutral filtering), mood state responsiveness, and multi-phase emotional arc coherence. Contributes 20% of the Soul Health Score.
- [D3 Personality Expression Evaluator](./d3-personality-expression-evaluator.md) — Tests that a soul's OCEAN personality traits and communication style are faithfully encoded in its system prompt, influence goal relevance scoring, remain stable under sustained interactions, and produce meaningfully different outputs for contrasting profiles. Contributes 15% of the Soul Health Score.
- [D4 Bond and Relationship Evaluator](./d4-bond-and-relationship-evaluator.md) — Validates the mathematical properties of the soul's bond growth system: logarithmic curve shape, emotional valence acceleration, milestone accuracy at N=50/100/200, and tier progression ordering. Contributes 15% of the Soul Health Score.
- [D5 Self-Model Evaluator](./d5-self-model-evaluator.md) — Measures the soul's ability to dynamically classify user interaction domains, emerge topic confidence quickly, isolate domains without cross-contamination, and extract relationship metadata from conversation. Contributes 15% of the Soul Health Score.
- [D6 Identity Continuity Evaluator](./d6-identity-continuity-evaluator.md) — Tests that a soul survives export/import round-trips with full fidelity—preserving DID, OCEAN traits, bond state, memory count, and recall results—and that reincarnation correctly tracks lineage across incarnation chains. Contributes 10% of the Soul Health Score.
- [D7 Portability Evaluator](./d7-portability-evaluator.md) — Verifies that a soul produces deterministic system prompts regardless of instantiation order, that recall results survive export/reimport unchanged, and that multi-hop engine swap chains preserve memory count and bond state. Contributes 5% of the Soul Health Score.
- [Evaluation UI Package Marker](./evaluation-ui-package-marker.md) — Package initializer for the `research/eval_ui/` module, created in early March 2026 as a namespace boundary for a planned web-based evaluation results viewer. Contains no executable logic.
- [Heuristic Rubric Evaluator and Evolution Trigger Detection](./heuristic-rubric-evaluator-and-evolution-trigger-detection.md) — `evaluation.py` provides a lightweight, LLM-free rubric evaluation system that scores soul interactions across dimensions like completeness, relevance, specificity, and empathy. Score history feeds the evolution system — when a soul shows a sustained high-performance streak in a domain, the evaluator surfaces a trigger that prompts a DNA mutation proposal.
- [Human Evaluation Study Web Application](./human-evaluation-study-web-application.md) — A FastAPI web application that runs a blinded A/B human evaluation study, presenting participants with two AI agents — one soul-enabled, one a plain baseline — and collecting Likert survey responses after each. The app manages sessions, randomizes agent order to eliminate position bias, and exports results to JSON and CSV for statistical analysis.
- [LLM Judge for Soul Health Score Evaluation](./llm-judge-for-soul-health-score-evaluation.md) — Uses Claude Haiku as an independent LLM judge to evaluate sentiment accuracy and personality prompt quality beyond what word-list heuristics can measure, running all verdicts in parallel and comparing results against the heuristic baseline. Provides a cost-tracked, auditable layer of evaluation for D2 and D3.
- [Research Metrics Schema — Six-Dimensional Agent Quality Measurement](./research-metrics-schema-six-dimensional-agent-quality-measurement.md) — Defines the full metric collection schema for Soul Protocol's research framework across six orthogonal dimensions: retrieval quality, emotional tracking, personality stability, memory efficiency, bond formation, and skill acquisition. All metric classes are dataclasses that aggregate per-interaction observations into summary statistics for factorial analysis.
- [Research Scenario Bank: Multi-Turn Interaction Templates for Four Use Cases](./research-scenario-bank-multi-turn-interaction-templates-for-four-use-cases.md) — The scenario bank provides parameterized, multi-turn interaction sequences for four use cases — customer support, coding assistant, personal companion, and knowledge worker — each with embedded ground truth: planted facts, recall queries, emotional tone markers, and importance hints. Scenarios are personalized by `UserProfile` to increase coverage and ecological validity.
- [Runtime Package: Reference Implementation Public API](./runtime-package-reference-implementation-public-api.md) — The runtime package `__init__.py` defines the public API surface for soul-protocol's reference implementation, currently exporting the Lossless Context Management (LCM) and evaluation subsystems. It serves as the 'batteries-included' layer built on top of the lower-level spec primitives.
- [SHS Dimension Evaluators Package](./shs-dimension-evaluators-package.md) — Package marker for the `research/eval/dimensions/` directory, which houses the seven individual Soul Health Score dimension evaluators. Each sub-module implements one evaluation axis of soul quality.
- [Soul Health Score Evaluation CLI Entry Point](./soul-health-score-evaluation-cli-entry-point.md) — Thin `__main__.py` that enables `python -m research.eval.suite` as a runnable command by delegating immediately to `suite.main()`. Provides zero additional logic, keeping the entry point surface minimal.
- [Soul Health Score Evaluation Framework Package](./soul-health-score-evaluation-framework-package.md) — Package marker and documentation anchor for the 7-dimension Soul Health Score (SHS) evaluation framework. Signals to Python's import system that `research/eval/` is a package while documenting the framework's creation context.
- [Soul Health Score Evaluation Suite Runner](./soul-health-score-evaluation-suite-runner.md) — Orchestrates the 7-dimension Soul Health Score evaluation, dynamically loading dimension runners, executing them sequentially, computing the weighted composite score, and returning a structured report. Serves as both a library function and a CLI tool.
- [Soul Health Score Report Renderer](./soul-health-score-report-renderer.md) — Renders SoulHealthReport objects into four output formats—markdown table, JSON, RAG comparison table, and an ANSI terminal dashboard—without external dependencies. Provides human-readable and machine-readable views of evaluation results.
- [Test Suite for Soul Protocol Evaluation System](./test-suite-for-soul-protocol-evaluation-system.md) — Comprehensive tests for the heuristic evaluation engine, rubric models, and Evaluator class used to score AI companion responses across multiple quality dimensions. Covers model validation, scoring algorithms, domain auto-selection, history capping, and streak detection.

### `event sourcing` — 4 article(s)

- [Org Journal — Append-Only Event Log with Actor Attribution, Scoped Writes, and Root Protection](./org-journal-append-only-event-log-with-actor-attribution-scoped-writes-and-root-.md) — The org journal is the immutable, UTC-stamped, scope-tagged event store that serves as a single source of truth for all mutations in an org instance. `EventEntry`, `Actor`, and `DataRef` are the core spec models; `check_root_undeletable` is an advisory safeguard that prevents any journal event from retiring or deleting the org's root soul.
- [Test Suite for Journal 0.3.2 Primitives](./test-suite-for-journal-032-primitives.md) — Smoke, end-to-end, and real-world simulation tests for the primitives introduced in soul-protocol v0.3.2, specifically the append-returns-committed-entry contract and action prefix querying. Tests mirror realistic consumer patterns from the PocketPaw widget store.
- [Test Suite for the Journal Engine](./test-suite-for-the-journal-engine.md) — Comprehensive test suite for the SQLite-backed journal engine — the event-sourcing backbone of soul-protocol. Covers append/query mechanics, every filter dimension, timezone enforcement, scope wildcards, monotonic timestamps, seq assignment, concurrent WAL writes, and schema migration from zero.
- [Test Suite: Org Journal Spec — Actor, DataRef, and EventEntry Primitives](./test-suite-org-journal-spec-actor-dataref-and-evententry-primitives.md) — Validates the foundational primitives of Soul Protocol's organizational journal spec: `Actor` (the entity writing an event), `DataRef` (an external data reference with cache policy), and `EventEntry` (an immutable journal record). The suite enforces UTC-only datetimes, non-anonymous writes, required scopes, and payload union handling.

### `evolution` — 5 article(s)

- [Evolution Manager: DNA Mutation Lifecycle](./evolution-manager-dna-mutation-lifecycle.md) — `EvolutionManager` orchestrates the full lifecycle of soul personality mutations — proposal, approval/rejection, and application to DNA — across three modes: disabled, supervised, and autonomous. It is the core mechanism by which souls evolve over time based on interaction performance.
- [Evolution Subpackage Public Interface](./evolution-subpackage-public-interface.md) — The `evolution/__init__.py` init re-exports `EvolutionManager` as the sole public entry point for the evolution subsystem. Callers import from `soul_protocol.runtime.evolution` rather than navigating to the `manager` submodule directly.
- [Heuristic Rubric Evaluator and Evolution Trigger Detection](./heuristic-rubric-evaluator-and-evolution-trigger-detection.md) — `evaluation.py` provides a lightweight, LLM-free rubric evaluation system that scores soul interactions across dimensions like completeness, relevance, specificity, and empathy. Score history feeds the evolution system — when a soul shows a sustained high-performance streak in a domain, the evaluator surfaces a trigger that prompts a DNA mutation proposal.
- [Skill and SkillRegistry: XP-Based Ability Progression for Souls](./skill-and-skillregistry-xp-based-ability-progression-for-souls.md) — Implements a leveled skill system where souls gain experience points (XP) through interactions, level up abilities (1-10) with exponential scaling thresholds, and lose XP through time-based decay when skills go unused. SkillRegistry manages the collection and bridges the learning event pipeline.
- [Test Suite for Soul Evolution Manager](./test-suite-for-soul-evolution-manager.md) — Tests for `EvolutionManager`, which controls how a soul's DNA traits mutate over time across three modes: supervised, autonomous, and disabled. Covers the full mutation lifecycle — propose, approve, reject, apply — and guards against illegal mutations on immutable traits.

### `examples` — 5 article(s)

- [Interactive CLI Agent Demonstrating the Full Soul Lifecycle](./interactive-cli-agent-demonstrating-the-full-soul-lifecycle.md) — A runnable conversational agent that exercises the complete Soul Protocol lifecycle — birth, observe, recall, reflect, and save — with pluggable LLM backends (Claude, OpenAI, Ollama, and a no-LLM heuristic mode). It uses a dual-model architecture where a cheaper model handles soul cognition and a stronger model handles user-facing chat, demonstrating the cost-efficiency pattern Soul Protocol enables.
- [Logging Configuration Guide for Soul Protocol](./logging-configuration-guide-for-soul-protocol.md) — Provides three ready-to-use logging configurations for the soul-protocol runtime — a human-readable development setup, a structured JSON production setup, and a dict-based config compatible with YAML/JSON config files. All configurations target the `soul_protocol.runtime` logger namespace to give operators precise control over verbosity without silencing third-party libraries.
- [Memory Categories and Salience Scoring Example](./memory-categories-and-salience-scoring-example.md) — Demonstrates Soul Protocol's v0.2.3 memory extraction taxonomy by walking through the observe pipeline with five realistic interactions, then inspecting how the system assigns categories, salience scores, and memory types to extracted facts. The example also shows the deduplication pipeline and progressive content loading (L0/L1/L2) through targeted recall queries.
- [Multi-Soul Management and Parallel Identity Example](./multi-soul-management-and-parallel-identity-example.md) — Shows how to create and manage multiple independent Soul instances in a single process, each with distinct archetypes, values, and memory spaces. The example covers the full lifecycle from birth to export and reawakening, demonstrating that memory isolation is maintained across souls and that soul files persist identity correctly across process restarts.
- [SoulProvider: PocketPaw Integration Bridge for Soul Protocol](./soulprovider-pocketpaw-integration-bridge-for-soul-protocol.md) — Defines `SoulProvider`, a reference integration class that replaces PocketPaw's `DefaultBootstrapProvider` with a soul-driven system prompt generator, interaction tracker, and auto-save mechanism. It bridges soul-protocol's memory and identity system into any agent loop that calls a `get_system_prompt()` / `on_interaction()` pattern, with zero dependency on PocketPaw internals.

### `exceptions` — 1 article(s)

- [Soul Protocol Custom Exception Hierarchy](./soul-protocol-custom-exception-hierarchy.md) — `exceptions.py` defines the typed exception hierarchy for soul-protocol operations, covering file-not-found, corruption, export failures, encryption errors, and protected role violations. Using typed exceptions instead of bare `Exception` lets callers write precise `except` clauses and present actionable error messages to users.

### `experimental-design` — 2 article(s)

- [Experiment Configuration and Enum Definitions for Soul Protocol Research](./experiment-configuration-and-enum-definitions-for-soul-protocol-research.md) — Defines the `ExperimentConfig` dataclass and supporting enums (`MemoryCondition`, `UseCase`) that control every parameter of the Soul Protocol validation study. All experiment parameters live in one place to guarantee reproducibility — any change to scale, thresholds, or conditions is a single-file edit.
- [Five Experimental Memory Conditions for Soul Protocol Ablation Study](./five-experimental-memory-conditions-for-soul-protocol-ablation-study.md) — Implements the five independent-variable conditions for the Soul Protocol validation study, ranging from a completely stateless baseline to the full psychology-informed stack. Each condition is a class that wraps a Soul (or no Soul) and exposes a uniform `observe / recall / get_state / reset` interface, allowing the experiment runner to swap conditions without changing any other code.

### `export` — 5 article(s)

- [AES-256-GCM Encryption for Soul Archive Files](./aes-256-gcm-encryption-for-soul-archive-files.md) — `crypto.py` provides password-based encryption and decryption for `.soul` file contents using AES-256-GCM authenticated encryption with scrypt key derivation. Each call to `encrypt_blob` generates fresh randomness, ensuring that the same password encrypts the same content to a different ciphertext every time.
- [Export Subpackage: Soul Archive I/O Interface](./export-subpackage-soul-archive-io-interface.md) — The `export/__init__.py` init exposes `pack_soul` and `unpack_soul` as the sole public interface for `.soul` archive I/O, hiding the internal split between `pack.py` and `unpack.py`. All callers that need to serialize or deserialize a soul use this single import point.
- [Soul Archive Packer: Building .soul ZIP Archives](./soul-archive-packer-building-soul-zip-archives.md) — `pack_soul` serializes a `SoulConfig` and all its memory tiers into a portable `.soul` ZIP archive, with optional AES-256-GCM encryption for every file except the manifest. It is the primary serialization primitive — every export, backup, and migration operation ultimately calls this function.
- [Soul Archive Unpacker: Loading .soul ZIP Archives](./soul-archive-unpacker-loading-soul-zip-archives.md) — `unpack_soul` deserializes a `.soul` ZIP archive back into a `(SoulConfig, memory_data)` tuple, handling both encrypted and unencrypted archives with clear error separation between missing-password and wrong-password scenarios. It is the read-side counterpart to `pack_soul` and is called on every `awaken()` operation.
- [Test Suite for Soul Export and Pack/Unpack Operations](./test-suite-for-soul-export-and-packunpack-operations.md) — Tests for the `.soul` zip archive format, covering the full pack/unpack roundtrip, required file inventory inside the archive, and memory tier data inclusion. Ensures that `SoulConfig` and memory content survive serialisation to bytes and back without loss.

### `extraction` — 1 article(s)

- [Test Suite for Fact and Entity Extraction from Interaction Text](./test-suite-for-fact-and-entity-extraction-from-interaction-text.md) — Validates the pattern-based extraction pipeline that parses user messages for facts (name, preferences, identity, tools, projects) and entities (tech terms, proper nouns with inferred relations). Tests cover each extraction pattern, punctuation stripping, stopword exclusion, and deduplication of repeated facts.

### `facade` — 1 article(s)

- [Test Suite: MemoryManager Facade](./test-suite-memorymanager-facade.md) — Core test suite for the MemoryManager facade, covering all five memory stores (core, episodic, semantic, procedural, knowledge graph), cross-store recall, significance-gated observation, and the Bug #15 edit_core behavior fix.

### `factory-pattern` — 1 article(s)

- [Embedding Provider Factory Test Suite](./embedding-provider-factory-test-suite.md) — Tests for `get_embedding_provider()`, the factory function that instantiates embedding backends by name. Covers all built-in providers (hash, TF-IDF), external providers with lazy imports (sentence-transformers, OpenAI, Ollama), kwargs passthrough, unknown provider errors, and import failure handling.

### `facts` — 1 article(s)

- [SemanticStore: Fact-Based Memory with Confidence and Eviction](./semanticstore-fact-based-memory-with-confidence-and-eviction.md) — Stores semantic memories — extracted facts and general knowledge about the world and the user — with importance scoring, confidence tracking, supersession logic for updated facts, and a bounded capacity that evicts least-important entries when full. GDPR deletion APIs are provided for targeted and time-based removal.

### `fallback resilience` — 1 article(s)

- [Test Suite: Cognitive Engine, Heuristic Routing, and LLM Fallback](./test-suite-cognitive-engine-heuristic-routing-and-llm-fallback.md) — Comprehensive test suite for the Soul Protocol cognitive processing layer, covering HeuristicEngine task routing, CognitiveProcessor LLM integration, JSON parsing helpers, and graceful fallback when LLMs fail or return garbage. Also includes regression tests for the heuristic-only mode bug.

### `federated sources` — 1 article(s)

- [Retrieval Spec — Request/Response Models, Adapter Protocols, Credential Broker, and Exception Hierarchy](./retrieval-spec-requestresponse-models-adapter-protocols-credential-broker-and-ex.md) — This module defines the complete vocabulary for soul retrieval: request and response models (`RetrievalRequest`, `RetrievalResult`), the `SourceAdapter` and `AsyncSourceAdapter` protocols, the `CredentialBroker` interface for federated source access, a `DataRef` type for Zero-Copy candidate payloads, and a structured exception hierarchy. Concrete implementations (the router, in-memory broker, projection adapters) live in the consuming runtime.

### `file-format` — 1 article(s)

- [Test Suite for Soul Export and Pack/Unpack Operations](./test-suite-for-soul-export-and-packunpack-operations.md) — Tests for the `.soul` zip archive format, covering the full pack/unpack roundtrip, required file inventory inside the archive, and memory tier data inclusion. Ensures that `SoulConfig` and memory content survive serialisation to bytes and back without loss.

### `filesystem` — 1 article(s)

- [FileStorage: Filesystem-Backed Soul Persistence with Atomic Writes](./filestorage-filesystem-backed-soul-persistence-with-atomic-writes.md) — `FileStorage` persists soul configs, DNA markdown, and all seven memory tiers to a structured directory layout under `~/.soul/`. The `save_soul_full` function uses a temp-dir-then-move pattern to guarantee atomic writes, preventing partial state on crash.

### `fixtures` — 1 article(s)

- [Shared Test Fixtures for the Soul Protocol Suite](./shared-test-fixtures-for-the-soul-protocol-suite.md) — `conftest.py` centralizes the four foundational fixtures used across all soul-protocol test modules: a sample `Identity`, a `SoulConfig` built from it, a fully-birthed `Soul`, and a temporary `.soul` file on disk. Centralizing these prevents fixture duplication and ensures all tests operate on consistent, valid starting state.

### `format-detection` — 1 article(s)

- [Test Suite for Soul Format Auto-Detection](./test-suite-for-soul-format-auto-detection.md) — Tests for `detect_format()`, the function that inspects a filesystem path and returns the soul character format it represents — SoulSpec, TavernAI, Soul Protocol native, or unknown. Covers directory structures, JSON shapes, file extensions, PNG files, and error cases.

### `functional-testing` — 1 article(s)

- [Simulate Script: Comprehensive Soul Protocol Scenario Test Suite](./simulate-script-comprehensive-soul-protocol-scenario-test-suite.md) — A standalone simulation script containing 30+ named scenarios that test every surface of the soul-protocol API — from basic lifecycle and memory recall to adversarial inputs, concurrent async safety, cross-platform migration, evolution system, knowledge graph, and graceful degradation. Each scenario is independently runnable and produces a detailed Rich-formatted report.

### `gamification` — 2 article(s)

- [Skill and SkillRegistry: XP-Based Ability Progression for Souls](./skill-and-skillregistry-xp-based-ability-progression-for-souls.md) — Implements a leveled skill system where souls gain experience points (XP) through interactions, level up abilities (1-10) with exponential scaling thresholds, and lose XP through time-based decay when skills go unused. SkillRegistry manages the collection and bridges the learning event pipeline.
- [Test Suite: Skills XP Progression, Level Cap, and Decay](./test-suite-skills-xp-progression-level-cap-and-decay.md) — Validates the Skills subsystem — a gamification layer that tracks a soul's proficiency across named abilities via an XP and level model. The suite covers default construction, XP accumulation with level-up triggers, overflow carry, the level 10 cap, significance-weighted XP grants, skill decay, and the `SkillRegistry` collection.

### `governance` — 3 article(s)

- [Evolution Manager: DNA Mutation Lifecycle](./evolution-manager-dna-mutation-lifecycle.md) — `EvolutionManager` orchestrates the full lifecycle of soul personality mutations — proposal, approval/rejection, and application to DNA — across three modes: disabled, supervised, and autonomous. It is the core mechanism by which souls evolve over time based on interaction performance.
- [Org Journal — Append-Only Event Log with Actor Attribution, Scoped Writes, and Root Protection](./org-journal-append-only-event-log-with-actor-attribution-scoped-writes-and-root-.md) — The org journal is the immutable, UTC-stamped, scope-tagged event store that serves as a single source of truth for all mutations in an org instance. `EventEntry`, `Actor`, and `DataRef` are the core spec models; `check_root_undeletable` is an advisory safeguard that prevents any journal event from retiring or deleting the org's root soul.
- [Soul Protocol Custom Exception Hierarchy](./soul-protocol-custom-exception-hierarchy.md) — `exceptions.py` defines the typed exception hierarchy for soul-protocol operations, covering file-not-found, corruption, export failures, encryption errors, and protected role violations. Using typed exceptions instead of bare `Exception` lets callers write precise `except` clauses and present actionable error messages to users.

### `hash-embedder` — 1 article(s)

- [Hash Embedder Test Suite — Determinism, Normalization, and Batch Embedding](./hash-embedder-test-suite-determinism-normalization-and-batch-embedding.md) — Test suite for `HashEmbedder`, soul-protocol's zero-dependency embedding backend that converts text to L2-normalized vectors via hashing. Covers dimensionality configuration, deterministic output, L2 normalization, edge cases (empty strings, whitespace), and batch embedding consistency.

### `heuristics` — 1 article(s)

- [Heuristic Rubric Evaluator and Evolution Trigger Detection](./heuristic-rubric-evaluator-and-evolution-trigger-detection.md) — `evaluation.py` provides a lightweight, LLM-free rubric evaluation system that scores soul interactions across dimensions like completeness, relevance, specificity, and empathy. Score history feeds the evolution system — when a soul shows a sustained high-performance streak in a domain, the evaluator surfaces a trigger that prompts a DNA mutation proposal.

### `identity` — 16 article(s)

- [A2A Agent Card Bridge: Soul Protocol and Google A2A Interoperability](./a2a-agent-card-bridge-soul-protocol-and-google-a2a-interoperability.md) — Provides bidirectional conversion between Soul Protocol souls and Google's A2A Agent Card JSON format, mapping soul identity, OCEAN personality traits, and skills in both directions. A stateless utility class with three static methods covering export, import, and non-destructive enrichment.
- [Bond Model: Human-Soul Relationship Strength](./bond-model-human-soul-relationship-strength.md) — Models the trust relationship between a human and their soul companion, tracking bond strength and interaction count. Uses logarithmic growth to make early bonding feel natural while making deep trust genuinely hard-earned through sustained interaction.
- [Core Memory Manager — Always-In-Context Identity Block](./core-memory-manager-always-in-context-identity-block.md) — Manages the ~2KB core memory block that stays loaded in context at all times. It holds two editable sections — `persona` (the soul's own identity) and `human` (profile of the bonded human) — with distinct `set`, `edit`, and `append` operations.
- [D6 Identity Continuity Evaluator](./d6-identity-continuity-evaluator.md) — Tests that a soul survives export/import round-trips with full fidelity—preserving DID, OCEAN traits, bond state, memory count, and recall results—and that reincarnation correctly tracks lineage across incarnation chains. Contributes 10% of the Soul Health Score.
- [DID Generation for Digital Souls](./did-generation-for-digital-souls.md) — Generates decentralized identifiers (DIDs) for soul instances using the `did:soul:` method. Each DID is deterministically random — human-readable yet collision-resistant — by combining the soul's name with a UUID4-seeded SHA-256 hash.
- [Identity Subpackage: DID Generation Interface](./identity-subpackage-did-generation-interface.md) — The `identity/__init__.py` init exposes `generate_did` as the single public entry point for the identity subsystem, making Decentralized Identifier (DID) creation available via `from soul_protocol.runtime.identity import generate_did` without exposing the underlying DID implementation details.
- [Multi-Soul Management and Parallel Identity Example](./multi-soul-management-and-parallel-identity-example.md) — Shows how to create and manage multiple independent Soul instances in a single process, each with distinct archetypes, values, and memory spaces. The example covers the full lifecycle from birth to export and reawakening, demonstrating that memory isolation is maintained across souls and that soul files persist identity correctly across process restarts.
- [Runtime Types: The Complete Soul Protocol Pydantic Model Library](./runtime-types-the-complete-soul-protocol-pydantic-model-library.md) — `types.py` is the canonical data model layer for Soul Protocol — over 30 Pydantic models covering identity, personality (OCEAN), psychology-informed memory, emotional state, evolution, and interaction. It is the schema backbone that all other runtime modules depend on.
- [SelfModelManager: Evolving Self-Concept for Digital Souls](./selfmodelmanager-evolving-self-concept-for-digital-souls.md) — Implements Klein's self-model theory for AI companions — the soul discovers what it is by observing its own interactions and building a map of the domains where it operates. Self-images surface as prompt fragments that anchor the soul's personality in generated responses.
- [Soul Identity — Schema-Free Portable Identity with Multi-Bond Support](./soul-identity-schema-free-portable-identity-with-multi-bond-support.md) — `Identity` is the portable identity primitive at the core of Soul Protocol. It carries a soul's name, creation timestamp, and an open `traits` dict that runtimes can populate with OCEAN scores, Myers-Briggs types, or any custom dimensions. The `feat/spec-multi-participant` update added `BondTarget` and a `bonds` list so a single soul can maintain relationships with multiple humans, agents, groups, and services.
- [Soul: The Central Digital Identity and Lifecycle Manager](./soul-the-central-digital-identity-and-lifecycle-manager.md) — The Soul class is the top-level API object in Soul Protocol — it unifies identity, memory, personality, skills, evolution, dreaming, bonding, and eternal storage into a single coherent interface. It is the primary object that consuming applications instantiate and interact with.
- [SoulTemplate — Blueprint for Batch Soul Spawning with Personality Variance](./soultemplate-blueprint-for-batch-soul-spawning-with-personality-variance.md) — `SoulTemplate` defines a reusable blueprint that a `SoulFactory` uses to spawn individual souls or batches with controlled personality variation. It carries a default personality (OCEAN traits as a dict), core memories to bootstrap, skill names, and variance settings for producing non-identical clones in batch operations.
- [Test Suite for Soul Evolution Manager](./test-suite-for-soul-evolution-manager.md) — Tests for `EvolutionManager`, which controls how a soul's DNA traits mutate over time across three modes: supervised, autonomous, and disabled. Covers the full mutation lifecycle — propose, approve, reject, apply — and guards against illegal mutations on immutable traits.
- [Test Suite: Identity Spec — Construction, Auto-ID, Traits, and Serialization](./test-suite-identity-spec-construction-auto-id-traits-and-serialization.md) — Validates the core `Identity` primitive from `soul_protocol.spec`, covering minimal construction, deterministic auto-ID generation, uniqueness guarantees, arbitrary traits storage, and Pydantic `model_dump`/`model_validate` round-trip fidelity. Identity is the foundational spec model that all other containers reference.
- [Test Suite: Multi-Participant Interactions and Multi-Bond Identity](./test-suite-multi-participant-interactions-and-multi-bond-identity.md) — Tests for the multi-participant Interaction model (issue #95) and multi-bond Identity (issue #94), including full backward compatibility with the legacy user_input/agent_output API, serialization round-trips, and coverage of the spec-layer equivalents.
- [Test Suite: Self-Model Manager (Soul's Self-Concept)](./test-suite-self-model-manager-souls-self-concept.md) — Tests for SelfModelManager, which implements Klein's self-concept model — the soul's ongoing self-knowledge about what domains it is competent in, which roles it plays, and how confident it is in those self-images based on accumulated evidence.

### `identity management` — 1 article(s)

- [Test Suite: Soul Reincarnation Lifecycle](./test-suite-soul-reincarnation-lifecycle.md) — Validates that `Soul.reincarnate()` correctly transitions a soul to a new identity while preserving accumulated state. The suite ensures that reincarnation is not a hard reset — personality, memories, values, archetype, and bond all carry forward, while the DID and incarnation counter change to mark the new life.

### `importers` — 6 article(s)

- [Soul Format Auto-Detection and Importer Registry](./soul-format-auto-detection-and-importer-registry.md) — The importers package entry point exposes `SoulSpecImporter`, `TavernAIImporter`, and the `detect_format()` utility. `detect_format()` inspects a file or directory and returns a format tag so callers can route to the correct importer without knowing the format in advance.
- [SoulSpec Format Importer and Exporter](./soulspec-format-importer-and-exporter.md) — Bridges the SoulSpec open standard (soulspec.org) and the Digital Soul Protocol. Reads a SoulSpec directory structure — `SOUL.md`, `IDENTITY.md`, `STYLE.md`, `soul.json` — and maps each file to the appropriate DSP memory tier and OCEAN personality model.
- [TavernAI Character Card V2 Importer and Exporter](./tavernai-character-card-v2-importer-and-exporter.md) — Supports full round-trip conversion between TavernAI Character Card V2 (JSON and PNG-embedded) and DSP Soul objects. Handles PNG binary parsing — extracting and embedding character JSON in `tEXt`/`iTXt` chunks — without any external image library dependency.
- [Test Suite for Soul Format Auto-Detection](./test-suite-for-soul-format-auto-detection.md) — Tests for `detect_format()`, the function that inspects a filesystem path and returns the soul character format it represents — SoulSpec, TavernAI, Soul Protocol native, or unknown. Covers directory structures, JSON shapes, file extensions, PNG files, and error cases.
- [Test Suite for SoulSpec Import and Export](./test-suite-for-soulspec-import-and-export.md) — Tests for the SoulSpec importer, which converts a human-authored directory of markdown files and soul.json into a Soul Protocol `SoulConfig`, and the corresponding exporter that writes those files back out. Covers OCEAN trait mapping, memory tier assignments, round-trip fidelity, and error handling.
- [Test Suite for TavernAI Character Card Import and Export](./test-suite-for-tavernai-character-card-import-and-export.md) — Tests for the TavernAI importer, which converts Character Card V2 JSON (and PNG-embedded cards) into Soul Protocol `SoulConfig`, and the exporter that writes cards back. Covers field mapping to memory tiers, validation errors, and PNG tEXt chunk embedding.

### `indexing` — 1 article(s)

- [BM25 and Token-Overlap Memory Search Engine](./bm25-and-token-overlap-memory-search-engine.md) — Implements the core text-matching primitives used by all three memory stores: a tokenizer that handles natural language and code identifiers, a BM25 index with IDF weighting, and a lightweight token-overlap relevance scorer kept as a fast fallback. Synonym expansion improves recall for common programming and technical terms.

### `infrastructure` — 9 article(s)

- [Engine Test Package Initializer](./engine-test-package-initializer.md) — Marks `tests/test_engine/` as a Python package so pytest can discover tests within it. Created as part of the journal-engine workstream in the Org Architecture RFC.
- [Eternal Storage Test Package Initializer](./eternal-storage-test-package-initializer.md) — Marks `tests/test_eternal/` as a Python package, enabling pytest discovery of the eternal storage subsystem's test modules. Created when the eternal storage subsystem was introduced on 2026-03-06.
- [Haiku Cognitive Engine — Claude Haiku LLM Backend with Rate Limiting](./haiku-cognitive-engine-claude-haiku-llm-backend-with-rate-limiting.md) — A thin async wrapper around the Anthropic SDK that implements the single-method `CognitiveEngine` protocol (`async think(prompt) -> str`) using Claude Haiku. Includes semaphore-based concurrency control, retry logic for rate limits and timeouts, and a `UsageTracker` dataclass for cost and throughput monitoring.
- [Research Package Entry Point (`python -m research`)](./research-package-entry-point-python-m-research.md) — A minimal `__main__.py` that enables the research package to be invoked directly with `python -m research`, delegating immediately to `research.run.main()`. This is the standard Python idiom for making a package executable without requiring the user to know the internal module structure.
- [Test Package Init: Evolution Subsystem](./test-package-init-evolution-subsystem.md) — Empty package initialiser that makes `tests/test_evolution/` a proper Python package, enabling pytest to discover and import the evolution test modules correctly. No executable code is present; the file's value is purely structural.
- [Test Package Init: Importers Subsystem](./test-package-init-importers-subsystem.md) — Empty package initialiser that designates `tests/test_importers/` as a Python package, enabling pytest to collect the importer-specific test modules contained within. No executable code is present.
- [Test Package Init: Install Invariants](./test-package-init-install-invariants.md) — Package initialiser for `tests/test_install/`, the sub-package that verifies install-time invariants such as the CLI being functional after a bare `pip install soul-protocol`. Created to address issue #157.
- [Test Package Init: Long-Horizon Ablation Study](./test-package-init-long-horizon-ablation-study.md) — Package initialiser for `tests/test_long_horizon/`, the sub-package housing ablation study tests that evaluate soul memory and context performance over extended interaction sequences. Created 2026-03-11.
- [Test Package Initializer: test_models](./test-package-initializer-testmodels.md) — Minimal package initializer for the test_models test directory, which makes the directory a Python package so pytest can discover and import test modules within it.

### `install` — 2 article(s)

- [Test Package Init: Install Invariants](./test-package-init-install-invariants.md) — Package initialiser for `tests/test_install/`, the sub-package that verifies install-time invariants such as the CLI being functional after a bare `pip install soul-protocol`. Created to address issue #157.
- [Test Suite for Base Install Invariants (Issue #157)](./test-suite-for-base-install-invariants-issue-157.md) — Regression tests that verify a bare `pip install soul-protocol` produces a working CLI, by checking that click, rich, pyyaml, and cryptography are declared as base dependencies, the deprecated `[engine]` extra still exists for backwards compatibility, and the CLI module imports cleanly without optional extras.

### `integration` — 12 article(s)

- [A2A Agent Card Bridge: Soul Protocol and Google A2A Interoperability](./a2a-agent-card-bridge-soul-protocol-and-google-a2a-interoperability.md) — Provides bidirectional conversion between Soul Protocol souls and Google's A2A Agent Card JSON format, mapping soul identity, OCEAN personality traits, and skills in both directions. A stateless utility class with three static methods covering export, import, and non-destructive enrichment.
- [CallableEngine: Wrap Any Function as a Cognitive Engine](./callableengine-wrap-any-function-as-a-cognitive-engine.md) — Wraps any sync or async callable as a `CognitiveEngine`, letting developers pass a simple lambda or function to `Soul.birth()` without implementing the full protocol. Handles both sync and async callables transparently using `asyncio.iscoroutinefunction` and `run_in_executor`.
- [End-to-End Integration Test Suite — Soul Lifecycle, Memory Persistence, and Error Handling](./end-to-end-integration-test-suite-soul-lifecycle-memory-persistence-and-error-ha.md) — Full end-to-end integration tests for the Soul class covering the complete lifecycle from birth through export and awakening. Tests validate memory persistence across all five tiers, self-model emergence, core memory editing, directory format save/load, and robust error handling for corrupt or missing soul files.
- [FastMCP Server: 28-Tool Soul Protocol Agent Integration](./fastmcp-server-28-tool-soul-protocol-agent-integration.md) — Implements the full soul-protocol MCP server using FastMCP, exposing 28 tools, 3 resources, and 2 prompts for AI agent integration. Manages a registry of multiple souls, handles auto-reload from disk, lazy cognitive engine wiring, and graceful shutdown with auto-save.
- [MCP Package Public Entry Points](./mcp-package-public-entry-points.md) — Thin package init that re-exports `create_server` and `run_server` from the MCP server module, providing a stable public surface for consumers who integrate soul-protocol's FastMCP server. Keeps the import path short (`from soul_protocol.mcp import create_server`) regardless of internal module organization.
- [Platform Auto-Detection and MCP Wiring — `setup.py`](./platform-auto-detection-and-mcp-wiring-setuppy.md) — Detects installed AI coding platforms (Claude Code, Cursor, VS Code, Windsurf, and 8 others), generates platform-appropriate MCP server config, and writes soul memory instructions into each platform's instruction file. Designed to be run once during `soul init --setup-targets`.
- [Protocol Bridges Package: A2A Interoperability](./protocol-bridges-package-a2a-interoperability.md) — Package init for the runtime bridges subsystem, currently exposing the `A2AAgentCardBridge` for bidirectional conversion between Soul Protocol and Google's A2A Agent Card format. Bridges enable soul-powered agents to participate in multi-agent ecosystems that use other protocols.
- [Real-World End-to-End Test Suite — Developer Onboarding, Bonding, and Evolution Scenarios](./real-world-end-to-end-test-suite-developer-onboarding-bonding-and-evolution-scen.md) — Scenario-driven end-to-end tests that simulate realistic human-soul conversations across multiple contexts: developer technical discussions, personal emotional bonding, and mixed sessions. Tests validate that all soul systems (entity extraction, knowledge graph, bond growth, evolution, self-model) fire correctly in natural usage patterns.
- [Soul Agent: Claude Agent SDK + Soul Protocol with 15 MCP Tools](./soul-agent-claude-agent-sdk-soul-protocol-with-15-mcp-tools.md) — Implements a full-featured conversational agent that combines the Claude Agent SDK's tool execution and streaming loop with Soul Protocol's v0.2.3 feature set, exposing 15 MCP tools covering memory, personality, emotion, bond tracking, skills, GDPR deletion, evolution proposals, core memory editing, reincarnation, and encrypted export. The module-level `_soul` singleton pattern mirrors the MCP server architecture for consistency.
- [Soul Context Injection — CLI Inject Module](./soul-context-injection-cli-inject-module.md) — Provides functions to write or replace a soul's identity, state, and recent memories into agent platform configuration files (CLAUDE.md, .cursorrules, etc.) using idempotent HTML comment markers. Supports six platforms and runs without MCP.
- [SoulProvider: PocketPaw Integration Bridge for Soul Protocol](./soulprovider-pocketpaw-integration-bridge-for-soul-protocol.md) — Defines `SoulProvider`, a reference integration class that replaces PocketPaw's `DefaultBootstrapProvider` with a soul-driven system prompt generator, interaction tracker, and auto-save mechanism. It bridges soul-protocol's memory and identity system into any agent loop that calls a `get_system_prompt()` / `on_interaction()` pattern, with zero dependency on PocketPaw internals.
- [Test Suite for SoulProvider PocketPaw Integration](./test-suite-for-soulprovider-pocketpaw-integration.md) — Covers the `SoulProvider` reference integration class with 14 async pytest tests verifying system prompt generation, memory recall injection, interaction tracking, auto-save behavior, factory constructors, and low-energy behavioral adaptation. Tests are isolated using fresh Soul instances per fixture to prevent cross-test memory contamination.

### `integration-testing` — 2 article(s)

- [PocketPaw E2E Integration Script: Full Soul Protocol Lifecycle Validation](./pocketpaw-e2e-integration-script-full-soul-protocol-lifecycle-validation.md) — An end-to-end integration script that validates the complete soul-protocol API lifecycle — birth, observe, remember, recall, reflect, export, awaken, core memory editing, state management, and MCP tool smoke tests — from the perspective of PocketPaw's integration layer, using realistic interaction datasets and Rich-formatted reporting.
- [Real-User Scenario Simulation Suite](./real-user-scenario-simulation-suite.md) — A comprehensive integration simulation that runs six realistic user scenarios against soul-protocol to validate lifecycle correctness, memory accuracy, multi-format support, personality divergence, and resilience. Each scenario produces a structured pass/fail report written to `.results/`.

### `interoperability` — 7 article(s)

- [A2A Agent Card Bridge: Soul Protocol and Google A2A Interoperability](./a2a-agent-card-bridge-soul-protocol-and-google-a2a-interoperability.md) — Provides bidirectional conversion between Soul Protocol souls and Google's A2A Agent Card JSON format, mapping soul identity, OCEAN personality traits, and skills in both directions. A stateless utility class with three static methods covering export, import, and non-destructive enrichment.
- [A2A Agent Card Models: Soul Protocol Interop with Google's Agent-to-Agent Protocol](./a2a-agent-card-models-soul-protocol-interop-with-googles-agent-to-agent-protocol.md) — `spec/a2a.py` provides Pydantic models that map Google's A2A Agent Card specification to Soul Protocol primitives. `A2AAgentCard` is the agent's public identity, `A2ASkill` advertises capabilities, and `SoulExtension` embeds a soul's DID and OCEAN personality into the card's extension block.
- [Protocol Bridges Package: A2A Interoperability](./protocol-bridges-package-a2a-interoperability.md) — Package init for the runtime bridges subsystem, currently exposing the `A2AAgentCardBridge` for bidirectional conversion between Soul Protocol and Google's A2A Agent Card format. Bridges enable soul-powered agents to participate in multi-agent ecosystems that use other protocols.
- [Soul Format Auto-Detection and Importer Registry](./soul-format-auto-detection-and-importer-registry.md) — The importers package entry point exposes `SoulSpecImporter`, `TavernAIImporter`, and the `detect_format()` utility. `detect_format()` inspects a file or directory and returns a format tag so callers can route to the correct importer without knowing the format in advance.
- [SoulSpec Format Importer and Exporter](./soulspec-format-importer-and-exporter.md) — Bridges the SoulSpec open standard (soulspec.org) and the Digital Soul Protocol. Reads a SoulSpec directory structure — `SOUL.md`, `IDENTITY.md`, `STYLE.md`, `soul.json` — and maps each file to the appropriate DSP memory tier and OCEAN personality model.
- [TavernAI Character Card V2 Importer and Exporter](./tavernai-character-card-v2-importer-and-exporter.md) — Supports full round-trip conversion between TavernAI Character Card V2 (JSON and PNG-embedded) and DSP Soul objects. Handles PNG binary parsing — extracting and embedding character JSON in `tEXt`/`iTXt` chunks — without any external image library dependency.
- [Test Suite for the A2A Agent Card Bridge](./test-suite-for-the-a2a-agent-card-bridge.md) — This test suite validates bidirectional conversion between Soul Protocol souls and Google A2A Agent Cards via `A2AAgentCardBridge`. It covers spec model serialization, full round-trip identity preservation (name, DID, OCEAN traits, skills), immutability guards on enrichment, and file-based CLI round-trips.

### `ipfs` — 1 article(s)

- [Mock IPFS Content-Addressed Storage Provider](./mock-ipfs-content-addressed-storage-provider.md) — `MockIPFSProvider` simulates IPFS's content-addressed storage model by generating deterministic CID-like references from the SHA-256 hash of the archived data and storing it in memory. It faithfully reproduces the key property of IPFS — the same content always produces the same address — without requiring a running IPFS node.

### `journal` — 4 article(s)

- [Journal Scope Matching: Pattern Grammar and Overlap Checks](./journal-scope-matching-pattern-grammar-and-overlap-checks.md) — Provides two public functions for colon-delimited scope pattern matching used throughout the journal engine, retrieval router, and credential broker. The module was extracted from the SQLite backend to make the shared logic accessible as a proper public API.
- [Journal SQLite Schema and Migration Helper](./journal-sqlite-schema-and-migration-helper.md) — Defines the SQLite schema for the soul-protocol journal engine and provides an idempotent migration function. Ensures the on-disk database is always brought up to the current schema version before use, preventing schema drift between engine upgrades.
- [Org Journal — Append-Only Event Log with Actor Attribution, Scoped Writes, and Root Protection](./org-journal-append-only-event-log-with-actor-attribution-scoped-writes-and-root-.md) — The org journal is the immutable, UTC-stamped, scope-tagged event store that serves as a single source of truth for all mutations in an org instance. `EventEntry`, `Actor`, and `DataRef` are the core spec models; `check_root_undeletable` is an advisory safeguard that prevents any journal event from retiring or deleting the org's root soul.
- [SQLite WAL-Mode Journal Backend](./sqlite-wal-mode-journal-backend.md) — Implements `JournalBackend` using SQLite with WAL mode, providing append-only event storage with strict sequential ordering and monotonic timestamp enforcement. A single threading lock serializes all writes while allowing concurrent readers through WAL isolation.

### `journal-engine` — 7 article(s)

- [Engine Test Package Initializer](./engine-test-package-initializer.md) — Marks `tests/test_engine/` as a Python package so pytest can discover tests within it. Created as part of the journal-engine workstream in the Org Architecture RFC.
- [Journal Backend Protocol — Storage Contract for Event Journals](./journal-backend-protocol-storage-contract-for-event-journals.md) — Defines `JournalBackend` as a `runtime_checkable` Protocol (structural interface) that any storage backend must satisfy. Specifies four operations — append, query, replay, and close — with precise filter semantics for the query method.
- [Journal Engine Error Hierarchy — `exceptions.py`](./journal-engine-error-hierarchy-exceptionspy.md) — Defines the four-class exception hierarchy for the journal engine, with `JournalError` as the catchable base class and three specialisations covering schema incompatibility, invariant violations, and missing records.
- [Journal Engine Public API — `engine.journal` Package](./journal-engine-public-api-enginejournal-package.md) — Re-exports the complete public surface of the journal engine: the `Journal` class, `JournalBackend` protocol, `SQLiteJournalBackend` implementation, the full exception hierarchy, and scope-matching utilities. Callers import from this package rather than individual submodules.
- [Journal High-Level Class — Invariant Enforcement and Hash Chaining](./journal-high-level-class-invariant-enforcement-and-hash-chaining.md) — The `Journal` class wraps a `JournalBackend` to enforce append invariants (timezone-aware timestamps, monotonic ordering) and maintain an opportunistic SHA-256 hash chain linking each event to the previous one. `open_journal()` is the factory for production use.
- [Test Suite for Journal 0.3.2 Primitives](./test-suite-for-journal-032-primitives.md) — Smoke, end-to-end, and real-world simulation tests for the primitives introduced in soul-protocol v0.3.2, specifically the append-returns-committed-entry contract and action prefix querying. Tests mirror realistic consumer patterns from the PocketPaw widget store.
- [Test Suite for the Journal Engine](./test-suite-for-the-journal-engine.md) — Comprehensive test suite for the SQLite-backed journal engine — the event-sourcing backbone of soul-protocol. Covers append/query mechanics, every filter dimension, timezone enforcement, scope wildcards, monotonic timestamps, seq assignment, concurrent WAL writes, and schema migration from zero.

### `knowledge-graph` — 5 article(s)

- [Knowledge Graph with Temporal Edges](./knowledge-graph-with-temporal-edges.md) — Implements `KnowledgeGraph` and `TemporalEdge` — a lightweight entity-relationship store with time-bounded validity, multi-hop traversal, shortest-path search, and progressive context loading for recall augmentation.
- [Test Suite for Knowledge Graph Traversal](./test-suite-for-knowledge-graph-traversal.md) — Tests for the `KnowledgeGraph` traversal API, covering BFS traversal, shortest-path finding, neighbourhood extraction, subgraph extraction, and progressive context rendering at three detail levels. Validates edge expiry, cycle handling, and node deduplication across a range of graph topologies.
- [Test Suite for Temporal Knowledge Graph](./test-suite-for-temporal-knowledge-graph.md) — This test suite validates the temporal knowledge graph (`KnowledgeGraph` and `TemporalEdge`) used by Soul Protocol to represent relationships between entities over time. It covers edge creation, time-range activation queries, relationship expiration, duplicate deduplication, point-in-time queries, evolution history, serialization roundtrips, and backward compatibility with older edge formats.
- [Test Suite for v0.3.4 Memory Model: MemoryCategory, Salience, and Temporal Edge Metadata](./test-suite-for-v034-memory-model-memorycategory-salience-and-temporal-edge-metad.md) — This test suite covers spec-level additions introduced in Soul Protocol v0.3.4: the `MemoryCategory` enum for classifying memory entries, `salience` as an additive activation boost on `MemoryEntry`, and `metadata` on `TemporalEdge` along with its surfacing through `KnowledgeGraph` query methods. The salience tests were rewritten from a multiplicative to an additive boost model to prevent low-importance memories from being penalized disproportionately.
- [Test Suite: Graph-Augmented Recall Pipeline](./test-suite-graph-augmented-recall-pipeline.md) — Validates the integration of KnowledgeGraph with RecallEngine, covering graph traversal via progressive_context at multiple depth levels, graph-wired recall with entity matching, deduplication, and graceful degradation when no graph is provided.

### `known-gaps` — 1 article(s)

- [Test Suite: Known Sentiment Detector Gaps](./test-suite-known-sentiment-detector-gaps.md) — A bug-documenting test suite that captures known failure modes in the heuristic sentiment detector — missing vocabulary, joy/excitement confusion, sadness/frustration confusion, gratitude misclassification, and neutral false positives — intended to go green once the corresponding fixes land in sentiment.py.

### `learning` — 4 article(s)

- [Heuristic Rubric Evaluator and Evolution Trigger Detection](./heuristic-rubric-evaluator-and-evolution-trigger-detection.md) — `evaluation.py` provides a lightweight, LLM-free rubric evaluation system that scores soul interactions across dimensions like completeness, relevance, specificity, and empathy. Score history feeds the evolution system — when a soul shows a sustained high-performance streak in a domain, the evaluator surfaces a trigger that prompts a DNA mutation proposal.
- [LearningEvent — Formalized Lesson Model for Procedural Memory and Skill XP](./learningevent-formalized-lesson-model-for-procedural-memory-and-skill-xp.md) — `LearningEvent` captures insights extracted from evaluating success and failure — distinct from episodic memory (what happened) and semantic memory (what is known). Stored in procedural memory and linked to skills, it drives reinforcement-style confidence updates through `apply()`, `reinforce()`, and `weaken()` methods.
- [Test Suite for Learning Events Pipeline](./test-suite-for-learning-events-pipeline.md) — Tests for the full learning events pipeline — from `RubricResult` score thresholding to `LearningEvent` creation, skill XP grant, and the `Soul.learn()` convenience method. Covers model validation, score boundary conditions, XP scaling, and spec module exportability.
- [Test Suite for Soul Protocol Evaluation System](./test-suite-for-soul-protocol-evaluation-system.md) — Comprehensive tests for the heuristic evaluation engine, rubric models, and Evaluator class used to score AI companion responses across multiple quality dimensions. Covers model validation, scoring algorithms, domain auto-selection, history capping, and streak detection.

### `llm` — 2 article(s)

- [AnthropicEngine: Claude API Cognitive Adapter](./anthropicengine-claude-api-cognitive-adapter.md) — Implements `CognitiveEngine` using Anthropic's Claude API, defaulting to `claude-haiku-4-5-20251001` for fast, cost-effective cognitive processing. Uses a soft import pattern so the module loads without errors even when the `anthropic` package is not installed.
- [LiteLLMEngine: Universal 100+ Provider Cognitive Adapter](./litellmengine-universal-100-provider-cognitive-adapter.md) — Implements `CognitiveEngine` using LiteLLM, providing a single adapter that routes cognitive calls to any of 100+ LLM providers including Anthropic, OpenAI, Ollama, Bedrock, Vertex, and Cohere. Provider is selected by model string format (e.g., `anthropic/claude-haiku-4-5-20251001`).

### `llm-backend` — 2 article(s)

- [Haiku Cognitive Engine — Claude Haiku LLM Backend with Rate Limiting](./haiku-cognitive-engine-claude-haiku-llm-backend-with-rate-limiting.md) — A thin async wrapper around the Anthropic SDK that implements the single-method `CognitiveEngine` protocol (`async think(prompt) -> str`) using Claude Haiku. Includes semaphore-based concurrency control, retry logic for rate limits and timeouts, and a `UsageTracker` dataclass for cost and throughput monitoring.
- [LiteLLM Cognitive Engine — Multi-Model Proxy Backend](./litellm-cognitive-engine-multi-model-proxy-backend.md) — An async `CognitiveEngine` implementation that routes LLM calls through a LiteLLM OpenAI-compatible proxy, enabling the research framework to use GPT-4o, Gemini, DeepSeek, Llama, and Qwen as judge or agent engines without changing call sites. It mirrors the `HaikuCognitiveEngine` interface exactly, making the two engines interchangeable.

### `llm-judge` — 2 article(s)

- [LLM-as-Judge — Six-Dimension Response Quality Scorer with Position-Bias Mitigation](./llm-as-judge-six-dimension-response-quality-scorer-with-position-bias-mitigation.md) — Implements the LLM-as-judge evaluation methodology with six quality dimensions scored on a 1-10 scale, supporting both single-response scoring and pairwise A/B comparison. The pairwise mode randomly assigns responses to positions A and B on each call to eliminate position bias from judge models that favor whichever response appears first.
- [LLM Judge for Soul Health Score Evaluation](./llm-judge-for-soul-health-score-evaluation.md) — Uses Claude Haiku as an independent LLM judge to evaluate sentiment accuracy and personality prompt quality beyond what word-list heuristics can measure, running all verdicts in parallel and comparing results against the heuristic baseline. Provides a cost-tracked, auditable layer of evaluation for D2 and D3.

### `local AI` — 2 article(s)

- [Ollama Embedding Provider: Local Semantic Embeddings](./ollama-embedding-provider-local-semantic-embeddings.md) — The `OllamaEmbeddingProvider` generates real semantic embeddings by calling a locally-running Ollama server, requiring no API key and keeping data on-device. It lazily initializes the Ollama client on first use and auto-detects vector dimensionality from the first embedding call.
- [SentenceTransformer Embedding Provider: Local Semantic Embeddings](./sentencetransformer-embedding-provider-local-semantic-embeddings.md) — The `SentenceTransformerProvider` generates real semantic embeddings using the `sentence-transformers` library, with lazy model loading to defer the heavy import until first use. The default model `all-MiniLM-L6-v2` balances quality and speed with 384-dimensional normalized vectors, and supports GPU acceleration via configurable device selection.

### `local inference` — 1 article(s)

- [OllamaEngine: Local Model Cognitive Adapter](./ollamaengine-local-model-cognitive-adapter.md) — OllamaEngine connects soul-protocol's cognitive pipeline to a locally running Ollama server, using the existing `httpx` dependency so no additional packages are required. It posts prompts to Ollama's REST API and returns the model response, enabling fully offline AI companion operation.

### `local-storage` — 1 article(s)

- [Local Filesystem Eternal Storage Provider](./local-filesystem-eternal-storage-provider.md) — `LocalStorageProvider` archives soul data as files on the local filesystem using a hash-based naming scheme to avoid collisions. It serves as the most basic, always-available eternal storage tier — no network, no external dependency — making it suitable for development, offline use, and as a fast local cache alongside remote backends.

### `logging` — 1 article(s)

- [Logging Configuration Guide for Soul Protocol](./logging-configuration-guide-for-soul-protocol.md) — Provides three ready-to-use logging configurations for the soul-protocol runtime — a human-readable development setup, a structured JSON production setup, and a dict-based config compatible with YAML/JSON config files. All configurations target the `soul_protocol.runtime` logger namespace to give operators precise control over verbosity without silencing third-party libraries.

### `long-horizon` — 1 article(s)

- [Test Package Init: Long-Horizon Ablation Study](./test-package-init-long-horizon-ablation-study.md) — Package initialiser for `tests/test_long_horizon/`, the sub-package housing ablation study tests that evaluate soul memory and context performance over extended interaction sequences. Created 2026-03-11.

### `long-horizon benchmark` — 3 article(s)

- [Test Suite for Long-Horizon Benchmark Analysis (Cohen's d, Effect Labels, Report Generation)](./test-suite-for-long-horizon-benchmark-analysis-cohens-d-effect-labels-report-gen.md) — Validates the statistical analysis layer of the long-horizon benchmark framework, covering Cohen's d calculation, effect-size labeling, and the LongHorizonAnalyzer that produces summary tables and Markdown reports from experimental results. Uses a deterministic mock-results factory to isolate the analysis logic from live LLM calls.
- [Test Suite for Long-Horizon Benchmark Runner](./test-suite-for-long-horizon-benchmark-runner.md) — Validates the multi-condition benchmark runner that processes conversation scenarios through different soul configurations (Full Soul, RAG Only, Personality Only, Bare) and records recall, memory growth, and bond-strength metrics. Async tests use a short 15-turn scenario fixture to keep CI fast while still exercising the full execution path.
- [Test Suite for Long-Horizon Benchmark Scenarios](./test-suite-for-long-horizon-benchmark-scenarios.md) — Validates the three benchmark scenario generators (Life Updates, Emotional Rollercoaster, Adversarial Burial) that produce 100+ turn conversation scripts with planted facts, test points, and recall challenges. Tests verify determinism, scenario structure, seeded variation, and the TestPoint dataclass.

### `machine-learning` — 2 article(s)

- [DSPy Optimization Pipeline: MIPROv2-Based Significance Gate and Query Expansion Tuning](./dspy-optimization-pipeline-miprov2-based-significance-gate-and-query-expansion-t.md) — This pipeline runs a four-phase experiment: baseline heuristic ablation, DSPy MIPROv2 optimization of the significance gate and query expander, post-optimization ablation with the trained modules active, and saved reports comparing before/after metrics. It exists to replace hand-tuned heuristics with learned prompts that generalize across diverse interaction types.
- [DSPy Training Dataset Generator](./dspy-training-dataset-generator.md) — Generates labeled training data for DSPy-optimized memory significance and recall query expansion. Produces two dataset types—significance classification and query expansion—split into train/val sets for fine-tuning the soul's memory filtering models.

### `manifest` — 1 article(s)

- [Soul Manifest — Archive Metadata Header for .soul Files](./soul-manifest-archive-metadata-header-for-soul-files.md) — The `Manifest` model defines the `manifest.json` file stored at the root of every `.soul` zip archive. It captures format version, soul identity, creation timestamp, an integrity checksum placeholder, and an open `stats` dict for runtime-specific metadata like memory counts and layer names.

### `markdown` — 1 article(s)

- [Markdown Soul Parser: soul.md to SoulConfig Conversion](./markdown-soul-parser-soulmd-to-soulconfig-conversion.md) — Parses human-authored soul.md files into SoulConfig, supporting optional YAML frontmatter for structured metadata alongside free-form markdown sections for identity, personality, core memory, and DNA. Automatically generates a DID if one is not provided.

### `math utilities` — 1 article(s)

- [Vector Similarity Functions — Pure-Python Cosine, Euclidean, and Dot Product](./vector-similarity-functions-pure-python-cosine-euclidean-and-dot-product.md) — This module provides three zero-dependency vector similarity functions (`cosine_similarity`, `euclidean_distance`, `dot_product`) using only the Python standard library. All three include mismatch guards added in 2026-03-06 to catch incompatible vectors from different embedding backends before producing silent garbage results.

### `mcp` — 2 article(s)

- [FastMCP Server: 28-Tool Soul Protocol Agent Integration](./fastmcp-server-28-tool-soul-protocol-agent-integration.md) — Implements the full soul-protocol MCP server using FastMCP, exposing 28 tools, 3 resources, and 2 prompts for AI agent integration. Manages a registry of multiple souls, handles auto-reload from disk, lazy cognitive engine wiring, and graceful shutdown with auto-save.
- [MCP Package Public Entry Points](./mcp-package-public-entry-points.md) — Thin package init that re-exports `create_server` and `run_server` from the MCP server module, providing a stable public surface for consumers who integrate soul-protocol's FastMCP server. Keeps the import path short (`from soul_protocol.mcp import create_server`) regardless of internal module organization.

### `mcp-server` — 1 article(s)

- [Test Suite for Zip Recall Bug: Soul File Export/Awaken Roundtrip and Stale MCP Cache](./test-suite-for-zip-recall-bug-soul-file-exportawaken-roundtrip-and-stale-mcp-cac.md) — This regression test suite targets a production bug where `soul_recall` via the MCP server returned zero results for memories that existed in the `.soul` file on disk. The root cause was the MCP server loading souls at startup and never reloading when the file was updated externally. Tests cover semantic and episodic recall after zip roundtrip, bytes-based awakening, multi-memory preservation, content fidelity, and the stale-cache simulation.

### `memory` — 56 article(s)

- [ACT-R Activation-Based Memory Scoring](./act-r-activation-based-memory-scoring.md) — Implements Anderson's ACT-R cognitive model for memory recall scoring. Combines power-law recency/frequency decay, query-spreading activation, emotional boost from somatic markers, and personality-modulated ranking to produce a single activation score per memory candidate.
- [Archival Memory Store for Conversation History](./archival-memory-store-for-conversation-history.md) — Provides long-term, compressed storage for complete conversation sessions via `ArchivalMemoryStore` and `ConversationArchive`. Supports keyword search across summaries and key moments, and date-range queries, without consuming active context.
- [BM25 and Token-Overlap Memory Search Engine](./bm25-and-token-overlap-memory-search-engine.md) — Implements the core text-matching primitives used by all three memory stores: a tokenizer that handles natural language and code identifiers, a BM25 index with IDF weighting, and a lightweight token-overlap relevance scorer kept as a fast fallback. Synonym expansion improves recall for common programming and technical terms.
- [Bond Model: Human-Soul Relationship Strength](./bond-model-human-soul-relationship-strength.md) — Models the trust relationship between a human and their soul companion, tracking bond strength and interaction count. Uses logarithmic growth to make early bonding feel natural while making deep trust genuinely hard-earned through sustained interaction.
- [CLI Test Suite: Core Commands (birth, inspect, status, remember, recall, init)](./cli-test-suite-core-commands-birth-inspect-status-remember-recall-init.md) — Comprehensive test suite for the Soul Protocol CLI covering the full lifecycle of a soul: creation, inspection, memory storage across tiers, multi-format output, and init options. Tests use click.testing.CliRunner to invoke commands in isolation and assert on exit codes, output text, and ZIP archive contents.
- [Core Memory Manager — Always-In-Context Identity Block](./core-memory-manager-always-in-context-identity-block.md) — Manages the ~2KB core memory block that stays loaded in context at all times. It holds two editable sections — `persona` (the soul's own identity) and `human` (profile of the bonded human) — with distinct `set`, `edit`, and `append` operations.
- [Dream Engine Test Suite — Memory Consolidation and Behavioral Analysis](./dream-engine-test-suite-memory-consolidation-and-behavioral-analysis.md) — Comprehensive test suite for the soul-protocol `dream` subsystem, which performs offline memory consolidation: clustering topics, detecting recurring procedures, pruning duplicates, and suggesting personality evolution. Tests span unit-level phase functions through full integration scenarios with before/after memory state comparisons.
- [Episodic Memory Store — Autobiographical Interaction History](./episodic-memory-store-autobiographical-interaction-history.md) — Provides `EpisodicStore`, the in-memory repository for timestamped interaction records. Supports both basic and psychology-enriched storage paths, significance-based eviction, GDPR-compliant deletion, and filtering of archived entries from search results.
- [FastMCP Server: 28-Tool Soul Protocol Agent Integration](./fastmcp-server-28-tool-soul-protocol-agent-integration.md) — Implements the full soul-protocol MCP server using FastMCP, exposing 28 tools, 3 resources, and 2 prompts for AI agent integration. Manages a registry of multiple souls, handles auto-reload from disk, lazy cognitive engine wiring, and graceful shutdown with auto-save.
- [Heuristic Sentiment Detection for Somatic Memory Markers](./heuristic-sentiment-detection-for-somatic-memory-markers.md) — Provides zero-dependency emotion detection using curated word lists and a valence-arousal coordinate model, returning a SomaticMarker that the memory pipeline attaches to entries for emotional recall prioritization. Implements Damasio's Somatic Marker Hypothesis without requiring an LLM call.
- [Journal-Backed Memory Store: Event-Sourced Memory with Rebuildable Projections](./journal-backed-memory-store-event-sourced-memory-with-rebuildable-projections.md) — `JournalBackedMemoryStore` is a spike implementation of the `MemoryStore` protocol that treats the append-only journal as the single source of truth, using SQLite FTS5 projections as a rebuildable read cache. It was designed to eliminate the class of bug where cleanup operations irreversibly wiped memory state.
- [Knowledge Graph with Temporal Edges](./knowledge-graph-with-temporal-edges.md) — Implements `KnowledgeGraph` and `TemporalEdge` — a lightweight entity-relationship store with time-bounded validity, multi-hop traversal, shortest-path search, and progressive context loading for recall augmentation.
- [LearningEvent — Formalized Lesson Model for Procedural Memory and Skill XP](./learningevent-formalized-lesson-model-for-procedural-memory-and-skill-xp.md) — `LearningEvent` captures insights extracted from evaluating success and failure — distinct from episodic memory (what happened) and semantic memory (what is known). Stored in procedural memory and linked to skills, it drives reinforcement-style confidence updates through `apply()`, `reinforce()`, and `weaken()` methods.
- [LIDA Significance Gate for Episodic Memory Storage](./lida-significance-gate-for-episodic-memory-storage.md) — Implements a LIDA-inspired attention gate that decides whether an interaction is significant enough to store as an episodic memory. Four dimensions — novelty, emotional intensity, goal relevance, and content richness — are blended into a single score, filtered by a tunable threshold.
- [Long-Horizon Ablation Runner — Four-Condition Infrastructure Benchmark](./long-horizon-ablation-runner-four-condition-infrastructure-benchmark.md) — The `LongHorizonRunner` executes 100+ turn synthetic conversation scenarios through four ablation conditions (Full Soul, RAG Only, Personality Only, Bare Baseline) and collects infrastructure metrics — recall precision, memory count, memory efficiency, bond trajectory — without requiring an LLM for response generation. It optionally integrates DSPy query expansion and significance gating.
- [Memory Categories and Salience Scoring Example](./memory-categories-and-salience-scoring-example.md) — Demonstrates Soul Protocol's v0.2.3 memory extraction taxonomy by walking through the observe pipeline with five realistic interactions, then inspecting how the system assigns categories, salience scores, and memory types to extracted facts. The example also shows the deduplication pipeline and progressive content loading (L0/L1/L2) through targeted recall queries.
- [Memory Primitives — MemoryEntry, MemoryStore Protocol, and DictMemoryStore](./memory-primitives-memoryentry-memorystore-protocol-and-dictmemorystore.md) — This module defines the atomic unit of soul memory (`MemoryEntry`), the backend-agnostic `MemoryStore` protocol, and a reference in-memory implementation (`DictMemoryStore`). It also introduces `Interaction` and `Participant` models for multi-party conversations, along with `MemoryVisibility` tiers for controlling what surfaces in public channel contexts.
- [Memory Subsystem Package Exports](./memory-subsystem-package-exports.md) — The `memory` package `__init__.py` re-exports every public symbol from the memory subsystem's modules into a single flat namespace. It acts as a versioned registry — each significant export addition is tagged in the file header — so consumers can import everything from `soul_protocol.runtime.memory` without knowing which submodule it lives in.
- [MemoryManager — Unified Memory Orchestration Facade](./memorymanager-unified-memory-orchestration-facade.md) — The `MemoryManager` is the top-level coordinator for all memory subsystems. It orchestrates the full observe pipeline — sentiment, attention gate, episodic storage, fact extraction, entity extraction, knowledge graph updates, contradiction detection, self-model updates — and exposes a unified recall interface.
- [Multi-Soul Management and Parallel Identity Example](./multi-soul-management-and-parallel-identity-example.md) — Shows how to create and manage multiple independent Soul instances in a single process, each with distinct archetypes, values, and memory spaces. The example covers the full lifecycle from birth to export and reawakening, demonstrating that memory isolation is maintained across souls and that soul files persist identity correctly across process restarts.
- [Personality-Modulated Memory Retrieval (OCEAN Trait Scoring)](./personality-modulated-memory-retrieval-ocean-trait-scoring.md) — Maps each of the five OCEAN personality traits to a memory-scoring signal so that a soul's recall results reflect its character rather than pure relevance rank. The module injects per-trait weight deltas into the activation pipeline, remaining fully backwards-compatible when all traits sit at the default 0.5 midpoint.
- [ProceduralStore: How-To Memory Storage and GDPR Deletion](./proceduralstore-how-to-memory-storage-and-gdpr-deletion.md) — Provides an in-memory store specifically for procedural memories — step-by-step how-tos and learned processes. Includes token-overlap relevance search and GDPR-targeted deletion operations added in v0.3.
- [RecallEngine: Cross-Store Memory Retrieval with ACT-R Activation](./recallengine-cross-store-memory-retrieval-with-act-r-activation.md) — The RecallEngine is Soul Protocol's central retrieval coordinator, querying episodic, semantic, and procedural stores in parallel, ranking results via ACT-R activation scores, applying OCEAN personality modulation and visibility filtering, and optionally enriching results through the knowledge graph. It is the single integration point through which all memory retrieval flows.
- [Rule-Based Memory Compressor](./rule-based-memory-compressor.md) — Provides `MemoryCompressor`, a SimpleMem-inspired compression pipeline that reduces memory footprint through deduplication, importance-based pruning, and grouped summarization — entirely without LLM calls.
- [Runtime Types: The Complete Soul Protocol Pydantic Model Library](./runtime-types-the-complete-soul-protocol-pydantic-model-library.md) — `types.py` is the canonical data model layer for Soul Protocol — over 30 Pydantic models covering identity, personality (OCEAN), psychology-informed memory, emotional state, evolution, and interaction. It is the schema backbone that all other runtime modules depend on.
- [SearchStrategy Protocol: Pluggable Memory Retrieval Scoring](./searchstrategy-protocol-pluggable-memory-retrieval-scoring.md) — Defines a Protocol interface for memory retrieval scoring, enabling consumers to replace the default BM25 scorer with embeddings, vector databases, or any custom implementation. Ships with two built-in strategies: a zero-dependency token-overlap fallback and a BM25-backed default.
- [SelfModelManager: Evolving Self-Concept for Digital Souls](./selfmodelmanager-evolving-self-concept-for-digital-souls.md) — Implements Klein's self-model theory for AI companions — the soul discovers what it is by observing its own interactions and building a map of the domains where it operates. Self-images surface as prompt fragments that anchor the soul's personality in generated responses.
- [Semantic Contradiction Detection for Memory Pipeline](./semantic-contradiction-detection-for-memory-pipeline.md) — Detects when a new memory contradicts an existing one using heuristic pattern matching (negation pairs, entity-attribute assertions, verb-based fact patterns) and optional LLM delegation. When a contradiction is detected, the superseded memory is marked rather than deleted.
- [Semantic Memory Deduplication and Fact Reconciliation](./semantic-memory-deduplication-and-fact-reconciliation.md) — Implements `reconcile_fact()`, the gating function that decides whether a new semantic fact should be created, merged into an existing entry, or skipped as a near-duplicate. Uses an enhanced Jaccard similarity with a containment coefficient to handle enriched/superset facts correctly.
- [SemanticStore: Fact-Based Memory with Confidence and Eviction](./semanticstore-fact-based-memory-with-confidence-and-eviction.md) — Stores semantic memories — extracted facts and general knowledge about the world and the user — with importance scoring, confidence tracking, supersession logic for updated facts, and a bounded capacity that evicts least-important entries when full. GDPR deletion APIs are provided for targeted and time-based removal.
- [Smart Memory Reranking via LLM (Prompt-Injection-Hardened)](./smart-memory-reranking-via-llm-prompt-injection-hardened.md) — Provides an optional second-pass reranking stage that uses a lightweight LLM call to select the most contextually relevant memories from a heuristically-ranked candidate pool. The implementation includes robust prompt injection defenses because memory content is untrusted user input that could manipulate the ranking decision.
- [Soul: The Central Digital Identity and Lifecycle Manager](./soul-the-central-digital-identity-and-lifecycle-manager.md) — The Soul class is the top-level API object in Soul Protocol — it unifies identity, memory, personality, skills, evolution, dreaming, bonding, and eternal storage into a single coherent interface. It is the primary object that consuming applications instantiate and interact with.
- [Test Package Initializer for Memory Subsystem Tests](./test-package-initializer-for-memory-subsystem-tests.md) — The `tests/test_memory/__init__.py` file marks the `test_memory` directory as a Python package, enabling pytest to discover and organize the memory subsystem test suite as a cohesive module. It contains no executable code.
- [Test Suite: Cross-Session Contradiction Detection in the Observe Pipeline](./test-suite-cross-session-contradiction-detection-in-the-observe-pipeline.md) — End-to-end regression tests for Soul Protocol's contradiction detection, targeting a specific bug where heuristic contradiction detection fired in isolation but silently failed during `observe()` when a user updated a fact across sessions (e.g., location or employer change). Tests verify that new facts correctly supersede old ones.
- [Test Suite for Archival Memory Integration: Compression Pipeline and Persistence](./test-suite-for-archival-memory-integration-compression-pipeline-and-persistence.md) — This integration test suite validates the full archival pipeline in `MemoryManager`: how old episodic entries are compressed into `ConversationArchive` objects, how high-importance entries become key moments, how archived entries are excluded from recall, and how archives survive serialization round-trips through `to_dict()` / `from_dict()`.
- [Test Suite for Auto-Scheduled Consolidation (F5)](./test-suite-for-auto-scheduled-consolidation-f5.md) — This test suite validates that `Soul.observe()` automatically triggers memory consolidation (archival and reflection) at a configurable interval, that the interval is configurable and disableable, and that the interaction count persists through export/awaken cycles so consolidation timing survives restarts.
- [Test Suite for Bi-Temporal Memory Fields (ingested_at and superseded)](./test-suite-for-bi-temporal-memory-fields-ingestedat-and-superseded.md) — Validates bi-temporal tracking on MemoryEntry — the `ingested_at` timestamp (when a fact entered this soul's memory vs. when it was created) and the `superseded` flag (marking outdated facts replaced by newer information). Tests cover defaults, explicit values, manager behavior, serialization round-trips, and backward compatibility with soul files that predate these fields.
- [Test Suite for Fact and Entity Extraction from Interaction Text](./test-suite-for-fact-and-entity-extraction-from-interaction-text.md) — Validates the pattern-based extraction pipeline that parses user messages for facts (name, preferences, identity, tools, projects) and entities (tech terms, proper nouns with inferred relations). Tests cover each extraction pattern, punctuation stripping, stopword exclusion, and deduplication of repeated facts.
- [Test Suite for GDPR-Compliant Memory Deletion](./test-suite-for-gdpr-compliant-memory-deletion.md) — Comprehensive tests for Soul Protocol's right-to-erasure implementation, covering query-based, entity-based, and time-based deletion across all memory tiers, with full audit trail verification. Ensures deleted memories become invisible to recall and that audit records never expose the deleted content itself.
- [Test Suite for Memory Activation Functions (ACT-R Base-Level, Spreading, Emotional Boost)](./test-suite-for-memory-activation-functions-act-r-base-level-spreading-emotional-.md) — Validates the three activation components that determine how retrievable a memory is at query time: the ACT-R base-level activation formula (recency and frequency), spreading activation (query-content token overlap), and emotional boost (somatic marker arousal and valence). Together these tests pin the mathematical contracts that underlie memory retrieval scoring.
- [Test Suite for Memory Attention and Interaction Significance Scoring](./test-suite-for-memory-attention-and-interaction-significance-scoring.md) — Validates the attention system that decides whether an interaction is significant enough to trigger memory storage. Tests cover novelty scoring, emotional intensity detection, goal-relevance matching against core values, and the composite SignificanceScore type.
- [Test Suite for Memory Consolidation, Reflection, and Fact Supersession](./test-suite-for-memory-consolidation-reflection-and-fact-supersession.md) — Validates the consolidation pipeline that transforms raw episodic memories into structured semantic knowledge — converting summaries into semantic memories, grouping episodes under general-event themes, storing self-insight and emotional patterns, and superseding outdated facts. Also tests the reflect() entry point and the apply=False dry-run mode.
- [Test Suite for Memory Contradiction Detection (Heuristic and LLM Modes)](./test-suite-for-memory-contradiction-detection-heuristic-and-llm-modes.md) — Validates the contradiction detector that identifies when a new memory conflicts with existing ones — using both fast heuristic rules (negation patterns, entity-attribute conflicts) and LLM-powered analysis. Tests cover true-positive detection, false-positive prevention, superseded-memory exclusion, similarity thresholds, and graceful fallback when the LLM errors.
- [Test Suite for Memory Deduplication (Jaccard Similarity, Short-Token False Positives, and Containment Merge)](./test-suite-for-memory-deduplication-jaccard-similarity-short-token-false-positiv.md) — Validates the deduplication system that prevents near-identical facts from being stored multiple times, covering the tokenizer, Jaccard similarity computation, short-token false-positive prevention (go vs. js, ai vs. ml), fact reconciliation decisions (CREATE/SKIP/MERGE), and the containment-merge logic for enriched facts.
- [Test Suite for the Archival Memory Store](./test-suite-for-the-archival-memory-store.md) — This test suite validates `ArchivalMemoryStore` — the fifth memory tier that stores compressed conversation summaries as `ConversationArchive` objects. It covers creation, keyword search with overlap-based ranking, date-range overlap queries, count tracking, and get-by-id retrieval.
- [Test Suite: Graph-Augmented Recall Pipeline](./test-suite-graph-augmented-recall-pipeline.md) — Validates the integration of KnowledgeGraph with RecallEngine, covering graph traversal via progressive_context at multiple depth levels, graph-wired recall with entity matching, deduplication, and graceful degradation when no graph is provided.
- [Test Suite: Heuristic Sentiment Detection (Somatic Markers)](./test-suite-heuristic-sentiment-detection-somatic-markers.md) — Tests for the heuristic sentiment detector that produces Damasio-style somatic markers (valence, arousal, label) from raw text, covering neutral text, positive/negative words, intensifiers, negation, mixed emotions, and label accuracy.
- [Test Suite: Memory Search Tokenizer and Synonym Expansion](./test-suite-memory-search-tokenizer-and-synonym-expansion.md) — Tests for two search quality improvements (issues #10 and #11): a tokenizer that splits on code-style separators like slashes and underscores, and a synonym expansion layer that maps programming terms to related aliases so queries for 'database' match memories containing 'postgresql'.
- [Test Suite: Memory Visibility Tiers](./test-suite-memory-visibility-tiers.md) — Tests for the three-tier memory visibility system (PUBLIC, BONDED, PRIVATE) that controls which memories are accessible to external requesters based on bond strength, preventing private memories from leaking while maintaining full system access for the soul itself.
- [Test Suite: MemoryManager Facade](./test-suite-memorymanager-facade.md) — Core test suite for the MemoryManager facade, covering all five memory stores (core, episodic, semantic, procedural, knowledge graph), cross-store recall, significance-gated observation, and the Bug #15 edit_core behavior fix.
- [Test Suite: OCEAN Personality-Modulated Memory Retrieval](./test-suite-ocean-personality-modulated-memory-retrieval.md) — Comprehensive tests for the personality modulation layer that adjusts memory recall scores based on OCEAN (Big Five) personality traits, covering per-trait signal functions, the composite boost calculation, and end-to-end recall ranking changes.
- [Test Suite: Phase 1 Ablation Fixes](./test-suite-phase-1-ablation-fixes.md) — Tests for five Phase 1 ablation fixes: the significance gate short-message penalty, mundane-interaction rejection, select_top_k attention selection, BM25Index scoring and the BM25SearchStrategy protocol, and logarithmic bond growth with diminishing returns.
- [Test Suite: Phase 2 Memory Architecture (Categories, Salience, Dedup)](./test-suite-phase-2-memory-architecture-categories-salience-dedup.md) — Tests for the Phase 2 memory architecture improvements: the MemoryCategory enum, generated abstracts, salience scoring, the deduplication reconcile_fact pipeline, TemporalEdge metadata in the knowledge graph, and the EpisodicStore entry-update API.
- [Test Suite: Pluggable Search Strategy Protocol](./test-suite-pluggable-search-strategy-protocol.md) — Tests for the SearchStrategy protocol and its propagation through the full memory stack — from Soul.birth() down through MemoryManager, RecallEngine, spreading_activation(), and compute_activation() — verifying that custom scoring functions can replace the default token-overlap approach at every layer.
- [Test Suite: Progressive Disclosure in Recall](./test-suite-progressive-disclosure-in-recall.md) — Tests for RecallEngine's progressive recall mode, which returns a primary set of full-content memories plus an overflow set of abstract-only summaries, allowing callers to receive more context without exceeding token budgets.
- [Test Suite: Self-Model Manager (Soul's Self-Concept)](./test-suite-self-model-manager-souls-self-concept.md) — Tests for SelfModelManager, which implements Klein's self-concept model — the soul's ongoing self-knowledge about what domains it is competent in, which roles it plays, and how confident it is in those self-images based on accumulated evidence.

### `memory continuity` — 1 article(s)

- [Test Suite: Soul Reincarnation Lifecycle](./test-suite-soul-reincarnation-lifecycle.md) — Validates that `Soul.reincarnate()` correctly transitions a soul to a new identity while preserving accumulated state. The suite ensures that reincarnation is not a hard reset — personality, memories, values, archetype, and bond all carry forward, while the DID and incarnation counter change to mark the new life.

### `memory filtering` — 1 article(s)

- [Test Suite: Scope Tags and Hierarchical Memory Access Control](./test-suite-scope-tags-and-hierarchical-memory-access-control.md) — Tests the `match_scope` and `match_scope_strict` helpers that implement hierarchical RBAC/ABAC filtering for memory recall. The suite covers bidirectional glob containment, strict one-way matching, scope normalization, export/awaken round-trips for scoped memories, and `Soul.recall` scope-filter integration.

### `memory management` — 2 article(s)

- [CLI Test Suite: Health, Cleanup, and Repair Commands](./cli-test-suite-health-cleanup-and-repair-commands.md) — Test suite for three soul maintenance CLI commands: `soul health` (diagnostic reporting), `soul cleanup` (duplicate/low-importance memory removal), and `soul repair` (energy reset). Covers dry-run safety guards, auto-mode mutations, and persistence verification.
- [Test Suite: Memory Compression -- Summarization, Deduplication, and Importance Pruning](./test-suite-memory-compression-summarization-deduplication-and-importance-pruning.md) — Tests for `MemoryCompressor`, which reduces the size of a soul's long-term memory store through four strategies: text summarization, near-duplicate removal, importance-based pruning, and export splitting. Covers edge cases like empty inputs, threshold tuning, and the rule that high-importance memories are never pruned regardless of age.

### `memory pipeline` — 4 article(s)

- [Cognitive Prompt Templates for LLM-Powered Memory Tasks](./cognitive-prompt-templates-for-llm-powered-memory-tasks.md) — This module contains all LLM prompt templates used by `CognitiveProcessor` for sentiment analysis, significance gating, fact extraction, entity extraction, and self-reflection. Each template embeds a `[TASK:xxx]` routing marker so that `HeuristicEngine` can dispatch to its regex fallbacks without parsing the full prompt.
- [CognitiveEngine Protocol, HeuristicEngine, and CognitiveProcessor](./cognitiveengine-protocol-heuristicengine-and-cognitiveprocessor.md) — This module defines the core cognitive abstraction layer for soul-protocol: the `CognitiveEngine` protocol (the one interface consumers implement), `HeuristicEngine` (the zero-dependency regex fallback), and `CognitiveProcessor` (the internal orchestrator that turns raw LLM responses into typed memory objects). Together they decouple memory pipeline logic from any specific LLM provider.
- [DSPy Cognitive Modules: Optimizable Memory Pipeline Components](./dspy-cognitive-modules-optimizable-memory-pipeline-components.md) — This module defines three DSPy-native modules — `SignificanceGate`, `QueryExpander`, and `FactExtractor` — that replace hand-written heuristics in soul-protocol's memory pipeline with learnable, optimizable LLM programs. All DSPy imports are lazy so the module loads safely in environments where DSPy is not installed.
- [DSPyCognitiveProcessor: Learnable Memory Cognition via DSPy](./dspycognitiveprocessor-learnable-memory-cognition-via-dspy.md) — DSPyCognitiveProcessor is a drop-in replacement for `CognitiveProcessor` that routes significance assessment, query expansion, and fact extraction through optimizable DSPy modules instead of hand-written prompts. It bridges DSPy's synchronous execution model to soul-protocol's async architecture via a thread pool executor.

### `memory retrieval` — 4 article(s)

- [Test Suite for Vector Similarity Functions](./test-suite-for-vector-similarity-functions.md) — Covers the three core similarity primitives — cosine similarity, Euclidean distance, and dot product — used by soul-protocol's embedding layer. Tests enforce correct mathematical behavior, zero-vector safety, empty-vector edge cases, and dimension-mismatch guards.
- [Test Suite for VectorSearchStrategy](./test-suite-for-vectorsearchstrategy.md) — Tests the VectorSearchStrategy class, which wraps an embedder to perform similarity-based memory search with configurable threshold filtering and pre-built index support. Covers both the deterministic HashEmbedder path and the semantic TFIDFEmbedder path.
- [Test Suite: LLM-Powered Memory Reranking and Smart Recall](./test-suite-llm-powered-memory-reranking-and-smart-recall.md) — Covers `rerank_memories()`, the `_parse_indices()` helper, and `Soul.smart_recall()` — the optional LLM-driven reranking layer on top of heuristic memory retrieval. The suite validates correct ordering, fallback behavior on engine failure or timeout, opt-in flag semantics, and prompt injection defenses.
- [Test Suite: Retrieval Trace — Observability Receipt for Memory Recall](./test-suite-retrieval-trace-observability-receipt-for-memory-recall.md) — Locks the shape and runtime behavior of `RetrievalTrace`, the observability receipt emitted by `Soul.recall()` and `Soul.smart_recall()` after every memory lookup. The suite verifies that traces are populated even on empty results, are not serialized into `.soul` files, and correctly record actor, query, candidates, and rerank metadata.

### `memory search` — 8 article(s)

- [Embeddings Subsystem: Factory and Public API](./embeddings-subsystem-factory-and-public-api.md) — The embeddings `__init__.py` provides the `get_embedding_provider()` factory function and re-exports all public types from the embedding subsystem. The factory uses lazy imports for optional providers (sentence-transformers, OpenAI, Ollama) so that users without those dependencies installed do not pay an import cost or receive confusing `ImportError` on module load.
- [Hash Embedder: Deterministic Pseudo-Embeddings for Testing](./hash-embedder-deterministic-pseudo-embeddings-for-testing.md) — The `HashEmbedder` produces deterministic fixed-length vectors from text using character n-gram hashing — no external dependencies, no model downloads, same input always yields the same output. It is intentionally non-semantic (similar texts do not produce similar vectors) and exists solely to enable embedding pipeline tests without requiring real embedding backends.
- [Ollama Embedding Provider: Local Semantic Embeddings](./ollama-embedding-provider-local-semantic-embeddings.md) — The `OllamaEmbeddingProvider` generates real semantic embeddings by calling a locally-running Ollama server, requiring no API key and keeping data on-device. It lazily initializes the Ollama client on first use and auto-detects vector dimensionality from the first embedding call.
- [OpenAI Embedding Provider with Retry Backoff](./openai-embedding-provider-with-retry-backoff.md) — The `OpenAIEmbeddingProvider` produces high-quality semantic embeddings via the OpenAI API, with built-in exponential backoff for rate limits and transient server errors. It lazily initializes the OpenAI client, pre-knows dimensions for standard models to avoid a probe call, and guarantees input ordering even when the API returns results out of order.
- [SentenceTransformer Embedding Provider: Local Semantic Embeddings](./sentencetransformer-embedding-provider-local-semantic-embeddings.md) — The `SentenceTransformerProvider` generates real semantic embeddings using the `sentence-transformers` library, with lazy model loading to defer the heavy import until first use. The default model `all-MiniLM-L6-v2` balances quality and speed with 384-dimensional normalized vectors, and supports GPU acceleration via configurable device selection.
- [Similarity Functions Re-export (Runtime Shim)](./similarity-functions-re-export-runtime-shim.md) — This shim re-exports the three vector similarity functions (`cosine_similarity`, `dot_product`, `euclidean_distance`) from their canonical spec-layer location into the runtime embeddings namespace. It provides a stable import path for runtime code without creating a direct dependency on the spec layer's internal structure.
- [TF-IDF Embedder: Vocabulary-Fitted Semantic Embeddings](./tf-idf-embedder-vocabulary-fitted-semantic-embeddings.md) — The `TFIDFEmbedder` builds corpus-fitted TF-IDF vectors using only Python's standard library — no external dependencies. It produces semantically meaningful similarity (unlike `HashEmbedder`) but requires a `fit()` call on a corpus before embedding, making it useful for lightweight production use cases where a known document set exists.
- [VectorSearchStrategy: Embedding-Based Semantic Memory Search](./vectorsearchstrategy-embedding-based-semantic-memory-search.md) — The `VectorSearchStrategy` wraps any `EmbeddingProvider` to implement semantic similarity search over memory entries. It maintains a pre-built index for performance, falls back to on-the-fly embedding for un-indexed candidates, and uses cosine similarity with a configurable threshold to rank and filter results.

### `memory store` — 1 article(s)

- [Test Suite: SoulContainer — Spec-Layer Soul Creation, Persistence, and Memory Access](./test-suite-soulcontainer-spec-layer-soul-creation-persistence-and-memory-access.md) — Validates the spec-layer `SoulContainer` — the lightweight soul abstraction for integrations that do not need the full runtime. The suite covers container construction, traits storage, `.soul` file save/open round-trips, multi-layer memory preservation, error handling for missing files, and the default `DictMemoryStore`.

### `memory system` — 1 article(s)

- [Dream: Offline Memory Consolidation Engine](./dream-offline-memory-consolidation-engine.md) — The `Dreamer` class implements offline memory consolidation for soul agents — the async background process that reviews accumulated episodic memories in batch, detects patterns, compacts redundant data, and synthesizes higher-order knowledge. It is the offline counterpart to `observe()`, which handles real-time interaction processing.

### `memory-model` — 1 article(s)

- [Test Suite for v0.3.4 Memory Model: MemoryCategory, Salience, and Temporal Edge Metadata](./test-suite-for-v034-memory-model-memorycategory-salience-and-temporal-edge-metad.md) — This test suite covers spec-level additions introduced in Soul Protocol v0.3.4: the `MemoryCategory` enum for classifying memory entries, `salience` as an additive activation boost on `MemoryEntry`, and `metadata` on `TemporalEdge` along with its surfacing through `KnowledgeGraph` query methods. The salience tests were rewritten from a multiplicative to an additive boost model to prevent low-importance memories from being penalized disproportionately.

### `memory-system` — 9 article(s)

- [D1 Memory Recall Evaluator](./d1-memory-recall-evaluator.md) — Evaluates a soul's ability to recall planted facts across long conversations, measuring recall precision, memory storage efficiency, and resistance to adversarial "burial" noise. Contributes 20% of the composite Soul Health Score.
- [DSPy Training Dataset Generator](./dspy-training-dataset-generator.md) — Generates labeled training data for DSPy-optimized memory significance and recall query expansion. Produces two dataset types—significance classification and query expansion—split into train/val sets for fine-tuning the soul's memory filtering models.
- [Memory Evolution Simulation Script](./memory-evolution-simulation-script.md) — A standalone simulation that exercises 100 interactions to demonstrate memory compression, archival storage growth, and temporal knowledge-graph evolution in soul-protocol. It serves as a developer-facing proof that the memory pipeline works end-to-end without a live LLM.
- [Soul Context Injection — CLI Inject Module](./soul-context-injection-cli-inject-module.md) — Provides functions to write or replace a soul's identity, state, and recent memories into agent platform configuration files (CLAUDE.md, .cursorrules, etc.) using idempotent HTML comment markers. Supports six platforms and runs without MCP.
- [Soul Protocol CLI — Primary Command Surface (`main.py`)](./soul-protocol-cli-primary-command-surface-mainpy.md) — The main Click application that powers the `soul` command-line tool, exposing 40+ subcommands covering the full soul lifecycle: initialisation, memory management, eternal storage, platform injection, format conversion, org management, and health tooling.
- [Test Suite for JournalBackedMemoryStore Benchmark Harness](./test-suite-for-journalbackedmemorystore-benchmark-harness.md) — This benchmark harness compares `JournalBackedMemoryStore` against the baseline `DictMemoryStore` across four dimensions: recall quality (recall@5), write latency, search latency, and delete correctness. It was created to produce a data-driven decision on whether the append-only journal architecture is worth adopting, with pass criteria derived from the spike design document.
- [Test Suite for JournalBackedMemoryStore: Unit, Smoke, E2E, and Real-World Scenarios](./test-suite-for-journalbackedmemorystore-unit-smoke-e2e-and-real-world-scenarios.md) — This comprehensive test suite for `JournalBackedMemoryStore` follows a four-tier discipline — unit, smoke, E2E, and real-world simulations — covering every public method and the failure modes that motivated the journal architecture. Special attention is given to disaster recovery (the April 2026 cleanup incident), GDPR-compliant tombstone deletion, idempotent rebuild, and latency budgets.
- [Test Suite for MemoryEntry, MemoryStore Protocol, and DictMemoryStore](./test-suite-for-memoryentry-memorystore-protocol-and-dictmemorystore.md) — This test suite validates the core in-memory building blocks of Soul Protocol's spec-level memory system: the `MemoryEntry` data model, the `MemoryStore` protocol interface, and the `DictMemoryStore` reference implementation. Tests cover entry creation, metadata handling, layer isolation, BM25-style search, delete semantics, and ordering guarantees.
- [Test Suite for Temporal Knowledge Graph](./test-suite-for-temporal-knowledge-graph.md) — This test suite validates the temporal knowledge graph (`KnowledgeGraph` and `TemporalEdge`) used by Soul Protocol to represent relationships between entities over time. It covers edge creation, time-range activation queries, relationship expiration, duplicate deduplication, point-in-time queries, evolution history, serialization roundtrips, and backward compatibility with older edge formats.

### `metrics` — 1 article(s)

- [Research Metrics Schema — Six-Dimensional Agent Quality Measurement](./research-metrics-schema-six-dimensional-agent-quality-measurement.md) — Defines the full metric collection schema for Soul Protocol's research framework across six orthogonal dimensions: retrieval quality, emotional tracking, personality stability, memory efficiency, bond formation, and skill acquisition. All metric classes are dataclasses that aggregate per-interaction observations into summary statistics for factorial analysis.

### `mood dynamics` — 1 article(s)

- [StateManager: Runtime Soul State and Mood Dynamics](./statemanager-runtime-soul-state-and-mood-dynamics.md) — `StateManager` owns all mutable runtime state for a digital soul — energy, social battery, mood, and interaction timestamps. It provides delta-based updates, time-based auto-regeneration, and EMA-smoothed mood transitions driven by somatic markers from the sentiment pipeline.

### `multi-model` — 2 article(s)

- [LiteLLM Cognitive Engine — Multi-Model Proxy Backend](./litellm-cognitive-engine-multi-model-proxy-backend.md) — An async `CognitiveEngine` implementation that routes LLM calls through a LiteLLM OpenAI-compatible proxy, enabling the research framework to use GPT-4o, Gemini, DeepSeek, Llama, and Qwen as judge or agent engines without changing call sites. It mirrors the `HaikuCognitiveEngine` interface exactly, making the two engines interchangeable.
- [Multi-Judge Quality Runner — Inter-Rater Reliability Across Five LLM Models](./multi-judge-quality-runner-inter-rater-reliability-across-five-llm-models.md) — Runs Soul Protocol's four quality test scenarios with five different judge models (Haiku, Gemini 3 Flash, Gemini 2.5 Flash Lite, DeepSeek V3, Llama 3.3 70B) to measure inter-rater reliability, ensuring that quality advantages observed for soul-enabled agents are not artifacts of a single judge model's preferences or biases.

### `multi-provider` — 1 article(s)

- [LiteLLMEngine: Universal 100+ Provider Cognitive Adapter](./litellmengine-universal-100-provider-cognitive-adapter.md) — Implements `CognitiveEngine` using LiteLLM, providing a single adapter that routes cognitive calls to any of 100+ LLM providers including Anthropic, OpenAI, Ollama, Bedrock, Vertex, and Cohere. Provider is selected by model string format (e.g., `anthropic/claude-haiku-4-5-20251001`).

### `natural language processing` — 1 article(s)

- [Test Suite for TF-IDF Embedder](./test-suite-for-tf-idf-embedder.md) — Validates the TFIDFEmbedder class, which converts text into fixed-length float vectors using term-frequency/inverse-document-frequency weighting. Tests cover the fit/embed lifecycle, semantic similarity behavior, edge cases for unfitted state, and batch consistency.

### `observability` — 3 article(s)

- [Logging Configuration Guide for Soul Protocol](./logging-configuration-guide-for-soul-protocol.md) — Provides three ready-to-use logging configurations for the soul-protocol runtime — a human-readable development setup, a structured JSON production setup, and a dict-based config compatible with YAML/JSON config files. All configurations target the `soul_protocol.runtime` logger namespace to give operators precise control over verbosity without silencing third-party libraries.
- [Retrieval Trace — Observability Receipt for Every Recall and Search Operation](./retrieval-trace-observability-receipt-for-every-recall-and-search-operation.md) — `RetrievalTrace` is a lightweight receipt written once per retrieval call, capturing the ranked candidate list, which candidates were actually used, latency, and a downstream reference. It feeds the paw-runtime JSONL sink, graduation policy, the "Why?" explainability drawer, and SoulBench fixture generation — all from a single portable shape.
- [Test Suite: Retrieval Trace — Observability Receipt for Memory Recall](./test-suite-retrieval-trace-observability-receipt-for-memory-recall.md) — Locks the shape and runtime behavior of `RetrievalTrace`, the observability receipt emitted by `Soul.recall()` and `Soul.smart_recall()` after every memory lookup. The suite verifies that traces are populated even on empty results, are not serialized into `.soul` files, and correctly record actor, query, candidates, and rerank metadata.

### `observe pipeline` — 1 article(s)

- [Test Suite: Cross-Session Contradiction Detection in the Observe Pipeline](./test-suite-cross-session-contradiction-detection-in-the-observe-pipeline.md) — End-to-end regression tests for Soul Protocol's contradiction detection, targeting a specific bug where heuristic contradiction detection fired in isolation but silently failed during `observe()` when a user updated a fact across sessions (e.g., location or employer change). Tests verify that new facts correctly supersede old ones.

### `offline processing` — 1 article(s)

- [Dream: Offline Memory Consolidation Engine](./dream-offline-memory-consolidation-engine.md) — The `Dreamer` class implements offline memory consolidation for soul agents — the async background process that reviews accumulated episodic memories in batch, detects patterns, compacts redundant data, and synthesizes higher-order knowledge. It is the offline counterpart to `observe()`, which handles real-time interaction processing.

### `ollama` — 1 article(s)

- [Ollama Embedding Provider Test Suite — Local Model Integration with Lazy Loading](./ollama-embedding-provider-test-suite-local-model-integration-with-lazy-loading.md) — Test suite for `OllamaEmbeddingProvider`, which connects to a locally-running Ollama server for embedding generation. Tests use a mocked `ollama` library to avoid requiring a real server in CI, covering protocol compliance, lazy client initialization, batch embedding, custom model and base URL configuration, and graceful import error handling.

### `onboarding` — 3 article(s)

- [CLI Test Suite: Full Onboarding, Multi-User Lifecycle, and Destroy Workflow](./cli-test-suite-full-onboarding-multi-user-lifecycle-and-destroy-workflow.md) — End-to-end test suite for the `soul init` onboarding wizard covering user scope creation, fleet provisioning, status reporting, soul deletion, and the destroy command that archives then wipes all data. Also validates journal append-only privacy constraints and users-dir precedence rules.
- [Developer Onboarding Demo Smoke Test](./developer-onboarding-demo-smoke-test.md) — A minimal async smoke test that runs the soul-protocol onboarding demo end-to-end in a non-interactive mode. It uses `monkeypatch` to set `SOUL_DEMO_NO_PAUSE=1`, preventing the demo from blocking on user input during CI.
- [Interactive Rich TUI Demo — `demo.py`](./interactive-rich-tui-demo-demopy.md) — A 5-act interactive terminal walkthrough of soul-protocol that demonstrates memory storage, emotional state, OCEAN personality, and cognitive processing without requiring an LLM or API key. Designed for GIF recording and first-run discovery.

### `openai` — 1 article(s)

- [OpenAI Embedding Provider Test Suite — API Key Validation, Retry Logic, and Batch Embedding](./openai-embedding-provider-test-suite-api-key-validation-retry-logic-and-batch-em.md) — Test suite for `OpenAIEmbeddingProvider`, which calls the OpenAI embeddings API for high-quality vector representations. Tests use a mocked `openai` library and cover protocol compliance, API key validation, exponential backoff retry logic for rate limits, model dimension detection, and graceful import error handling.

### `optimization` — 1 article(s)

- [SoulOptimizer: MIPROv2 Training Harness for Cognitive Modules](./souloptimizer-miprov2-training-harness-for-cognitive-modules.md) — `SoulOptimizer` is an offline training harness that uses DSPy's MIPROv2 optimizer to tune `SignificanceGate` and `QueryExpander` against labeled interaction data derived from ablation scenarios or production logs. Optimized module weights are serialized to disk and loaded by `DSPyCognitiveProcessor` at runtime.

### `orchestration` — 4 article(s)

- [MemoryManager — Unified Memory Orchestration Facade](./memorymanager-unified-memory-orchestration-facade.md) — The `MemoryManager` is the top-level coordinator for all memory subsystems. It orchestrates the full observe pipeline — sentiment, attention gate, episodic storage, fact extraction, entity extraction, knowledge graph updates, contradiction detection, self-model updates — and exposes a unified recall interface.
- [Simulation Engine: Full Factorial Experiment Orchestrator for Soul Protocol Research](./simulation-engine-full-factorial-experiment-orchestrator-for-soul-protocol-resea.md) — The `SimulationEngine` orchestrates the core ablation study by running every combination of (agent, condition, use_case) as an async task, collecting `AgentRunMetrics` for each triple, and aggregating them into a `SimulationResults` object. Failed individual runs are logged and skipped rather than crashing the whole experiment, ensuring partial results are always captured.
- [Soul Health Score Evaluation Suite Runner](./soul-health-score-evaluation-suite-runner.md) — Orchestrates the 7-dimension Soul Health Score evaluation, dynamically loading dimension runners, executing them sequentially, computing the weighted composite score, and returning a structured report. Serves as both a library function and a CLI tool.
- [Test Suite for EternalStorageManager](./test-suite-for-eternalstoragemanager.md) — Unit tests for EternalStorageManager — the orchestration layer that routes archive, recover, and verify operations across multiple storage providers. Covers provider registration/unregistration, selective tier archiving, fallback recovery chains, unavailable-provider skipping, and verification status.

### `org architecture` — 1 article(s)

- [Test Suite: Decision Trace Spec — Agent Proposals, Human Corrections, and Graduation](./test-suite-decision-trace-spec-agent-proposals-human-corrections-and-graduation.md) — Validates the decision-trace spec from Workstream D of the Org Architecture RFC, covering the three payload types (`AgentProposal`, `HumanCorrection`, `DecisionGraduation`), their JSON round-trips, field validation, and the builder helpers that emit correctly shaped `EventEntry` records to the org journal.

### `org initialization` — 1 article(s)

- [CLI Test Suite: Org Initialization (`soul org init`)](./cli-test-suite-org-initialization-soul-org-init.md) — Test suite for `soul org init`, which bootstraps an organization by creating a root soul, a journal, and cryptographic keys. Covers happy-path artifact creation, idempotency guards, force-overwrite behavior, key file permissions, journal privacy, and round-trip loadability.

### `org journal` — 1 article(s)

- [Test Suite: Org Journal Spec — Actor, DataRef, and EventEntry Primitives](./test-suite-org-journal-spec-actor-dataref-and-evententry-primitives.md) — Validates the foundational primitives of Soul Protocol's organizational journal spec: `Actor` (the entity writing an event), `DataRef` (an external data reference with cache policy), and `EventEntry` (an immutable journal record). The suite enforces UTC-only datetimes, non-anonymous writes, required scopes, and payload union handling.

### `org-layer` — 1 article(s)

- [Org Management CLI — `soul org` and `soul user`](./org-management-cli-soul-org-and-soul-user.md) — Implements the `soul org init`, `soul org status`, `soul org destroy`, and `soul user invite` commands for managing multi-soul organisational deployments. Creates a governance root soul, writes journal events for every lifecycle action, and enforces layered protections against root-soul deletion.

### `package structure` — 9 article(s)

- [Context Package Exports: LCMContext and SQLiteContextStore](./context-package-exports-lcmcontext-and-sqlitecontextstore.md) — The `runtime/context` package's `__init__.py` provides a clean public surface for the Lossless Context Management (LCM) subsystem, re-exporting `LCMContext` (the reference `ContextEngine` implementation) and `SQLiteContextStore` (the persistence layer) so consumers import from a single stable path rather than internal submodules.
- [DNA Subpackage Public API (Re-export Module)](./dna-subpackage-public-api-re-export-module.md) — The `dna` subpackage `__init__.py` re-exports the two public functions from `dna/prompt.py` — `dna_to_system_prompt` and `dna_to_markdown` — providing a clean, stable import surface for consumers. This thin shim was fixed during a runtime restructure to use absolute import paths rooted at `soul_protocol.runtime`.
- [EmbeddingProvider Protocol Re-export (Runtime Shim)](./embeddingprovider-protocol-re-export-runtime-shim.md) — This file is a one-line shim that re-exports `EmbeddingProvider` from its canonical location in `soul_protocol.spec.embeddings.protocol` into the runtime embeddings namespace. It exists to provide a stable import path for runtime code while keeping the authoritative protocol definition in the spec layer.
- [Embeddings Subsystem: Factory and Public API](./embeddings-subsystem-factory-and-public-api.md) — The embeddings `__init__.py` provides the `get_embedding_provider()` factory function and re-exports all public types from the embedding subsystem. The factory uses lazy imports for optional providers (sentence-transformers, OpenAI, Ollama) so that users without those dependencies installed do not pay an import cost or receive confusing `ImportError` on module load.
- [Eternal Storage Subsystem Public API](./eternal-storage-subsystem-public-api.md) — The `eternal` subpackage `__init__.py` re-exports the four public symbols from the eternal storage subsystem: `EternalStorageProvider`, `EternalStorageManager`, `ArchiveResult`, and `RecoverySource`. This clean facade hides the internal module layout and provides a single import target for callers persisting soul data to permanent storage.
- [Similarity Functions Re-export (Runtime Shim)](./similarity-functions-re-export-runtime-shim.md) — This shim re-exports the three vector similarity functions (`cosine_similarity`, `dot_product`, `euclidean_distance`) from their canonical spec-layer location into the runtime embeddings namespace. It provides a stable import path for runtime code without creating a direct dependency on the spec layer's internal structure.
- [Test Package Init: Cognitive Engine Test Suite Namespace](./test-package-init-cognitive-engine-test-suite-namespace.md) — Empty `__init__.py` that marks `tests/test_cognitive/` as a Python package, enabling pytest to discover and import tests in the cognitive engine subdirectory. No executable code; its presence is a structural requirement for the test namespace.
- [Test Package Initializer for MCP Server Integration Tests](./test-package-initializer-for-mcp-server-integration-tests.md) — The `tests/test_mcp/__init__.py` file marks the `test_mcp` directory as a Python package, enabling pytest to discover and organize MCP server integration tests as a cohesive module. It contains no executable code — its presence is structural.
- [Test Package Initializer for Memory Subsystem Tests](./test-package-initializer-for-memory-subsystem-tests.md) — The `tests/test_memory/__init__.py` file marks the `test_memory` directory as a Python package, enabling pytest to discover and organize the memory subsystem test suite as a cohesive module. It contains no executable code.

### `package-structure` — 15 article(s)

- [CLI Package Namespace — `soul_protocol.cli`](./cli-package-namespace-soulprotocolcli.md) — An intentionally empty package initialiser that establishes `soul_protocol.cli` as a Python package namespace. Its sole function is structural: making the directory importable so that `cli.main`, `cli.inject`, `cli.org`, and `cli.setup` are discoverable.
- [Engine Package Namespace — `soul_protocol.engine`](./engine-package-namespace-soulprotocolengine.md) — An intentionally minimal package initialiser that marks `soul_protocol.engine` as a Python package and documents its role as the runtime implementation layer behind the protocol's `spec/` models.
- [Eternal Storage Providers Package Init](./eternal-storage-providers-package-init.md) — The `eternal/providers/` package init collects all four storage provider implementations — `LocalStorageProvider`, `MockIPFSProvider`, `MockArweaveProvider`, and `MockBlockchainProvider` — and exposes them through a single import point. This makes provider selection ergonomic for callers who want to pick a backend without navigating nested modules.
- [Evaluation UI Package Marker](./evaluation-ui-package-marker.md) — Package initializer for the `research/eval_ui/` module, created in early March 2026 as a namespace boundary for a planned web-based evaluation results viewer. Contains no executable logic.
- [Evolution Subpackage Public Interface](./evolution-subpackage-public-interface.md) — The `evolution/__init__.py` init re-exports `EvolutionManager` as the sole public entry point for the evolution subsystem. Callers import from `soul_protocol.runtime.evolution` rather than navigating to the `manager` submodule directly.
- [Export Subpackage: Soul Archive I/O Interface](./export-subpackage-soul-archive-io-interface.md) — The `export/__init__.py` init exposes `pack_soul` and `unpack_soul` as the sole public interface for `.soul` archive I/O, hiding the internal split between `pack.py` and `unpack.py`. All callers that need to serialize or deserialize a soul use this single import point.
- [Identity Subpackage: DID Generation Interface](./identity-subpackage-did-generation-interface.md) — The `identity/__init__.py` init exposes `generate_did` as the single public entry point for the identity subsystem, making Decentralized Identifier (DID) creation available via `from soul_protocol.runtime.identity import generate_did` without exposing the underlying DID implementation details.
- [Long-Horizon Ablation Study Package](./long-horizon-ablation-study-package.md) — The `research/long_horizon` package bundles the scenario generators, ablation runner, and statistical analyzer for Soul Protocol's long-horizon study, which proves that the psychology stack (significance gating, activation decay, somatic markers) produces measurable advantages at 100+ turn conversation scales. The `__init__.py` re-exports the three key classes and five scenario generator functions as the public API.
- [Memory Subsystem Package Exports](./memory-subsystem-package-exports.md) — The `memory` package `__init__.py` re-exports every public symbol from the memory subsystem's modules into a single flat namespace. It acts as a versioned registry — each significant export addition is tagged in the file header — so consumers can import everything from `soul_protocol.runtime.memory` without knowing which submodule it lives in.
- [Module Entry Point — `python -m soul_protocol`](./module-entry-point-python-m-soulprotocol.md) — A minimal `__main__.py` that enables running soul-protocol as a module with `python -m soul_protocol`, delegating immediately to the interactive demo. The `__name__` guard prevents accidental execution on import.
- [Quality Validation Package Overview](./quality-validation-package-overview.md) — The `research/quality` package contains Soul Protocol's response quality validation infrastructure: test scenarios, experimental condition definitions, LLM-as-judge scoring, and multi-model benchmarking runners. It is the layer that measures subjective quality (how good does the response feel?) rather than infrastructure metrics (how many memories were stored?).
- [SHS Dimension Evaluators Package](./shs-dimension-evaluators-package.md) — Package marker for the `research/eval/dimensions/` directory, which houses the seven individual Soul Health Score dimension evaluators. Each sub-module implements one evaluation axis of soul quality.
- [Soul Health Score Evaluation CLI Entry Point](./soul-health-score-evaluation-cli-entry-point.md) — Thin `__main__.py` that enables `python -m research.eval.suite` as a runnable command by delegating immediately to `suite.main()`. Provides zero additional logic, keeping the entry point surface minimal.
- [Soul Health Score Evaluation Framework Package](./soul-health-score-evaluation-framework-package.md) — Package marker and documentation anchor for the 7-dimension Soul Health Score (SHS) evaluation framework. Signals to Python's import system that `research/eval/` is a package while documenting the framework's creation context.
- [soul-protocol Public API — Package Root](./soul-protocol-public-api-package-root.md) — The top-level `__init__.py` is the single import surface for all public soul-protocol types, runtime objects, and exceptions. It bridges the two-layer architecture (spec primitives and runtime engine) into a stable, versioned API.

### `packaging` — 1 article(s)

- [Test Suite: test_spec Package Namespace Marker](./test-suite-testspec-package-namespace-marker.md) — An empty `__init__.py` that declares `tests/test_spec/` as a Python package. This file contains no executable code but is structurally required so that pytest and Python's import machinery can discover and run the test modules inside `test_spec/` as part of the broader test suite.

### `parsers` — 4 article(s)

- [JSON Soul Configuration Parser (parse_soul_json)](./json-soul-configuration-parser-parsesouljson.md) — A single-function module that parses a JSON string into a validated SoulConfig using Pydantic's model_validate_json, combining deserialization and schema validation in one atomic step.
- [Markdown Soul Parser: soul.md to SoulConfig Conversion](./markdown-soul-parser-soulmd-to-soulconfig-conversion.md) — Parses human-authored soul.md files into SoulConfig, supporting optional YAML frontmatter for structured metadata alongside free-form markdown sections for identity, personality, core memory, and DNA. Automatically generates a DID if one is not provided.
- [Parsers Subpackage: Soul Configuration File Loader Registry](./parsers-subpackage-soul-configuration-file-loader-registry.md) — The parsers subpackage is the namespace for soul configuration format readers — soul.md, soul.yaml, and soul.json parsers live here. The __init__.py is intentionally empty so each parser module is imported directly, avoiding circular dependency risks.
- [YAML Soul Configuration Parser (parse_soul_yaml)](./yaml-soul-configuration-parser-parsesoulyaml.md) — Parses a YAML soul configuration string into a SoulConfig using yaml.safe_load for deserialization and Pydantic model_validate for schema enforcement, with a two-step approach that separates parsing errors from validation errors.

### `patterns` — 1 article(s)

- [Journal Scope Matching: Pattern Grammar and Overlap Checks](./journal-scope-matching-pattern-grammar-and-overlap-checks.md) — Provides two public functions for colon-delimited scope pattern matching used throughout the journal engine, retrieval router, and credential broker. The module was extracted from the SQLite backend to make the shared logic accessible as a proper public API.

### `persistence` — 6 article(s)

- [FileStorage: Filesystem-Backed Soul Persistence with Atomic Writes](./filestorage-filesystem-backed-soul-persistence-with-atomic-writes.md) — `FileStorage` persists soul configs, DNA markdown, and all seven memory tiers to a structured directory layout under `~/.soul/`. The `save_soul_full` function uses a temp-dir-then-move pattern to guarantee atomic writes, preventing partial state on crash.
- [Journal SQLite Schema and Migration Helper](./journal-sqlite-schema-and-migration-helper.md) — Defines the SQLite schema for the soul-protocol journal engine and provides an idempotent migration function. Ensures the on-disk database is always brought up to the current schema version before use, preventing schema drift between engine upgrades.
- [SQLite WAL-Mode Journal Backend](./sqlite-wal-mode-journal-backend.md) — Implements `JournalBackend` using SQLite with WAL mode, providing append-only event storage with strict sequential ordering and monotonic timestamp enforcement. A single threading lock serializes all writes while allowing concurrent readers through WAL isolation.
- [SQLiteContextStore: Immutable Message Store and Compaction DAG](./sqlitecontextstore-immutable-message-store-and-compaction-dag.md) — `SQLiteContextStore` is the persistence layer for LCM, implementing an append-only SQLite database with three tables: immutable conversation messages, compaction DAG nodes, and DAG edges. It uses `asyncio.to_thread()` to make synchronous SQLite calls safe in async code without adding any external dependencies.
- [Storage Subpackage Public Interface](./storage-subpackage-public-interface.md) — The `storage/__init__.py` re-exports all public storage symbols — `StorageProtocol`, `FileStorage`, `InMemoryStorage`, and the persistence functions `save_soul`, `load_soul`, `save_soul_full`, and `load_soul_full` — from a single stable import path. A `save_soul_full` addition in 2026-02-22 corrected the earlier API's silent memory-tier data loss.
- [Test Suite for InMemoryStorage and FileStorage Backends](./test-suite-for-inmemorystorage-and-filestorage-backends.md) — This test suite validates both storage backends provided by Soul Protocol's runtime: `InMemoryStorage` (dict-backed, in-process) and `FileStorage` (disk-backed, directory-per-soul). It covers the full CRUD surface — save, load, delete, and list — with particular attention to idempotent delete behavior and the expected on-disk directory structure for `FileStorage`.

### `personality` — 7 article(s)

- [D3 Personality Expression Evaluator](./d3-personality-expression-evaluator.md) — Tests that a soul's OCEAN personality traits and communication style are faithfully encoded in its system prompt, influence goal relevance scoring, remain stable under sustained interactions, and produce meaningfully different outputs for contrasting profiles. Contributes 15% of the Soul Health Score.
- [Heuristic Sentiment Detection for Somatic Memory Markers](./heuristic-sentiment-detection-for-somatic-memory-markers.md) — Provides zero-dependency emotion detection using curated word lists and a valence-arousal coordinate model, returning a SomaticMarker that the memory pipeline attaches to entries for emotional recall prioritization. Implements Damasio's Somatic Marker Hypothesis without requiring an LLM call.
- [Personality-Modulated Memory Retrieval (OCEAN Trait Scoring)](./personality-modulated-memory-retrieval-ocean-trait-scoring.md) — Maps each of the five OCEAN personality traits to a memory-scoring signal so that a soul's recall results reflect its character rather than pure relevance rank. The module injects per-trait weight deltas into the activation pipeline, remaining fully backwards-compatible when all traits sit at the default 0.5 midpoint.
- [SelfModelManager: Evolving Self-Concept for Digital Souls](./selfmodelmanager-evolving-self-concept-for-digital-souls.md) — Implements Klein's self-model theory for AI companions — the soul discovers what it is by observing its own interactions and building a map of the domains where it operates. Self-images surface as prompt fragments that anchor the soul's personality in generated responses.
- [SoulSpec Format Importer and Exporter](./soulspec-format-importer-and-exporter.md) — Bridges the SoulSpec open standard (soulspec.org) and the Digital Soul Protocol. Reads a SoulSpec directory structure — `SOUL.md`, `IDENTITY.md`, `STYLE.md`, `soul.json` — and maps each file to the appropriate DSP memory tier and OCEAN personality model.
- [Test Suite: OCEAN Personality-Modulated Memory Retrieval](./test-suite-ocean-personality-modulated-memory-retrieval.md) — Comprehensive tests for the personality modulation layer that adjusts memory recall scores based on OCEAN (Big Five) personality traits, covering per-trait signal functions, the composite boost calculation, and end-to-end recall ranking changes.
- [Test Suite: Soul Configuration — OCEAN, Communication, Biorhythms, Persona, and Config Files](./test-suite-soul-configuration-ocean-communication-biorhythms-persona-and-config-.md) — Validates the flexible birth configuration system for `Soul`, covering direct-kwarg birth with OCEAN personality traits, communication style, biorhythms, and persona text, as well as file-based birth from YAML and JSON configs via `Soul.birth_from_config()`. The suite also covers CLI `--config` and `--openness/--neuroticism` flags and verifies that all settings survive export → awaken round-trips.

### `personality-modeling` — 1 article(s)

- [Simulated Agent and User Profile Generator for Research Experiments](./simulated-agent-and-user-profile-generator-for-research-experiments.md) — Generates 1,000 diverse simulated agent profiles with realistic OCEAN personality distributions and 1,000 matched user profiles for a specified use case domain, used as the participant pool for Soul Protocol's validation experiments. All sampling uses a seeded RNG to guarantee reproducible results across experiment runs.

### `phase-1` — 1 article(s)

- [Test Suite: Phase 1 Ablation Fixes](./test-suite-phase-1-ablation-fixes.md) — Tests for five Phase 1 ablation fixes: the significance gate short-message penalty, mundane-interaction rejection, select_top_k attention selection, BM25Index scoring and the BM25SearchStrategy protocol, and logarithmic bond growth with diminishing returns.

### `phase-2` — 1 article(s)

- [Test Suite: Phase 2 Memory Architecture (Categories, Salience, Dedup)](./test-suite-phase-2-memory-architecture-categories-salience-dedup.md) — Tests for the Phase 2 memory architecture improvements: the MemoryCategory enum, generated abstracts, salience scoring, the deduplication reconcile_fact pipeline, TemporalEdge metadata in the knowledge graph, and the EpisodicStore entry-update API.

### `platform setup` — 1 article(s)

- [CLI Test Suite: Agent Platform Integration Setup (`soul setup`)](./cli-test-suite-agent-platform-integration-setup-soul-setup.md) — Test suite for `soul setup`, which wires a Soul Protocol MCP server into AI coding platforms (Cursor, VS Code, Claude Code, Continue, Windsurf). Covers platform auto-detection, MCP config file writing and merging, instruction file injection, `.gitignore` updates, and `uvx` path resolution.

### `platform-support` — 1 article(s)

- [Platform Auto-Detection and MCP Wiring — `setup.py`](./platform-auto-detection-and-mcp-wiring-setuppy.md) — Detects installed AI coding platforms (Claude Code, Cursor, VS Code, Windsurf, and 8 others), generates platform-appropriate MCP server config, and writes soul memory instructions into each platform's instruction file. Designed to be run once during `soul init --setup-targets`.

### `pocketpaw` — 1 article(s)

- [PocketPaw E2E Integration Script: Full Soul Protocol Lifecycle Validation](./pocketpaw-e2e-integration-script-full-soul-protocol-lifecycle-validation.md) — An end-to-end integration script that validates the complete soul-protocol API lifecycle — birth, observe, remember, recall, reflect, export, awaken, core memory editing, state management, and MCP tool smoke tests — from the perspective of PocketPaw's integration layer, using realistic interaction datasets and Rich-formatted reporting.

### `portability` — 3 article(s)

- [D6 Identity Continuity Evaluator](./d6-identity-continuity-evaluator.md) — Tests that a soul survives export/import round-trips with full fidelity—preserving DID, OCEAN traits, bond state, memory count, and recall results—and that reincarnation correctly tracks lineage across incarnation chains. Contributes 10% of the Soul Health Score.
- [D7 Portability Evaluator](./d7-portability-evaluator.md) — Verifies that a soul produces deterministic system prompts regardless of instantiation order, that recall results survive export/reimport unchanged, and that multi-hop engine swap chains preserve memory count and bond state. Contributes 5% of the Soul Health Score.
- [SoulContainer: The Schema-Free Portable Soul Primitive](./soulcontainer-the-schema-free-portable-soul-primitive.md) — `SoulContainer` is the core-layer primitive introduced in v0.4.0 — a schema-free portable AI identity that holds an `Identity` and a `MemoryStore` with no opinions about personality models, cognitive engines, or evolution systems. It supports a `create`/`open`/`save` lifecycle for `.soul` file archives.

### `privacy` — 3 article(s)

- [RecallEngine: Cross-Store Memory Retrieval with ACT-R Activation](./recallengine-cross-store-memory-retrieval-with-act-r-activation.md) — The RecallEngine is Soul Protocol's central retrieval coordinator, querying episodic, semantic, and procedural stores in parallel, ranking results via ACT-R activation scores, applying OCEAN personality modulation and visibility filtering, and optionally enriching results through the knowledge graph. It is the single integration point through which all memory retrieval flows.
- [Test Suite for GDPR-Compliant Memory Deletion](./test-suite-for-gdpr-compliant-memory-deletion.md) — Comprehensive tests for Soul Protocol's right-to-erasure implementation, covering query-based, entity-based, and time-based deletion across all memory tiers, with full audit trail verification. Ensures deleted memories become invisible to recall and that audit records never expose the deleted content itself.
- [Test Suite: Memory Visibility Tiers](./test-suite-memory-visibility-tiers.md) — Tests for the three-tier memory visibility system (PUBLIC, BONDED, PRIVATE) that controls which memories are accessible to external requesters based on bond strength, preventing private memories from leaking while maintaining full system access for the soul itself.

### `procedural` — 1 article(s)

- [ProceduralStore: How-To Memory Storage and GDPR Deletion](./proceduralstore-how-to-memory-storage-and-gdpr-deletion.md) — Provides an in-memory store specifically for procedural memories — step-by-step how-tos and learned processes. Includes token-overlap relevance search and GDPR-targeted deletion operations added in v0.3.

### `procedural memory` — 1 article(s)

- [LearningEvent — Formalized Lesson Model for Procedural Memory and Skill XP](./learningevent-formalized-lesson-model-for-procedural-memory-and-skill-xp.md) — `LearningEvent` captures insights extracted from evaluating success and failure — distinct from episodic memory (what happened) and semantic memory (what is known). Stored in procedural memory and linked to skills, it drives reinforcement-style confidence updates through `apply()`, `reinforce()`, and `weaken()` methods.

### `progressive-disclosure` — 1 article(s)

- [Test Suite: Progressive Disclosure in Recall](./test-suite-progressive-disclosure-in-recall.md) — Tests for RecallEngine's progressive recall mode, which returns a primary set of full-content memories plus an overflow set of abstract-only summaries, allowing callers to receive more context without exceeding token budgets.

### `project structure` — 1 article(s)

- [Test Suite: test_spec Package Namespace Marker](./test-suite-testspec-package-namespace-marker.md) — An empty `__init__.py` that declares `tests/test_spec/` as a Python package. This file contains no executable code but is structurally required so that pytest and Python's import machinery can discover and run the test modules inside `test_spec/` as part of the broader test suite.

### `project-structure` — 4 article(s)

- [Embeddings Test Package Initializer](./embeddings-test-package-initializer.md) — Package initializer for the `tests/test_embeddings/` test subpackage, created to group all embedding-related test modules under a common namespace. The file contains no executable code — its presence makes the directory a Python package discoverable by pytest.
- [Test Suite for the Spec Sub-Package](./test-suite-for-the-spec-sub-package.md) — The `tests/spec/__init__.py` file marks the `tests/spec/` directory as a Python package, enabling pytest to discover and correctly import the spec-level test modules it contains. This is a structural init with no executable code.
- [Test Suite Package Initializer for CLI Tests](./test-suite-package-initializer-for-cli-tests.md) — The `tests/test_cli/__init__.py` file marks the `tests/test_cli/` directory as a Python package, enabling pytest to discover and correctly namespace the CLI test modules it contains. It contains no executable code.
- [Tests Package Initializer](./tests-package-initializer.md) — The `tests/__init__.py` file marks the top-level `tests/` directory as a Python package, enabling pytest and import machinery to resolve test modules via standard dotted paths. It contains no executable code — its presence is purely structural.

### `prompt engineering` — 4 article(s)

- [Cognitive Prompt Templates for LLM-Powered Memory Tasks](./cognitive-prompt-templates-for-llm-powered-memory-tasks.md) — This module contains all LLM prompt templates used by `CognitiveProcessor` for sentiment analysis, significance gating, fact extraction, entity extraction, and self-reflection. Each template embeds a `[TASK:xxx]` routing marker so that `HeuristicEngine` can dispatch to its regex fallbacks without parsing the full prompt.
- [DNA to System Prompt and Markdown Renderer](./dna-to-system-prompt-and-markdown-renderer.md) — Converts a soul's identity, DNA (personality blueprint), core memory, and current state into either an LLM system prompt or a human-readable markdown document. The system prompt renderer is the primary mechanism by which a soul's personality becomes an active influence on its LLM-driven behavior.
- [LCM Compaction Prompt Templates: SUMMARY and BULLETS](./lcm-compaction-prompt-templates-summary-and-bullets.md) — This module provides the two LLM prompt templates used by `ThreeLevelCompactor` when a `CognitiveEngine` is available: `SUMMARY_PROMPT` for Level 1 prose summarization and `BULLETS_PROMPT` for Level 2 bullet-point compression. Both follow the `[TASK:xxx]` routing convention for compatibility with `HeuristicEngine`.
- [Test Suite: Cognitive Prompt Template Formatting and JSON Parsability](./test-suite-cognitive-prompt-template-formatting-and-json-parsability.md) — Tests that every cognitive prompt template in Soul Protocol formats without error and produces output parseable by the JSON extraction helper. Validates both the template variables contract and the round-trip between typical LLM responses and the `_parse_json` function.

### `protocol` — 1 article(s)

- [Test Suite for Eternal Storage Protocol Compliance](./test-suite-for-eternal-storage-protocol-compliance.md) — Verifies that all eternal storage providers satisfy the EternalStorageProvider protocol at runtime, and validates the Pydantic models ArchiveResult and RecoverySource for field defaults, full construction, and serialization round-trips.

### `protocol design` — 3 article(s)

- [ContextEngine Protocol: Interface for Lossless Context Window Management](./contextengine-protocol-interface-for-lossless-context-window-management.md) — `ContextEngine` is a `@runtime_checkable` Python `Protocol` that defines the six operations any context management implementation must provide: `ingest`, `assemble`, `grep`, `expand`, `describe`, and `compact`. It decouples the soul runtime from any specific context engine implementation.
- [Spec Layer Public Interface: The Headless Standard for Soul Protocol](./spec-layer-public-interface-the-headless-standard-for-soul-protocol.md) — The `spec/__init__.py` is the public surface of Soul Protocol's headless spec layer — minimal, unopinionated primitives that any runtime can implement with zero dependencies on opinionated modules like memory, cognitive engines, or evolution systems. It aggregates over 60 exported symbols spanning identity, memory, retrieval, context management, A2A interop, decision traces, and eternal storage.
- [StorageProtocol: Runtime-Checkable Interface for Soul Storage Backends](./storageprotocol-runtime-checkable-interface-for-soul-storage-backends.md) — `StorageProtocol` is a `@runtime_checkable` Python `Protocol` that defines the four operations all soul storage backends must implement: `save`, `load`, `delete`, and `list_souls`. It decouples the runtime from any specific storage implementation, enabling `FileStorage` and `InMemoryStorage` to be swapped without modifying callers.

### `protocol interfaces` — 5 article(s)

- [EmbeddingProvider Protocol — Pluggable Embedding Backend Interface](./embeddingprovider-protocol-pluggable-embedding-backend-interface.md) — `EmbeddingProvider` is a runtime-checkable `Protocol` class that defines the three-method contract any embedding backend must satisfy. It decouples Soul Protocol's retrieval and search features from any specific ML framework by requiring only `dimensions`, `embed`, and `embed_batch`.
- [Embeddings Spec Package — Public API Surface](./embeddings-spec-package-public-api-surface.md) — The `spec/embeddings` package defines the protocol-level public interface for all embedding operations in Soul Protocol. It re-exports the `EmbeddingProvider` protocol and three vector math primitives so consumers import from a single stable namespace.
- [Eternal Storage Protocol — Archive, Retrieve, and Verify Interface for Permanent Soul Backends](./eternal-storage-protocol-archive-retrieve-and-verify-interface-for-permanent-sou.md) — This module defines the canonical protocol and result models for Soul Protocol's eternal (permanent) storage tier. `EternalStorageProvider` is a runtime-checkable async `Protocol` that any backend (IPFS, Arweave, local) must satisfy, while `ArchiveResult` and `RecoverySource` capture what an archival operation produces and how a soul can later be restored.
- [Memory Primitives — MemoryEntry, MemoryStore Protocol, and DictMemoryStore](./memory-primitives-memoryentry-memorystore-protocol-and-dictmemorystore.md) — This module defines the atomic unit of soul memory (`MemoryEntry`), the backend-agnostic `MemoryStore` protocol, and a reference in-memory implementation (`DictMemoryStore`). It also introduces `Interaction` and `Participant` models for multi-party conversations, along with `MemoryVisibility` tiers for controlling what surfaces in public channel contexts.
- [Retrieval Spec — Request/Response Models, Adapter Protocols, Credential Broker, and Exception Hierarchy](./retrieval-spec-requestresponse-models-adapter-protocols-credential-broker-and-ex.md) — This module defines the complete vocabulary for soul retrieval: request and response models (`RetrievalRequest`, `RetrievalResult`), the `SourceAdapter` and `AsyncSourceAdapter` protocols, the `CredentialBroker` interface for federated source access, a `DataRef` type for Zero-Copy candidate payloads, and a structured exception hierarchy. Concrete implementations (the router, in-memory broker, projection adapters) live in the consuming runtime.

### `protocol-design` — 1 article(s)

- [Embedding Provider Protocol Compliance Test Suite](./embedding-provider-protocol-compliance-test-suite.md) — Tests that verify `HashEmbedder` and `TFIDFEmbedder` correctly satisfy the `EmbeddingProvider` structural protocol at runtime. Also validates that partial or non-compliant implementations fail the `isinstance` check, ensuring the protocol is genuinely enforced rather than nominal.

### `provider integration` — 1 article(s)

- [Test Suite: Cognitive Adapter Layer -- CallableEngine, Auto-Detection, and Provider Integration](./test-suite-cognitive-adapter-layer-callableengine-auto-detection-and-provider-in.md) — Tests for the adapter layer that bridges Soul Protocol's `CognitiveEngine` protocol with real LLM providers (Anthropic, OpenAI, Ollama) and user-supplied callables. Covers callable wrapping, environment-variable-driven auto-detection, Soul factory integration, import safety for optional dependencies, and HTTP-level mocking for Ollama.

### `providers` — 7 article(s)

- [Cognitive Adapters Package: Provider Adapter Exports](./cognitive-adapters-package-provider-adapter-exports.md) — Package init for the cognitive adapters subpackage, re-exporting all provider-specific engine implementations and the `engine_from_env()` auto-detection helper. All adapters use soft imports that raise only at instantiation time, not on module load.
- [Eternal Storage Providers Package Init](./eternal-storage-providers-package-init.md) — The `eternal/providers/` package init collects all four storage provider implementations — `LocalStorageProvider`, `MockIPFSProvider`, `MockArweaveProvider`, and `MockBlockchainProvider` — and exposes them through a single import point. This makes provider selection ergonomic for callers who want to pick a backend without navigating nested modules.
- [Local Filesystem Eternal Storage Provider](./local-filesystem-eternal-storage-provider.md) — `LocalStorageProvider` archives soul data as files on the local filesystem using a hash-based naming scheme to avoid collisions. It serves as the most basic, always-available eternal storage tier — no network, no external dependency — making it suitable for development, offline use, and as a fast local cache alongside remote backends.
- [Mock Arweave Eternal Storage Provider](./mock-arweave-eternal-storage-provider.md) — `MockArweaveProvider` simulates Arweave's permanent, pay-once storage model using an in-memory dict and deterministic transaction IDs — no gateway required. It lets test suites and local development exercise the full Arweave archive/retrieve/verify cycle without real AR tokens or network access.
- [Mock Blockchain Soul Registry Provider](./mock-blockchain-soul-registry-provider.md) — `MockBlockchainProvider` simulates an on-chain soul registry where each soul is minted as a unique NFT-like token with a hash-based token ID. It enables testing of blockchain-based soul identity ownership without a real wallet, chain, or gas fees.
- [Mock IPFS Content-Addressed Storage Provider](./mock-ipfs-content-addressed-storage-provider.md) — `MockIPFSProvider` simulates IPFS's content-addressed storage model by generating deterministic CID-like references from the SHA-256 hash of the archived data and storing it in memory. It faithfully reproduces the key property of IPFS — the same content always produces the same address — without requiring a running IPFS node.
- [Test Suite for Eternal Storage Providers](./test-suite-for-eternal-storage-providers.md) — Per-provider tests for the four eternal storage backends: MockIPFSProvider, MockArweaveProvider, MockBlockchainProvider, and LocalStorageProvider. Each provider is tested for archive, retrieve, verify, content addressing, unique identifiers, and error behavior on missing references.

### `psychology` — 1 article(s)

- [Bond Model: Human-Soul Relationship Strength](./bond-model-human-soul-relationship-strength.md) — Models the trust relationship between a human and their soul companion, tracking bond strength and interaction count. Uses logarithmic growth to make early bonding feel natural while making deep trust genuinely hard-earned through sustained interaction.

### `psychology pipeline` — 1 article(s)

- [Test Suite: Core Soul API — Birth, Memory, Emotion, Export, and Psychology Pipeline](./test-suite-core-soul-api-birth-memory-emotion-export-and-psychology-pipeline.md) — The primary integration test suite for the `Soul` class, covering the full API surface from construction through the psychology pipeline. It validates that soul birth, memory operations, emotional state, import/export round-trips, system prompt generation, and the attention gate all work correctly together.

### `psychology tools` — 1 article(s)

- [Test Suite for MCP Psychology Tools (Skills, Evaluate, Learn, Evolve)](./test-suite-for-mcp-psychology-tools-skills-evaluate-learn-evolve.md) — Validates the higher-order MCP tools that expose soul psychology — skill tracking, rubric-based evaluation, learning events, and the propose/approve/reject evolution workflow. Tests use FastMCP's in-process client to exercise the full MCP tool call path without a network round-trip.

### `public API` — 1 article(s)

- [Spec Layer Public Interface: The Headless Standard for Soul Protocol](./spec-layer-public-interface-the-headless-standard-for-soul-protocol.md) — The `spec/__init__.py` is the public surface of Soul Protocol's headless spec layer — minimal, unopinionated primitives that any runtime can implement with zero dependencies on opinionated modules like memory, cognitive engines, or evolution systems. It aggregates over 60 exported symbols spanning identity, memory, retrieval, context management, A2A interop, decision traces, and eternal storage.

### `pydantic` — 1 article(s)

- [Test Suite: Core Pydantic Data Models](./test-suite-core-pydantic-data-models.md) — Validates the foundational Pydantic models in soul_protocol.types — Identity defaults, Personality trait bounds, SoulConfig JSON serialization round-trip, MemoryEntry defaults, Mood enum completeness, and EvolutionMode enum values.

### `quality-evaluation` — 10 article(s)

- [Enhanced Quality Validation Runner — N-Variation Factorial Benchmark with Checkpointing](./enhanced-quality-validation-runner-n-variation-factorial-benchmark-with-checkpoi.md) — Orchestrates a full factorial quality benchmark: N scenario variations × 3 test types × M judge models × 4 ablation conditions, accumulating statistically meaningful results with 95% confidence intervals. Checkpoint/resume logic survives process crashes, and a formatted results table with win rates is printed on completion.
- [LLM-as-Judge — Six-Dimension Response Quality Scorer with Position-Bias Mitigation](./llm-as-judge-six-dimension-response-quality-scorer-with-position-bias-mitigation.md) — Implements the LLM-as-judge evaluation methodology with six quality dimensions scored on a 1-10 scale, supporting both single-response scoring and pairwise A/B comparison. The pairwise mode randomly assigns responses to positions A and B on each call to eliminate position bias from judge models that favor whichever response appears first.
- [Mem0 Comparison Benchmark — Soul Protocol vs. Vector Memory vs. Stateless Baseline](./mem0-comparison-benchmark-soul-protocol-vs-vector-memory-vs-stateless-baseline.md) — Runs a three-way quality comparison between Soul Protocol, Mem0 (embedding-based vector memory), and a stateless baseline across four test types, producing a scorecard that quantifies what the soul stack adds beyond raw memory storage. The benchmark is designed to appear in the Soul Protocol research paper as evidence that personality, emotional tracking, and bond are not incidental features.
- [Multi-Condition Response Generator — Isolating Soul Protocol Components](./multi-condition-response-generator-isolating-soul-protocol-components.md) — Defines four experimental conditions (`FULL_SOUL`, `RAG_ONLY`, `PROMPT_PERSONALITY`, `BARE_BASELINE`) and the `MultiConditionResponder` class, which generates responses under each condition against the same underlying soul state to isolate the contribution of personality, memory, and emotional scaffolding to response quality.
- [Multi-Judge Quality Runner — Inter-Rater Reliability Across Five LLM Models](./multi-judge-quality-runner-inter-rater-reliability-across-five-llm-models.md) — Runs Soul Protocol's four quality test scenarios with five different judge models (Haiku, Gemini 3 Flash, Gemini 2.5 Flash Lite, DeepSeek V3, Llama 3.3 70B) to measure inter-rater reliability, ensuring that quality advantages observed for soul-enabled agents are not artifacts of a single judge model's preferences or biases.
- [Quality Scenario Generator: Randomized Test Variations for Statistical Validity](./quality-scenario-generator-randomized-test-variations-for-statistical-validity.md) — The scenario generator produces 10 randomized but reproducible variations of each of the four quality test types, enabling error bars and statistical significance checks rather than single-point measurements. A fixed seed (`SEED = 42`) ensures results are reproducible across runs while the random variation catches edge cases a single hardcoded scenario would miss.
- [Quality Validation Package Overview](./quality-validation-package-overview.md) — The `research/quality` package contains Soul Protocol's response quality validation infrastructure: test scenarios, experimental condition definitions, LLM-as-judge scoring, and multi-model benchmarking runners. It is the layer that measures subjective quality (how good does the response feel?) rather than infrastructure metrics (how many memories were stored?).
- [Quality Validation Runner: CLI Orchestrator for Soul Protocol Test Suite](./quality-validation-runner-cli-orchestrator-for-soul-protocol-test-suite.md) — The quality runner provides a command-line interface to execute one or more of the four fixed validation tests (response quality, personality consistency, hard recall, emotional continuity) and outputs both a live scorecard and saved JSON/Markdown reports. It uses concurrent `HaikuCognitiveEngine` instances for agent and judge roles to keep cost tracking separate.
- [Soul Responder: Paired Response Generation for Quality Evaluation](./soul-responder-paired-response-generation-for-quality-evaluation.md) — The `SoulResponder` class generates agent responses in two modes — enriched by a soul's personality and memories, or from a bare baseline — enabling paired comparisons that quantify whether having a soul actually improves output quality. It uses a dedicated `HaikuCognitiveEngine` for response generation, intentionally separate from any cognitive engine the soul uses internally.
- [Test Suite: Four Core Quality Validation Scenarios for Soul Protocol](./test-suite-four-core-quality-validation-scenarios-for-soul-protocol.md) — This test suite implements the four hardest quality probes for Soul Protocol — response quality uplift, personality consistency across OCEAN profiles, hard recall after 30 filler interactions, and emotional continuity via somatic markers — using paired LLM comparisons with a `ResponseJudge` to score soul-enriched versus baseline responses.

### `real-world-scenarios` — 1 article(s)

- [Real-World End-to-End Test Suite — Developer Onboarding, Bonding, and Evolution Scenarios](./real-world-end-to-end-test-suite-developer-onboarding-bonding-and-evolution-scen.md) — Scenario-driven end-to-end tests that simulate realistic human-soul conversations across multiple contexts: developer technical discussions, personal emotional bonding, and mixed sessions. Tests validate that all soul systems (entity extraction, knowledge graph, bond growth, evolution, self-model) fire correctly in natural usage patterns.

### `recall` — 7 article(s)

- [ACT-R Activation-Based Memory Scoring](./act-r-activation-based-memory-scoring.md) — Implements Anderson's ACT-R cognitive model for memory recall scoring. Combines power-law recency/frequency decay, query-spreading activation, emotional boost from somatic markers, and personality-modulated ranking to produce a single activation score per memory candidate.
- [Personality-Modulated Memory Retrieval (OCEAN Trait Scoring)](./personality-modulated-memory-retrieval-ocean-trait-scoring.md) — Maps each of the five OCEAN personality traits to a memory-scoring signal so that a soul's recall results reflect its character rather than pure relevance rank. The module injects per-trait weight deltas into the activation pipeline, remaining fully backwards-compatible when all traits sit at the default 0.5 midpoint.
- [RecallEngine: Cross-Store Memory Retrieval with ACT-R Activation](./recallengine-cross-store-memory-retrieval-with-act-r-activation.md) — The RecallEngine is Soul Protocol's central retrieval coordinator, querying episodic, semantic, and procedural stores in parallel, ranking results via ACT-R activation scores, applying OCEAN personality modulation and visibility filtering, and optionally enriching results through the knowledge graph. It is the single integration point through which all memory retrieval flows.
- [Smart Memory Reranking via LLM (Prompt-Injection-Hardened)](./smart-memory-reranking-via-llm-prompt-injection-hardened.md) — Provides an optional second-pass reranking stage that uses a lightweight LLM call to select the most contextually relevant memories from a heuristically-ranked candidate pool. The implementation includes robust prompt injection defenses because memory content is untrusted user input that could manipulate the ranking decision.
- [Test Suite: Graph-Augmented Recall Pipeline](./test-suite-graph-augmented-recall-pipeline.md) — Validates the integration of KnowledgeGraph with RecallEngine, covering graph traversal via progressive_context at multiple depth levels, graph-wired recall with entity matching, deduplication, and graceful degradation when no graph is provided.
- [Test Suite: OCEAN Personality-Modulated Memory Retrieval](./test-suite-ocean-personality-modulated-memory-retrieval.md) — Comprehensive tests for the personality modulation layer that adjusts memory recall scores based on OCEAN (Big Five) personality traits, covering per-trait signal functions, the composite boost calculation, and end-to-end recall ranking changes.
- [Test Suite: Progressive Disclosure in Recall](./test-suite-progressive-disclosure-in-recall.md) — Tests for RecallEngine's progressive recall mode, which returns a primary set of full-content memories plus an overflow set of abstract-only summaries, allowing callers to receive more context without exceeding token budgets.

### `regression` — 1 article(s)

- [Test Suite for Zip Recall Bug: Soul File Export/Awaken Roundtrip and Stale MCP Cache](./test-suite-for-zip-recall-bug-soul-file-exportawaken-roundtrip-and-stale-mcp-cac.md) — This regression test suite targets a production bug where `soul_recall` via the MCP server returned zero results for memories that existed in the `.soul` file on disk. The root cause was the MCP server loading souls at startup and never reloading when the file was updated externally. Tests cover semantic and episodic recall after zip roundtrip, bytes-based awakening, multi-memory preservation, content fidelity, and the stale-cache simulation.

### `relationship` — 1 article(s)

- [D4 Bond and Relationship Evaluator](./d4-bond-and-relationship-evaluator.md) — Validates the mathematical properties of the soul's bond growth system: logarithmic curve shape, emotional valence acceleration, milestone accuracy at N=50/100/200, and tier progression ordering. Contributes 15% of the Soul Health Score.

### `relationships` — 1 article(s)

- [Soul Identity — Schema-Free Portable Identity with Multi-Bond Support](./soul-identity-schema-free-portable-identity-with-multi-bond-support.md) — `Identity` is the portable identity primitive at the core of Soul Protocol. It carries a soul's name, creation timestamp, and an open `traits` dict that runtimes can populate with OCEAN scores, Myers-Briggs types, or any custom dimensions. The `feat/spec-multi-participant` update added `BondTarget` and a `bonds` list so a single soul can maintain relationships with multiple humans, agents, groups, and services.

### `reporting` — 1 article(s)

- [Soul Health Score Report Renderer](./soul-health-score-report-renderer.md) — Renders SoulHealthReport objects into four output formats—markdown table, JSON, RAG comparison table, and an ANSI terminal dashboard—without external dependencies. Provides human-readable and machine-readable views of evaluation results.

### `research` — 37 article(s)

- [DSPy Optimization Pipeline: MIPROv2-Based Significance Gate and Query Expansion Tuning](./dspy-optimization-pipeline-miprov2-based-significance-gate-and-query-expansion-t.md) — This pipeline runs a four-phase experiment: baseline heuristic ablation, DSPy MIPROv2 optimization of the significance gate and query expander, post-optimization ablation with the trained modules active, and saved reports comparing before/after metrics. It exists to replace hand-tuned heuristics with learned prompts that generalize across diverse interaction types.
- [DSPy Training Data Generation Subpackage](./dspy-training-data-generation-subpackage.md) — The `dspy_training/` subpackage provides training data generation for optimizing Soul Protocol's DSPy modules — the LLM-powered components used for memory extraction, significance scoring, and entity recognition. Created under the `feat/dspy-integration` branch, it is a nascent subpackage with the actual generation logic in sibling modules.
- [DSPy Training Dataset Generator](./dspy-training-dataset-generator.md) — Generates labeled training data for DSPy-optimized memory significance and recall query expansion. Produces two dataset types—significance classification and query expansion—split into train/val sets for fine-tuning the soul's memory filtering models.
- [Enhanced Quality Validation Runner — N-Variation Factorial Benchmark with Checkpointing](./enhanced-quality-validation-runner-n-variation-factorial-benchmark-with-checkpoi.md) — Orchestrates a full factorial quality benchmark: N scenario variations × 3 test types × M judge models × 4 ablation conditions, accumulating statistically meaningful results with 95% confidence intervals. Checkpoint/resume logic survives process crashes, and a formatted results table with win rates is printed on completion.
- [Experiment Configuration and Enum Definitions for Soul Protocol Research](./experiment-configuration-and-enum-definitions-for-soul-protocol-research.md) — Defines the `ExperimentConfig` dataclass and supporting enums (`MemoryCondition`, `UseCase`) that control every parameter of the Soul Protocol validation study. All experiment parameters live in one place to guarantee reproducibility — any change to scale, thresholds, or conditions is a single-file edit.
- [Five Experimental Memory Conditions for Soul Protocol Ablation Study](./five-experimental-memory-conditions-for-soul-protocol-ablation-study.md) — Implements the five independent-variable conditions for the Soul Protocol validation study, ranging from a completely stateless baseline to the full psychology-informed stack. Each condition is a class that wraps a Soul (or no Soul) and exposes a uniform `observe / recall / get_state / reset` interface, allowing the experiment runner to swap conditions without changing any other code.
- [Haiku Cognitive Engine — Claude Haiku LLM Backend with Rate Limiting](./haiku-cognitive-engine-claude-haiku-llm-backend-with-rate-limiting.md) — A thin async wrapper around the Anthropic SDK that implements the single-method `CognitiveEngine` protocol (`async think(prompt) -> str`) using Claude Haiku. Includes semaphore-based concurrency control, retry logic for rate limits and timeouts, and a `UsageTracker` dataclass for cost and throughput monitoring.
- [Human Evaluation Study Web Application](./human-evaluation-study-web-application.md) — A FastAPI web application that runs a blinded A/B human evaluation study, presenting participants with two AI agents — one soul-enabled, one a plain baseline — and collecting Likert survey responses after each. The app manages sessions, randomizes agent order to eliminate position bias, and exports results to JSON and CSV for statistical analysis.
- [LiteLLM Cognitive Engine — Multi-Model Proxy Backend](./litellm-cognitive-engine-multi-model-proxy-backend.md) — An async `CognitiveEngine` implementation that routes LLM calls through a LiteLLM OpenAI-compatible proxy, enabling the research framework to use GPT-4o, Gemini, DeepSeek, Llama, and Qwen as judge or agent engines without changing call sites. It mirrors the `HaikuCognitiveEngine` interface exactly, making the two engines interchangeable.
- [LLM-as-Judge — Six-Dimension Response Quality Scorer with Position-Bias Mitigation](./llm-as-judge-six-dimension-response-quality-scorer-with-position-bias-mitigation.md) — Implements the LLM-as-judge evaluation methodology with six quality dimensions scored on a 1-10 scale, supporting both single-response scoring and pairwise A/B comparison. The pairwise mode randomly assigns responses to positions A and B on each call to eliminate position bias from judge models that favor whichever response appears first.
- [Long-Horizon Ablation Results Analyzer](./long-horizon-ablation-results-analyzer.md) — The `LongHorizonAnalyzer` takes `LongHorizonResults` produced by the runner and computes summary statistics, pairwise Cohen's d effect sizes, and per-scenario breakdowns, then renders a structured markdown report. It is the statistical layer that transforms raw recall hits and memory counts into publishable evidence.
- [Long-Horizon Ablation Runner — Four-Condition Infrastructure Benchmark](./long-horizon-ablation-runner-four-condition-infrastructure-benchmark.md) — The `LongHorizonRunner` executes 100+ turn synthetic conversation scenarios through four ablation conditions (Full Soul, RAG Only, Personality Only, Bare Baseline) and collects infrastructure metrics — recall precision, memory count, memory efficiency, bond trajectory — without requiring an LLM for response generation. It optionally integrates DSPy query expansion and significance gating.
- [Long-Horizon Ablation Study Package](./long-horizon-ablation-study-package.md) — The `research/long_horizon` package bundles the scenario generators, ablation runner, and statistical analyzer for Soul Protocol's long-horizon study, which proves that the psychology stack (significance gating, activation decay, somatic markers) produces measurable advantages at 100+ turn conversation scales. The `__init__.py` re-exports the three key classes and five scenario generator functions as the public API.
- [Long-Horizon Conversation Scenario Generators — Three Ablation Test Cases](./long-horizon-conversation-scenario-generators-three-ablation-test-cases.md) — Defines the `LongHorizonScenario` and `TestPoint` data structures and generates three distinct 100-160 turn synthetic conversation scenarios designed to stress different aspects of Soul Protocol's psychology stack: long-range recall, emotional continuity under extreme valence swings, and recall precision under adversarial fact burial.
- [Marathon Scale Scenario — 1000-Turn Conversation for Scale Ablation](./marathon-scale-scenario-1000-turn-conversation-for-scale-ablation.md) — Generates a fully deterministic 1000-turn synthetic conversation (`marathon_1000`) with 35 strategically planted facts and 40 recall test points designed to reveal the point at which Soul Protocol's selective memory corpus outperforms a naive RAG store. Facts are seeded across the full 1000-turn span — including deeply buried and callback facts — to stress-test long-range recall.
- [Mem0 Comparison Benchmark — Soul Protocol vs. Vector Memory vs. Stateless Baseline](./mem0-comparison-benchmark-soul-protocol-vs-vector-memory-vs-stateless-baseline.md) — Runs a three-way quality comparison between Soul Protocol, Mem0 (embedding-based vector memory), and a stateless baseline across four test types, producing a scorecard that quantifies what the soul stack adds beyond raw memory storage. The benchmark is designed to appear in the Soul Protocol research paper as evidence that personality, emotional tracking, and bond are not incidental features.
- [Multi-Condition Response Generator — Isolating Soul Protocol Components](./multi-condition-response-generator-isolating-soul-protocol-components.md) — Defines four experimental conditions (`FULL_SOUL`, `RAG_ONLY`, `PROMPT_PERSONALITY`, `BARE_BASELINE`) and the `MultiConditionResponder` class, which generates responses under each condition against the same underlying soul state to isolate the contribution of personality, memory, and emotional scaffolding to response quality.
- [Multi-Judge Quality Runner — Inter-Rater Reliability Across Five LLM Models](./multi-judge-quality-runner-inter-rater-reliability-across-five-llm-models.md) — Runs Soul Protocol's four quality test scenarios with five different judge models (Haiku, Gemini 3 Flash, Gemini 2.5 Flash Lite, DeepSeek V3, Llama 3.3 70B) to measure inter-rater reliability, ensuring that quality advantages observed for soul-enabled agents are not artifacts of a single judge model's preferences or biases.
- [Quality Scenario Generator: Randomized Test Variations for Statistical Validity](./quality-scenario-generator-randomized-test-variations-for-statistical-validity.md) — The scenario generator produces 10 randomized but reproducible variations of each of the four quality test types, enabling error bars and statistical significance checks rather than single-point measurements. A fixed seed (`SEED = 42`) ensures results are reproducible across runs while the random variation catches edge cases a single hardcoded scenario would miss.
- [Quality Validation Package Overview](./quality-validation-package-overview.md) — The `research/quality` package contains Soul Protocol's response quality validation infrastructure: test scenarios, experimental condition definitions, LLM-as-judge scoring, and multi-model benchmarking runners. It is the layer that measures subjective quality (how good does the response feel?) rather than infrastructure metrics (how many memories were stored?).
- [Quality Validation Runner: CLI Orchestrator for Soul Protocol Test Suite](./quality-validation-runner-cli-orchestrator-for-soul-protocol-test-suite.md) — The quality runner provides a command-line interface to execute one or more of the four fixed validation tests (response quality, personality consistency, hard recall, emotional continuity) and outputs both a live scorecard and saved JSON/Markdown reports. It uses concurrent `HaikuCognitiveEngine` instances for agent and judge roles to keep cost tracking separate.
- [Quick Ablation Runner: Heuristic-Only Long-Horizon Scenario Executor](./quick-ablation-runner-heuristic-only-long-horizon-scenario-executor.md) — A minimal, API-key-free script that runs all long-horizon scenarios through the `LongHorizonRunner` and prints a structured ablation table comparing recall precision, memory count, and bond strength across conditions. It is the fastest way to verify the heuristic pipeline without incurring LLM costs.
- [Research Metrics Schema — Six-Dimensional Agent Quality Measurement](./research-metrics-schema-six-dimensional-agent-quality-measurement.md) — Defines the full metric collection schema for Soul Protocol's research framework across six orthogonal dimensions: retrieval quality, emotional tracking, personality stability, memory efficiency, bond formation, and skill acquisition. All metric classes are dataclasses that aggregate per-interaction observations into summary statistics for factorial analysis.
- [Research Package Entry Point (`python -m research`)](./research-package-entry-point-python-m-research.md) — A minimal `__main__.py` that enables the research package to be invoked directly with `python -m research`, delegating immediately to `research.run.main()`. This is the standard Python idiom for making a package executable without requiring the user to know the internal module structure.
- [Research Package: Soul Protocol Validation Simulation Framework](./research-package-soul-protocol-validation-simulation-framework.md) — The `research/` package is a large-scale simulation framework designed to validate Soul Protocol's psychology-informed memory claims at publication grade, running 1,000 simulated agents across 5 memory conditions and 4 use cases for more than 20,000 experimental runs. It is structured for reproducibility and statistical rigor.
- [Research Scenario Bank: Multi-Turn Interaction Templates for Four Use Cases](./research-scenario-bank-multi-turn-interaction-templates-for-four-use-cases.md) — The scenario bank provides parameterized, multi-turn interaction sequences for four use cases — customer support, coding assistant, personal companion, and knowledge worker — each with embedded ground truth: planted facts, recall queries, emotional tone markers, and importance hints. Scenarios are personalized by `UserProfile` to increase coverage and ecological validity.
- [Research Simulation CLI: Entry Point for Full-Scale Ablation Experiments](./research-simulation-cli-entry-point-for-full-scale-ablation-experiments.md) — The `run.py` module is the primary CLI entry point for the Soul Protocol research simulation framework, translating command-line arguments into an `ExperimentConfig` and orchestrating the full simulation, analysis, and report pipeline. It supports configuring agent count, random seed, memory conditions, use cases, output directory, batch size, and a quick mode for rapid iteration.
- [Scale Ablation Runner: 1000-Turn Memory Efficiency Study](./scale-ablation-runner-1000-turn-memory-efficiency-study.md) — A dedicated runner for the 1000-turn marathon scenario that benchmarks Soul Protocol's selective storage against RAG (store-everything) and two null conditions at scale. At 1000 turns, the soul achieves 85% recall with 175 memories (4.9x more efficient than RAG's 1000), and the study breaks down recall by memory age to identify decay patterns.
- [Simulated Agent and User Profile Generator for Research Experiments](./simulated-agent-and-user-profile-generator-for-research-experiments.md) — Generates 1,000 diverse simulated agent profiles with realistic OCEAN personality distributions and 1,000 matched user profiles for a specified use case domain, used as the participant pool for Soul Protocol's validation experiments. All sampling uses a seeded RNG to guarantee reproducible results across experiment runs.
- [Simulation Engine: Full Factorial Experiment Orchestrator for Soul Protocol Research](./simulation-engine-full-factorial-experiment-orchestrator-for-soul-protocol-resea.md) — The `SimulationEngine` orchestrates the core ablation study by running every combination of (agent, condition, use_case) as an async task, collecting `AgentRunMetrics` for each triple, and aggregating them into a `SimulationResults` object. Failed individual runs are logged and skipped rather than crashing the whole experiment, ensuring partial results are always captured.
- [Soul Responder: Paired Response Generation for Quality Evaluation](./soul-responder-paired-response-generation-for-quality-evaluation.md) — The `SoulResponder` class generates agent responses in two modes — enriched by a soul's personality and memories, or from a bare baseline — enabling paired comparisons that quantify whether having a soul actually improves output quality. It uses a dedicated `HaikuCognitiveEngine` for response generation, intentionally separate from any cognitive engine the soul uses internally.
- [SoulOptimizer: MIPROv2 Training Harness for Cognitive Modules](./souloptimizer-miprov2-training-harness-for-cognitive-modules.md) — `SoulOptimizer` is an offline training harness that uses DSPy's MIPROv2 optimizer to tune `SignificanceGate` and `QueryExpander` against labeled interaction data derived from ablation scenarios or production logs. Optimized module weights are serialized to disk and loaded by `DSPyCognitiveProcessor` at runtime.
- [Statistical Analysis Engine for Soul Protocol Research Results](./statistical-analysis-engine-for-soul-protocol-research-results.md) — Provides `ResultsAnalyzer`, which takes flat metric rows from experiment runs and produces statistical summary tables, pairwise condition comparisons with Cohen's d effect sizes and Mann-Whitney U p-values, ablation chains, per-use-case breakdowns, and a full markdown report. All analysis uses stdlib-only dependencies to maximize portability.
- [Test Suite: Four Core Quality Validation Scenarios for Soul Protocol](./test-suite-four-core-quality-validation-scenarios-for-soul-protocol.md) — This test suite implements the four hardest quality probes for Soul Protocol — response quality uplift, personality consistency across OCEAN profiles, hard recall after 30 filler interactions, and emotional continuity via somatic markers — using paired LLM comparisons with a `ResponseJudge` to score soul-enriched versus baseline responses.
- [Test Suite: Smoke Tests for the Research Simulation Framework](./test-suite-smoke-tests-for-the-research-simulation-framework.md) — This test suite validates that all research framework components — agent generation, user generation, scenario generation, experiment configuration, metrics, statistical utilities, and memory conditions — work correctly at minimal scale without requiring any LLM API calls. It is fast enough for CI and guards against regressions in the framework before running expensive full ablations.
- [Tier 2 Validation Runner: 100-Agent Real LLM Benchmark](./tier-2-validation-runner-100-agent-real-llm-benchmark.md) — The Tier 2 runner validates Soul Protocol with real Anthropic API calls across 100 simulated agents, comparing heuristic-only cognitive processing against LLM-backed processing using `HaikuCognitiveEngine`. It collects a comprehensive set of metrics — memory, personality, recall, bond, emotional, and skill dimensions — and saves the results as JSON.
- [Whitepaper Chart Generator for Soul Protocol Research](./whitepaper-chart-generator-for-soul-protocol-research.md) — Generates five publication-quality PNG charts that visualize Soul Protocol's research validation results, covering multi-judge quality tests, component ablation, competitive comparisons (vs. Mem0), judge agreement heatmaps, and a waterfall plot of cumulative score improvements. Each chart is designed to standalone as a figure in the Soul Protocol whitepaper with accessible color coding and clean academic styling.

### `resilience` — 2 article(s)

- [EternalStorageManager: Multi-Tier Eternal Storage Orchestrator](./eternalstoragemanager-multi-tier-eternal-storage-orchestrator.md) — The `EternalStorageManager` coordinates multiple eternal storage backends (Arweave, IPFS, etc.) for archiving and recovering soul data. It registers providers by tier name, fans out archive operations across requested tiers, and attempts ordered recovery from tracked sources — failing gracefully if individual tiers are unavailable.
- [OpenAI Embedding Provider with Retry Backoff](./openai-embedding-provider-with-retry-backoff.md) — The `OpenAIEmbeddingProvider` produces high-quality semantic embeddings via the OpenAI API, with built-in exponential backoff for rate limits and transient server errors. It lazily initializes the OpenAI client, pre-knows dimensions for standard models to avoid a probe call, and guarantees input ordering even when the API returns results out of order.

### `response-generation` — 1 article(s)

- [Soul Responder: Paired Response Generation for Quality Evaluation](./soul-responder-paired-response-generation-for-quality-evaluation.md) — The `SoulResponder` class generates agent responses in two modes — enriched by a soul's personality and memories, or from a bare baseline — enabling paired comparisons that quantify whether having a soul actually improves output quality. It uses a dedicated `HaikuCognitiveEngine` for response generation, intentionally separate from any cognitive engine the soul uses internally.

### `retrieval` — 7 article(s)

- [LCM Retrieval Tools: grep, expand, and describe](./lcm-retrieval-tools-grep-expand-and-describe.md) — This module provides three retrieval functions — `grep`, `expand`, and `describe` — that expose different views into the immutable LCM message store. `grep` searches by regex, `expand` recursively reconstructs original messages from compacted DAG nodes, and `describe` returns a metadata snapshot of the entire store.
- [RecallEngine: Cross-Store Memory Retrieval with ACT-R Activation](./recallengine-cross-store-memory-retrieval-with-act-r-activation.md) — The RecallEngine is Soul Protocol's central retrieval coordinator, querying episodic, semantic, and procedural stores in parallel, ranking results via ACT-R activation scores, applying OCEAN personality modulation and visibility filtering, and optionally enriching results through the knowledge graph. It is the single integration point through which all memory retrieval flows.
- [Retrieval Spec — Request/Response Models, Adapter Protocols, Credential Broker, and Exception Hierarchy](./retrieval-spec-requestresponse-models-adapter-protocols-credential-broker-and-ex.md) — This module defines the complete vocabulary for soul retrieval: request and response models (`RetrievalRequest`, `RetrievalResult`), the `SourceAdapter` and `AsyncSourceAdapter` protocols, the `CredentialBroker` interface for federated source access, a `DataRef` type for Zero-Copy candidate payloads, and a structured exception hierarchy. Concrete implementations (the router, in-memory broker, projection adapters) live in the consuming runtime.
- [Retrieval Trace — Observability Receipt for Every Recall and Search Operation](./retrieval-trace-observability-receipt-for-every-recall-and-search-operation.md) — `RetrievalTrace` is a lightweight receipt written once per retrieval call, capturing the ranked candidate list, which candidates were actually used, latency, and a downstream reference. It feeds the paw-runtime JSONL sink, graduation policy, the "Why?" explainability drawer, and SoulBench fixture generation — all from a single portable shape.
- [SearchStrategy Protocol: Pluggable Memory Retrieval Scoring](./searchstrategy-protocol-pluggable-memory-retrieval-scoring.md) — Defines a Protocol interface for memory retrieval scoring, enabling consumers to replace the default BM25 scorer with embeddings, vector databases, or any custom implementation. Ships with two built-in strategies: a zero-dependency token-overlap fallback and a BM25-backed default.
- [Test Suite: Context Store Retrieval — Grep, Expand, and Describe](./test-suite-context-store-retrieval-grep-expand-and-describe.md) — Validates the three retrieval operations on Soul Protocol's conversation context store: regex-based message search (`grep`), DAG node expansion to recover original messages (`expand`), and metadata snapshot generation (`describe`). These operations power agent tools that let an LLM inspect its own conversation history without re-loading the full context window.
- [Test Suite for the Retrieval Spec: Credentials, Protocols, and Request Contracts](./test-suite-for-the-retrieval-spec-credentials-protocols-and-request-contracts.md) — This test suite validates the spec-level contracts defined in `soul_protocol.spec.retrieval` — covering `Credential` validation and expiry logic, structural Protocol conformance for sync and async adapters, `DataRef` promotion from dicts, and timezone-aware `RetrievalRequest` point-in-time fields. These tests intentionally contain no runtime machinery, serving as a conformance suite for any implementation of the retrieval standard.

### `retrieval filtering` — 1 article(s)

- [Scope Matching — Bidirectional Hierarchical RBAC/ABAC Scope Evaluation](./scope-matching-bidirectional-hierarchical-rbacabac-scope-evaluation.md) — This module provides `match_scope`, `match_scope_strict`, and `normalise_scopes` — pure-stdlib functions for evaluating hierarchical scope tags used across all Soul Protocol retrieval paths. The `match_scope` function uses bidirectional containment after a bug was found where agents with concrete scopes (`org:sales:leads`) could not see memories tagged with parent glob scopes (`org:sales:*`) from bundled archetypes.

### `routing` — 1 article(s)

- [Journal Scope Matching: Pattern Grammar and Overlap Checks](./journal-scope-matching-pattern-grammar-and-overlap-checks.md) — Provides two public functions for colon-delimited scope pattern matching used throughout the journal engine, retrieval router, and credential broker. The module was extracted from the SQLite backend to make the shared logic accessible as a proper public API.

### `runner` — 1 article(s)

- [Test Suite for Long-Horizon Benchmark Runner](./test-suite-for-long-horizon-benchmark-runner.md) — Validates the multi-condition benchmark runner that processes conversation scenarios through different soul configurations (Full Soul, RAG Only, Personality Only, Bare) and records recall, memory growth, and bond-strength metrics. Async tests use a short 15-turn scenario fixture to keep CI fast while still exercising the full execution path.

### `runtime` — 23 article(s)

- [Bond Model: Human-Soul Relationship Strength](./bond-model-human-soul-relationship-strength.md) — Models the trust relationship between a human and their soul companion, tracking bond strength and interaction count. Uses logarithmic growth to make early bonding feel natural while making deep trust genuinely hard-earned through sustained interaction.
- [Cognitive Prompt Templates for LLM-Powered Memory Tasks](./cognitive-prompt-templates-for-llm-powered-memory-tasks.md) — This module contains all LLM prompt templates used by `CognitiveProcessor` for sentiment analysis, significance gating, fact extraction, entity extraction, and self-reflection. Each template embeds a `[TASK:xxx]` routing marker so that `HeuristicEngine` can dispatch to its regex fallbacks without parsing the full prompt.
- [CognitiveEngine Protocol, HeuristicEngine, and CognitiveProcessor](./cognitiveengine-protocol-heuristicengine-and-cognitiveprocessor.md) — This module defines the core cognitive abstraction layer for soul-protocol: the `CognitiveEngine` protocol (the one interface consumers implement), `HeuristicEngine` (the zero-dependency regex fallback), and `CognitiveProcessor` (the internal orchestrator that turns raw LLM responses into typed memory objects). Together they decouple memory pipeline logic from any specific LLM provider.
- [Context Package Exports: LCMContext and SQLiteContextStore](./context-package-exports-lcmcontext-and-sqlitecontextstore.md) — The `runtime/context` package's `__init__.py` provides a clean public surface for the Lossless Context Management (LCM) subsystem, re-exporting `LCMContext` (the reference `ContextEngine` implementation) and `SQLiteContextStore` (the persistence layer) so consumers import from a single stable path rather than internal submodules.
- [Crypto Package Exports: Soul File Encryption Helpers](./crypto-package-exports-soul-file-encryption-helpers.md) — The `runtime/crypto` package's `__init__.py` re-exports `derive_key`, `encrypt_data`, and `decrypt_data` from the internal `encrypt` module, providing a clean single-import path for the symmetric encryption primitives used to protect `.soul` archive files.
- [DSPy Cognitive Modules: Optimizable Memory Pipeline Components](./dspy-cognitive-modules-optimizable-memory-pipeline-components.md) — This module defines three DSPy-native modules — `SignificanceGate`, `QueryExpander`, and `FactExtractor` — that replace hand-written heuristics in soul-protocol's memory pipeline with learnable, optimizable LLM programs. All DSPy imports are lazy so the module loads safely in environments where DSPy is not installed.
- [DSPyCognitiveProcessor: Learnable Memory Cognition via DSPy](./dspycognitiveprocessor-learnable-memory-cognition-via-dspy.md) — DSPyCognitiveProcessor is a drop-in replacement for `CognitiveProcessor` that routes significance assessment, query expansion, and fact extraction through optimizable DSPy modules instead of hand-written prompts. It bridges DSPy's synchronous execution model to soul-protocol's async architecture via a thread pool executor.
- [FileStorage: Filesystem-Backed Soul Persistence with Atomic Writes](./filestorage-filesystem-backed-soul-persistence-with-atomic-writes.md) — `FileStorage` persists soul configs, DNA markdown, and all seven memory tiers to a structured directory layout under `~/.soul/`. The `save_soul_full` function uses a temp-dir-then-move pattern to guarantee atomic writes, preventing partial state on crash.
- [InMemoryStorage: Dict-Backed Soul Backend for Tests and Ephemeral Processes](./inmemorystorage-dict-backed-soul-backend-for-tests-and-ephemeral-processes.md) — `InMemoryStorage` is a zero-dependency, dict-backed implementation of `StorageProtocol` for unit tests and short-lived processes. All methods are `async` to satisfy the protocol interface, even though no I/O occurs.
- [LCM Retrieval Tools: grep, expand, and describe](./lcm-retrieval-tools-grep-expand-and-describe.md) — This module provides three retrieval functions — `grep`, `expand`, and `describe` — that expose different views into the immutable LCM message store. `grep` searches by regex, `expand` recursively reconstructs original messages from compacted DAG nodes, and `describe` returns a metadata snapshot of the entire store.
- [LCMContext: Reference Lossless Context Management Engine](./lcmcontext-reference-lossless-context-management-engine.md) — `LCMContext` is soul-protocol's reference implementation of the `ContextEngine` protocol. It ingests conversation messages into an append-only SQLite store, assembles token-bounded context windows with automatic three-level compaction, and exposes grep/expand/describe retrieval tools — all without requiring a `Soul` instance.
- [MCPSamplingEngine: Host-LLM Cognitive Routing via MCP](./mcpsamplingengine-host-llm-cognitive-routing-via-mcp.md) — MCPSamplingEngine routes cognitive prompts to the host LLM (Claude, GPT-4, etc.) through the Model Context Protocol sampling API, so soul-protocol tools embedded in Claude Code or Claude Desktop can leverage the host model without needing a separate API key. It falls back to HeuristicEngine whenever the MCP client does not support sampling or when called outside an MCP context.
- [OllamaEngine: Local Model Cognitive Adapter](./ollamaengine-local-model-cognitive-adapter.md) — OllamaEngine connects soul-protocol's cognitive pipeline to a locally running Ollama server, using the existing `httpx` dependency so no additional packages are required. It posts prompts to Ollama's REST API and returns the model response, enabling fully offline AI companion operation.
- [OpenAIEngine: OpenAI and OpenAI-Compatible Cognitive Adapter](./openaiengine-openai-and-openai-compatible-cognitive-adapter.md) — OpenAIEngine connects soul-protocol's cognitive pipeline to OpenAI's chat completions API, or any OpenAI-compatible endpoint such as a local Ollama server in OpenAI mode, vLLM, or LM Studio. The adapter is an optional extra that requires `pip install soul-protocol[openai]` and performs a soft import to avoid breaking the core package for users who don't need it.
- [Protocol Bridges Package: A2A Interoperability](./protocol-bridges-package-a2a-interoperability.md) — Package init for the runtime bridges subsystem, currently exposing the `A2AAgentCardBridge` for bidirectional conversion between Soul Protocol and Google's A2A Agent Card format. Bridges enable soul-powered agents to participate in multi-agent ecosystems that use other protocols.
- [Runtime Package: Reference Implementation Public API](./runtime-package-reference-implementation-public-api.md) — The runtime package `__init__.py` defines the public API surface for soul-protocol's reference implementation, currently exporting the Lossless Context Management (LCM) and evaluation subsystems. It serves as the 'batteries-included' layer built on top of the lower-level spec primitives.
- [Runtime Types: The Complete Soul Protocol Pydantic Model Library](./runtime-types-the-complete-soul-protocol-pydantic-model-library.md) — `types.py` is the canonical data model layer for Soul Protocol — over 30 Pydantic models covering identity, personality (OCEAN), psychology-informed memory, emotional state, evolution, and interaction. It is the schema backbone that all other runtime modules depend on.
- [SoulFactory: Template-Driven Soul Creation and Batch Spawning](./soulfactory-template-driven-soul-creation-and-batch-spawning.md) — `SoulFactory` creates souls from `SoulTemplate` blueprints via `from_template()` (single soul) and `batch_spawn()` (N souls with controlled OCEAN personality variance). Both methods propagate `default_scope` from template metadata into seeded core memories, enabling RBAC/ABAC recall filtering on freshly created souls.
- [State Subpackage Public Interface](./state-subpackage-public-interface.md) — The `state/__init__.py` module is the stable public entry point for the runtime state management subpackage, re-exporting `StateManager` so callers never couple to internal module paths. This thin facade absorbs internal restructures without breaking consumers.
- [StateManager: Runtime Soul State and Mood Dynamics](./statemanager-runtime-soul-state-and-mood-dynamics.md) — `StateManager` owns all mutable runtime state for a digital soul — energy, social battery, mood, and interaction timestamps. It provides delta-based updates, time-based auto-regeneration, and EMA-smoothed mood transitions driven by somatic markers from the sentiment pipeline.
- [Storage Subpackage Public Interface](./storage-subpackage-public-interface.md) — The `storage/__init__.py` re-exports all public storage symbols — `StorageProtocol`, `FileStorage`, `InMemoryStorage`, and the persistence functions `save_soul`, `load_soul`, `save_soul_full`, and `load_soul_full` — from a single stable import path. A `save_soul_full` addition in 2026-02-22 corrected the earlier API's silent memory-tier data loss.
- [StorageProtocol: Runtime-Checkable Interface for Soul Storage Backends](./storageprotocol-runtime-checkable-interface-for-soul-storage-backends.md) — `StorageProtocol` is a `@runtime_checkable` Python `Protocol` that defines the four operations all soul storage backends must implement: `save`, `load`, `delete`, and `list_souls`. It decouples the runtime from any specific storage implementation, enabling `FileStorage` and `InMemoryStorage` to be swapped without modifying callers.
- [ThreeLevelCompactor: Escalating Context Window Management](./threelevelcompactor-escalating-context-window-management.md) — `ThreeLevelCompactor` keeps the active context window within a token budget using three escalating strategies: LLM-generated prose summaries, LLM-generated bullet-point compressions, and deterministic head truncation. The compactor always applies the least aggressive level that achieves the budget, and guarantees convergence even without an LLM.

### `sampling engine` — 1 article(s)

- [Test Suite for MCP Sampling Engine (LLM Think with Heuristic Fallback)](./test-suite-for-mcp-sampling-engine-llm-think-with-heuristic-fallback.md) — Validates the MCPSamplingEngine, which wraps the MCP context's sampling capability to give souls LLM-powered reflection, with graceful fallback to the HeuristicEngine when the MCP context is unavailable or raises. Tests cover the success path, three failure scenarios, the no-context path, and the server's engine wiring lifecycle.

### `scale-testing` — 2 article(s)

- [Marathon Scale Scenario — 1000-Turn Conversation for Scale Ablation](./marathon-scale-scenario-1000-turn-conversation-for-scale-ablation.md) — Generates a fully deterministic 1000-turn synthetic conversation (`marathon_1000`) with 35 strategically planted facts and 40 recall test points designed to reveal the point at which Soul Protocol's selective memory corpus outperforms a naive RAG store. Facts are seeded across the full 1000-turn span — including deeply buried and callback facts — to stress-test long-range recall.
- [Scale Ablation Runner: 1000-Turn Memory Efficiency Study](./scale-ablation-runner-1000-turn-memory-efficiency-study.md) — A dedicated runner for the 1000-turn marathon scenario that benchmarks Soul Protocol's selective storage against RAG (store-everything) and two null conditions at scale. At 1000 turns, the soul achieves 85% recall with 175 memories (4.9x more efficient than RAG's 1000), and the study breaks down recall by memory age to identify decay patterns.

### `scenario-generation` — 2 article(s)

- [Long-Horizon Conversation Scenario Generators — Three Ablation Test Cases](./long-horizon-conversation-scenario-generators-three-ablation-test-cases.md) — Defines the `LongHorizonScenario` and `TestPoint` data structures and generates three distinct 100-160 turn synthetic conversation scenarios designed to stress different aspects of Soul Protocol's psychology stack: long-range recall, emotional continuity under extreme valence swings, and recall precision under adversarial fact burial.
- [Quality Scenario Generator: Randomized Test Variations for Statistical Validity](./quality-scenario-generator-randomized-test-variations-for-statistical-validity.md) — The scenario generator produces 10 randomized but reproducible variations of each of the four quality test types, enabling error bars and statistical significance checks rather than single-point measurements. A fixed seed (`SEED = 42`) ensures results are reproducible across runs while the random variation catches edge cases a single hardcoded scenario would miss.

### `scenarios` — 2 article(s)

- [Research Scenario Bank: Multi-Turn Interaction Templates for Four Use Cases](./research-scenario-bank-multi-turn-interaction-templates-for-four-use-cases.md) — The scenario bank provides parameterized, multi-turn interaction sequences for four use cases — customer support, coding assistant, personal companion, and knowledge worker — each with embedded ground truth: planted facts, recall queries, emotional tone markers, and importance hints. Scenarios are personalized by `UserProfile` to increase coverage and ecological validity.
- [Test Suite for Long-Horizon Benchmark Scenarios](./test-suite-for-long-horizon-benchmark-scenarios.md) — Validates the three benchmark scenario generators (Life Updates, Emotional Rollercoaster, Adversarial Burial) that produce 100+ turn conversation scripts with planted facts, test points, and recall challenges. Tests verify determinism, scenario structure, seeded variation, and the TestPoint dataclass.

### `schema` — 2 article(s)

- [Journal SQLite Schema and Migration Helper](./journal-sqlite-schema-and-migration-helper.md) — Defines the SQLite schema for the soul-protocol journal engine and provides an idempotent migration function. Ensures the on-disk database is always brought up to the current schema version before use, preventing schema drift between engine upgrades.
- [Schema Generator: JSON Schema Export for All Soul Protocol Pydantic Models](./schema-generator-json-schema-export-for-all-soul-protocol-pydantic-models.md) — A utility script that introspects every Pydantic model in `soul_protocol.runtime.types` and writes individual JSON Schema files plus a combined bundle to the `schemas/` directory, enabling cross-language clients (TypeScript, Rust, Go) to validate soul data structures without depending on the Python package.

### `schema validation` — 1 article(s)

- [Test Suite: JSON Schema Generation from Pydantic Models](./test-suite-json-schema-generation-from-pydantic-models.md) — Verifies that every Soul Protocol Pydantic model can produce a valid, JSON-serializable schema, that a sample `SoulConfig` dict validates against the generated schema, and that the `generate_schemas.py` script produces the expected on-disk files. This acts as a contract test for external consumers who depend on the published JSON schemas.

### `scope` — 2 article(s)

- [Scope Matching — Bidirectional Hierarchical RBAC/ABAC Scope Evaluation](./scope-matching-bidirectional-hierarchical-rbacabac-scope-evaluation.md) — This module provides `match_scope`, `match_scope_strict`, and `normalise_scopes` — pure-stdlib functions for evaluating hierarchical scope tags used across all Soul Protocol retrieval paths. The `match_scope` function uses bidirectional containment after a bug was found where agents with concrete scopes (`org:sales:leads`) could not see memories tagged with parent glob scopes (`org:sales:*`) from bundled archetypes.
- [Test Suite for Bundled Role Templates and SoulFactory Template Loading](./test-suite-for-bundled-role-templates-and-soulfactory-template-loading.md) — This test suite locks the contracts for the four bundled archetypes (Arrow, Flash, Cyborg, Analyst), the YAML/JSON template loaders, template instantiation into real souls, and the scope propagation behavior that ensures Arrow's sales-scoped core memories are visible to concrete `org:sales:leads` callers.

### `scripts` — 5 article(s)

- [Memory Evolution Simulation Script](./memory-evolution-simulation-script.md) — A standalone simulation that exercises 100 interactions to demonstrate memory compression, archival storage growth, and temporal knowledge-graph evolution in soul-protocol. It serves as a developer-facing proof that the memory pipeline works end-to-end without a live LLM.
- [PocketPaw E2E Integration Script: Full Soul Protocol Lifecycle Validation](./pocketpaw-e2e-integration-script-full-soul-protocol-lifecycle-validation.md) — An end-to-end integration script that validates the complete soul-protocol API lifecycle — birth, observe, remember, recall, reflect, export, awaken, core memory editing, state management, and MCP tool smoke tests — from the perspective of PocketPaw's integration layer, using realistic interaction datasets and Rich-formatted reporting.
- [Real-User Scenario Simulation Suite](./real-user-scenario-simulation-suite.md) — A comprehensive integration simulation that runs six realistic user scenarios against soul-protocol to validate lifecycle correctness, memory accuracy, multi-format support, personality divergence, and resilience. Each scenario produces a structured pass/fail report written to `.results/`.
- [Schema Generator: JSON Schema Export for All Soul Protocol Pydantic Models](./schema-generator-json-schema-export-for-all-soul-protocol-pydantic-models.md) — A utility script that introspects every Pydantic model in `soul_protocol.runtime.types` and writes individual JSON Schema files plus a combined bundle to the `schemas/` directory, enabling cross-language clients (TypeScript, Rust, Go) to validate soul data structures without depending on the Python package.
- [Simulate Script: Comprehensive Soul Protocol Scenario Test Suite](./simulate-script-comprehensive-soul-protocol-scenario-test-suite.md) — A standalone simulation script containing 30+ named scenarios that test every surface of the soul-protocol API — from basic lifecycle and memory recall to adversarial inputs, concurrent async safety, cross-platform migration, evolution system, knowledge graph, and graceful degradation. Each scenario is independently runnable and produces a detailed Rich-formatted report.

### `search` — 4 article(s)

- [BM25 and Token-Overlap Memory Search Engine](./bm25-and-token-overlap-memory-search-engine.md) — Implements the core text-matching primitives used by all three memory stores: a tokenizer that handles natural language and code identifiers, a BM25 index with IDF weighting, and a lightweight token-overlap relevance scorer kept as a fast fallback. Synonym expansion improves recall for common programming and technical terms.
- [SearchStrategy Protocol: Pluggable Memory Retrieval Scoring](./searchstrategy-protocol-pluggable-memory-retrieval-scoring.md) — Defines a Protocol interface for memory retrieval scoring, enabling consumers to replace the default BM25 scorer with embeddings, vector databases, or any custom implementation. Ships with two built-in strategies: a zero-dependency token-overlap fallback and a BM25-backed default.
- [Test Suite: Memory Search Tokenizer and Synonym Expansion](./test-suite-memory-search-tokenizer-and-synonym-expansion.md) — Tests for two search quality improvements (issues #10 and #11): a tokenizer that splits on code-style separators like slashes and underscores, and a synonym expansion layer that maps programming terms to related aliases so queries for 'database' match memories containing 'postgresql'.
- [Test Suite: Pluggable Search Strategy Protocol](./test-suite-pluggable-search-strategy-protocol.md) — Tests for the SearchStrategy protocol and its propagation through the full memory stack — from Soul.birth() down through MemoryManager, RecallEngine, spreading_activation(), and compute_activation() — verifying that custom scoring functions can replace the default token-overlap approach at every layer.

### `security` — 14 article(s)

- [AES-256-GCM Encryption for Soul Archive Files](./aes-256-gcm-encryption-for-soul-archive-files.md) — `crypto.py` provides password-based encryption and decryption for `.soul` file contents using AES-256-GCM authenticated encryption with scrypt key derivation. Each call to `encrypt_blob` generates fresh randomness, ensuring that the same password encrypts the same content to a different ciphertext every time.
- [CLI Test Suite: Org Initialization (`soul org init`)](./cli-test-suite-org-initialization-soul-org-init.md) — Test suite for `soul org init`, which bootstraps an organization by creating a root soul, a journal, and cryptographic keys. Covers happy-path artifact creation, idempotency guards, force-overwrite behavior, key file permissions, journal privacy, and round-trip loadability.
- [Crypto Package Exports: Soul File Encryption Helpers](./crypto-package-exports-soul-file-encryption-helpers.md) — The `runtime/crypto` package's `__init__.py` re-exports `derive_key`, `encrypt_data`, and `decrypt_data` from the internal `encrypt` module, providing a clean single-import path for the symmetric encryption primitives used to protect `.soul` archive files.
- [Cryptographic Encryption Test Suite (Fernet + PBKDF2)](./cryptographic-encryption-test-suite-fernet-pbkdf2.md) — Test suite validating the `soul_protocol.runtime.crypto.encrypt` module, which provides passphrase-based symmetric encryption for soul data. Covers the full encrypt/decrypt roundtrip, wrong-passphrase rejection, and deterministic key derivation.
- [Org Management CLI — `soul org` and `soul user`](./org-management-cli-soul-org-and-soul-user.md) — Implements the `soul org init`, `soul org status`, `soul org destroy`, and `soul user invite` commands for managing multi-soul organisational deployments. Creates a governance root soul, writes journal events for every lifecycle action, and enforces layered protections against root-soul deletion.
- [Smart Memory Reranking via LLM (Prompt-Injection-Hardened)](./smart-memory-reranking-via-llm-prompt-injection-hardened.md) — Provides an optional second-pass reranking stage that uses a lightweight LLM call to select the most contextually relevant memories from a heuristically-ranked candidate pool. The implementation includes robust prompt injection defenses because memory content is untrusted user input that could manipulate the ranking decision.
- [Soul Archive Packer: Building .soul ZIP Archives](./soul-archive-packer-building-soul-zip-archives.md) — `pack_soul` serializes a `SoulConfig` and all its memory tiers into a portable `.soul` ZIP archive, with optional AES-256-GCM encryption for every file except the manifest. It is the primary serialization primitive — every export, backup, and migration operation ultimately calls this function.
- [Soul Archive Unpacker: Loading .soul ZIP Archives](./soul-archive-unpacker-loading-soul-zip-archives.md) — `unpack_soul` deserializes a `.soul` ZIP archive back into a `(SoulConfig, memory_data)` tuple, handling both encrypted and unencrypted archives with clear error separation between missing-password and wrong-password scenarios. It is the read-side counterpart to `pack_soul` and is called on every `awaken()` operation.
- [Soul Protocol Custom Exception Hierarchy](./soul-protocol-custom-exception-hierarchy.md) — `exceptions.py` defines the typed exception hierarchy for soul-protocol operations, covering file-not-found, corruption, export failures, encryption errors, and protected role violations. Using typed exceptions instead of bare `Exception` lets callers write precise `except` clauses and present actionable error messages to users.
- [Symmetric Encryption for Soul Files (Fernet + PBKDF2)](./symmetric-encryption-for-soul-files-fernet-pbkdf2.md) — Provides passphrase-based encryption and decryption for soul file data using Fernet symmetric encryption with PBKDF2-HMAC-SHA256 key derivation. The salt is embedded in the ciphertext so only the passphrase is needed for decryption — no separate salt management required.
- [Test Suite for Soul File Encryption](./test-suite-for-soul-file-encryption.md) — Validates the full encryption stack for .soul files, from low-level blob crypto primitives through pack/unpack pipeline integration to the high-level Soul.export() and Soul.awaken() API. Covers round-trips, wrong-password errors, backward compatibility with unencrypted files, and manifest readability without a password.
- [Test Suite: LLM-Powered Memory Reranking and Smart Recall](./test-suite-llm-powered-memory-reranking-and-smart-recall.md) — Covers `rerank_memories()`, the `_parse_indices()` helper, and `Soul.smart_recall()` — the optional LLM-driven reranking layer on top of heuristic memory retrieval. The suite validates correct ordering, fallback behavior on engine failure or timeout, opt-in flag semantics, and prompt injection defenses.
- [Test Suite: Scope Tags and Hierarchical Memory Access Control](./test-suite-scope-tags-and-hierarchical-memory-access-control.md) — Tests the `match_scope` and `match_scope_strict` helpers that implement hierarchical RBAC/ABAC filtering for memory recall. The suite covers bidirectional glob containment, strict one-way matching, scope normalization, export/awaken round-trips for scoped memories, and `Soul.recall` scope-filter integration.
- [YAML Soul Configuration Parser (parse_soul_yaml)](./yaml-soul-configuration-parser-parsesoulyaml.md) — Parses a YAML soul configuration string into a SoulConfig using yaml.safe_load for deserialization and Pydantic model_validate for schema enforcement, with a two-step approach that separates parsing errors from validation errors.

### `self-concept` — 1 article(s)

- [SelfModelManager: Evolving Self-Concept for Digital Souls](./selfmodelmanager-evolving-self-concept-for-digital-souls.md) — Implements Klein's self-model theory for AI companions — the soul discovers what it is by observing its own interactions and building a map of the domains where it operates. Self-images surface as prompt fragments that anchor the soul's personality in generated responses.

### `self-model` — 2 article(s)

- [D5 Self-Model Evaluator](./d5-self-model-evaluator.md) — Measures the soul's ability to dynamically classify user interaction domains, emerge topic confidence quickly, isolate domains without cross-contamination, and extract relationship metadata from conversation. Contributes 15% of the Soul Health Score.
- [Test Suite: Self-Model Manager (Soul's Self-Concept)](./test-suite-self-model-manager-souls-self-concept.md) — Tests for SelfModelManager, which implements Klein's self-concept model — the soul's ongoing self-knowledge about what domains it is competent in, which roles it plays, and how confident it is in those self-images based on accumulated evidence.

### `semantic` — 1 article(s)

- [Semantic Memory Deduplication and Fact Reconciliation](./semantic-memory-deduplication-and-fact-reconciliation.md) — Implements `reconcile_fact()`, the gating function that decides whether a new semantic fact should be created, merged into an existing entry, or skipped as a near-duplicate. Uses an enhanced Jaccard similarity with a containment coefficient to handle enriched/superset facts correctly.

### `semantic retrieval` — 1 article(s)

- [VectorSearchStrategy: Embedding-Based Semantic Memory Search](./vectorsearchstrategy-embedding-based-semantic-memory-search.md) — The `VectorSearchStrategy` wraps any `EmbeddingProvider` to implement semantic similarity search over memory entries. It maintains a pre-built index for performance, falls back to on-the-fly embedding for un-indexed candidates, and uses cosine similarity with a configurable threshold to rank and filter results.

### `sentence-transformers` — 1 article(s)

- [Sentence Transformer Embedding Provider Test Suite — Lazy Loading and Mock-Based Validation](./sentence-transformer-embedding-provider-test-suite-lazy-loading-and-mock-based-v.md) — Test suite for `SentenceTransformerProvider`, which wraps the `sentence-transformers` library for high-quality local embedding generation. Tests mock the heavy ML dependency to run in CI, covering protocol compliance, lazy model loading, batch embedding, custom model and device configuration, and import error handling.

### `sentiment` — 2 article(s)

- [Test Suite: Heuristic Sentiment Detection (Somatic Markers)](./test-suite-heuristic-sentiment-detection-somatic-markers.md) — Tests for the heuristic sentiment detector that produces Damasio-style somatic markers (valence, arousal, label) from raw text, covering neutral text, positive/negative words, intensifiers, negation, mixed emotions, and label accuracy.
- [Test Suite: Known Sentiment Detector Gaps](./test-suite-known-sentiment-detector-gaps.md) — A bug-documenting test suite that captures known failure modes in the heuristic sentiment detector — missing vocabulary, joy/excitement confusion, sadness/frustration confusion, gratitude misclassification, and neutral false positives — intended to go green once the corresponding fixes land in sentiment.py.

### `serialization` — 6 article(s)

- [Soul Archive Packer: Building .soul ZIP Archives](./soul-archive-packer-building-soul-zip-archives.md) — `pack_soul` serializes a `SoulConfig` and all its memory tiers into a portable `.soul` ZIP archive, with optional AES-256-GCM encryption for every file except the manifest. It is the primary serialization primitive — every export, backup, and migration operation ultimately calls this function.
- [Soul File I/O — Pack and Unpack .soul Zip Archives](./soul-file-io-pack-and-unpack-soul-zip-archives.md) — This module provides `pack_soul`, `unpack_soul`, and `unpack_to_container` — the three functions that serialize and deserialize a soul's identity and memories to and from the `.soul` zip archive format. It depends only on stdlib (`zipfile`, `json`, `io`) and the spec-layer models, keeping the archival format fully portable.
- [Test Suite for Soul File Pack, Unpack, and Container Helpers](./test-suite-for-soul-file-pack-unpack-and-container-helpers.md) — This test suite validates the `.soul` archive format's core serialization functions: `pack_soul()`, `unpack_soul()`, and the `unpack_to_container()` convenience helper. It verifies identity and memory fidelity across the binary zip roundtrip, multi-layer preservation, empty-store edge cases, and robust error handling for malformed or incomplete archives.
- [Test Suite for the Manifest Model](./test-suite-for-the-manifest-model.md) — This test suite validates the `Manifest` Pydantic model, which acts as the metadata header of a `.soul` archive. It covers default field values, explicit field assignment, and the full Pydantic serialization roundtrip to ensure no data is silently dropped during pack/unpack.
- [Test Suite: Identity Spec — Construction, Auto-ID, Traits, and Serialization](./test-suite-identity-spec-construction-auto-id-traits-and-serialization.md) — Validates the core `Identity` primitive from `soul_protocol.spec`, covering minimal construction, deterministic auto-ID generation, uniqueness guarantees, arbitrary traits storage, and Pydantic `model_dump`/`model_validate` round-trip fidelity. Identity is the foundational spec model that all other containers reference.
- [Test Suite: JSON Schema Generation from Pydantic Models](./test-suite-json-schema-generation-from-pydantic-models.md) — Verifies that every Soul Protocol Pydantic model can produce a valid, JSON-serializable schema, that a sample `SoulConfig` dict validates against the generated schema, and that the `generate_schemas.py` script produces the expected on-disk files. This acts as a contract test for external consumers who depend on the published JSON schemas.

### `server` — 2 article(s)

- [FastMCP Server: 28-Tool Soul Protocol Agent Integration](./fastmcp-server-28-tool-soul-protocol-agent-integration.md) — Implements the full soul-protocol MCP server using FastMCP, exposing 28 tools, 3 resources, and 2 prompts for AI agent integration. Manages a registry of multiple souls, handles auto-reload from disk, lazy cognitive engine wiring, and graceful shutdown with auto-save.
- [MCP Package Public Entry Points](./mcp-package-public-entry-points.md) — Thin package init that re-exports `create_server` and `run_server` from the MCP server module, providing a stable public surface for consumers who integrate soul-protocol's FastMCP server. Keeps the import path short (`from soul_protocol.mcp import create_server`) regardless of internal module organization.

### `simulation` — 9 article(s)

- [Quick Ablation Runner: Heuristic-Only Long-Horizon Scenario Executor](./quick-ablation-runner-heuristic-only-long-horizon-scenario-executor.md) — A minimal, API-key-free script that runs all long-horizon scenarios through the `LongHorizonRunner` and prints a structured ablation table comparing recall precision, memory count, and bond strength across conditions. It is the fastest way to verify the heuristic pipeline without incurring LLM costs.
- [Real-User Scenario Simulation Suite](./real-user-scenario-simulation-suite.md) — A comprehensive integration simulation that runs six realistic user scenarios against soul-protocol to validate lifecycle correctness, memory accuracy, multi-format support, personality divergence, and resilience. Each scenario produces a structured pass/fail report written to `.results/`.
- [Research Package: Soul Protocol Validation Simulation Framework](./research-package-soul-protocol-validation-simulation-framework.md) — The `research/` package is a large-scale simulation framework designed to validate Soul Protocol's psychology-informed memory claims at publication grade, running 1,000 simulated agents across 5 memory conditions and 4 use cases for more than 20,000 experimental runs. It is structured for reproducibility and statistical rigor.
- [Research Simulation CLI: Entry Point for Full-Scale Ablation Experiments](./research-simulation-cli-entry-point-for-full-scale-ablation-experiments.md) — The `run.py` module is the primary CLI entry point for the Soul Protocol research simulation framework, translating command-line arguments into an `ExperimentConfig` and orchestrating the full simulation, analysis, and report pipeline. It supports configuring agent count, random seed, memory conditions, use cases, output directory, batch size, and a quick mode for rapid iteration.
- [Simulate Script: Comprehensive Soul Protocol Scenario Test Suite](./simulate-script-comprehensive-soul-protocol-scenario-test-suite.md) — A standalone simulation script containing 30+ named scenarios that test every surface of the soul-protocol API — from basic lifecycle and memory recall to adversarial inputs, concurrent async safety, cross-platform migration, evolution system, knowledge graph, and graceful degradation. Each scenario is independently runnable and produces a detailed Rich-formatted report.
- [Simulated Agent and User Profile Generator for Research Experiments](./simulated-agent-and-user-profile-generator-for-research-experiments.md) — Generates 1,000 diverse simulated agent profiles with realistic OCEAN personality distributions and 1,000 matched user profiles for a specified use case domain, used as the participant pool for Soul Protocol's validation experiments. All sampling uses a seeded RNG to guarantee reproducible results across experiment runs.
- [Simulation Engine: Full Factorial Experiment Orchestrator for Soul Protocol Research](./simulation-engine-full-factorial-experiment-orchestrator-for-soul-protocol-resea.md) — The `SimulationEngine` orchestrates the core ablation study by running every combination of (agent, condition, use_case) as an async task, collecting `AgentRunMetrics` for each triple, and aggregating them into a `SimulationResults` object. Failed individual runs are logged and skipped rather than crashing the whole experiment, ensuring partial results are always captured.
- [Test Suite: Smoke Tests for the Research Simulation Framework](./test-suite-smoke-tests-for-the-research-simulation-framework.md) — This test suite validates that all research framework components — agent generation, user generation, scenario generation, experiment configuration, metrics, statistical utilities, and memory conditions — work correctly at minimal scale without requiring any LLM API calls. It is fast enough for CI and guards against regressions in the framework before running expensive full ablations.
- [Tier 2 Validation Runner: 100-Agent Real LLM Benchmark](./tier-2-validation-runner-100-agent-real-llm-benchmark.md) — The Tier 2 runner validates Soul Protocol with real Anthropic API calls across 100 simulated agents, comparing heuristic-only cognitive processing against LLM-backed processing using `HaikuCognitiveEngine`. It collects a comprehensive set of metrics — memory, personality, recall, bond, emotional, and skill dimensions — and saves the results as JSON.

### `skills` — 2 article(s)

- [Skill and SkillRegistry: XP-Based Ability Progression for Souls](./skill-and-skillregistry-xp-based-ability-progression-for-souls.md) — Implements a leveled skill system where souls gain experience points (XP) through interactions, level up abilities (1-10) with exponential scaling thresholds, and lose XP through time-based decay when skills go unused. SkillRegistry manages the collection and bridges the learning event pipeline.
- [Test Suite for Learning Events Pipeline](./test-suite-for-learning-events-pipeline.md) — Tests for the full learning events pipeline — from `RubricResult` score thresholding to `LearningEvent` creation, skill XP grant, and the `Soul.learn()` convenience method. Covers model validation, score boundary conditions, XP scaling, and spec module exportability.

### `skills system` — 1 article(s)

- [Test Suite: Skills XP Progression, Level Cap, and Decay](./test-suite-skills-xp-progression-level-cap-and-decay.md) — Validates the Skills subsystem — a gamification layer that tracks a soul's proficiency across named abilities via an XP and level model. The suite covers default construction, XP accumulation with level-up triggers, overflow carry, the level 10 cap, significance-weighted XP grants, skill decay, and the `SkillRegistry` collection.

### `soul DNA` — 2 article(s)

- [DNA Subpackage Public API (Re-export Module)](./dna-subpackage-public-api-re-export-module.md) — The `dna` subpackage `__init__.py` re-exports the two public functions from `dna/prompt.py` — `dna_to_system_prompt` and `dna_to_markdown` — providing a clean, stable import surface for consumers. This thin shim was fixed during a runtime restructure to use absolute import paths rooted at `soul_protocol.runtime`.
- [DNA to System Prompt and Markdown Renderer](./dna-to-system-prompt-and-markdown-renderer.md) — Converts a soul's identity, DNA (personality blueprint), core memory, and current state into either an LLM system prompt or a human-readable markdown document. The system prompt renderer is the primary mechanism by which a soul's personality becomes an active influence on its LLM-driven behavior.

### `soul archival` — 2 article(s)

- [Eternal Storage Protocol — Archive, Retrieve, and Verify Interface for Permanent Soul Backends](./eternal-storage-protocol-archive-retrieve-and-verify-interface-for-permanent-sou.md) — This module defines the canonical protocol and result models for Soul Protocol's eternal (permanent) storage tier. `EternalStorageProvider` is a runtime-checkable async `Protocol` that any backend (IPFS, Arweave, local) must satisfy, while `ArchiveResult` and `RecoverySource` capture what an archival operation produces and how a soul can later be restored.
- [Eternal Storage Spec Package — Public Interface for Permanent Soul Archival](./eternal-storage-spec-package-public-interface-for-permanent-soul-archival.md) — The `spec/eternal` package exposes the three types that define Soul Protocol's permanent archival tier: `EternalStorageProvider` (the backend protocol), `ArchiveResult` (what an archive operation returns), and `RecoverySource` (what a soul can be restored from). All implementation details live in the engine layer.

### `soul configuration` — 1 article(s)

- [Test Suite: Soul Configuration — OCEAN, Communication, Biorhythms, Persona, and Config Files](./test-suite-soul-configuration-ocean-communication-biorhythms-persona-and-config-.md) — Validates the flexible birth configuration system for `Soul`, covering direct-kwarg birth with OCEAN personality traits, communication style, biorhythms, and persona text, as well as file-based birth from YAML and JSON configs via `Soul.birth_from_config()`. The suite also covers CLI `--config` and `--openness/--neuroticism` flags and verifies that all settings survive export → awaken round-trips.

### `soul creation` — 2 article(s)

- [SoulFactory: Template-Driven Soul Creation and Batch Spawning](./soulfactory-template-driven-soul-creation-and-batch-spawning.md) — `SoulFactory` creates souls from `SoulTemplate` blueprints via `from_template()` (single soul) and `batch_spawn()` (N souls with controlled OCEAN personality variance). Both methods propagate `default_scope` from template metadata into seeded core memories, enabling RBAC/ABAC recall filtering on freshly created souls.
- [SoulTemplate — Blueprint for Batch Soul Spawning with Personality Variance](./soultemplate-blueprint-for-batch-soul-spawning-with-personality-variance.md) — `SoulTemplate` defines a reusable blueprint that a `SoulFactory` uses to spawn individual souls or batches with controlled personality variation. It carries a default personality (OCEAN traits as a dict), core memories to bootstrap, skill names, and variance settings for producing non-identical clones in batch operations.

### `soul evolution` — 2 article(s)

- [Dream: Offline Memory Consolidation Engine](./dream-offline-memory-consolidation-engine.md) — The `Dreamer` class implements offline memory consolidation for soul agents — the async background process that reviews accumulated episodic memories in batch, detects patterns, compacts redundant data, and synthesizes higher-order knowledge. It is the offline counterpart to `observe()`, which handles real-time interaction processing.
- [Test Suite: Skills XP Progression, Level Cap, and Decay](./test-suite-skills-xp-progression-level-cap-and-decay.md) — Validates the Skills subsystem — a gamification layer that tracks a soul's proficiency across named abilities via an XP and level model. The suite covers default construction, XP accumulation with level-up triggers, overflow carry, the level 10 cap, significance-weighted XP grants, skill decay, and the `SkillRegistry` collection.

### `soul factory` — 1 article(s)

- [Test Suite: Soul Templates and Batch Spawning via SoulFactory](./test-suite-soul-templates-and-batch-spawning-via-soulfactory.md) — Validates `SoulTemplate` (reusable soul blueprints with personality profiles, memories, and skills) and `SoulFactory` (the factory that instantiates souls from templates, including batch spawning with controlled personality variance). The suite covers model validation, override semantics, variance clamping, reproducible seeding, and public API exports.

### `soul file format` — 2 article(s)

- [Crypto Package Exports: Soul File Encryption Helpers](./crypto-package-exports-soul-file-encryption-helpers.md) — The `runtime/crypto` package's `__init__.py` re-exports `derive_key`, `encrypt_data`, and `decrypt_data` from the internal `encrypt` module, providing a clean single-import path for the symmetric encryption primitives used to protect `.soul` archive files.
- [Symmetric Encryption for Soul Files (Fernet + PBKDF2)](./symmetric-encryption-for-soul-files-fernet-pbkdf2.md) — Provides passphrase-based encryption and decryption for soul file data using Fernet symmetric encryption with PBKDF2-HMAC-SHA256 key derivation. The salt is embedded in the ciphertext so only the passphrase is needed for decryption — no separate salt management required.

### `soul format` — 3 article(s)

- [Soul File I/O — Pack and Unpack .soul Zip Archives](./soul-file-io-pack-and-unpack-soul-zip-archives.md) — This module provides `pack_soul`, `unpack_soul`, and `unpack_to_container` — the three functions that serialize and deserialize a soul's identity and memories to and from the `.soul` zip archive format. It depends only on stdlib (`zipfile`, `json`, `io`) and the spec-layer models, keeping the archival format fully portable.
- [Soul Identity — Schema-Free Portable Identity with Multi-Bond Support](./soul-identity-schema-free-portable-identity-with-multi-bond-support.md) — `Identity` is the portable identity primitive at the core of Soul Protocol. It carries a soul's name, creation timestamp, and an open `traits` dict that runtimes can populate with OCEAN scores, Myers-Briggs types, or any custom dimensions. The `feat/spec-multi-participant` update added `BondTarget` and a `bonds` list so a single soul can maintain relationships with multiple humans, agents, groups, and services.
- [Soul Manifest — Archive Metadata Header for .soul Files](./soul-manifest-archive-metadata-header-for-soul-files.md) — The `Manifest` model defines the `manifest.json` file stored at the root of every `.soul` zip archive. It captures format version, soul identity, creation timestamp, an integrity checksum placeholder, and an open `stats` dict for runtime-specific metadata like memory counts and layer names.

### `soul inject` — 1 article(s)

- [CLI Test Suite: Soul Context Injection into AI IDE Config Files](./cli-test-suite-soul-context-injection-into-ai-ide-config-files.md) — Test suite for `soul inject`, which writes a soul's identity and recent memories into AI IDE configuration files (Claude Code, Cursor, VS Code, Windsurf, Cline, Continue). Covers target path resolution, idempotent section replacement, context block formatting, memory truncation, and auto-discovery of soul directories.

### `soul lifecycle` — 7 article(s)

- [CLI Test Suite: Core Commands (birth, inspect, status, remember, recall, init)](./cli-test-suite-core-commands-birth-inspect-status-remember-recall-init.md) — Comprehensive test suite for the Soul Protocol CLI covering the full lifecycle of a soul: creation, inspection, memory storage across tiers, multi-format output, and init options. Tests use click.testing.CliRunner to invoke commands in isolation and assert on exit codes, output text, and ZIP archive contents.
- [Test Suite: Core Soul API — Birth, Memory, Emotion, Export, and Psychology Pipeline](./test-suite-core-soul-api-birth-memory-emotion-export-and-psychology-pipeline.md) — The primary integration test suite for the `Soul` class, covering the full API surface from construction through the psychology pipeline. It validates that soul birth, memory operations, emotional state, import/export round-trips, system prompt generation, and the attention gate all work correctly together.
- [Test Suite for MCP Server Core Tools (Birth, Observe, Memory, State, Lifecycle)](./test-suite-for-mcp-server-core-tools-birth-observe-memory-state-lifecycle.md) — Integration tests for the core soul_protocol MCP server tools, covering soul lifecycle (birth, save, export), memory operations (remember, recall), emotional state (feel), observation, reflection, and multi-soul management (list, switch). Uses a registry reset helper and in-process FastMCP client to achieve full isolation between tests.
- [Test Suite for Soul Error Handling](./test-suite-for-soul-error-handling.md) — Verifies that Soul's three main lifecycle operations — awaken(), export(), and retire() — raise specific, descriptive exception types rather than raw Python exceptions. Also tests exception class structure to ensure callers can extract actionable information like file paths and failure reasons.
- [Test Suite for Soul Eternal Storage Integration](./test-suite-for-soul-eternal-storage-integration.md) — Tests the integration between the Soul class and EternalStorageManager, covering Soul.archive(), the with_mocks() factory, the eternal= parameter on Soul.birth() and Soul.awaken(), and the export(archive=True) convenience flag.
- [Test Suite: Memory Compression -- Summarization, Deduplication, and Importance Pruning](./test-suite-memory-compression-summarization-deduplication-and-importance-pruning.md) — Tests for `MemoryCompressor`, which reduces the size of a soul's long-term memory store through four strategies: text summarization, near-duplicate removal, importance-based pruning, and export splitting. Covers edge cases like empty inputs, threshold tuning, and the rule that high-importance memories are never pruned regardless of age.
- [Test Suite: Soul Reincarnation Lifecycle](./test-suite-soul-reincarnation-lifecycle.md) — Validates that `Soul.reincarnate()` correctly transitions a soul to a new identity while preserving accumulated state. The suite ensures that reincarnation is not a hard reset — personality, memories, values, archetype, and bond all carry forward, while the DID and incarnation counter change to mark the new life.

### `soul maintenance` — 1 article(s)

- [CLI Test Suite: Health, Cleanup, and Repair Commands](./cli-test-suite-health-cleanup-and-repair-commands.md) — Test suite for three soul maintenance CLI commands: `soul health` (diagnostic reporting), `soul cleanup` (duplicate/low-importance memory removal), and `soul repair` (energy reset). Covers dry-run safety guards, auto-mode mutations, and persistence verification.

### `soul persistence` — 2 article(s)

- [Memory Primitives — MemoryEntry, MemoryStore Protocol, and DictMemoryStore](./memory-primitives-memoryentry-memorystore-protocol-and-dictmemorystore.md) — This module defines the atomic unit of soul memory (`MemoryEntry`), the backend-agnostic `MemoryStore` protocol, and a reference in-memory implementation (`DictMemoryStore`). It also introduces `Interaction` and `Participant` models for multi-party conversations, along with `MemoryVisibility` tiers for controlling what surfaces in public channel contexts.
- [Test Suite: SoulContainer — Spec-Layer Soul Creation, Persistence, and Memory Access](./test-suite-soulcontainer-spec-layer-soul-creation-persistence-and-memory-access.md) — Validates the spec-layer `SoulContainer` — the lightweight soul abstraction for integrations that do not need the full runtime. The suite covers container construction, traits storage, `.soul` file save/open round-trips, multi-layer memory preservation, error handling for missing files, and the default `DictMemoryStore`.

### `soul portability` — 2 article(s)

- [Eternal Storage Subsystem Public API](./eternal-storage-subsystem-public-api.md) — The `eternal` subpackage `__init__.py` re-exports the four public symbols from the eternal storage subsystem: `EternalStorageProvider`, `EternalStorageManager`, `ArchiveResult`, and `RecoverySource`. This clean facade hides the internal module layout and provides a single import target for callers persisting soul data to permanent storage.
- [EternalStorageManager: Multi-Tier Eternal Storage Orchestrator](./eternalstoragemanager-multi-tier-eternal-storage-orchestrator.md) — The `EternalStorageManager` coordinates multiple eternal storage backends (Arweave, IPFS, etc.) for archiving and recovering soul data. It registers providers by tier name, fans out archive operations across requested tiers, and attempts ordered recovery from tracked sources — failing gracefully if individual tiers are unavailable.

### `soul-format` — 3 article(s)

- [Test Suite for Soul File Pack, Unpack, and Container Helpers](./test-suite-for-soul-file-pack-unpack-and-container-helpers.md) — This test suite validates the `.soul` archive format's core serialization functions: `pack_soul()`, `unpack_soul()`, and the `unpack_to_container()` convenience helper. It verifies identity and memory fidelity across the binary zip roundtrip, multi-layer preservation, empty-store edge cases, and robust error handling for malformed or incomplete archives.
- [Test Suite for the Manifest Model](./test-suite-for-the-manifest-model.md) — This test suite validates the `Manifest` Pydantic model, which acts as the metadata header of a `.soul` archive. It covers default field values, explicit field assignment, and the full Pydantic serialization roundtrip to ensure no data is silently dropped during pack/unpack.
- [Test Suite for Zip Recall Bug: Soul File Export/Awaken Roundtrip and Stale MCP Cache](./test-suite-for-zip-recall-bug-soul-file-exportawaken-roundtrip-and-stale-mcp-cac.md) — This regression test suite targets a production bug where `soul_recall` via the MCP server returned zero results for memories that existed in the `.soul` file on disk. The root cause was the MCP server loading souls at startup and never reloading when the file was updated externally. Tests cover semantic and episodic recall after zip roundtrip, bytes-based awakening, multi-memory preservation, content fidelity, and the stale-cache simulation.

### `soul-health-score` — 12 article(s)

- [D1 Memory Recall Evaluator](./d1-memory-recall-evaluator.md) — Evaluates a soul's ability to recall planted facts across long conversations, measuring recall precision, memory storage efficiency, and resistance to adversarial "burial" noise. Contributes 20% of the composite Soul Health Score.
- [D2 Emotional Intelligence Evaluator](./d2-emotional-intelligence-evaluator.md) — Evaluates four aspects of emotional intelligence: sentiment classification accuracy, significance gate calibration (emotional vs. neutral filtering), mood state responsiveness, and multi-phase emotional arc coherence. Contributes 20% of the Soul Health Score.
- [D3 Personality Expression Evaluator](./d3-personality-expression-evaluator.md) — Tests that a soul's OCEAN personality traits and communication style are faithfully encoded in its system prompt, influence goal relevance scoring, remain stable under sustained interactions, and produce meaningfully different outputs for contrasting profiles. Contributes 15% of the Soul Health Score.
- [D4 Bond and Relationship Evaluator](./d4-bond-and-relationship-evaluator.md) — Validates the mathematical properties of the soul's bond growth system: logarithmic curve shape, emotional valence acceleration, milestone accuracy at N=50/100/200, and tier progression ordering. Contributes 15% of the Soul Health Score.
- [D5 Self-Model Evaluator](./d5-self-model-evaluator.md) — Measures the soul's ability to dynamically classify user interaction domains, emerge topic confidence quickly, isolate domains without cross-contamination, and extract relationship metadata from conversation. Contributes 15% of the Soul Health Score.
- [D6 Identity Continuity Evaluator](./d6-identity-continuity-evaluator.md) — Tests that a soul survives export/import round-trips with full fidelity—preserving DID, OCEAN traits, bond state, memory count, and recall results—and that reincarnation correctly tracks lineage across incarnation chains. Contributes 10% of the Soul Health Score.
- [D7 Portability Evaluator](./d7-portability-evaluator.md) — Verifies that a soul produces deterministic system prompts regardless of instantiation order, that recall results survive export/reimport unchanged, and that multi-hop engine swap chains preserve memory count and bond state. Contributes 5% of the Soul Health Score.
- [LLM Judge for Soul Health Score Evaluation](./llm-judge-for-soul-health-score-evaluation.md) — Uses Claude Haiku as an independent LLM judge to evaluate sentiment accuracy and personality prompt quality beyond what word-list heuristics can measure, running all verdicts in parallel and comparing results against the heuristic baseline. Provides a cost-tracked, auditable layer of evaluation for D2 and D3.
- [SHS Dimension Evaluators Package](./shs-dimension-evaluators-package.md) — Package marker for the `research/eval/dimensions/` directory, which houses the seven individual Soul Health Score dimension evaluators. Each sub-module implements one evaluation axis of soul quality.
- [Soul Health Score Evaluation Framework Package](./soul-health-score-evaluation-framework-package.md) — Package marker and documentation anchor for the 7-dimension Soul Health Score (SHS) evaluation framework. Signals to Python's import system that `research/eval/` is a package while documenting the framework's creation context.
- [Soul Health Score Evaluation Suite Runner](./soul-health-score-evaluation-suite-runner.md) — Orchestrates the 7-dimension Soul Health Score evaluation, dynamically loading dimension runners, executing them sequentially, computing the weighted composite score, and returning a structured report. Serves as both a library function and a CLI tool.
- [Soul Health Score Report Renderer](./soul-health-score-report-renderer.md) — Renders SoulHealthReport objects into four output formats—markdown table, JSON, RAG comparison table, and an ANSI terminal dashboard—without external dependencies. Provides human-readable and machine-readable views of evaluation results.

### `soul-identity` — 2 article(s)

- [Bundled Soul Templates: Role Archetype Registry](./bundled-soul-templates-role-archetype-registry.md) — The `soul_protocol.templates` package ships four pre-built YAML role archetypes — Arrow (sales), Flash (content), Cyborg (recruiting), and Analyst (research) — alongside a small discovery API. It gives consumers a zero-configuration starting point while keeping custom templates fully supported via path-based loading.
- [Test Suite for the Human-Soul Bond Model](./test-suite-for-the-human-soul-bond-model.md) — This test suite validates the `Bond` model that tracks the relationship strength between a soul and its bonded human, covering construction defaults, logarithmic growth on `strengthen()`, floor/ceiling clamping on `weaken()`, and Pydantic validation of out-of-range `bond_strength` values.

### `soul-lifecycle` — 9 article(s)

- [End-to-End Integration Test Suite — Soul Lifecycle, Memory Persistence, and Error Handling](./end-to-end-integration-test-suite-soul-lifecycle-memory-persistence-and-error-ha.md) — Full end-to-end integration tests for the Soul class covering the complete lifecycle from birth through export and awakening. Tests validate memory persistence across all five tiers, self-model emergence, core memory editing, directory format save/load, and robust error handling for corrupt or missing soul files.
- [End-to-End New Soul Primitives Test — Bond, Skills, Retirement, and Reincarnation](./end-to-end-new-soul-primitives-test-bond-skills-retirement-and-reincarnation.md) — End-to-end test suite for newer soul-protocol primitives introduced post-v1: bond mechanics, skill registry, lifecycle state transitions (active → retired → reincarnated), and `.soul` archive integrity. Tests validate the logarithmic bond growth curve and the presence of `dna.md` in exported archives.
- [Evolution Manager: DNA Mutation Lifecycle](./evolution-manager-dna-mutation-lifecycle.md) — `EvolutionManager` orchestrates the full lifecycle of soul personality mutations — proposal, approval/rejection, and application to DNA — across three modes: disabled, supervised, and autonomous. It is the core mechanism by which souls evolve over time based on interaction performance.
- [Export Subpackage: Soul Archive I/O Interface](./export-subpackage-soul-archive-io-interface.md) — The `export/__init__.py` init exposes `pack_soul` and `unpack_soul` as the sole public interface for `.soul` archive I/O, hiding the internal split between `pack.py` and `unpack.py`. All callers that need to serialize or deserialize a soul use this single import point.
- [Skill and SkillRegistry: XP-Based Ability Progression for Souls](./skill-and-skillregistry-xp-based-ability-progression-for-souls.md) — Implements a leveled skill system where souls gain experience points (XP) through interactions, level up abilities (1-10) with exponential scaling thresholds, and lose XP through time-based decay when skills go unused. SkillRegistry manages the collection and bridges the learning event pipeline.
- [Soul Archive Packer: Building .soul ZIP Archives](./soul-archive-packer-building-soul-zip-archives.md) — `pack_soul` serializes a `SoulConfig` and all its memory tiers into a portable `.soul` ZIP archive, with optional AES-256-GCM encryption for every file except the manifest. It is the primary serialization primitive — every export, backup, and migration operation ultimately calls this function.
- [Soul Archive Unpacker: Loading .soul ZIP Archives](./soul-archive-unpacker-loading-soul-zip-archives.md) — `unpack_soul` deserializes a `.soul` ZIP archive back into a `(SoulConfig, memory_data)` tuple, handling both encrypted and unencrypted archives with clear error separation between missing-password and wrong-password scenarios. It is the read-side counterpart to `pack_soul` and is called on every `awaken()` operation.
- [Soul: The Central Digital Identity and Lifecycle Manager](./soul-the-central-digital-identity-and-lifecycle-manager.md) — The Soul class is the top-level API object in Soul Protocol — it unifies identity, memory, personality, skills, evolution, dreaming, bonding, and eternal storage into a single coherent interface. It is the primary object that consuming applications instantiate and interact with.
- [Test Suite for Auto-Scheduled Consolidation (F5)](./test-suite-for-auto-scheduled-consolidation-f5.md) — This test suite validates that `Soul.observe()` automatically triggers memory consolidation (archival and reflection) at a configurable interval, that the interval is configurable and disableable, and that the interaction count persists through export/awaken cycles so consolidation timing survives restarts.

### `soul-protocol` — 97 article(s)

- [DSPy Optimization Pipeline: MIPROv2-Based Significance Gate and Query Expansion Tuning](./dspy-optimization-pipeline-miprov2-based-significance-gate-and-query-expansion-t.md) — This pipeline runs a four-phase experiment: baseline heuristic ablation, DSPy MIPROv2 optimization of the significance gate and query expander, post-optimization ablation with the trained modules active, and saved reports comparing before/after metrics. It exists to replace hand-tuned heuristics with learned prompts that generalize across diverse interaction types.
- [DSPy Training Data Generation Subpackage](./dspy-training-data-generation-subpackage.md) — The `dspy_training/` subpackage provides training data generation for optimizing Soul Protocol's DSPy modules — the LLM-powered components used for memory extraction, significance scoring, and entity recognition. Created under the `feat/dspy-integration` branch, it is a nascent subpackage with the actual generation logic in sibling modules.
- [End-to-End Test Suite for Eternal Storage Lifecycle](./end-to-end-test-suite-for-eternal-storage-lifecycle.md) — End-to-end tests covering the full eternal storage lifecycle: birth a soul, archive to multiple tiers, verify archives exist, recover from each tier individually, and confirm recovered data can be awakened into a valid Soul. Also tests multi-tier fallback recovery when primary sources fail.
- [Engine Test Package Initializer](./engine-test-package-initializer.md) — Marks `tests/test_engine/` as a Python package so pytest can discover tests within it. Created as part of the journal-engine workstream in the Org Architecture RFC.
- [Enhanced Quality Validation Runner — N-Variation Factorial Benchmark with Checkpointing](./enhanced-quality-validation-runner-n-variation-factorial-benchmark-with-checkpoi.md) — Orchestrates a full factorial quality benchmark: N scenario variations × 3 test types × M judge models × 4 ablation conditions, accumulating statistically meaningful results with 95% confidence intervals. Checkpoint/resume logic survives process crashes, and a formatted results table with win rates is printed on completion.
- [Eternal Storage Test Package Initializer](./eternal-storage-test-package-initializer.md) — Marks `tests/test_eternal/` as a Python package, enabling pytest discovery of the eternal storage subsystem's test modules. Created when the eternal storage subsystem was introduced on 2026-03-06.
- [Experiment Configuration and Enum Definitions for Soul Protocol Research](./experiment-configuration-and-enum-definitions-for-soul-protocol-research.md) — Defines the `ExperimentConfig` dataclass and supporting enums (`MemoryCondition`, `UseCase`) that control every parameter of the Soul Protocol validation study. All experiment parameters live in one place to guarantee reproducibility — any change to scale, thresholds, or conditions is a single-file edit.
- [Five Experimental Memory Conditions for Soul Protocol Ablation Study](./five-experimental-memory-conditions-for-soul-protocol-ablation-study.md) — Implements the five independent-variable conditions for the Soul Protocol validation study, ranging from a completely stateless baseline to the full psychology-informed stack. Each condition is a class that wraps a Soul (or no Soul) and exposes a uniform `observe / recall / get_state / reset` interface, allowing the experiment runner to swap conditions without changing any other code.
- [Haiku Cognitive Engine — Claude Haiku LLM Backend with Rate Limiting](./haiku-cognitive-engine-claude-haiku-llm-backend-with-rate-limiting.md) — A thin async wrapper around the Anthropic SDK that implements the single-method `CognitiveEngine` protocol (`async think(prompt) -> str`) using Claude Haiku. Includes semaphore-based concurrency control, retry logic for rate limits and timeouts, and a `UsageTracker` dataclass for cost and throughput monitoring.
- [Human Evaluation Study Web Application](./human-evaluation-study-web-application.md) — A FastAPI web application that runs a blinded A/B human evaluation study, presenting participants with two AI agents — one soul-enabled, one a plain baseline — and collecting Likert survey responses after each. The app manages sessions, randomizes agent order to eliminate position bias, and exports results to JSON and CSV for statistical analysis.
- [Interactive CLI Agent Demonstrating the Full Soul Lifecycle](./interactive-cli-agent-demonstrating-the-full-soul-lifecycle.md) — A runnable conversational agent that exercises the complete Soul Protocol lifecycle — birth, observe, recall, reflect, and save — with pluggable LLM backends (Claude, OpenAI, Ollama, and a no-LLM heuristic mode). It uses a dual-model architecture where a cheaper model handles soul cognition and a stronger model handles user-facing chat, demonstrating the cost-efficiency pattern Soul Protocol enables.
- [LiteLLM Cognitive Engine — Multi-Model Proxy Backend](./litellm-cognitive-engine-multi-model-proxy-backend.md) — An async `CognitiveEngine` implementation that routes LLM calls through a LiteLLM OpenAI-compatible proxy, enabling the research framework to use GPT-4o, Gemini, DeepSeek, Llama, and Qwen as judge or agent engines without changing call sites. It mirrors the `HaikuCognitiveEngine` interface exactly, making the two engines interchangeable.
- [LLM-as-Judge — Six-Dimension Response Quality Scorer with Position-Bias Mitigation](./llm-as-judge-six-dimension-response-quality-scorer-with-position-bias-mitigation.md) — Implements the LLM-as-judge evaluation methodology with six quality dimensions scored on a 1-10 scale, supporting both single-response scoring and pairwise A/B comparison. The pairwise mode randomly assigns responses to positions A and B on each call to eliminate position bias from judge models that favor whichever response appears first.
- [Long-Horizon Ablation Results Analyzer](./long-horizon-ablation-results-analyzer.md) — The `LongHorizonAnalyzer` takes `LongHorizonResults` produced by the runner and computes summary statistics, pairwise Cohen's d effect sizes, and per-scenario breakdowns, then renders a structured markdown report. It is the statistical layer that transforms raw recall hits and memory counts into publishable evidence.
- [Long-Horizon Ablation Runner — Four-Condition Infrastructure Benchmark](./long-horizon-ablation-runner-four-condition-infrastructure-benchmark.md) — The `LongHorizonRunner` executes 100+ turn synthetic conversation scenarios through four ablation conditions (Full Soul, RAG Only, Personality Only, Bare Baseline) and collects infrastructure metrics — recall precision, memory count, memory efficiency, bond trajectory — without requiring an LLM for response generation. It optionally integrates DSPy query expansion and significance gating.
- [Long-Horizon Ablation Study Package](./long-horizon-ablation-study-package.md) — The `research/long_horizon` package bundles the scenario generators, ablation runner, and statistical analyzer for Soul Protocol's long-horizon study, which proves that the psychology stack (significance gating, activation decay, somatic markers) produces measurable advantages at 100+ turn conversation scales. The `__init__.py` re-exports the three key classes and five scenario generator functions as the public API.
- [Long-Horizon Conversation Scenario Generators — Three Ablation Test Cases](./long-horizon-conversation-scenario-generators-three-ablation-test-cases.md) — Defines the `LongHorizonScenario` and `TestPoint` data structures and generates three distinct 100-160 turn synthetic conversation scenarios designed to stress different aspects of Soul Protocol's psychology stack: long-range recall, emotional continuity under extreme valence swings, and recall precision under adversarial fact burial.
- [Marathon Scale Scenario — 1000-Turn Conversation for Scale Ablation](./marathon-scale-scenario-1000-turn-conversation-for-scale-ablation.md) — Generates a fully deterministic 1000-turn synthetic conversation (`marathon_1000`) with 35 strategically planted facts and 40 recall test points designed to reveal the point at which Soul Protocol's selective memory corpus outperforms a naive RAG store. Facts are seeded across the full 1000-turn span — including deeply buried and callback facts — to stress-test long-range recall.
- [Mem0 Comparison Benchmark — Soul Protocol vs. Vector Memory vs. Stateless Baseline](./mem0-comparison-benchmark-soul-protocol-vs-vector-memory-vs-stateless-baseline.md) — Runs a three-way quality comparison between Soul Protocol, Mem0 (embedding-based vector memory), and a stateless baseline across four test types, producing a scorecard that quantifies what the soul stack adds beyond raw memory storage. The benchmark is designed to appear in the Soul Protocol research paper as evidence that personality, emotional tracking, and bond are not incidental features.
- [Memory Categories and Salience Scoring Example](./memory-categories-and-salience-scoring-example.md) — Demonstrates Soul Protocol's v0.2.3 memory extraction taxonomy by walking through the observe pipeline with five realistic interactions, then inspecting how the system assigns categories, salience scores, and memory types to extracted facts. The example also shows the deduplication pipeline and progressive content loading (L0/L1/L2) through targeted recall queries.
- [Multi-Condition Response Generator — Isolating Soul Protocol Components](./multi-condition-response-generator-isolating-soul-protocol-components.md) — Defines four experimental conditions (`FULL_SOUL`, `RAG_ONLY`, `PROMPT_PERSONALITY`, `BARE_BASELINE`) and the `MultiConditionResponder` class, which generates responses under each condition against the same underlying soul state to isolate the contribution of personality, memory, and emotional scaffolding to response quality.
- [Multi-Judge Quality Runner — Inter-Rater Reliability Across Five LLM Models](./multi-judge-quality-runner-inter-rater-reliability-across-five-llm-models.md) — Runs Soul Protocol's four quality test scenarios with five different judge models (Haiku, Gemini 3 Flash, Gemini 2.5 Flash Lite, DeepSeek V3, Llama 3.3 70B) to measure inter-rater reliability, ensuring that quality advantages observed for soul-enabled agents are not artifacts of a single judge model's preferences or biases.
- [Multi-Soul Management and Parallel Identity Example](./multi-soul-management-and-parallel-identity-example.md) — Shows how to create and manage multiple independent Soul instances in a single process, each with distinct archetypes, values, and memory spaces. The example covers the full lifecycle from birth to export and reawakening, demonstrating that memory isolation is maintained across souls and that soul files persist identity correctly across process restarts.
- [PocketPaw E2E Integration Script: Full Soul Protocol Lifecycle Validation](./pocketpaw-e2e-integration-script-full-soul-protocol-lifecycle-validation.md) — An end-to-end integration script that validates the complete soul-protocol API lifecycle — birth, observe, remember, recall, reflect, export, awaken, core memory editing, state management, and MCP tool smoke tests — from the perspective of PocketPaw's integration layer, using realistic interaction datasets and Rich-formatted reporting.
- [Quality Scenario Generator: Randomized Test Variations for Statistical Validity](./quality-scenario-generator-randomized-test-variations-for-statistical-validity.md) — The scenario generator produces 10 randomized but reproducible variations of each of the four quality test types, enabling error bars and statistical significance checks rather than single-point measurements. A fixed seed (`SEED = 42`) ensures results are reproducible across runs while the random variation catches edge cases a single hardcoded scenario would miss.
- [Quality Validation Package Overview](./quality-validation-package-overview.md) — The `research/quality` package contains Soul Protocol's response quality validation infrastructure: test scenarios, experimental condition definitions, LLM-as-judge scoring, and multi-model benchmarking runners. It is the layer that measures subjective quality (how good does the response feel?) rather than infrastructure metrics (how many memories were stored?).
- [Quality Validation Runner: CLI Orchestrator for Soul Protocol Test Suite](./quality-validation-runner-cli-orchestrator-for-soul-protocol-test-suite.md) — The quality runner provides a command-line interface to execute one or more of the four fixed validation tests (response quality, personality consistency, hard recall, emotional continuity) and outputs both a live scorecard and saved JSON/Markdown reports. It uses concurrent `HaikuCognitiveEngine` instances for agent and judge roles to keep cost tracking separate.
- [Quick Ablation Runner: Heuristic-Only Long-Horizon Scenario Executor](./quick-ablation-runner-heuristic-only-long-horizon-scenario-executor.md) — A minimal, API-key-free script that runs all long-horizon scenarios through the `LongHorizonRunner` and prints a structured ablation table comparing recall precision, memory count, and bond strength across conditions. It is the fastest way to verify the heuristic pipeline without incurring LLM costs.
- [Research Metrics Schema — Six-Dimensional Agent Quality Measurement](./research-metrics-schema-six-dimensional-agent-quality-measurement.md) — Defines the full metric collection schema for Soul Protocol's research framework across six orthogonal dimensions: retrieval quality, emotional tracking, personality stability, memory efficiency, bond formation, and skill acquisition. All metric classes are dataclasses that aggregate per-interaction observations into summary statistics for factorial analysis.
- [Research Package Entry Point (`python -m research`)](./research-package-entry-point-python-m-research.md) — A minimal `__main__.py` that enables the research package to be invoked directly with `python -m research`, delegating immediately to `research.run.main()`. This is the standard Python idiom for making a package executable without requiring the user to know the internal module structure.
- [Research Package: Soul Protocol Validation Simulation Framework](./research-package-soul-protocol-validation-simulation-framework.md) — The `research/` package is a large-scale simulation framework designed to validate Soul Protocol's psychology-informed memory claims at publication grade, running 1,000 simulated agents across 5 memory conditions and 4 use cases for more than 20,000 experimental runs. It is structured for reproducibility and statistical rigor.
- [Research Scenario Bank: Multi-Turn Interaction Templates for Four Use Cases](./research-scenario-bank-multi-turn-interaction-templates-for-four-use-cases.md) — The scenario bank provides parameterized, multi-turn interaction sequences for four use cases — customer support, coding assistant, personal companion, and knowledge worker — each with embedded ground truth: planted facts, recall queries, emotional tone markers, and importance hints. Scenarios are personalized by `UserProfile` to increase coverage and ecological validity.
- [Research Simulation CLI: Entry Point for Full-Scale Ablation Experiments](./research-simulation-cli-entry-point-for-full-scale-ablation-experiments.md) — The `run.py` module is the primary CLI entry point for the Soul Protocol research simulation framework, translating command-line arguments into an `ExperimentConfig` and orchestrating the full simulation, analysis, and report pipeline. It supports configuring agent count, random seed, memory conditions, use cases, output directory, batch size, and a quick mode for rapid iteration.
- [Scale Ablation Runner: 1000-Turn Memory Efficiency Study](./scale-ablation-runner-1000-turn-memory-efficiency-study.md) — A dedicated runner for the 1000-turn marathon scenario that benchmarks Soul Protocol's selective storage against RAG (store-everything) and two null conditions at scale. At 1000 turns, the soul achieves 85% recall with 175 memories (4.9x more efficient than RAG's 1000), and the study breaks down recall by memory age to identify decay patterns.
- [Schema Generator: JSON Schema Export for All Soul Protocol Pydantic Models](./schema-generator-json-schema-export-for-all-soul-protocol-pydantic-models.md) — A utility script that introspects every Pydantic model in `soul_protocol.runtime.types` and writes individual JSON Schema files plus a combined bundle to the `schemas/` directory, enabling cross-language clients (TypeScript, Rust, Go) to validate soul data structures without depending on the Python package.
- [Simulate Script: Comprehensive Soul Protocol Scenario Test Suite](./simulate-script-comprehensive-soul-protocol-scenario-test-suite.md) — A standalone simulation script containing 30+ named scenarios that test every surface of the soul-protocol API — from basic lifecycle and memory recall to adversarial inputs, concurrent async safety, cross-platform migration, evolution system, knowledge graph, and graceful degradation. Each scenario is independently runnable and produces a detailed Rich-formatted report.
- [Simulated Agent and User Profile Generator for Research Experiments](./simulated-agent-and-user-profile-generator-for-research-experiments.md) — Generates 1,000 diverse simulated agent profiles with realistic OCEAN personality distributions and 1,000 matched user profiles for a specified use case domain, used as the participant pool for Soul Protocol's validation experiments. All sampling uses a seeded RNG to guarantee reproducible results across experiment runs.
- [Simulation Engine: Full Factorial Experiment Orchestrator for Soul Protocol Research](./simulation-engine-full-factorial-experiment-orchestrator-for-soul-protocol-resea.md) — The `SimulationEngine` orchestrates the core ablation study by running every combination of (agent, condition, use_case) as an async task, collecting `AgentRunMetrics` for each triple, and aggregating them into a `SimulationResults` object. Failed individual runs are logged and skipped rather than crashing the whole experiment, ensuring partial results are always captured.
- [Soul Agent: Claude Agent SDK + Soul Protocol with 15 MCP Tools](./soul-agent-claude-agent-sdk-soul-protocol-with-15-mcp-tools.md) — Implements a full-featured conversational agent that combines the Claude Agent SDK's tool execution and streaming loop with Soul Protocol's v0.2.3 feature set, exposing 15 MCP tools covering memory, personality, emotion, bond tracking, skills, GDPR deletion, evolution proposals, core memory editing, reincarnation, and encrypted export. The module-level `_soul` singleton pattern mirrors the MCP server architecture for consistency.
- [Soul Responder: Paired Response Generation for Quality Evaluation](./soul-responder-paired-response-generation-for-quality-evaluation.md) — The `SoulResponder` class generates agent responses in two modes — enriched by a soul's personality and memories, or from a bare baseline — enabling paired comparisons that quantify whether having a soul actually improves output quality. It uses a dedicated `HaikuCognitiveEngine` for response generation, intentionally separate from any cognitive engine the soul uses internally.
- [SoulProvider: PocketPaw Integration Bridge for Soul Protocol](./soulprovider-pocketpaw-integration-bridge-for-soul-protocol.md) — Defines `SoulProvider`, a reference integration class that replaces PocketPaw's `DefaultBootstrapProvider` with a soul-driven system prompt generator, interaction tracker, and auto-save mechanism. It bridges soul-protocol's memory and identity system into any agent loop that calls a `get_system_prompt()` / `on_interaction()` pattern, with zero dependency on PocketPaw internals.
- [Statistical Analysis Engine for Soul Protocol Research Results](./statistical-analysis-engine-for-soul-protocol-research-results.md) — Provides `ResultsAnalyzer`, which takes flat metric rows from experiment runs and produces statistical summary tables, pairwise condition comparisons with Cohen's d effect sizes and Mann-Whitney U p-values, ablation chains, per-use-case breakdowns, and a full markdown report. All analysis uses stdlib-only dependencies to maximize portability.
- [Test Package Init: Evolution Subsystem](./test-package-init-evolution-subsystem.md) — Empty package initialiser that makes `tests/test_evolution/` a proper Python package, enabling pytest to discover and import the evolution test modules correctly. No executable code is present; the file's value is purely structural.
- [Test Package Init: Importers Subsystem](./test-package-init-importers-subsystem.md) — Empty package initialiser that designates `tests/test_importers/` as a Python package, enabling pytest to collect the importer-specific test modules contained within. No executable code is present.
- [Test Package Init: Install Invariants](./test-package-init-install-invariants.md) — Package initialiser for `tests/test_install/`, the sub-package that verifies install-time invariants such as the CLI being functional after a bare `pip install soul-protocol`. Created to address issue #157.
- [Test Package Init: Long-Horizon Ablation Study](./test-package-init-long-horizon-ablation-study.md) — Package initialiser for `tests/test_long_horizon/`, the sub-package housing ablation study tests that evaluate soul memory and context performance over extended interaction sequences. Created 2026-03-11.
- [Test Package Init: test_spike](./test-package-init-testspike.md) — This is the empty package initializer for the `tests/test_spike/` directory. It has no executable content but is required by Python to treat the directory as an importable package, enabling pytest to discover and collect the spike test files within it.
- [Test Package Init: test_state](./test-package-init-teststate.md) — This is the empty package initializer for the `tests/test_state/` directory. It has no executable content but enables pytest to discover and collect tests within the state management test package.
- [Test Package Init: test_storage](./test-package-init-teststorage.md) — This is the package initializer for the `tests/test_storage/` directory, containing a minimal comment identifying the package. It enables pytest to discover storage backend tests and scopes them under a dedicated package namespace.
- [Test Package Initializer for MCP Server Integration Tests](./test-package-initializer-for-mcp-server-integration-tests.md) — The `tests/test_mcp/__init__.py` file marks the `test_mcp` directory as a Python package, enabling pytest to discover and organize MCP server integration tests as a cohesive module. It contains no executable code — its presence is structural.
- [Test Package Initializer for Memory Subsystem Tests](./test-package-initializer-for-memory-subsystem-tests.md) — The `tests/test_memory/__init__.py` file marks the `test_memory` directory as a Python package, enabling pytest to discover and organize the memory subsystem test suite as a cohesive module. It contains no executable code.
- [Test Suite: Core Pydantic Data Models](./test-suite-core-pydantic-data-models.md) — Validates the foundational Pydantic models in soul_protocol.types — Identity defaults, Personality trait bounds, SoulConfig JSON serialization round-trip, MemoryEntry defaults, Mood enum completeness, and EvolutionMode enum values.
- [Test Suite for Base Install Invariants (Issue #157)](./test-suite-for-base-install-invariants-issue-157.md) — Regression tests that verify a bare `pip install soul-protocol` produces a working CLI, by checking that click, rich, pyyaml, and cryptography are declared as base dependencies, the deprecated `[engine]` extra still exists for backwards compatibility, and the CLI module imports cleanly without optional extras.
- [Test Suite for Bi-Temporal Memory Fields (ingested_at and superseded)](./test-suite-for-bi-temporal-memory-fields-ingestedat-and-superseded.md) — Validates bi-temporal tracking on MemoryEntry — the `ingested_at` timestamp (when a fact entered this soul's memory vs. when it was created) and the `superseded` flag (marking outdated facts replaced by newer information). Tests cover defaults, explicit values, manager behavior, serialization round-trips, and backward compatibility with soul files that predate these fields.
- [Test Suite for Eternal Storage CLI Commands](./test-suite-for-eternal-storage-cli-commands.md) — Tests the soul-protocol CLI commands for eternal storage operations: archive (with optional tier selection), eternal-status (tier health check), and recover (with failure path handling). Uses Click's CliRunner for isolated, in-process command invocation.
- [Test Suite for Eternal Storage Protocol Compliance](./test-suite-for-eternal-storage-protocol-compliance.md) — Verifies that all eternal storage providers satisfy the EternalStorageProvider protocol at runtime, and validates the Pydantic models ArchiveResult and RecoverySource for field defaults, full construction, and serialization round-trips.
- [Test Suite for Eternal Storage Providers](./test-suite-for-eternal-storage-providers.md) — Per-provider tests for the four eternal storage backends: MockIPFSProvider, MockArweaveProvider, MockBlockchainProvider, and LocalStorageProvider. Each provider is tested for archive, retrieve, verify, content addressing, unique identifiers, and error behavior on missing references.
- [Test Suite for EternalStorageManager](./test-suite-for-eternalstoragemanager.md) — Unit tests for EternalStorageManager — the orchestration layer that routes archive, recover, and verify operations across multiple storage providers. Covers provider registration/unregistration, selective tier archiving, fallback recovery chains, unavailable-provider skipping, and verification status.
- [Test Suite for Fact and Entity Extraction from Interaction Text](./test-suite-for-fact-and-entity-extraction-from-interaction-text.md) — Validates the pattern-based extraction pipeline that parses user messages for facts (name, preferences, identity, tools, projects) and entities (tech terms, proper nouns with inferred relations). Tests cover each extraction pattern, punctuation stripping, stopword exclusion, and deduplication of repeated facts.
- [Test Suite for GDPR-Compliant Memory Deletion](./test-suite-for-gdpr-compliant-memory-deletion.md) — Comprehensive tests for Soul Protocol's right-to-erasure implementation, covering query-based, entity-based, and time-based deletion across all memory tiers, with full audit trail verification. Ensures deleted memories become invisible to recall and that audit records never expose the deleted content itself.
- [Test Suite for InMemoryStorage and FileStorage Backends](./test-suite-for-inmemorystorage-and-filestorage-backends.md) — This test suite validates both storage backends provided by Soul Protocol's runtime: `InMemoryStorage` (dict-backed, in-process) and `FileStorage` (disk-backed, directory-per-soul). It covers the full CRUD surface — save, load, delete, and list — with particular attention to idempotent delete behavior and the expected on-disk directory structure for `FileStorage`.
- [Test Suite for Journal 0.3.2 Primitives](./test-suite-for-journal-032-primitives.md) — Smoke, end-to-end, and real-world simulation tests for the primitives introduced in soul-protocol v0.3.2, specifically the append-returns-committed-entry contract and action prefix querying. Tests mirror realistic consumer patterns from the PocketPaw widget store.
- [Test Suite for Knowledge Graph Traversal](./test-suite-for-knowledge-graph-traversal.md) — Tests for the `KnowledgeGraph` traversal API, covering BFS traversal, shortest-path finding, neighbourhood extraction, subgraph extraction, and progressive context rendering at three detail levels. Validates edge expiry, cycle handling, and node deduplication across a range of graph topologies.
- [Test Suite for LCMContext (Long-Context Memory)](./test-suite-for-lcmcontext-long-context-memory.md) — Integration tests for `LCMContext`, the context engine that ingests conversation messages, assembles token-budgeted context windows, and auto-compacts history using a `CognitiveEngine`. Covers protocol compliance, initialization guards, token estimation, compaction triggers, grep search, and lifecycle close/reopen semantics.
- [Test Suite for Learning Events Pipeline](./test-suite-for-learning-events-pipeline.md) — Tests for the full learning events pipeline — from `RubricResult` score thresholding to `LearningEvent` creation, skill XP grant, and the `Soul.learn()` convenience method. Covers model validation, score boundary conditions, XP scaling, and spec module exportability.
- [Test Suite for Long-Horizon Benchmark Analysis (Cohen's d, Effect Labels, Report Generation)](./test-suite-for-long-horizon-benchmark-analysis-cohens-d-effect-labels-report-gen.md) — Validates the statistical analysis layer of the long-horizon benchmark framework, covering Cohen's d calculation, effect-size labeling, and the LongHorizonAnalyzer that produces summary tables and Markdown reports from experimental results. Uses a deterministic mock-results factory to isolate the analysis logic from live LLM calls.
- [Test Suite for Long-Horizon Benchmark Runner](./test-suite-for-long-horizon-benchmark-runner.md) — Validates the multi-condition benchmark runner that processes conversation scenarios through different soul configurations (Full Soul, RAG Only, Personality Only, Bare) and records recall, memory growth, and bond-strength metrics. Async tests use a short 15-turn scenario fixture to keep CI fast while still exercising the full execution path.
- [Test Suite for Long-Horizon Benchmark Scenarios](./test-suite-for-long-horizon-benchmark-scenarios.md) — Validates the three benchmark scenario generators (Life Updates, Emotional Rollercoaster, Adversarial Burial) that produce 100+ turn conversation scripts with planted facts, test points, and recall challenges. Tests verify determinism, scenario structure, seeded variation, and the TestPoint dataclass.
- [Test Suite for MCP Psychology Tools (Skills, Evaluate, Learn, Evolve)](./test-suite-for-mcp-psychology-tools-skills-evaluate-learn-evolve.md) — Validates the higher-order MCP tools that expose soul psychology — skill tracking, rubric-based evaluation, learning events, and the propose/approve/reject evolution workflow. Tests use FastMCP's in-process client to exercise the full MCP tool call path without a network round-trip.
- [Test Suite for MCP Sampling Engine (LLM Think with Heuristic Fallback)](./test-suite-for-mcp-sampling-engine-llm-think-with-heuristic-fallback.md) — Validates the MCPSamplingEngine, which wraps the MCP context's sampling capability to give souls LLM-powered reflection, with graceful fallback to the HeuristicEngine when the MCP context is unavailable or raises. Tests cover the success path, three failure scenarios, the no-context path, and the server's engine wiring lifecycle.
- [Test Suite for MCP Server Core Tools (Birth, Observe, Memory, State, Lifecycle)](./test-suite-for-mcp-server-core-tools-birth-observe-memory-state-lifecycle.md) — Integration tests for the core soul_protocol MCP server tools, covering soul lifecycle (birth, save, export), memory operations (remember, recall), emotional state (feel), observation, reflection, and multi-soul management (list, switch). Uses a registry reset helper and in-process FastMCP client to achieve full isolation between tests.
- [Test Suite for Memory Activation Functions (ACT-R Base-Level, Spreading, Emotional Boost)](./test-suite-for-memory-activation-functions-act-r-base-level-spreading-emotional-.md) — Validates the three activation components that determine how retrievable a memory is at query time: the ACT-R base-level activation formula (recency and frequency), spreading activation (query-content token overlap), and emotional boost (somatic marker arousal and valence). Together these tests pin the mathematical contracts that underlie memory retrieval scoring.
- [Test Suite for Memory Attention and Interaction Significance Scoring](./test-suite-for-memory-attention-and-interaction-significance-scoring.md) — Validates the attention system that decides whether an interaction is significant enough to trigger memory storage. Tests cover novelty scoring, emotional intensity detection, goal-relevance matching against core values, and the composite SignificanceScore type.
- [Test Suite for Memory Consolidation, Reflection, and Fact Supersession](./test-suite-for-memory-consolidation-reflection-and-fact-supersession.md) — Validates the consolidation pipeline that transforms raw episodic memories into structured semantic knowledge — converting summaries into semantic memories, grouping episodes under general-event themes, storing self-insight and emotional patterns, and superseding outdated facts. Also tests the reflect() entry point and the apply=False dry-run mode.
- [Test Suite for Memory Contradiction Detection (Heuristic and LLM Modes)](./test-suite-for-memory-contradiction-detection-heuristic-and-llm-modes.md) — Validates the contradiction detector that identifies when a new memory conflicts with existing ones — using both fast heuristic rules (negation patterns, entity-attribute conflicts) and LLM-powered analysis. Tests cover true-positive detection, false-positive prevention, superseded-memory exclusion, similarity thresholds, and graceful fallback when the LLM errors.
- [Test Suite for Memory Deduplication (Jaccard Similarity, Short-Token False Positives, and Containment Merge)](./test-suite-for-memory-deduplication-jaccard-similarity-short-token-false-positiv.md) — Validates the deduplication system that prevents near-identical facts from being stored multiple times, covering the tokenizer, Jaccard similarity computation, short-token false-positive prevention (go vs. js, ai vs. ml), fact reconciliation decisions (CREATE/SKIP/MERGE), and the containment-merge logic for enriched facts.
- [Test Suite for MemoryEntry, MemoryStore Protocol, and DictMemoryStore](./test-suite-for-memoryentry-memorystore-protocol-and-dictmemorystore.md) — This test suite validates the core in-memory building blocks of Soul Protocol's spec-level memory system: the `MemoryEntry` data model, the `MemoryStore` protocol interface, and the `DictMemoryStore` reference implementation. Tests cover entry creation, metadata handling, layer isolation, BM25-style search, delete semantics, and ordering guarantees.
- [Test Suite for Soul Error Handling](./test-suite-for-soul-error-handling.md) — Verifies that Soul's three main lifecycle operations — awaken(), export(), and retire() — raise specific, descriptive exception types rather than raw Python exceptions. Also tests exception class structure to ensure callers can extract actionable information like file paths and failure reasons.
- [Test Suite for Soul Eternal Storage Integration](./test-suite-for-soul-eternal-storage-integration.md) — Tests the integration between the Soul class and EternalStorageManager, covering Soul.archive(), the with_mocks() factory, the eternal= parameter on Soul.birth() and Soul.awaken(), and the export(archive=True) convenience flag.
- [Test Suite for Soul Evolution Manager](./test-suite-for-soul-evolution-manager.md) — Tests for `EvolutionManager`, which controls how a soul's DNA traits mutate over time across three modes: supervised, autonomous, and disabled. Covers the full mutation lifecycle — propose, approve, reject, apply — and guards against illegal mutations on immutable traits.
- [Test Suite for Soul Export and Pack/Unpack Operations](./test-suite-for-soul-export-and-packunpack-operations.md) — Tests for the `.soul` zip archive format, covering the full pack/unpack roundtrip, required file inventory inside the archive, and memory tier data inclusion. Ensures that `SoulConfig` and memory content survive serialisation to bytes and back without loss.
- [Test Suite for Soul File Encryption](./test-suite-for-soul-file-encryption.md) — Validates the full encryption stack for .soul files, from low-level blob crypto primitives through pack/unpack pipeline integration to the high-level Soul.export() and Soul.awaken() API. Covers round-trips, wrong-password errors, backward compatibility with unencrypted files, and manifest readability without a password.
- [Test Suite for Soul Format Auto-Detection](./test-suite-for-soul-format-auto-detection.md) — Tests for `detect_format()`, the function that inspects a filesystem path and returns the soul character format it represents — SoulSpec, TavernAI, Soul Protocol native, or unknown. Covers directory structures, JSON shapes, file extensions, PNG files, and error cases.
- [Test Suite for Soul Protocol Evaluation System](./test-suite-for-soul-protocol-evaluation-system.md) — Comprehensive tests for the heuristic evaluation engine, rubric models, and Evaluator class used to score AI companion responses across multiple quality dimensions. Covers model validation, scoring algorithms, domain auto-selection, history capping, and streak detection.
- [Test Suite for SoulProvider PocketPaw Integration](./test-suite-for-soulprovider-pocketpaw-integration.md) — Covers the `SoulProvider` reference integration class with 14 async pytest tests verifying system prompt generation, memory recall injection, interaction tracking, auto-save behavior, factory constructors, and low-energy behavioral adaptation. Tests are isolated using fresh Soul instances per fixture to prevent cross-test memory contamination.
- [Test Suite for SoulSpec Import and Export](./test-suite-for-soulspec-import-and-export.md) — Tests for the SoulSpec importer, which converts a human-authored directory of markdown files and soul.json into a Soul Protocol `SoulConfig`, and the corresponding exporter that writes those files back out. Covers OCEAN trait mapping, memory tier assignments, round-trip fidelity, and error handling.
- [Test Suite for StateManager: Mood Inertia, Label Mapping, Energy, and Auto-Regen](./test-suite-for-statemanager-mood-inertia-label-mapping-energy-and-auto-regen.md) — This test suite covers the `StateManager` component responsible for tracking a soul's emotional and physical state across interactions. It validates EMA-based mood inertia, label-to-mood mapping, the TIRED energy override, and the fully configurable `Biorhythms` parameter set including time-based auto-regeneration — with a particular focus on preventing mood instability and ensuring always-on agent deployments work correctly.
- [Test Suite for TavernAI Character Card Import and Export](./test-suite-for-tavernai-character-card-import-and-export.md) — Tests for the TavernAI importer, which converts Character Card V2 JSON (and PNG-embedded cards) into Soul Protocol `SoulConfig`, and the exporter that writes cards back. Covers field mapping to memory tiers, validation errors, and PNG tEXt chunk embedding.
- [Test Suite for TF-IDF Embedder](./test-suite-for-tf-idf-embedder.md) — Validates the TFIDFEmbedder class, which converts text into fixed-length float vectors using term-frequency/inverse-document-frequency weighting. Tests cover the fit/embed lifecycle, semantic similarity behavior, edge cases for unfitted state, and batch consistency.
- [Test Suite for the Journal Engine](./test-suite-for-the-journal-engine.md) — Comprehensive test suite for the SQLite-backed journal engine — the event-sourcing backbone of soul-protocol. Covers append/query mechanics, every filter dimension, timezone enforcement, scope wildcards, monotonic timestamps, seq assignment, concurrent WAL writes, and schema migration from zero.
- [Test Suite for v0.3.4 Memory Model: MemoryCategory, Salience, and Temporal Edge Metadata](./test-suite-for-v034-memory-model-memorycategory-salience-and-temporal-edge-metad.md) — This test suite covers spec-level additions introduced in Soul Protocol v0.3.4: the `MemoryCategory` enum for classifying memory entries, `salience` as an additive activation boost on `MemoryEntry`, and `metadata` on `TemporalEdge` along with its surfacing through `KnowledgeGraph` query methods. The salience tests were rewritten from a multiplicative to an additive boost model to prevent low-importance memories from being penalized disproportionately.
- [Test Suite for Vector Similarity Functions](./test-suite-for-vector-similarity-functions.md) — Covers the three core similarity primitives — cosine similarity, Euclidean distance, and dot product — used by soul-protocol's embedding layer. Tests enforce correct mathematical behavior, zero-vector safety, empty-vector edge cases, and dimension-mismatch guards.
- [Test Suite for VectorSearchStrategy](./test-suite-for-vectorsearchstrategy.md) — Tests the VectorSearchStrategy class, which wraps an embedder to perform similarity-based memory search with configurable threshold filtering and pre-built index support. Covers both the deterministic HashEmbedder path and the semantic TFIDFEmbedder path.
- [Test Suite: Four Core Quality Validation Scenarios for Soul Protocol](./test-suite-four-core-quality-validation-scenarios-for-soul-protocol.md) — This test suite implements the four hardest quality probes for Soul Protocol — response quality uplift, personality consistency across OCEAN profiles, hard recall after 30 filler interactions, and emotional continuity via somatic markers — using paired LLM comparisons with a `ResponseJudge` to score soul-enriched versus baseline responses.
- [Test Suite: MemoryManager Facade](./test-suite-memorymanager-facade.md) — Core test suite for the MemoryManager facade, covering all five memory stores (core, episodic, semantic, procedural, knowledge graph), cross-store recall, significance-gated observation, and the Bug #15 edit_core behavior fix.
- [Test Suite: Smoke Tests for the Research Simulation Framework](./test-suite-smoke-tests-for-the-research-simulation-framework.md) — This test suite validates that all research framework components — agent generation, user generation, scenario generation, experiment configuration, metrics, statistical utilities, and memory conditions — work correctly at minimal scale without requiring any LLM API calls. It is fast enough for CI and guards against regressions in the framework before running expensive full ablations.
- [Tier 2 Validation Runner: 100-Agent Real LLM Benchmark](./tier-2-validation-runner-100-agent-real-llm-benchmark.md) — The Tier 2 runner validates Soul Protocol with real Anthropic API calls across 100 simulated agents, comparing heuristic-only cognitive processing against LLM-backed processing using `HaikuCognitiveEngine`. It collects a comprehensive set of metrics — memory, personality, recall, bond, emotional, and skill dimensions — and saves the results as JSON.

### `soul-protocol-core` — 9 article(s)

- [Archival Memory Store for Conversation History](./archival-memory-store-for-conversation-history.md) — Provides long-term, compressed storage for complete conversation sessions via `ArchivalMemoryStore` and `ConversationArchive`. Supports keyword search across summaries and key moments, and date-range queries, without consuming active context.
- [Core Memory Manager — Always-In-Context Identity Block](./core-memory-manager-always-in-context-identity-block.md) — Manages the ~2KB core memory block that stays loaded in context at all times. It holds two editable sections — `persona` (the soul's own identity) and `human` (profile of the bonded human) — with distinct `set`, `edit`, and `append` operations.
- [DID Generation for Digital Souls](./did-generation-for-digital-souls.md) — Generates decentralized identifiers (DIDs) for soul instances using the `did:soul:` method. Each DID is deterministically random — human-readable yet collision-resistant — by combining the soul's name with a UUID4-seeded SHA-256 hash.
- [Episodic Memory Store — Autobiographical Interaction History](./episodic-memory-store-autobiographical-interaction-history.md) — Provides `EpisodicStore`, the in-memory repository for timestamped interaction records. Supports both basic and psychology-enriched storage paths, significance-based eviction, GDPR-compliant deletion, and filtering of archived entries from search results.
- [Knowledge Graph with Temporal Edges](./knowledge-graph-with-temporal-edges.md) — Implements `KnowledgeGraph` and `TemporalEdge` — a lightweight entity-relationship store with time-bounded validity, multi-hop traversal, shortest-path search, and progressive context loading for recall augmentation.
- [Memory Subsystem Package Exports](./memory-subsystem-package-exports.md) — The `memory` package `__init__.py` re-exports every public symbol from the memory subsystem's modules into a single flat namespace. It acts as a versioned registry — each significant export addition is tagged in the file header — so consumers can import everything from `soul_protocol.runtime.memory` without knowing which submodule it lives in.
- [MemoryManager — Unified Memory Orchestration Facade](./memorymanager-unified-memory-orchestration-facade.md) — The `MemoryManager` is the top-level coordinator for all memory subsystems. It orchestrates the full observe pipeline — sentiment, attention gate, episodic storage, fact extraction, entity extraction, knowledge graph updates, contradiction detection, self-model updates — and exposes a unified recall interface.
- [Rule-Based Memory Compressor](./rule-based-memory-compressor.md) — Provides `MemoryCompressor`, a SimpleMem-inspired compression pipeline that reduces memory footprint through deduplication, importance-based pruning, and grouped summarization — entirely without LLM calls.
- [Soul Format Auto-Detection and Importer Registry](./soul-format-auto-detection-and-importer-registry.md) — The importers package entry point exposes `SoulSpecImporter`, `TavernAIImporter`, and the `detect_format()` utility. `detect_format()` inspects a file or directory and returns a format tag so callers can route to the correct importer without knowing the format in advance.

### `spec` — 11 article(s)

- [A2A Agent Card Models: Soul Protocol Interop with Google's Agent-to-Agent Protocol](./a2a-agent-card-models-soul-protocol-interop-with-googles-agent-to-agent-protocol.md) — `spec/a2a.py` provides Pydantic models that map Google's A2A Agent Card specification to Soul Protocol primitives. `A2AAgentCard` is the agent's public identity, `A2ASkill` advertises capabilities, and `SoulExtension` embeds a soul's DID and OCEAN personality into the card's extension block.
- [Context Subpackage Interface: Lossless Context Management Spec Layer](./context-subpackage-interface-lossless-context-management-spec-layer.md) — The `spec/context/__init__.py` re-exports all Lossless Context Management (LCM) primitives — the `ContextEngine` protocol and seven Pydantic models — as the public interface for context window management. This subpackage is spec-only: no opinionated implementations, no runtime imports.
- [ContextEngine Protocol: Interface for Lossless Context Window Management](./contextengine-protocol-interface-for-lossless-context-window-management.md) — `ContextEngine` is a `@runtime_checkable` Python `Protocol` that defines the six operations any context management implementation must provide: `ingest`, `assemble`, `grep`, `expand`, `describe`, and `compact`. It decouples the soul runtime from any specific context engine implementation.
- [Decision Trace Models: Structured Agent Proposal and Human Correction Events](./decision-trace-models-structured-agent-proposal-and-human-correction-events.md) — `spec/decisions.py` defines the payload types and helpers for the Org Journal's decision trace workstream — structuring every agent proposal, human correction, and pattern graduation as auditable, linkable `EventEntry` pairs. The module enables agents to learn from correction patterns without requiring ad-hoc feedback loops.
- [LCM Data Models: Context Messages, DAG Nodes, and Compaction Levels](./lcm-data-models-context-messages-dag-nodes-and-compaction-levels.md) — The LCM data models define the atomic units of Lossless Context Management: `ContextMessage` (immutable ingested messages), `ContextNode` (compacted DAG nodes), `CompactionLevel` (verbatim to truncated), and result types for assembly, search, expansion, and metadata queries.
- [SoulContainer: The Schema-Free Portable Soul Primitive](./soulcontainer-the-schema-free-portable-soul-primitive.md) — `SoulContainer` is the core-layer primitive introduced in v0.4.0 — a schema-free portable AI identity that holds an `Identity` and a `MemoryStore` with no opinions about personality models, cognitive engines, or evolution systems. It supports a `create`/`open`/`save` lifecycle for `.soul` file archives.
- [Spec Layer Public Interface: The Headless Standard for Soul Protocol](./spec-layer-public-interface-the-headless-standard-for-soul-protocol.md) — The `spec/__init__.py` is the public surface of Soul Protocol's headless spec layer — minimal, unopinionated primitives that any runtime can implement with zero dependencies on opinionated modules like memory, cognitive engines, or evolution systems. It aggregates over 60 exported symbols spanning identity, memory, retrieval, context management, A2A interop, decision traces, and eternal storage.
- [Test Suite for MemoryEntry, MemoryStore Protocol, and DictMemoryStore](./test-suite-for-memoryentry-memorystore-protocol-and-dictmemorystore.md) — This test suite validates the core in-memory building blocks of Soul Protocol's spec-level memory system: the `MemoryEntry` data model, the `MemoryStore` protocol interface, and the `DictMemoryStore` reference implementation. Tests cover entry creation, metadata handling, layer isolation, BM25-style search, delete semantics, and ordering guarantees.
- [Test Suite for Soul File Pack, Unpack, and Container Helpers](./test-suite-for-soul-file-pack-unpack-and-container-helpers.md) — This test suite validates the `.soul` archive format's core serialization functions: `pack_soul()`, `unpack_soul()`, and the `unpack_to_container()` convenience helper. It verifies identity and memory fidelity across the binary zip roundtrip, multi-layer preservation, empty-store edge cases, and robust error handling for malformed or incomplete archives.
- [Test Suite for the Manifest Model](./test-suite-for-the-manifest-model.md) — This test suite validates the `Manifest` Pydantic model, which acts as the metadata header of a `.soul` archive. It covers default field values, explicit field assignment, and the full Pydantic serialization roundtrip to ensure no data is silently dropped during pack/unpack.
- [Test Suite for the Retrieval Spec: Credentials, Protocols, and Request Contracts](./test-suite-for-the-retrieval-spec-credentials-protocols-and-request-contracts.md) — This test suite validates the spec-level contracts defined in `soul_protocol.spec.retrieval` — covering `Credential` validation and expiry logic, structural Protocol conformance for sync and async adapters, `DataRef` promotion from dicts, and timezone-aware `RetrievalRequest` point-in-time fields. These tests intentionally contain no runtime machinery, serving as a conformance suite for any implementation of the retrieval standard.

### `spec layer` — 18 article(s)

- [EmbeddingProvider Protocol — Pluggable Embedding Backend Interface](./embeddingprovider-protocol-pluggable-embedding-backend-interface.md) — `EmbeddingProvider` is a runtime-checkable `Protocol` class that defines the three-method contract any embedding backend must satisfy. It decouples Soul Protocol's retrieval and search features from any specific ML framework by requiring only `dimensions`, `embed`, and `embed_batch`.
- [Embeddings Spec Package — Public API Surface](./embeddings-spec-package-public-api-surface.md) — The `spec/embeddings` package defines the protocol-level public interface for all embedding operations in Soul Protocol. It re-exports the `EmbeddingProvider` protocol and three vector math primitives so consumers import from a single stable namespace.
- [Eternal Storage Protocol — Archive, Retrieve, and Verify Interface for Permanent Soul Backends](./eternal-storage-protocol-archive-retrieve-and-verify-interface-for-permanent-sou.md) — This module defines the canonical protocol and result models for Soul Protocol's eternal (permanent) storage tier. `EternalStorageProvider` is a runtime-checkable async `Protocol` that any backend (IPFS, Arweave, local) must satisfy, while `ArchiveResult` and `RecoverySource` capture what an archival operation produces and how a soul can later be restored.
- [Eternal Storage Spec Package — Public Interface for Permanent Soul Archival](./eternal-storage-spec-package-public-interface-for-permanent-soul-archival.md) — The `spec/eternal` package exposes the three types that define Soul Protocol's permanent archival tier: `EternalStorageProvider` (the backend protocol), `ArchiveResult` (what an archive operation returns), and `RecoverySource` (what a soul can be restored from). All implementation details live in the engine layer.
- [LearningEvent — Formalized Lesson Model for Procedural Memory and Skill XP](./learningevent-formalized-lesson-model-for-procedural-memory-and-skill-xp.md) — `LearningEvent` captures insights extracted from evaluating success and failure — distinct from episodic memory (what happened) and semantic memory (what is known). Stored in procedural memory and linked to skills, it drives reinforcement-style confidence updates through `apply()`, `reinforce()`, and `weaken()` methods.
- [Memory Primitives — MemoryEntry, MemoryStore Protocol, and DictMemoryStore](./memory-primitives-memoryentry-memorystore-protocol-and-dictmemorystore.md) — This module defines the atomic unit of soul memory (`MemoryEntry`), the backend-agnostic `MemoryStore` protocol, and a reference in-memory implementation (`DictMemoryStore`). It also introduces `Interaction` and `Participant` models for multi-party conversations, along with `MemoryVisibility` tiers for controlling what surfaces in public channel contexts.
- [Org Journal — Append-Only Event Log with Actor Attribution, Scoped Writes, and Root Protection](./org-journal-append-only-event-log-with-actor-attribution-scoped-writes-and-root-.md) — The org journal is the immutable, UTC-stamped, scope-tagged event store that serves as a single source of truth for all mutations in an org instance. `EventEntry`, `Actor`, and `DataRef` are the core spec models; `check_root_undeletable` is an advisory safeguard that prevents any journal event from retiring or deleting the org's root soul.
- [Retrieval Spec — Request/Response Models, Adapter Protocols, Credential Broker, and Exception Hierarchy](./retrieval-spec-requestresponse-models-adapter-protocols-credential-broker-and-ex.md) — This module defines the complete vocabulary for soul retrieval: request and response models (`RetrievalRequest`, `RetrievalResult`), the `SourceAdapter` and `AsyncSourceAdapter` protocols, the `CredentialBroker` interface for federated source access, a `DataRef` type for Zero-Copy candidate payloads, and a structured exception hierarchy. Concrete implementations (the router, in-memory broker, projection adapters) live in the consuming runtime.
- [Retrieval Trace — Observability Receipt for Every Recall and Search Operation](./retrieval-trace-observability-receipt-for-every-recall-and-search-operation.md) — `RetrievalTrace` is a lightweight receipt written once per retrieval call, capturing the ranked candidate list, which candidates were actually used, latency, and a downstream reference. It feeds the paw-runtime JSONL sink, graduation policy, the "Why?" explainability drawer, and SoulBench fixture generation — all from a single portable shape.
- [Scope Matching — Bidirectional Hierarchical RBAC/ABAC Scope Evaluation](./scope-matching-bidirectional-hierarchical-rbacabac-scope-evaluation.md) — This module provides `match_scope`, `match_scope_strict`, and `normalise_scopes` — pure-stdlib functions for evaluating hierarchical scope tags used across all Soul Protocol retrieval paths. The `match_scope` function uses bidirectional containment after a bug was found where agents with concrete scopes (`org:sales:leads`) could not see memories tagged with parent glob scopes (`org:sales:*`) from bundled archetypes.
- [Soul File I/O — Pack and Unpack .soul Zip Archives](./soul-file-io-pack-and-unpack-soul-zip-archives.md) — This module provides `pack_soul`, `unpack_soul`, and `unpack_to_container` — the three functions that serialize and deserialize a soul's identity and memories to and from the `.soul` zip archive format. It depends only on stdlib (`zipfile`, `json`, `io`) and the spec-layer models, keeping the archival format fully portable.
- [Soul Identity — Schema-Free Portable Identity with Multi-Bond Support](./soul-identity-schema-free-portable-identity-with-multi-bond-support.md) — `Identity` is the portable identity primitive at the core of Soul Protocol. It carries a soul's name, creation timestamp, and an open `traits` dict that runtimes can populate with OCEAN scores, Myers-Briggs types, or any custom dimensions. The `feat/spec-multi-participant` update added `BondTarget` and a `bonds` list so a single soul can maintain relationships with multiple humans, agents, groups, and services.
- [Soul Manifest — Archive Metadata Header for .soul Files](./soul-manifest-archive-metadata-header-for-soul-files.md) — The `Manifest` model defines the `manifest.json` file stored at the root of every `.soul` zip archive. It captures format version, soul identity, creation timestamp, an integrity checksum placeholder, and an open `stats` dict for runtime-specific metadata like memory counts and layer names.
- [SoulTemplate — Blueprint for Batch Soul Spawning with Personality Variance](./soultemplate-blueprint-for-batch-soul-spawning-with-personality-variance.md) — `SoulTemplate` defines a reusable blueprint that a `SoulFactory` uses to spawn individual souls or batches with controlled personality variation. It carries a default personality (OCEAN traits as a dict), core memories to bootstrap, skill names, and variance settings for producing non-identical clones in batch operations.
- [Test Suite: Identity Spec — Construction, Auto-ID, Traits, and Serialization](./test-suite-identity-spec-construction-auto-id-traits-and-serialization.md) — Validates the core `Identity` primitive from `soul_protocol.spec`, covering minimal construction, deterministic auto-ID generation, uniqueness guarantees, arbitrary traits storage, and Pydantic `model_dump`/`model_validate` round-trip fidelity. Identity is the foundational spec model that all other containers reference.
- [Test Suite: Org Journal Spec — Actor, DataRef, and EventEntry Primitives](./test-suite-org-journal-spec-actor-dataref-and-evententry-primitives.md) — Validates the foundational primitives of Soul Protocol's organizational journal spec: `Actor` (the entity writing an event), `DataRef` (an external data reference with cache policy), and `EventEntry` (an immutable journal record). The suite enforces UTC-only datetimes, non-anonymous writes, required scopes, and payload union handling.
- [Test Suite: SoulContainer — Spec-Layer Soul Creation, Persistence, and Memory Access](./test-suite-soulcontainer-spec-layer-soul-creation-persistence-and-memory-access.md) — Validates the spec-layer `SoulContainer` — the lightweight soul abstraction for integrations that do not need the full runtime. The suite covers container construction, traits storage, `.soul` file save/open round-trips, multi-layer memory preservation, error handling for missing files, and the default `DictMemoryStore`.
- [Vector Similarity Functions — Pure-Python Cosine, Euclidean, and Dot Product](./vector-similarity-functions-pure-python-cosine-euclidean-and-dot-product.md) — This module provides three zero-dependency vector similarity functions (`cosine_similarity`, `euclidean_distance`, `dot_product`) using only the Python standard library. All three include mismatch guards added in 2026-03-06 to catch incompatible vectors from different embedding backends before producing silent garbage results.

### `spike` — 3 article(s)

- [Journal-Backed Memory Store: Event-Sourced Memory with Rebuildable Projections](./journal-backed-memory-store-event-sourced-memory-with-rebuildable-projections.md) — `JournalBackedMemoryStore` is a spike implementation of the `MemoryStore` protocol that treats the append-only journal as the single source of truth, using SQLite FTS5 projections as a rebuildable read cache. It was designed to eliminate the class of bug where cleanup operations irreversibly wiped memory state.
- [Test Suite for JournalBackedMemoryStore Benchmark Harness](./test-suite-for-journalbackedmemorystore-benchmark-harness.md) — This benchmark harness compares `JournalBackedMemoryStore` against the baseline `DictMemoryStore` across four dimensions: recall quality (recall@5), write latency, search latency, and delete correctness. It was created to produce a data-driven decision on whether the append-only journal architecture is worth adopting, with pass criteria derived from the spike design document.
- [Test Suite for JournalBackedMemoryStore: Unit, Smoke, E2E, and Real-World Scenarios](./test-suite-for-journalbackedmemorystore-unit-smoke-e2e-and-real-world-scenarios.md) — This comprehensive test suite for `JournalBackedMemoryStore` follows a four-tier discipline — unit, smoke, E2E, and real-world simulations — covering every public method and the failure modes that motivated the journal architecture. Special attention is given to disaster recovery (the April 2026 cleanup incident), GDPR-compliant tombstone deletion, idempotent rebuild, and latency budgets.

### `state management` — 2 article(s)

- [State Subpackage Public Interface](./state-subpackage-public-interface.md) — The `state/__init__.py` module is the stable public entry point for the runtime state management subpackage, re-exporting `StateManager` so callers never couple to internal module paths. This thin facade absorbs internal restructures without breaking consumers.
- [StateManager: Runtime Soul State and Mood Dynamics](./statemanager-runtime-soul-state-and-mood-dynamics.md) — `StateManager` owns all mutable runtime state for a digital soul — energy, social battery, mood, and interaction timestamps. It provides delta-based updates, time-based auto-regeneration, and EMA-smoothed mood transitions driven by somatic markers from the sentiment pipeline.

### `state-management` — 1 article(s)

- [Test Suite for StateManager: Mood Inertia, Label Mapping, Energy, and Auto-Regen](./test-suite-for-statemanager-mood-inertia-label-mapping-energy-and-auto-regen.md) — This test suite covers the `StateManager` component responsible for tracking a soul's emotional and physical state across interactions. It validates EMA-based mood inertia, label-to-mood mapping, the TIRED energy override, and the fully configurable `Biorhythms` parameter set including time-based auto-regeneration — with a particular focus on preventing mood instability and ensuring always-on agent deployments work correctly.

### `statistics` — 3 article(s)

- [Long-Horizon Ablation Results Analyzer](./long-horizon-ablation-results-analyzer.md) — The `LongHorizonAnalyzer` takes `LongHorizonResults` produced by the runner and computes summary statistics, pairwise Cohen's d effect sizes, and per-scenario breakdowns, then renders a structured markdown report. It is the statistical layer that transforms raw recall hits and memory counts into publishable evidence.
- [Statistical Analysis Engine for Soul Protocol Research Results](./statistical-analysis-engine-for-soul-protocol-research-results.md) — Provides `ResultsAnalyzer`, which takes flat metric rows from experiment runs and produces statistical summary tables, pairwise condition comparisons with Cohen's d effect sizes and Mann-Whitney U p-values, ablation chains, per-use-case breakdowns, and a full markdown report. All analysis uses stdlib-only dependencies to maximize portability.
- [Test Suite for Long-Horizon Benchmark Analysis (Cohen's d, Effect Labels, Report Generation)](./test-suite-for-long-horizon-benchmark-analysis-cohens-d-effect-labels-report-gen.md) — Validates the statistical analysis layer of the long-horizon benchmark framework, covering Cohen's d calculation, effect-size labeling, and the LongHorizonAnalyzer that produces summary tables and Markdown reports from experimental results. Uses a deterministic mock-results factory to isolate the analysis logic from live LLM calls.

### `storage` — 11 article(s)

- [FileStorage: Filesystem-Backed Soul Persistence with Atomic Writes](./filestorage-filesystem-backed-soul-persistence-with-atomic-writes.md) — `FileStorage` persists soul configs, DNA markdown, and all seven memory tiers to a structured directory layout under `~/.soul/`. The `save_soul_full` function uses a temp-dir-then-move pattern to guarantee atomic writes, preventing partial state on crash.
- [InMemoryStorage: Dict-Backed Soul Backend for Tests and Ephemeral Processes](./inmemorystorage-dict-backed-soul-backend-for-tests-and-ephemeral-processes.md) — `InMemoryStorage` is a zero-dependency, dict-backed implementation of `StorageProtocol` for unit tests and short-lived processes. All methods are `async` to satisfy the protocol interface, even though no I/O occurs.
- [Journal-Backed Memory Store: Event-Sourced Memory with Rebuildable Projections](./journal-backed-memory-store-event-sourced-memory-with-rebuildable-projections.md) — `JournalBackedMemoryStore` is a spike implementation of the `MemoryStore` protocol that treats the append-only journal as the single source of truth, using SQLite FTS5 projections as a rebuildable read cache. It was designed to eliminate the class of bug where cleanup operations irreversibly wiped memory state.
- [Journal Backend Protocol — Storage Contract for Event Journals](./journal-backend-protocol-storage-contract-for-event-journals.md) — Defines `JournalBackend` as a `runtime_checkable` Protocol (structural interface) that any storage backend must satisfy. Specifies four operations — append, query, replay, and close — with precise filter semantics for the query method.
- [Journal High-Level Class — Invariant Enforcement and Hash Chaining](./journal-high-level-class-invariant-enforcement-and-hash-chaining.md) — The `Journal` class wraps a `JournalBackend` to enforce append invariants (timezone-aware timestamps, monotonic ordering) and maintain an opportunistic SHA-256 hash chain linking each event to the previous one. `open_journal()` is the factory for production use.
- [ProceduralStore: How-To Memory Storage and GDPR Deletion](./proceduralstore-how-to-memory-storage-and-gdpr-deletion.md) — Provides an in-memory store specifically for procedural memories — step-by-step how-tos and learned processes. Includes token-overlap relevance search and GDPR-targeted deletion operations added in v0.3.
- [SemanticStore: Fact-Based Memory with Confidence and Eviction](./semanticstore-fact-based-memory-with-confidence-and-eviction.md) — Stores semantic memories — extracted facts and general knowledge about the world and the user — with importance scoring, confidence tracking, supersession logic for updated facts, and a bounded capacity that evicts least-important entries when full. GDPR deletion APIs are provided for targeted and time-based removal.
- [Storage Subpackage Public Interface](./storage-subpackage-public-interface.md) — The `storage/__init__.py` re-exports all public storage symbols — `StorageProtocol`, `FileStorage`, `InMemoryStorage`, and the persistence functions `save_soul`, `load_soul`, `save_soul_full`, and `load_soul_full` — from a single stable import path. A `save_soul_full` addition in 2026-02-22 corrected the earlier API's silent memory-tier data loss.
- [StorageProtocol: Runtime-Checkable Interface for Soul Storage Backends](./storageprotocol-runtime-checkable-interface-for-soul-storage-backends.md) — `StorageProtocol` is a `@runtime_checkable` Python `Protocol` that defines the four operations all soul storage backends must implement: `save`, `load`, `delete`, and `list_souls`. It decouples the runtime from any specific storage implementation, enabling `FileStorage` and `InMemoryStorage` to be swapped without modifying callers.
- [Test Package Init: test_storage](./test-package-init-teststorage.md) — This is the package initializer for the `tests/test_storage/` directory, containing a minimal comment identifying the package. It enables pytest to discover storage backend tests and scopes them under a dedicated package namespace.
- [Test Suite for InMemoryStorage and FileStorage Backends](./test-suite-for-inmemorystorage-and-filestorage-backends.md) — This test suite validates both storage backends provided by Soul Protocol's runtime: `InMemoryStorage` (dict-backed, in-process) and `FileStorage` (disk-backed, directory-per-soul). It covers the full CRUD surface — save, load, delete, and list — with particular attention to idempotent delete behavior and the expected on-disk directory structure for `FileStorage`.

### `strategy-pattern` — 1 article(s)

- [Test Suite: Pluggable Search Strategy Protocol](./test-suite-pluggable-search-strategy-protocol.md) — Tests for the SearchStrategy protocol and its propagation through the full memory stack — from Soul.birth() down through MemoryManager, RecallEngine, spreading_activation(), and compute_activation() — verifying that custom scoring functions can replace the default token-overlap approach at every layer.

### `templates` — 5 article(s)

- [Bundled Soul Templates: Role Archetype Registry](./bundled-soul-templates-role-archetype-registry.md) — The `soul_protocol.templates` package ships four pre-built YAML role archetypes — Arrow (sales), Flash (content), Cyborg (recruiting), and Analyst (research) — alongside a small discovery API. It gives consumers a zero-configuration starting point while keeping custom templates fully supported via path-based loading.
- [SoulFactory: Template-Driven Soul Creation and Batch Spawning](./soulfactory-template-driven-soul-creation-and-batch-spawning.md) — `SoulFactory` creates souls from `SoulTemplate` blueprints via `from_template()` (single soul) and `batch_spawn()` (N souls with controlled OCEAN personality variance). Both methods propagate `default_scope` from template metadata into seeded core memories, enabling RBAC/ABAC recall filtering on freshly created souls.
- [SoulTemplate — Blueprint for Batch Soul Spawning with Personality Variance](./soultemplate-blueprint-for-batch-soul-spawning-with-personality-variance.md) — `SoulTemplate` defines a reusable blueprint that a `SoulFactory` uses to spawn individual souls or batches with controlled personality variation. It carries a default personality (OCEAN traits as a dict), core memories to bootstrap, skill names, and variance settings for producing non-identical clones in batch operations.
- [Test Suite for Bundled Role Templates and SoulFactory Template Loading](./test-suite-for-bundled-role-templates-and-soulfactory-template-loading.md) — This test suite locks the contracts for the four bundled archetypes (Arrow, Flash, Cyborg, Analyst), the YAML/JSON template loaders, template instantiation into real souls, and the scope propagation behavior that ensures Arrow's sales-scoped core memories are visible to concrete `org:sales:leads` callers.
- [Test Suite: Soul Templates and Batch Spawning via SoulFactory](./test-suite-soul-templates-and-batch-spawning-via-soulfactory.md) — Validates `SoulTemplate` (reusable soul blueprints with personality profiles, memories, and skills) and `SoulFactory` (the factory that instantiates souls from templates, including batch spawning with controlled personality variance). The suite covers model validation, override semantics, variance clamping, reproducible seeding, and public API exports.

### `temporal-reasoning` — 1 article(s)

- [Test Suite for Temporal Knowledge Graph](./test-suite-for-temporal-knowledge-graph.md) — This test suite validates the temporal knowledge graph (`KnowledgeGraph` and `TemporalEdge`) used by Soul Protocol to represent relationships between entities over time. It covers edge creation, time-range activation queries, relationship expiration, duplicate deduplication, point-in-time queries, evolution history, serialization roundtrips, and backward compatibility with older edge formats.

### `test` — 131 article(s)

- [CLI Test Suite: Agent Platform Integration Setup (`soul setup`)](./cli-test-suite-agent-platform-integration-setup-soul-setup.md) — Test suite for `soul setup`, which wires a Soul Protocol MCP server into AI coding platforms (Cursor, VS Code, Claude Code, Continue, Windsurf). Covers platform auto-detection, MCP config file writing and merging, instruction file injection, `.gitignore` updates, and `uvx` path resolution.
- [CLI Test Suite: Core Commands (birth, inspect, status, remember, recall, init)](./cli-test-suite-core-commands-birth-inspect-status-remember-recall-init.md) — Comprehensive test suite for the Soul Protocol CLI covering the full lifecycle of a soul: creation, inspection, memory storage across tiers, multi-format output, and init options. Tests use click.testing.CliRunner to invoke commands in isolation and assert on exit codes, output text, and ZIP archive contents.
- [CLI Test Suite: Full Onboarding, Multi-User Lifecycle, and Destroy Workflow](./cli-test-suite-full-onboarding-multi-user-lifecycle-and-destroy-workflow.md) — End-to-end test suite for the `soul init` onboarding wizard covering user scope creation, fleet provisioning, status reporting, soul deletion, and the destroy command that archives then wipes all data. Also validates journal append-only privacy constraints and users-dir precedence rules.
- [CLI Test Suite: Health, Cleanup, and Repair Commands](./cli-test-suite-health-cleanup-and-repair-commands.md) — Test suite for three soul maintenance CLI commands: `soul health` (diagnostic reporting), `soul cleanup` (duplicate/low-importance memory removal), and `soul repair` (energy reset). Covers dry-run safety guards, auto-mode mutations, and persistence verification.
- [CLI Test Suite: Org Initialization (`soul org init`)](./cli-test-suite-org-initialization-soul-org-init.md) — Test suite for `soul org init`, which bootstraps an organization by creating a root soul, a journal, and cryptographic keys. Covers happy-path artifact creation, idempotency guards, force-overwrite behavior, key file permissions, journal privacy, and round-trip loadability.
- [CLI Test Suite: Soul Context Injection into AI IDE Config Files](./cli-test-suite-soul-context-injection-into-ai-ide-config-files.md) — Test suite for `soul inject`, which writes a soul's identity and recent memories into AI IDE configuration files (Claude Code, Cursor, VS Code, Windsurf, Cline, Continue). Covers target path resolution, idempotent section replacement, context block formatting, memory truncation, and auto-discovery of soul directories.
- [Cryptographic Encryption Test Suite (Fernet + PBKDF2)](./cryptographic-encryption-test-suite-fernet-pbkdf2.md) — Test suite validating the `soul_protocol.runtime.crypto.encrypt` module, which provides passphrase-based symmetric encryption for soul data. Covers the full encrypt/decrypt roundtrip, wrong-passphrase rejection, and deterministic key derivation.
- [Developer Onboarding Demo Smoke Test](./developer-onboarding-demo-smoke-test.md) — A minimal async smoke test that runs the soul-protocol onboarding demo end-to-end in a non-interactive mode. It uses `monkeypatch` to set `SOUL_DEMO_NO_PAUSE=1`, preventing the demo from blocking on user input during CI.
- [Dream Engine Test Suite — Memory Consolidation and Behavioral Analysis](./dream-engine-test-suite-memory-consolidation-and-behavioral-analysis.md) — Comprehensive test suite for the soul-protocol `dream` subsystem, which performs offline memory consolidation: clustering topics, detecting recurring procedures, pruning duplicates, and suggesting personality evolution. Tests span unit-level phase functions through full integration scenarios with before/after memory state comparisons.
- [DSPy Integration Test Suite — Cognitive Processor, Adapter, and Optimizer](./dspy-integration-test-suite-cognitive-processor-adapter-and-optimizer.md) — Test suite for the DSPy-powered cognitive layer in soul-protocol, covering module instantiation, the `DSPyCognitiveProcessor` adapter bridge, training data generation, graceful fallback when DSPy is absent, and the `SoulOptimizer`. All LLM calls are mocked — no real API calls are made.
- [Embedding Provider Factory Test Suite](./embedding-provider-factory-test-suite.md) — Tests for `get_embedding_provider()`, the factory function that instantiates embedding backends by name. Covers all built-in providers (hash, TF-IDF), external providers with lazy imports (sentence-transformers, OpenAI, Ollama), kwargs passthrough, unknown provider errors, and import failure handling.
- [Embedding Provider Protocol Compliance Test Suite](./embedding-provider-protocol-compliance-test-suite.md) — Tests that verify `HashEmbedder` and `TFIDFEmbedder` correctly satisfy the `EmbeddingProvider` structural protocol at runtime. Also validates that partial or non-compliant implementations fail the `isinstance` check, ensuring the protocol is genuinely enforced rather than nominal.
- [Embeddings Test Package Initializer](./embeddings-test-package-initializer.md) — Package initializer for the `tests/test_embeddings/` test subpackage, created to group all embedding-related test modules under a common namespace. The file contains no executable code — its presence makes the directory a Python package discoverable by pytest.
- [End-to-End Integration Test Suite — Soul Lifecycle, Memory Persistence, and Error Handling](./end-to-end-integration-test-suite-soul-lifecycle-memory-persistence-and-error-ha.md) — Full end-to-end integration tests for the Soul class covering the complete lifecycle from birth through export and awakening. Tests validate memory persistence across all five tiers, self-model emergence, core memory editing, directory format save/load, and robust error handling for corrupt or missing soul files.
- [End-to-End New Soul Primitives Test — Bond, Skills, Retirement, and Reincarnation](./end-to-end-new-soul-primitives-test-bond-skills-retirement-and-reincarnation.md) — End-to-end test suite for newer soul-protocol primitives introduced post-v1: bond mechanics, skill registry, lifecycle state transitions (active → retired → reincarnated), and `.soul` archive integrity. Tests validate the logarithmic bond growth curve and the presence of `dna.md` in exported archives.
- [End-to-End Test Suite for Eternal Storage Lifecycle](./end-to-end-test-suite-for-eternal-storage-lifecycle.md) — End-to-end tests covering the full eternal storage lifecycle: birth a soul, archive to multiple tiers, verify archives exist, recover from each tier individually, and confirm recovered data can be awakened into a valid Soul. Also tests multi-tier fallback recovery when primary sources fail.
- [End-to-End Vector Search Test Suite — TF-IDF Semantic Memory Retrieval](./end-to-end-vector-search-test-suite-tf-idf-semantic-memory-retrieval.md) — End-to-end tests for soul-protocol's semantic memory search pipeline using `TFIDFEmbedder` and `VectorSearchStrategy`. Tests create memory entries on distinct topics, fit the embedder, and verify that topical queries return relevant results in the correct order.
- [Engine Test Package Initializer](./engine-test-package-initializer.md) — Marks `tests/test_engine/` as a Python package so pytest can discover tests within it. Created as part of the journal-engine workstream in the Org Architecture RFC.
- [Eternal Storage Test Package Initializer](./eternal-storage-test-package-initializer.md) — Marks `tests/test_eternal/` as a Python package, enabling pytest discovery of the eternal storage subsystem's test modules. Created when the eternal storage subsystem was introduced on 2026-03-06.
- [Hash Embedder Test Suite — Determinism, Normalization, and Batch Embedding](./hash-embedder-test-suite-determinism-normalization-and-batch-embedding.md) — Test suite for `HashEmbedder`, soul-protocol's zero-dependency embedding backend that converts text to L2-normalized vectors via hashing. Covers dimensionality configuration, deterministic output, L2 normalization, edge cases (empty strings, whitespace), and batch embedding consistency.
- [Ollama Embedding Provider Test Suite — Local Model Integration with Lazy Loading](./ollama-embedding-provider-test-suite-local-model-integration-with-lazy-loading.md) — Test suite for `OllamaEmbeddingProvider`, which connects to a locally-running Ollama server for embedding generation. Tests use a mocked `ollama` library to avoid requiring a real server in CI, covering protocol compliance, lazy client initialization, batch embedding, custom model and base URL configuration, and graceful import error handling.
- [OpenAI Embedding Provider Test Suite — API Key Validation, Retry Logic, and Batch Embedding](./openai-embedding-provider-test-suite-api-key-validation-retry-logic-and-batch-em.md) — Test suite for `OpenAIEmbeddingProvider`, which calls the OpenAI embeddings API for high-quality vector representations. Tests use a mocked `openai` library and cover protocol compliance, API key validation, exponential backoff retry logic for rate limits, model dimension detection, and graceful import error handling.
- [Real-World End-to-End Test Suite — Developer Onboarding, Bonding, and Evolution Scenarios](./real-world-end-to-end-test-suite-developer-onboarding-bonding-and-evolution-scen.md) — Scenario-driven end-to-end tests that simulate realistic human-soul conversations across multiple contexts: developer technical discussions, personal emotional bonding, and mixed sessions. Tests validate that all soul systems (entity extraction, knowledge graph, bond growth, evolution, self-model) fire correctly in natural usage patterns.
- [Regression Tests for Bug #15: Core Memory Replace vs. Append](./regression-tests-for-bug-15-core-memory-replace-vs-append.md) — This regression suite locks the fix for Bug #15, where `edit_core_memory()` was appending new values to existing persona and human fields instead of replacing them. The tests verify replace semantics, partial-field isolation, and the explicit absence of the old append behavior.
- [Sentence Transformer Embedding Provider Test Suite — Lazy Loading and Mock-Based Validation](./sentence-transformer-embedding-provider-test-suite-lazy-loading-and-mock-based-v.md) — Test suite for `SentenceTransformerProvider`, which wraps the `sentence-transformers` library for high-quality local embedding generation. Tests mock the heavy ML dependency to run in CI, covering protocol compliance, lazy model loading, batch embedding, custom model and device configuration, and import error handling.
- [Test Package Init: Cognitive Engine Test Suite Namespace](./test-package-init-cognitive-engine-test-suite-namespace.md) — Empty `__init__.py` that marks `tests/test_cognitive/` as a Python package, enabling pytest to discover and import tests in the cognitive engine subdirectory. No executable code; its presence is a structural requirement for the test namespace.
- [Test Package Init: Evolution Subsystem](./test-package-init-evolution-subsystem.md) — Empty package initialiser that makes `tests/test_evolution/` a proper Python package, enabling pytest to discover and import the evolution test modules correctly. No executable code is present; the file's value is purely structural.
- [Test Package Init: Importers Subsystem](./test-package-init-importers-subsystem.md) — Empty package initialiser that designates `tests/test_importers/` as a Python package, enabling pytest to collect the importer-specific test modules contained within. No executable code is present.
- [Test Package Init: Install Invariants](./test-package-init-install-invariants.md) — Package initialiser for `tests/test_install/`, the sub-package that verifies install-time invariants such as the CLI being functional after a bare `pip install soul-protocol`. Created to address issue #157.
- [Test Package Init: Long-Horizon Ablation Study](./test-package-init-long-horizon-ablation-study.md) — Package initialiser for `tests/test_long_horizon/`, the sub-package housing ablation study tests that evaluate soul memory and context performance over extended interaction sequences. Created 2026-03-11.
- [Test Package Init: test_spike](./test-package-init-testspike.md) — This is the empty package initializer for the `tests/test_spike/` directory. It has no executable content but is required by Python to treat the directory as an importable package, enabling pytest to discover and collect the spike test files within it.
- [Test Package Init: test_state](./test-package-init-teststate.md) — This is the empty package initializer for the `tests/test_state/` directory. It has no executable content but enables pytest to discover and collect tests within the state management test package.
- [Test Package Init: test_storage](./test-package-init-teststorage.md) — This is the package initializer for the `tests/test_storage/` directory, containing a minimal comment identifying the package. It enables pytest to discover storage backend tests and scopes them under a dedicated package namespace.
- [Test Package Initializer for MCP Server Integration Tests](./test-package-initializer-for-mcp-server-integration-tests.md) — The `tests/test_mcp/__init__.py` file marks the `test_mcp` directory as a Python package, enabling pytest to discover and organize MCP server integration tests as a cohesive module. It contains no executable code — its presence is structural.
- [Test Package Initializer for Memory Subsystem Tests](./test-package-initializer-for-memory-subsystem-tests.md) — The `tests/test_memory/__init__.py` file marks the `test_memory` directory as a Python package, enabling pytest to discover and organize the memory subsystem test suite as a cohesive module. It contains no executable code.
- [Test Package Initializer: test_models](./test-package-initializer-testmodels.md) — Minimal package initializer for the test_models test directory, which makes the directory a Python package so pytest can discover and import test modules within it.
- [Test Suite: Cognitive Adapter Layer -- CallableEngine, Auto-Detection, and Provider Integration](./test-suite-cognitive-adapter-layer-callableengine-auto-detection-and-provider-in.md) — Tests for the adapter layer that bridges Soul Protocol's `CognitiveEngine` protocol with real LLM providers (Anthropic, OpenAI, Ollama) and user-supplied callables. Covers callable wrapping, environment-variable-driven auto-detection, Soul factory integration, import safety for optional dependencies, and HTTP-level mocking for Ollama.
- [Test Suite: Cognitive Engine, Heuristic Routing, and LLM Fallback](./test-suite-cognitive-engine-heuristic-routing-and-llm-fallback.md) — Comprehensive test suite for the Soul Protocol cognitive processing layer, covering HeuristicEngine task routing, CognitiveProcessor LLM integration, JSON parsing helpers, and graceful fallback when LLMs fail or return garbage. Also includes regression tests for the heuristic-only mode bug.
- [Test Suite: Cognitive Prompt Template Formatting and JSON Parsability](./test-suite-cognitive-prompt-template-formatting-and-json-parsability.md) — Tests that every cognitive prompt template in Soul Protocol formats without error and produces output parseable by the JSON extraction helper. Validates both the template variables contract and the round-trip between typical LLM responses and the `_parse_json` function.
- [Test Suite: Context Compaction -- Three-Level Token Budget Enforcement](./test-suite-context-compaction-three-level-token-budget-enforcement.md) — Tests for Soul Protocol's context compaction system, which keeps conversation history within a token budget through three escalating strategies: LLM-generated summaries, bullet compression, and hard truncation. Covers zero-cost paths, summary/bullet/truncation tiers, idempotency, and guaranteed convergence.
- [Test Suite: Context Spec Models -- Pydantic Schemas for Conversation Context](./test-suite-context-spec-models-pydantic-schemas-for-conversation-context.md) — Tests for the Pydantic models that define Soul Protocol's conversation context data layer, including `CompactionLevel` enum, `ContextMessage`, `ContextNode`, and the result types (`AssembleResult`, `GrepResult`, `ExpandResult`, `DescribeResult`). Validates defaults, serialization round-trips, unique ID generation, and string-enum comparability.
- [Test Suite: Context Store Retrieval — Grep, Expand, and Describe](./test-suite-context-store-retrieval-grep-expand-and-describe.md) — Validates the three retrieval operations on Soul Protocol's conversation context store: regex-based message search (`grep`), DAG node expansion to recover original messages (`expand`), and metadata snapshot generation (`describe`). These operations power agent tools that let an LLM inspect its own conversation history without re-loading the full context window.
- [Test Suite: Context Store -- SQLite-Backed Conversation History](./test-suite-context-store-sqlite-backed-conversation-history.md) — Tests for Soul Protocol's SQLite-backed context store, which persists conversation messages and compaction nodes with sequential ordering, full-text grep, date-range filtering, and cross-reconnect durability. Covers both in-memory (test) and file-backed (persistence) store configurations.
- [Test Suite: Core Pydantic Data Models](./test-suite-core-pydantic-data-models.md) — Validates the foundational Pydantic models in soul_protocol.types — Identity defaults, Personality trait bounds, SoulConfig JSON serialization round-trip, MemoryEntry defaults, Mood enum completeness, and EvolutionMode enum values.
- [Test Suite: Core Soul API — Birth, Memory, Emotion, Export, and Psychology Pipeline](./test-suite-core-soul-api-birth-memory-emotion-export-and-psychology-pipeline.md) — The primary integration test suite for the `Soul` class, covering the full API surface from construction through the psychology pipeline. It validates that soul birth, memory operations, emotional state, import/export round-trips, system prompt generation, and the attention gate all work correctly together.
- [Test Suite: Cross-Session Contradiction Detection in the Observe Pipeline](./test-suite-cross-session-contradiction-detection-in-the-observe-pipeline.md) — End-to-end regression tests for Soul Protocol's contradiction detection, targeting a specific bug where heuristic contradiction detection fired in isolation but silently failed during `observe()` when a user updated a fact across sessions (e.g., location or employer change). Tests verify that new facts correctly supersede old ones.
- [Test Suite: Decision Trace Spec — Agent Proposals, Human Corrections, and Graduation](./test-suite-decision-trace-spec-agent-proposals-human-corrections-and-graduation.md) — Validates the decision-trace spec from Workstream D of the Org Architecture RFC, covering the three payload types (`AgentProposal`, `HumanCorrection`, `DecisionGraduation`), their JSON round-trips, field validation, and the builder helpers that emit correctly shaped `EventEntry` records to the org journal.
- [Test Suite for Archival Memory Integration: Compression Pipeline and Persistence](./test-suite-for-archival-memory-integration-compression-pipeline-and-persistence.md) — This integration test suite validates the full archival pipeline in `MemoryManager`: how old episodic entries are compressed into `ConversationArchive` objects, how high-importance entries become key moments, how archived entries are excluded from recall, and how archives survive serialization round-trips through `to_dict()` / `from_dict()`.
- [Test Suite for Auto-Scheduled Consolidation (F5)](./test-suite-for-auto-scheduled-consolidation-f5.md) — This test suite validates that `Soul.observe()` automatically triggers memory consolidation (archival and reflection) at a configurable interval, that the interval is configurable and disableable, and that the interaction count persists through export/awaken cycles so consolidation timing survives restarts.
- [Test Suite for Base Install Invariants (Issue #157)](./test-suite-for-base-install-invariants-issue-157.md) — Regression tests that verify a bare `pip install soul-protocol` produces a working CLI, by checking that click, rich, pyyaml, and cryptography are declared as base dependencies, the deprecated `[engine]` extra still exists for backwards compatibility, and the CLI module imports cleanly without optional extras.
- [Test Suite for Bi-Temporal Memory Fields (ingested_at and superseded)](./test-suite-for-bi-temporal-memory-fields-ingestedat-and-superseded.md) — Validates bi-temporal tracking on MemoryEntry — the `ingested_at` timestamp (when a fact entered this soul's memory vs. when it was created) and the `superseded` flag (marking outdated facts replaced by newer information). Tests cover defaults, explicit values, manager behavior, serialization round-trips, and backward compatibility with soul files that predate these fields.
- [Test Suite for Bundled Role Templates and SoulFactory Template Loading](./test-suite-for-bundled-role-templates-and-soulfactory-template-loading.md) — This test suite locks the contracts for the four bundled archetypes (Arrow, Flash, Cyborg, Analyst), the YAML/JSON template loaders, template instantiation into real souls, and the scope propagation behavior that ensures Arrow's sales-scoped core memories are visible to concrete `org:sales:leads` callers.
- [Test Suite for Eternal Storage CLI Commands](./test-suite-for-eternal-storage-cli-commands.md) — Tests the soul-protocol CLI commands for eternal storage operations: archive (with optional tier selection), eternal-status (tier health check), and recover (with failure path handling). Uses Click's CliRunner for isolated, in-process command invocation.
- [Test Suite for Eternal Storage Protocol Compliance](./test-suite-for-eternal-storage-protocol-compliance.md) — Verifies that all eternal storage providers satisfy the EternalStorageProvider protocol at runtime, and validates the Pydantic models ArchiveResult and RecoverySource for field defaults, full construction, and serialization round-trips.
- [Test Suite for Eternal Storage Providers](./test-suite-for-eternal-storage-providers.md) — Per-provider tests for the four eternal storage backends: MockIPFSProvider, MockArweaveProvider, MockBlockchainProvider, and LocalStorageProvider. Each provider is tested for archive, retrieve, verify, content addressing, unique identifiers, and error behavior on missing references.
- [Test Suite for EternalStorageManager](./test-suite-for-eternalstoragemanager.md) — Unit tests for EternalStorageManager — the orchestration layer that routes archive, recover, and verify operations across multiple storage providers. Covers provider registration/unregistration, selective tier archiving, fallback recovery chains, unavailable-provider skipping, and verification status.
- [Test Suite for Fact and Entity Extraction from Interaction Text](./test-suite-for-fact-and-entity-extraction-from-interaction-text.md) — Validates the pattern-based extraction pipeline that parses user messages for facts (name, preferences, identity, tools, projects) and entities (tech terms, proper nouns with inferred relations). Tests cover each extraction pattern, punctuation stripping, stopword exclusion, and deduplication of repeated facts.
- [Test Suite for GDPR-Compliant Memory Deletion](./test-suite-for-gdpr-compliant-memory-deletion.md) — Comprehensive tests for Soul Protocol's right-to-erasure implementation, covering query-based, entity-based, and time-based deletion across all memory tiers, with full audit trail verification. Ensures deleted memories become invisible to recall and that audit records never expose the deleted content itself.
- [Test Suite for InMemoryStorage and FileStorage Backends](./test-suite-for-inmemorystorage-and-filestorage-backends.md) — This test suite validates both storage backends provided by Soul Protocol's runtime: `InMemoryStorage` (dict-backed, in-process) and `FileStorage` (disk-backed, directory-per-soul). It covers the full CRUD surface — save, load, delete, and list — with particular attention to idempotent delete behavior and the expected on-disk directory structure for `FileStorage`.
- [Test Suite for Journal 0.3.2 Primitives](./test-suite-for-journal-032-primitives.md) — Smoke, end-to-end, and real-world simulation tests for the primitives introduced in soul-protocol v0.3.2, specifically the append-returns-committed-entry contract and action prefix querying. Tests mirror realistic consumer patterns from the PocketPaw widget store.
- [Test Suite for JournalBackedMemoryStore Benchmark Harness](./test-suite-for-journalbackedmemorystore-benchmark-harness.md) — This benchmark harness compares `JournalBackedMemoryStore` against the baseline `DictMemoryStore` across four dimensions: recall quality (recall@5), write latency, search latency, and delete correctness. It was created to produce a data-driven decision on whether the append-only journal architecture is worth adopting, with pass criteria derived from the spike design document.
- [Test Suite for JournalBackedMemoryStore: Unit, Smoke, E2E, and Real-World Scenarios](./test-suite-for-journalbackedmemorystore-unit-smoke-e2e-and-real-world-scenarios.md) — This comprehensive test suite for `JournalBackedMemoryStore` follows a four-tier discipline — unit, smoke, E2E, and real-world simulations — covering every public method and the failure modes that motivated the journal architecture. Special attention is given to disaster recovery (the April 2026 cleanup incident), GDPR-compliant tombstone deletion, idempotent rebuild, and latency budgets.
- [Test Suite for Knowledge Graph Traversal](./test-suite-for-knowledge-graph-traversal.md) — Tests for the `KnowledgeGraph` traversal API, covering BFS traversal, shortest-path finding, neighbourhood extraction, subgraph extraction, and progressive context rendering at three detail levels. Validates edge expiry, cycle handling, and node deduplication across a range of graph topologies.
- [Test Suite for LCMContext (Long-Context Memory)](./test-suite-for-lcmcontext-long-context-memory.md) — Integration tests for `LCMContext`, the context engine that ingests conversation messages, assembles token-budgeted context windows, and auto-compacts history using a `CognitiveEngine`. Covers protocol compliance, initialization guards, token estimation, compaction triggers, grep search, and lifecycle close/reopen semantics.
- [Test Suite for Learning Events Pipeline](./test-suite-for-learning-events-pipeline.md) — Tests for the full learning events pipeline — from `RubricResult` score thresholding to `LearningEvent` creation, skill XP grant, and the `Soul.learn()` convenience method. Covers model validation, score boundary conditions, XP scaling, and spec module exportability.
- [Test Suite for Long-Horizon Benchmark Analysis (Cohen's d, Effect Labels, Report Generation)](./test-suite-for-long-horizon-benchmark-analysis-cohens-d-effect-labels-report-gen.md) — Validates the statistical analysis layer of the long-horizon benchmark framework, covering Cohen's d calculation, effect-size labeling, and the LongHorizonAnalyzer that produces summary tables and Markdown reports from experimental results. Uses a deterministic mock-results factory to isolate the analysis logic from live LLM calls.
- [Test Suite for Long-Horizon Benchmark Runner](./test-suite-for-long-horizon-benchmark-runner.md) — Validates the multi-condition benchmark runner that processes conversation scenarios through different soul configurations (Full Soul, RAG Only, Personality Only, Bare) and records recall, memory growth, and bond-strength metrics. Async tests use a short 15-turn scenario fixture to keep CI fast while still exercising the full execution path.
- [Test Suite for Long-Horizon Benchmark Scenarios](./test-suite-for-long-horizon-benchmark-scenarios.md) — Validates the three benchmark scenario generators (Life Updates, Emotional Rollercoaster, Adversarial Burial) that produce 100+ turn conversation scripts with planted facts, test points, and recall challenges. Tests verify determinism, scenario structure, seeded variation, and the TestPoint dataclass.
- [Test Suite for MCP Psychology Tools (Skills, Evaluate, Learn, Evolve)](./test-suite-for-mcp-psychology-tools-skills-evaluate-learn-evolve.md) — Validates the higher-order MCP tools that expose soul psychology — skill tracking, rubric-based evaluation, learning events, and the propose/approve/reject evolution workflow. Tests use FastMCP's in-process client to exercise the full MCP tool call path without a network round-trip.
- [Test Suite for MCP Sampling Engine (LLM Think with Heuristic Fallback)](./test-suite-for-mcp-sampling-engine-llm-think-with-heuristic-fallback.md) — Validates the MCPSamplingEngine, which wraps the MCP context's sampling capability to give souls LLM-powered reflection, with graceful fallback to the HeuristicEngine when the MCP context is unavailable or raises. Tests cover the success path, three failure scenarios, the no-context path, and the server's engine wiring lifecycle.
- [Test Suite for MCP Server Core Tools (Birth, Observe, Memory, State, Lifecycle)](./test-suite-for-mcp-server-core-tools-birth-observe-memory-state-lifecycle.md) — Integration tests for the core soul_protocol MCP server tools, covering soul lifecycle (birth, save, export), memory operations (remember, recall), emotional state (feel), observation, reflection, and multi-soul management (list, switch). Uses a registry reset helper and in-process FastMCP client to achieve full isolation between tests.
- [Test Suite for Memory Activation Functions (ACT-R Base-Level, Spreading, Emotional Boost)](./test-suite-for-memory-activation-functions-act-r-base-level-spreading-emotional-.md) — Validates the three activation components that determine how retrievable a memory is at query time: the ACT-R base-level activation formula (recency and frequency), spreading activation (query-content token overlap), and emotional boost (somatic marker arousal and valence). Together these tests pin the mathematical contracts that underlie memory retrieval scoring.
- [Test Suite for Memory Attention and Interaction Significance Scoring](./test-suite-for-memory-attention-and-interaction-significance-scoring.md) — Validates the attention system that decides whether an interaction is significant enough to trigger memory storage. Tests cover novelty scoring, emotional intensity detection, goal-relevance matching against core values, and the composite SignificanceScore type.
- [Test Suite for Memory Consolidation, Reflection, and Fact Supersession](./test-suite-for-memory-consolidation-reflection-and-fact-supersession.md) — Validates the consolidation pipeline that transforms raw episodic memories into structured semantic knowledge — converting summaries into semantic memories, grouping episodes under general-event themes, storing self-insight and emotional patterns, and superseding outdated facts. Also tests the reflect() entry point and the apply=False dry-run mode.
- [Test Suite for Memory Contradiction Detection (Heuristic and LLM Modes)](./test-suite-for-memory-contradiction-detection-heuristic-and-llm-modes.md) — Validates the contradiction detector that identifies when a new memory conflicts with existing ones — using both fast heuristic rules (negation patterns, entity-attribute conflicts) and LLM-powered analysis. Tests cover true-positive detection, false-positive prevention, superseded-memory exclusion, similarity thresholds, and graceful fallback when the LLM errors.
- [Test Suite for Memory Deduplication (Jaccard Similarity, Short-Token False Positives, and Containment Merge)](./test-suite-for-memory-deduplication-jaccard-similarity-short-token-false-positiv.md) — Validates the deduplication system that prevents near-identical facts from being stored multiple times, covering the tokenizer, Jaccard similarity computation, short-token false-positive prevention (go vs. js, ai vs. ml), fact reconciliation decisions (CREATE/SKIP/MERGE), and the containment-merge logic for enriched facts.
- [Test Suite for MemoryEntry, MemoryStore Protocol, and DictMemoryStore](./test-suite-for-memoryentry-memorystore-protocol-and-dictmemorystore.md) — This test suite validates the core in-memory building blocks of Soul Protocol's spec-level memory system: the `MemoryEntry` data model, the `MemoryStore` protocol interface, and the `DictMemoryStore` reference implementation. Tests cover entry creation, metadata handling, layer isolation, BM25-style search, delete semantics, and ordering guarantees.
- [Test Suite for Soul Error Handling](./test-suite-for-soul-error-handling.md) — Verifies that Soul's three main lifecycle operations — awaken(), export(), and retire() — raise specific, descriptive exception types rather than raw Python exceptions. Also tests exception class structure to ensure callers can extract actionable information like file paths and failure reasons.
- [Test Suite for Soul Eternal Storage Integration](./test-suite-for-soul-eternal-storage-integration.md) — Tests the integration between the Soul class and EternalStorageManager, covering Soul.archive(), the with_mocks() factory, the eternal= parameter on Soul.birth() and Soul.awaken(), and the export(archive=True) convenience flag.
- [Test Suite for Soul Evolution Manager](./test-suite-for-soul-evolution-manager.md) — Tests for `EvolutionManager`, which controls how a soul's DNA traits mutate over time across three modes: supervised, autonomous, and disabled. Covers the full mutation lifecycle — propose, approve, reject, apply — and guards against illegal mutations on immutable traits.
- [Test Suite for Soul Export and Pack/Unpack Operations](./test-suite-for-soul-export-and-packunpack-operations.md) — Tests for the `.soul` zip archive format, covering the full pack/unpack roundtrip, required file inventory inside the archive, and memory tier data inclusion. Ensures that `SoulConfig` and memory content survive serialisation to bytes and back without loss.
- [Test Suite for Soul File Encryption](./test-suite-for-soul-file-encryption.md) — Validates the full encryption stack for .soul files, from low-level blob crypto primitives through pack/unpack pipeline integration to the high-level Soul.export() and Soul.awaken() API. Covers round-trips, wrong-password errors, backward compatibility with unencrypted files, and manifest readability without a password.
- [Test Suite for Soul File Pack, Unpack, and Container Helpers](./test-suite-for-soul-file-pack-unpack-and-container-helpers.md) — This test suite validates the `.soul` archive format's core serialization functions: `pack_soul()`, `unpack_soul()`, and the `unpack_to_container()` convenience helper. It verifies identity and memory fidelity across the binary zip roundtrip, multi-layer preservation, empty-store edge cases, and robust error handling for malformed or incomplete archives.
- [Test Suite for Soul Format Auto-Detection](./test-suite-for-soul-format-auto-detection.md) — Tests for `detect_format()`, the function that inspects a filesystem path and returns the soul character format it represents — SoulSpec, TavernAI, Soul Protocol native, or unknown. Covers directory structures, JSON shapes, file extensions, PNG files, and error cases.
- [Test Suite for Soul Protocol Evaluation System](./test-suite-for-soul-protocol-evaluation-system.md) — Comprehensive tests for the heuristic evaluation engine, rubric models, and Evaluator class used to score AI companion responses across multiple quality dimensions. Covers model validation, scoring algorithms, domain auto-selection, history capping, and streak detection.
- [Test Suite for SoulProvider PocketPaw Integration](./test-suite-for-soulprovider-pocketpaw-integration.md) — Covers the `SoulProvider` reference integration class with 14 async pytest tests verifying system prompt generation, memory recall injection, interaction tracking, auto-save behavior, factory constructors, and low-energy behavioral adaptation. Tests are isolated using fresh Soul instances per fixture to prevent cross-test memory contamination.
- [Test Suite for SoulSpec Import and Export](./test-suite-for-soulspec-import-and-export.md) — Tests for the SoulSpec importer, which converts a human-authored directory of markdown files and soul.json into a Soul Protocol `SoulConfig`, and the corresponding exporter that writes those files back out. Covers OCEAN trait mapping, memory tier assignments, round-trip fidelity, and error handling.
- [Test Suite for StateManager: Mood Inertia, Label Mapping, Energy, and Auto-Regen](./test-suite-for-statemanager-mood-inertia-label-mapping-energy-and-auto-regen.md) — This test suite covers the `StateManager` component responsible for tracking a soul's emotional and physical state across interactions. It validates EMA-based mood inertia, label-to-mood mapping, the TIRED energy override, and the fully configurable `Biorhythms` parameter set including time-based auto-regeneration — with a particular focus on preventing mood instability and ensuring always-on agent deployments work correctly.
- [Test Suite for TavernAI Character Card Import and Export](./test-suite-for-tavernai-character-card-import-and-export.md) — Tests for the TavernAI importer, which converts Character Card V2 JSON (and PNG-embedded cards) into Soul Protocol `SoulConfig`, and the exporter that writes cards back. Covers field mapping to memory tiers, validation errors, and PNG tEXt chunk embedding.
- [Test Suite for Temporal Knowledge Graph](./test-suite-for-temporal-knowledge-graph.md) — This test suite validates the temporal knowledge graph (`KnowledgeGraph` and `TemporalEdge`) used by Soul Protocol to represent relationships between entities over time. It covers edge creation, time-range activation queries, relationship expiration, duplicate deduplication, point-in-time queries, evolution history, serialization roundtrips, and backward compatibility with older edge formats.
- [Test Suite for TF-IDF Embedder](./test-suite-for-tf-idf-embedder.md) — Validates the TFIDFEmbedder class, which converts text into fixed-length float vectors using term-frequency/inverse-document-frequency weighting. Tests cover the fit/embed lifecycle, semantic similarity behavior, edge cases for unfitted state, and batch consistency.
- [Test Suite for the A2A Agent Card Bridge](./test-suite-for-the-a2a-agent-card-bridge.md) — This test suite validates bidirectional conversion between Soul Protocol souls and Google A2A Agent Cards via `A2AAgentCardBridge`. It covers spec model serialization, full round-trip identity preservation (name, DID, OCEAN traits, skills), immutability guards on enrichment, and file-based CLI round-trips.
- [Test Suite for the Archival Memory Store](./test-suite-for-the-archival-memory-store.md) — This test suite validates `ArchivalMemoryStore` — the fifth memory tier that stores compressed conversation summaries as `ConversationArchive` objects. It covers creation, keyword search with overlap-based ranking, date-range overlap queries, count tracking, and get-by-id retrieval.
- [Test Suite for the Human-Soul Bond Model](./test-suite-for-the-human-soul-bond-model.md) — This test suite validates the `Bond` model that tracks the relationship strength between a soul and its bonded human, covering construction defaults, logarithmic growth on `strengthen()`, floor/ceiling clamping on `weaken()`, and Pydantic validation of out-of-range `bond_strength` values.
- [Test Suite for the Journal Engine](./test-suite-for-the-journal-engine.md) — Comprehensive test suite for the SQLite-backed journal engine — the event-sourcing backbone of soul-protocol. Covers append/query mechanics, every filter dimension, timezone enforcement, scope wildcards, monotonic timestamps, seq assignment, concurrent WAL writes, and schema migration from zero.
- [Test Suite for the Manifest Model](./test-suite-for-the-manifest-model.md) — This test suite validates the `Manifest` Pydantic model, which acts as the metadata header of a `.soul` archive. It covers default field values, explicit field assignment, and the full Pydantic serialization roundtrip to ensure no data is silently dropped during pack/unpack.
- [Test Suite for the Retrieval Spec: Credentials, Protocols, and Request Contracts](./test-suite-for-the-retrieval-spec-credentials-protocols-and-request-contracts.md) — This test suite validates the spec-level contracts defined in `soul_protocol.spec.retrieval` — covering `Credential` validation and expiry logic, structural Protocol conformance for sync and async adapters, `DataRef` promotion from dicts, and timezone-aware `RetrievalRequest` point-in-time fields. These tests intentionally contain no runtime machinery, serving as a conformance suite for any implementation of the retrieval standard.
- [Test Suite for the Spec Sub-Package](./test-suite-for-the-spec-sub-package.md) — The `tests/spec/__init__.py` file marks the `tests/spec/` directory as a Python package, enabling pytest to discover and correctly import the spec-level test modules it contains. This is a structural init with no executable code.
- [Test Suite for v0.3.4 Memory Model: MemoryCategory, Salience, and Temporal Edge Metadata](./test-suite-for-v034-memory-model-memorycategory-salience-and-temporal-edge-metad.md) — This test suite covers spec-level additions introduced in Soul Protocol v0.3.4: the `MemoryCategory` enum for classifying memory entries, `salience` as an additive activation boost on `MemoryEntry`, and `metadata` on `TemporalEdge` along with its surfacing through `KnowledgeGraph` query methods. The salience tests were rewritten from a multiplicative to an additive boost model to prevent low-importance memories from being penalized disproportionately.
- [Test Suite for Vector Similarity Functions](./test-suite-for-vector-similarity-functions.md) — Covers the three core similarity primitives — cosine similarity, Euclidean distance, and dot product — used by soul-protocol's embedding layer. Tests enforce correct mathematical behavior, zero-vector safety, empty-vector edge cases, and dimension-mismatch guards.
- [Test Suite for VectorSearchStrategy](./test-suite-for-vectorsearchstrategy.md) — Tests the VectorSearchStrategy class, which wraps an embedder to perform similarity-based memory search with configurable threshold filtering and pre-built index support. Covers both the deterministic HashEmbedder path and the semantic TFIDFEmbedder path.
- [Test Suite for Zip Recall Bug: Soul File Export/Awaken Roundtrip and Stale MCP Cache](./test-suite-for-zip-recall-bug-soul-file-exportawaken-roundtrip-and-stale-mcp-cac.md) — This regression test suite targets a production bug where `soul_recall` via the MCP server returned zero results for memories that existed in the `.soul` file on disk. The root cause was the MCP server loading souls at startup and never reloading when the file was updated externally. Tests cover semantic and episodic recall after zip roundtrip, bytes-based awakening, multi-memory preservation, content fidelity, and the stale-cache simulation.
- [Test Suite: Four Core Quality Validation Scenarios for Soul Protocol](./test-suite-four-core-quality-validation-scenarios-for-soul-protocol.md) — This test suite implements the four hardest quality probes for Soul Protocol — response quality uplift, personality consistency across OCEAN profiles, hard recall after 30 filler interactions, and emotional continuity via somatic markers — using paired LLM comparisons with a `ResponseJudge` to score soul-enriched versus baseline responses.
- [Test Suite: Graph-Augmented Recall Pipeline](./test-suite-graph-augmented-recall-pipeline.md) — Validates the integration of KnowledgeGraph with RecallEngine, covering graph traversal via progressive_context at multiple depth levels, graph-wired recall with entity matching, deduplication, and graceful degradation when no graph is provided.
- [Test Suite: Heuristic Sentiment Detection (Somatic Markers)](./test-suite-heuristic-sentiment-detection-somatic-markers.md) — Tests for the heuristic sentiment detector that produces Damasio-style somatic markers (valence, arousal, label) from raw text, covering neutral text, positive/negative words, intensifiers, negation, mixed emotions, and label accuracy.
- [Test Suite: Identity Spec — Construction, Auto-ID, Traits, and Serialization](./test-suite-identity-spec-construction-auto-id-traits-and-serialization.md) — Validates the core `Identity` primitive from `soul_protocol.spec`, covering minimal construction, deterministic auto-ID generation, uniqueness guarantees, arbitrary traits storage, and Pydantic `model_dump`/`model_validate` round-trip fidelity. Identity is the foundational spec model that all other containers reference.
- [Test Suite: JSON Schema Generation from Pydantic Models](./test-suite-json-schema-generation-from-pydantic-models.md) — Verifies that every Soul Protocol Pydantic model can produce a valid, JSON-serializable schema, that a sample `SoulConfig` dict validates against the generated schema, and that the `generate_schemas.py` script produces the expected on-disk files. This acts as a contract test for external consumers who depend on the published JSON schemas.
- [Test Suite: Known Sentiment Detector Gaps](./test-suite-known-sentiment-detector-gaps.md) — A bug-documenting test suite that captures known failure modes in the heuristic sentiment detector — missing vocabulary, joy/excitement confusion, sadness/frustration confusion, gratitude misclassification, and neutral false positives — intended to go green once the corresponding fixes land in sentiment.py.
- [Test Suite: LLM-Powered Memory Reranking and Smart Recall](./test-suite-llm-powered-memory-reranking-and-smart-recall.md) — Covers `rerank_memories()`, the `_parse_indices()` helper, and `Soul.smart_recall()` — the optional LLM-driven reranking layer on top of heuristic memory retrieval. The suite validates correct ordering, fallback behavior on engine failure or timeout, opt-in flag semantics, and prompt injection defenses.
- [Test Suite: Memory Compression -- Summarization, Deduplication, and Importance Pruning](./test-suite-memory-compression-summarization-deduplication-and-importance-pruning.md) — Tests for `MemoryCompressor`, which reduces the size of a soul's long-term memory store through four strategies: text summarization, near-duplicate removal, importance-based pruning, and export splitting. Covers edge cases like empty inputs, threshold tuning, and the rule that high-importance memories are never pruned regardless of age.
- [Test Suite: Memory Search Tokenizer and Synonym Expansion](./test-suite-memory-search-tokenizer-and-synonym-expansion.md) — Tests for two search quality improvements (issues #10 and #11): a tokenizer that splits on code-style separators like slashes and underscores, and a synonym expansion layer that maps programming terms to related aliases so queries for 'database' match memories containing 'postgresql'.
- [Test Suite: Memory Visibility Tiers](./test-suite-memory-visibility-tiers.md) — Tests for the three-tier memory visibility system (PUBLIC, BONDED, PRIVATE) that controls which memories are accessible to external requesters based on bond strength, preventing private memories from leaking while maintaining full system access for the soul itself.
- [Test Suite: MemoryManager Facade](./test-suite-memorymanager-facade.md) — Core test suite for the MemoryManager facade, covering all five memory stores (core, episodic, semantic, procedural, knowledge graph), cross-store recall, significance-gated observation, and the Bug #15 edit_core behavior fix.
- [Test Suite: Multi-Participant Interactions and Multi-Bond Identity](./test-suite-multi-participant-interactions-and-multi-bond-identity.md) — Tests for the multi-participant Interaction model (issue #95) and multi-bond Identity (issue #94), including full backward compatibility with the legacy user_input/agent_output API, serialization round-trips, and coverage of the spec-layer equivalents.
- [Test Suite: OCEAN Personality-Modulated Memory Retrieval](./test-suite-ocean-personality-modulated-memory-retrieval.md) — Comprehensive tests for the personality modulation layer that adjusts memory recall scores based on OCEAN (Big Five) personality traits, covering per-trait signal functions, the composite boost calculation, and end-to-end recall ranking changes.
- [Test Suite: Org Journal Spec — Actor, DataRef, and EventEntry Primitives](./test-suite-org-journal-spec-actor-dataref-and-evententry-primitives.md) — Validates the foundational primitives of Soul Protocol's organizational journal spec: `Actor` (the entity writing an event), `DataRef` (an external data reference with cache policy), and `EventEntry` (an immutable journal record). The suite enforces UTC-only datetimes, non-anonymous writes, required scopes, and payload union handling.
- [Test Suite Package Initializer for CLI Tests](./test-suite-package-initializer-for-cli-tests.md) — The `tests/test_cli/__init__.py` file marks the `tests/test_cli/` directory as a Python package, enabling pytest to discover and correctly namespace the CLI test modules it contains. It contains no executable code.
- [Test Suite: Phase 1 Ablation Fixes](./test-suite-phase-1-ablation-fixes.md) — Tests for five Phase 1 ablation fixes: the significance gate short-message penalty, mundane-interaction rejection, select_top_k attention selection, BM25Index scoring and the BM25SearchStrategy protocol, and logarithmic bond growth with diminishing returns.
- [Test Suite: Phase 2 Memory Architecture (Categories, Salience, Dedup)](./test-suite-phase-2-memory-architecture-categories-salience-dedup.md) — Tests for the Phase 2 memory architecture improvements: the MemoryCategory enum, generated abstracts, salience scoring, the deduplication reconcile_fact pipeline, TemporalEdge metadata in the knowledge graph, and the EpisodicStore entry-update API.
- [Test Suite: Pluggable Search Strategy Protocol](./test-suite-pluggable-search-strategy-protocol.md) — Tests for the SearchStrategy protocol and its propagation through the full memory stack — from Soul.birth() down through MemoryManager, RecallEngine, spreading_activation(), and compute_activation() — verifying that custom scoring functions can replace the default token-overlap approach at every layer.
- [Test Suite: Progressive Disclosure in Recall](./test-suite-progressive-disclosure-in-recall.md) — Tests for RecallEngine's progressive recall mode, which returns a primary set of full-content memories plus an overflow set of abstract-only summaries, allowing callers to receive more context without exceeding token budgets.
- [Test Suite: Retrieval Trace — Observability Receipt for Memory Recall](./test-suite-retrieval-trace-observability-receipt-for-memory-recall.md) — Locks the shape and runtime behavior of `RetrievalTrace`, the observability receipt emitted by `Soul.recall()` and `Soul.smart_recall()` after every memory lookup. The suite verifies that traces are populated even on empty results, are not serialized into `.soul` files, and correctly record actor, query, candidates, and rerank metadata.
- [Test Suite: Scope Tags and Hierarchical Memory Access Control](./test-suite-scope-tags-and-hierarchical-memory-access-control.md) — Tests the `match_scope` and `match_scope_strict` helpers that implement hierarchical RBAC/ABAC filtering for memory recall. The suite covers bidirectional glob containment, strict one-way matching, scope normalization, export/awaken round-trips for scoped memories, and `Soul.recall` scope-filter integration.
- [Test Suite: Self-Model Manager (Soul's Self-Concept)](./test-suite-self-model-manager-souls-self-concept.md) — Tests for SelfModelManager, which implements Klein's self-concept model — the soul's ongoing self-knowledge about what domains it is competent in, which roles it plays, and how confident it is in those self-images based on accumulated evidence.
- [Test Suite: Skills XP Progression, Level Cap, and Decay](./test-suite-skills-xp-progression-level-cap-and-decay.md) — Validates the Skills subsystem — a gamification layer that tracks a soul's proficiency across named abilities via an XP and level model. The suite covers default construction, XP accumulation with level-up triggers, overflow carry, the level 10 cap, significance-weighted XP grants, skill decay, and the `SkillRegistry` collection.
- [Test Suite: Smoke Tests for the Research Simulation Framework](./test-suite-smoke-tests-for-the-research-simulation-framework.md) — This test suite validates that all research framework components — agent generation, user generation, scenario generation, experiment configuration, metrics, statistical utilities, and memory conditions — work correctly at minimal scale without requiring any LLM API calls. It is fast enough for CI and guards against regressions in the framework before running expensive full ablations.
- [Test Suite: Soul Configuration — OCEAN, Communication, Biorhythms, Persona, and Config Files](./test-suite-soul-configuration-ocean-communication-biorhythms-persona-and-config-.md) — Validates the flexible birth configuration system for `Soul`, covering direct-kwarg birth with OCEAN personality traits, communication style, biorhythms, and persona text, as well as file-based birth from YAML and JSON configs via `Soul.birth_from_config()`. The suite also covers CLI `--config` and `--openness/--neuroticism` flags and verifies that all settings survive export → awaken round-trips.
- [Test Suite: Soul Reincarnation Lifecycle](./test-suite-soul-reincarnation-lifecycle.md) — Validates that `Soul.reincarnate()` correctly transitions a soul to a new identity while preserving accumulated state. The suite ensures that reincarnation is not a hard reset — personality, memories, values, archetype, and bond all carry forward, while the DID and incarnation counter change to mark the new life.
- [Test Suite: Soul Templates and Batch Spawning via SoulFactory](./test-suite-soul-templates-and-batch-spawning-via-soulfactory.md) — Validates `SoulTemplate` (reusable soul blueprints with personality profiles, memories, and skills) and `SoulFactory` (the factory that instantiates souls from templates, including batch spawning with controlled personality variance). The suite covers model validation, override semantics, variance clamping, reproducible seeding, and public API exports.
- [Test Suite: SoulContainer — Spec-Layer Soul Creation, Persistence, and Memory Access](./test-suite-soulcontainer-spec-layer-soul-creation-persistence-and-memory-access.md) — Validates the spec-layer `SoulContainer` — the lightweight soul abstraction for integrations that do not need the full runtime. The suite covers container construction, traits storage, `.soul` file save/open round-trips, multi-layer memory preservation, error handling for missing files, and the default `DictMemoryStore`.
- [Test Suite: test_spec Package Namespace Marker](./test-suite-testspec-package-namespace-marker.md) — An empty `__init__.py` that declares `tests/test_spec/` as a Python package. This file contains no executable code but is structurally required so that pytest and Python's import machinery can discover and run the test modules inside `test_spec/` as part of the broader test suite.

### `test-infrastructure` — 3 article(s)

- [Test Package Init: test_spike](./test-package-init-testspike.md) — This is the empty package initializer for the `tests/test_spike/` directory. It has no executable content but is required by Python to treat the directory as an importable package, enabling pytest to discover and collect the spike test files within it.
- [Test Package Init: test_state](./test-package-init-teststate.md) — This is the empty package initializer for the `tests/test_state/` directory. It has no executable content but enables pytest to discover and collect tests within the state management test package.
- [Test Package Init: test_storage](./test-package-init-teststorage.md) — This is the package initializer for the `tests/test_storage/` directory, containing a minimal comment identifying the package. It enables pytest to discover storage backend tests and scopes them under a dedicated package namespace.

### `testing` — 139 article(s)

- [CallableEngine: Wrap Any Function as a Cognitive Engine](./callableengine-wrap-any-function-as-a-cognitive-engine.md) — Wraps any sync or async callable as a `CognitiveEngine`, letting developers pass a simple lambda or function to `Soul.birth()` without implementing the full protocol. Handles both sync and async callables transparently using `asyncio.iscoroutinefunction` and `run_in_executor`.
- [CLI Test Suite: Agent Platform Integration Setup (`soul setup`)](./cli-test-suite-agent-platform-integration-setup-soul-setup.md) — Test suite for `soul setup`, which wires a Soul Protocol MCP server into AI coding platforms (Cursor, VS Code, Claude Code, Continue, Windsurf). Covers platform auto-detection, MCP config file writing and merging, instruction file injection, `.gitignore` updates, and `uvx` path resolution.
- [CLI Test Suite: Core Commands (birth, inspect, status, remember, recall, init)](./cli-test-suite-core-commands-birth-inspect-status-remember-recall-init.md) — Comprehensive test suite for the Soul Protocol CLI covering the full lifecycle of a soul: creation, inspection, memory storage across tiers, multi-format output, and init options. Tests use click.testing.CliRunner to invoke commands in isolation and assert on exit codes, output text, and ZIP archive contents.
- [CLI Test Suite: Full Onboarding, Multi-User Lifecycle, and Destroy Workflow](./cli-test-suite-full-onboarding-multi-user-lifecycle-and-destroy-workflow.md) — End-to-end test suite for the `soul init` onboarding wizard covering user scope creation, fleet provisioning, status reporting, soul deletion, and the destroy command that archives then wipes all data. Also validates journal append-only privacy constraints and users-dir precedence rules.
- [CLI Test Suite: Health, Cleanup, and Repair Commands](./cli-test-suite-health-cleanup-and-repair-commands.md) — Test suite for three soul maintenance CLI commands: `soul health` (diagnostic reporting), `soul cleanup` (duplicate/low-importance memory removal), and `soul repair` (energy reset). Covers dry-run safety guards, auto-mode mutations, and persistence verification.
- [CLI Test Suite: Org Initialization (`soul org init`)](./cli-test-suite-org-initialization-soul-org-init.md) — Test suite for `soul org init`, which bootstraps an organization by creating a root soul, a journal, and cryptographic keys. Covers happy-path artifact creation, idempotency guards, force-overwrite behavior, key file permissions, journal privacy, and round-trip loadability.
- [CLI Test Suite: Soul Context Injection into AI IDE Config Files](./cli-test-suite-soul-context-injection-into-ai-ide-config-files.md) — Test suite for `soul inject`, which writes a soul's identity and recent memories into AI IDE configuration files (Claude Code, Cursor, VS Code, Windsurf, Cline, Continue). Covers target path resolution, idempotent section replacement, context block formatting, memory truncation, and auto-discovery of soul directories.
- [Cryptographic Encryption Test Suite (Fernet + PBKDF2)](./cryptographic-encryption-test-suite-fernet-pbkdf2.md) — Test suite validating the `soul_protocol.runtime.crypto.encrypt` module, which provides passphrase-based symmetric encryption for soul data. Covers the full encrypt/decrypt roundtrip, wrong-passphrase rejection, and deterministic key derivation.
- [Developer Onboarding Demo Smoke Test](./developer-onboarding-demo-smoke-test.md) — A minimal async smoke test that runs the soul-protocol onboarding demo end-to-end in a non-interactive mode. It uses `monkeypatch` to set `SOUL_DEMO_NO_PAUSE=1`, preventing the demo from blocking on user input during CI.
- [Dream Engine Test Suite — Memory Consolidation and Behavioral Analysis](./dream-engine-test-suite-memory-consolidation-and-behavioral-analysis.md) — Comprehensive test suite for the soul-protocol `dream` subsystem, which performs offline memory consolidation: clustering topics, detecting recurring procedures, pruning duplicates, and suggesting personality evolution. Tests span unit-level phase functions through full integration scenarios with before/after memory state comparisons.
- [DSPy Integration Test Suite — Cognitive Processor, Adapter, and Optimizer](./dspy-integration-test-suite-cognitive-processor-adapter-and-optimizer.md) — Test suite for the DSPy-powered cognitive layer in soul-protocol, covering module instantiation, the `DSPyCognitiveProcessor` adapter bridge, training data generation, graceful fallback when DSPy is absent, and the `SoulOptimizer`. All LLM calls are mocked — no real API calls are made.
- [Embedding Provider Factory Test Suite](./embedding-provider-factory-test-suite.md) — Tests for `get_embedding_provider()`, the factory function that instantiates embedding backends by name. Covers all built-in providers (hash, TF-IDF), external providers with lazy imports (sentence-transformers, OpenAI, Ollama), kwargs passthrough, unknown provider errors, and import failure handling.
- [Embedding Provider Protocol Compliance Test Suite](./embedding-provider-protocol-compliance-test-suite.md) — Tests that verify `HashEmbedder` and `TFIDFEmbedder` correctly satisfy the `EmbeddingProvider` structural protocol at runtime. Also validates that partial or non-compliant implementations fail the `isinstance` check, ensuring the protocol is genuinely enforced rather than nominal.
- [Embeddings Test Package Initializer](./embeddings-test-package-initializer.md) — Package initializer for the `tests/test_embeddings/` test subpackage, created to group all embedding-related test modules under a common namespace. The file contains no executable code — its presence makes the directory a Python package discoverable by pytest.
- [End-to-End Integration Test Suite — Soul Lifecycle, Memory Persistence, and Error Handling](./end-to-end-integration-test-suite-soul-lifecycle-memory-persistence-and-error-ha.md) — Full end-to-end integration tests for the Soul class covering the complete lifecycle from birth through export and awakening. Tests validate memory persistence across all five tiers, self-model emergence, core memory editing, directory format save/load, and robust error handling for corrupt or missing soul files.
- [End-to-End New Soul Primitives Test — Bond, Skills, Retirement, and Reincarnation](./end-to-end-new-soul-primitives-test-bond-skills-retirement-and-reincarnation.md) — End-to-end test suite for newer soul-protocol primitives introduced post-v1: bond mechanics, skill registry, lifecycle state transitions (active → retired → reincarnated), and `.soul` archive integrity. Tests validate the logarithmic bond growth curve and the presence of `dna.md` in exported archives.
- [End-to-End Test Suite for Eternal Storage Lifecycle](./end-to-end-test-suite-for-eternal-storage-lifecycle.md) — End-to-end tests covering the full eternal storage lifecycle: birth a soul, archive to multiple tiers, verify archives exist, recover from each tier individually, and confirm recovered data can be awakened into a valid Soul. Also tests multi-tier fallback recovery when primary sources fail.
- [End-to-End Vector Search Test Suite — TF-IDF Semantic Memory Retrieval](./end-to-end-vector-search-test-suite-tf-idf-semantic-memory-retrieval.md) — End-to-end tests for soul-protocol's semantic memory search pipeline using `TFIDFEmbedder` and `VectorSearchStrategy`. Tests create memory entries on distinct topics, fit the embedder, and verify that topical queries return relevant results in the correct order.
- [Engine Test Package Initializer](./engine-test-package-initializer.md) — Marks `tests/test_engine/` as a Python package so pytest can discover tests within it. Created as part of the journal-engine workstream in the Org Architecture RFC.
- [Eternal Storage Test Package Initializer](./eternal-storage-test-package-initializer.md) — Marks `tests/test_eternal/` as a Python package, enabling pytest discovery of the eternal storage subsystem's test modules. Created when the eternal storage subsystem was introduced on 2026-03-06.
- [Hash Embedder: Deterministic Pseudo-Embeddings for Testing](./hash-embedder-deterministic-pseudo-embeddings-for-testing.md) — The `HashEmbedder` produces deterministic fixed-length vectors from text using character n-gram hashing — no external dependencies, no model downloads, same input always yields the same output. It is intentionally non-semantic (similar texts do not produce similar vectors) and exists solely to enable embedding pipeline tests without requiring real embedding backends.
- [Hash Embedder Test Suite — Determinism, Normalization, and Batch Embedding](./hash-embedder-test-suite-determinism-normalization-and-batch-embedding.md) — Test suite for `HashEmbedder`, soul-protocol's zero-dependency embedding backend that converts text to L2-normalized vectors via hashing. Covers dimensionality configuration, deterministic output, L2 normalization, edge cases (empty strings, whitespace), and batch embedding consistency.
- [InMemoryStorage: Dict-Backed Soul Backend for Tests and Ephemeral Processes](./inmemorystorage-dict-backed-soul-backend-for-tests-and-ephemeral-processes.md) — `InMemoryStorage` is a zero-dependency, dict-backed implementation of `StorageProtocol` for unit tests and short-lived processes. All methods are `async` to satisfy the protocol interface, even though no I/O occurs.
- [Mock Arweave Eternal Storage Provider](./mock-arweave-eternal-storage-provider.md) — `MockArweaveProvider` simulates Arweave's permanent, pay-once storage model using an in-memory dict and deterministic transaction IDs — no gateway required. It lets test suites and local development exercise the full Arweave archive/retrieve/verify cycle without real AR tokens or network access.
- [Mock Blockchain Soul Registry Provider](./mock-blockchain-soul-registry-provider.md) — `MockBlockchainProvider` simulates an on-chain soul registry where each soul is minted as a unique NFT-like token with a hash-based token ID. It enables testing of blockchain-based soul identity ownership without a real wallet, chain, or gas fees.
- [Mock IPFS Content-Addressed Storage Provider](./mock-ipfs-content-addressed-storage-provider.md) — `MockIPFSProvider` simulates IPFS's content-addressed storage model by generating deterministic CID-like references from the SHA-256 hash of the archived data and storing it in memory. It faithfully reproduces the key property of IPFS — the same content always produces the same address — without requiring a running IPFS node.
- [Ollama Embedding Provider Test Suite — Local Model Integration with Lazy Loading](./ollama-embedding-provider-test-suite-local-model-integration-with-lazy-loading.md) — Test suite for `OllamaEmbeddingProvider`, which connects to a locally-running Ollama server for embedding generation. Tests use a mocked `ollama` library to avoid requiring a real server in CI, covering protocol compliance, lazy client initialization, batch embedding, custom model and base URL configuration, and graceful import error handling.
- [OpenAI Embedding Provider Test Suite — API Key Validation, Retry Logic, and Batch Embedding](./openai-embedding-provider-test-suite-api-key-validation-retry-logic-and-batch-em.md) — Test suite for `OpenAIEmbeddingProvider`, which calls the OpenAI embeddings API for high-quality vector representations. Tests use a mocked `openai` library and cover protocol compliance, API key validation, exponential backoff retry logic for rate limits, model dimension detection, and graceful import error handling.
- [Real-World End-to-End Test Suite — Developer Onboarding, Bonding, and Evolution Scenarios](./real-world-end-to-end-test-suite-developer-onboarding-bonding-and-evolution-scen.md) — Scenario-driven end-to-end tests that simulate realistic human-soul conversations across multiple contexts: developer technical discussions, personal emotional bonding, and mixed sessions. Tests validate that all soul systems (entity extraction, knowledge graph, bond growth, evolution, self-model) fire correctly in natural usage patterns.
- [Regression Tests for Bug #15: Core Memory Replace vs. Append](./regression-tests-for-bug-15-core-memory-replace-vs-append.md) — This regression suite locks the fix for Bug #15, where `edit_core_memory()` was appending new values to existing persona and human fields instead of replacing them. The tests verify replace semantics, partial-field isolation, and the explicit absence of the old append behavior.
- [Sentence Transformer Embedding Provider Test Suite — Lazy Loading and Mock-Based Validation](./sentence-transformer-embedding-provider-test-suite-lazy-loading-and-mock-based-v.md) — Test suite for `SentenceTransformerProvider`, which wraps the `sentence-transformers` library for high-quality local embedding generation. Tests mock the heavy ML dependency to run in CI, covering protocol compliance, lazy model loading, batch embedding, custom model and device configuration, and import error handling.
- [Shared Test Fixtures for the Soul Protocol Suite](./shared-test-fixtures-for-the-soul-protocol-suite.md) — `conftest.py` centralizes the four foundational fixtures used across all soul-protocol test modules: a sample `Identity`, a `SoulConfig` built from it, a fully-birthed `Soul`, and a temporary `.soul` file on disk. Centralizing these prevents fixture duplication and ensures all tests operate on consistent, valid starting state.
- [Test Package Init: Cognitive Engine Test Suite Namespace](./test-package-init-cognitive-engine-test-suite-namespace.md) — Empty `__init__.py` that marks `tests/test_cognitive/` as a Python package, enabling pytest to discover and import tests in the cognitive engine subdirectory. No executable code; its presence is a structural requirement for the test namespace.
- [Test Package Init: Evolution Subsystem](./test-package-init-evolution-subsystem.md) — Empty package initialiser that makes `tests/test_evolution/` a proper Python package, enabling pytest to discover and import the evolution test modules correctly. No executable code is present; the file's value is purely structural.
- [Test Package Init: Importers Subsystem](./test-package-init-importers-subsystem.md) — Empty package initialiser that designates `tests/test_importers/` as a Python package, enabling pytest to collect the importer-specific test modules contained within. No executable code is present.
- [Test Package Init: Install Invariants](./test-package-init-install-invariants.md) — Package initialiser for `tests/test_install/`, the sub-package that verifies install-time invariants such as the CLI being functional after a bare `pip install soul-protocol`. Created to address issue #157.
- [Test Package Init: Long-Horizon Ablation Study](./test-package-init-long-horizon-ablation-study.md) — Package initialiser for `tests/test_long_horizon/`, the sub-package housing ablation study tests that evaluate soul memory and context performance over extended interaction sequences. Created 2026-03-11.
- [Test Package Init: test_spike](./test-package-init-testspike.md) — This is the empty package initializer for the `tests/test_spike/` directory. It has no executable content but is required by Python to treat the directory as an importable package, enabling pytest to discover and collect the spike test files within it.
- [Test Package Init: test_state](./test-package-init-teststate.md) — This is the empty package initializer for the `tests/test_state/` directory. It has no executable content but enables pytest to discover and collect tests within the state management test package.
- [Test Package Init: test_storage](./test-package-init-teststorage.md) — This is the package initializer for the `tests/test_storage/` directory, containing a minimal comment identifying the package. It enables pytest to discover storage backend tests and scopes them under a dedicated package namespace.
- [Test Package Initializer for MCP Server Integration Tests](./test-package-initializer-for-mcp-server-integration-tests.md) — The `tests/test_mcp/__init__.py` file marks the `test_mcp` directory as a Python package, enabling pytest to discover and organize MCP server integration tests as a cohesive module. It contains no executable code — its presence is structural.
- [Test Package Initializer for Memory Subsystem Tests](./test-package-initializer-for-memory-subsystem-tests.md) — The `tests/test_memory/__init__.py` file marks the `test_memory` directory as a Python package, enabling pytest to discover and organize the memory subsystem test suite as a cohesive module. It contains no executable code.
- [Test Package Initializer: test_models](./test-package-initializer-testmodels.md) — Minimal package initializer for the test_models test directory, which makes the directory a Python package so pytest can discover and import test modules within it.
- [Test Suite: Cognitive Adapter Layer -- CallableEngine, Auto-Detection, and Provider Integration](./test-suite-cognitive-adapter-layer-callableengine-auto-detection-and-provider-in.md) — Tests for the adapter layer that bridges Soul Protocol's `CognitiveEngine` protocol with real LLM providers (Anthropic, OpenAI, Ollama) and user-supplied callables. Covers callable wrapping, environment-variable-driven auto-detection, Soul factory integration, import safety for optional dependencies, and HTTP-level mocking for Ollama.
- [Test Suite: Cognitive Engine, Heuristic Routing, and LLM Fallback](./test-suite-cognitive-engine-heuristic-routing-and-llm-fallback.md) — Comprehensive test suite for the Soul Protocol cognitive processing layer, covering HeuristicEngine task routing, CognitiveProcessor LLM integration, JSON parsing helpers, and graceful fallback when LLMs fail or return garbage. Also includes regression tests for the heuristic-only mode bug.
- [Test Suite: Cognitive Prompt Template Formatting and JSON Parsability](./test-suite-cognitive-prompt-template-formatting-and-json-parsability.md) — Tests that every cognitive prompt template in Soul Protocol formats without error and produces output parseable by the JSON extraction helper. Validates both the template variables contract and the round-trip between typical LLM responses and the `_parse_json` function.
- [Test Suite: Context Compaction -- Three-Level Token Budget Enforcement](./test-suite-context-compaction-three-level-token-budget-enforcement.md) — Tests for Soul Protocol's context compaction system, which keeps conversation history within a token budget through three escalating strategies: LLM-generated summaries, bullet compression, and hard truncation. Covers zero-cost paths, summary/bullet/truncation tiers, idempotency, and guaranteed convergence.
- [Test Suite: Context Spec Models -- Pydantic Schemas for Conversation Context](./test-suite-context-spec-models-pydantic-schemas-for-conversation-context.md) — Tests for the Pydantic models that define Soul Protocol's conversation context data layer, including `CompactionLevel` enum, `ContextMessage`, `ContextNode`, and the result types (`AssembleResult`, `GrepResult`, `ExpandResult`, `DescribeResult`). Validates defaults, serialization round-trips, unique ID generation, and string-enum comparability.
- [Test Suite: Context Store Retrieval — Grep, Expand, and Describe](./test-suite-context-store-retrieval-grep-expand-and-describe.md) — Validates the three retrieval operations on Soul Protocol's conversation context store: regex-based message search (`grep`), DAG node expansion to recover original messages (`expand`), and metadata snapshot generation (`describe`). These operations power agent tools that let an LLM inspect its own conversation history without re-loading the full context window.
- [Test Suite: Context Store -- SQLite-Backed Conversation History](./test-suite-context-store-sqlite-backed-conversation-history.md) — Tests for Soul Protocol's SQLite-backed context store, which persists conversation messages and compaction nodes with sequential ordering, full-text grep, date-range filtering, and cross-reconnect durability. Covers both in-memory (test) and file-backed (persistence) store configurations.
- [Test Suite: Core Pydantic Data Models](./test-suite-core-pydantic-data-models.md) — Validates the foundational Pydantic models in soul_protocol.types — Identity defaults, Personality trait bounds, SoulConfig JSON serialization round-trip, MemoryEntry defaults, Mood enum completeness, and EvolutionMode enum values.
- [Test Suite: Core Soul API — Birth, Memory, Emotion, Export, and Psychology Pipeline](./test-suite-core-soul-api-birth-memory-emotion-export-and-psychology-pipeline.md) — The primary integration test suite for the `Soul` class, covering the full API surface from construction through the psychology pipeline. It validates that soul birth, memory operations, emotional state, import/export round-trips, system prompt generation, and the attention gate all work correctly together.
- [Test Suite: Cross-Session Contradiction Detection in the Observe Pipeline](./test-suite-cross-session-contradiction-detection-in-the-observe-pipeline.md) — End-to-end regression tests for Soul Protocol's contradiction detection, targeting a specific bug where heuristic contradiction detection fired in isolation but silently failed during `observe()` when a user updated a fact across sessions (e.g., location or employer change). Tests verify that new facts correctly supersede old ones.
- [Test Suite: Decision Trace Spec — Agent Proposals, Human Corrections, and Graduation](./test-suite-decision-trace-spec-agent-proposals-human-corrections-and-graduation.md) — Validates the decision-trace spec from Workstream D of the Org Architecture RFC, covering the three payload types (`AgentProposal`, `HumanCorrection`, `DecisionGraduation`), their JSON round-trips, field validation, and the builder helpers that emit correctly shaped `EventEntry` records to the org journal.
- [Test Suite for Archival Memory Integration: Compression Pipeline and Persistence](./test-suite-for-archival-memory-integration-compression-pipeline-and-persistence.md) — This integration test suite validates the full archival pipeline in `MemoryManager`: how old episodic entries are compressed into `ConversationArchive` objects, how high-importance entries become key moments, how archived entries are excluded from recall, and how archives survive serialization round-trips through `to_dict()` / `from_dict()`.
- [Test Suite for Auto-Scheduled Consolidation (F5)](./test-suite-for-auto-scheduled-consolidation-f5.md) — This test suite validates that `Soul.observe()` automatically triggers memory consolidation (archival and reflection) at a configurable interval, that the interval is configurable and disableable, and that the interaction count persists through export/awaken cycles so consolidation timing survives restarts.
- [Test Suite for Base Install Invariants (Issue #157)](./test-suite-for-base-install-invariants-issue-157.md) — Regression tests that verify a bare `pip install soul-protocol` produces a working CLI, by checking that click, rich, pyyaml, and cryptography are declared as base dependencies, the deprecated `[engine]` extra still exists for backwards compatibility, and the CLI module imports cleanly without optional extras.
- [Test Suite for Bi-Temporal Memory Fields (ingested_at and superseded)](./test-suite-for-bi-temporal-memory-fields-ingestedat-and-superseded.md) — Validates bi-temporal tracking on MemoryEntry — the `ingested_at` timestamp (when a fact entered this soul's memory vs. when it was created) and the `superseded` flag (marking outdated facts replaced by newer information). Tests cover defaults, explicit values, manager behavior, serialization round-trips, and backward compatibility with soul files that predate these fields.
- [Test Suite for Bundled Role Templates and SoulFactory Template Loading](./test-suite-for-bundled-role-templates-and-soulfactory-template-loading.md) — This test suite locks the contracts for the four bundled archetypes (Arrow, Flash, Cyborg, Analyst), the YAML/JSON template loaders, template instantiation into real souls, and the scope propagation behavior that ensures Arrow's sales-scoped core memories are visible to concrete `org:sales:leads` callers.
- [Test Suite for Eternal Storage CLI Commands](./test-suite-for-eternal-storage-cli-commands.md) — Tests the soul-protocol CLI commands for eternal storage operations: archive (with optional tier selection), eternal-status (tier health check), and recover (with failure path handling). Uses Click's CliRunner for isolated, in-process command invocation.
- [Test Suite for Eternal Storage Protocol Compliance](./test-suite-for-eternal-storage-protocol-compliance.md) — Verifies that all eternal storage providers satisfy the EternalStorageProvider protocol at runtime, and validates the Pydantic models ArchiveResult and RecoverySource for field defaults, full construction, and serialization round-trips.
- [Test Suite for Eternal Storage Providers](./test-suite-for-eternal-storage-providers.md) — Per-provider tests for the four eternal storage backends: MockIPFSProvider, MockArweaveProvider, MockBlockchainProvider, and LocalStorageProvider. Each provider is tested for archive, retrieve, verify, content addressing, unique identifiers, and error behavior on missing references.
- [Test Suite for EternalStorageManager](./test-suite-for-eternalstoragemanager.md) — Unit tests for EternalStorageManager — the orchestration layer that routes archive, recover, and verify operations across multiple storage providers. Covers provider registration/unregistration, selective tier archiving, fallback recovery chains, unavailable-provider skipping, and verification status.
- [Test Suite for Fact and Entity Extraction from Interaction Text](./test-suite-for-fact-and-entity-extraction-from-interaction-text.md) — Validates the pattern-based extraction pipeline that parses user messages for facts (name, preferences, identity, tools, projects) and entities (tech terms, proper nouns with inferred relations). Tests cover each extraction pattern, punctuation stripping, stopword exclusion, and deduplication of repeated facts.
- [Test Suite for GDPR-Compliant Memory Deletion](./test-suite-for-gdpr-compliant-memory-deletion.md) — Comprehensive tests for Soul Protocol's right-to-erasure implementation, covering query-based, entity-based, and time-based deletion across all memory tiers, with full audit trail verification. Ensures deleted memories become invisible to recall and that audit records never expose the deleted content itself.
- [Test Suite for InMemoryStorage and FileStorage Backends](./test-suite-for-inmemorystorage-and-filestorage-backends.md) — This test suite validates both storage backends provided by Soul Protocol's runtime: `InMemoryStorage` (dict-backed, in-process) and `FileStorage` (disk-backed, directory-per-soul). It covers the full CRUD surface — save, load, delete, and list — with particular attention to idempotent delete behavior and the expected on-disk directory structure for `FileStorage`.
- [Test Suite for Journal 0.3.2 Primitives](./test-suite-for-journal-032-primitives.md) — Smoke, end-to-end, and real-world simulation tests for the primitives introduced in soul-protocol v0.3.2, specifically the append-returns-committed-entry contract and action prefix querying. Tests mirror realistic consumer patterns from the PocketPaw widget store.
- [Test Suite for JournalBackedMemoryStore Benchmark Harness](./test-suite-for-journalbackedmemorystore-benchmark-harness.md) — This benchmark harness compares `JournalBackedMemoryStore` against the baseline `DictMemoryStore` across four dimensions: recall quality (recall@5), write latency, search latency, and delete correctness. It was created to produce a data-driven decision on whether the append-only journal architecture is worth adopting, with pass criteria derived from the spike design document.
- [Test Suite for JournalBackedMemoryStore: Unit, Smoke, E2E, and Real-World Scenarios](./test-suite-for-journalbackedmemorystore-unit-smoke-e2e-and-real-world-scenarios.md) — This comprehensive test suite for `JournalBackedMemoryStore` follows a four-tier discipline — unit, smoke, E2E, and real-world simulations — covering every public method and the failure modes that motivated the journal architecture. Special attention is given to disaster recovery (the April 2026 cleanup incident), GDPR-compliant tombstone deletion, idempotent rebuild, and latency budgets.
- [Test Suite for Knowledge Graph Traversal](./test-suite-for-knowledge-graph-traversal.md) — Tests for the `KnowledgeGraph` traversal API, covering BFS traversal, shortest-path finding, neighbourhood extraction, subgraph extraction, and progressive context rendering at three detail levels. Validates edge expiry, cycle handling, and node deduplication across a range of graph topologies.
- [Test Suite for LCMContext (Long-Context Memory)](./test-suite-for-lcmcontext-long-context-memory.md) — Integration tests for `LCMContext`, the context engine that ingests conversation messages, assembles token-budgeted context windows, and auto-compacts history using a `CognitiveEngine`. Covers protocol compliance, initialization guards, token estimation, compaction triggers, grep search, and lifecycle close/reopen semantics.
- [Test Suite for Learning Events Pipeline](./test-suite-for-learning-events-pipeline.md) — Tests for the full learning events pipeline — from `RubricResult` score thresholding to `LearningEvent` creation, skill XP grant, and the `Soul.learn()` convenience method. Covers model validation, score boundary conditions, XP scaling, and spec module exportability.
- [Test Suite for Long-Horizon Benchmark Analysis (Cohen's d, Effect Labels, Report Generation)](./test-suite-for-long-horizon-benchmark-analysis-cohens-d-effect-labels-report-gen.md) — Validates the statistical analysis layer of the long-horizon benchmark framework, covering Cohen's d calculation, effect-size labeling, and the LongHorizonAnalyzer that produces summary tables and Markdown reports from experimental results. Uses a deterministic mock-results factory to isolate the analysis logic from live LLM calls.
- [Test Suite for Long-Horizon Benchmark Runner](./test-suite-for-long-horizon-benchmark-runner.md) — Validates the multi-condition benchmark runner that processes conversation scenarios through different soul configurations (Full Soul, RAG Only, Personality Only, Bare) and records recall, memory growth, and bond-strength metrics. Async tests use a short 15-turn scenario fixture to keep CI fast while still exercising the full execution path.
- [Test Suite for Long-Horizon Benchmark Scenarios](./test-suite-for-long-horizon-benchmark-scenarios.md) — Validates the three benchmark scenario generators (Life Updates, Emotional Rollercoaster, Adversarial Burial) that produce 100+ turn conversation scripts with planted facts, test points, and recall challenges. Tests verify determinism, scenario structure, seeded variation, and the TestPoint dataclass.
- [Test Suite for MCP Psychology Tools (Skills, Evaluate, Learn, Evolve)](./test-suite-for-mcp-psychology-tools-skills-evaluate-learn-evolve.md) — Validates the higher-order MCP tools that expose soul psychology — skill tracking, rubric-based evaluation, learning events, and the propose/approve/reject evolution workflow. Tests use FastMCP's in-process client to exercise the full MCP tool call path without a network round-trip.
- [Test Suite for MCP Sampling Engine (LLM Think with Heuristic Fallback)](./test-suite-for-mcp-sampling-engine-llm-think-with-heuristic-fallback.md) — Validates the MCPSamplingEngine, which wraps the MCP context's sampling capability to give souls LLM-powered reflection, with graceful fallback to the HeuristicEngine when the MCP context is unavailable or raises. Tests cover the success path, three failure scenarios, the no-context path, and the server's engine wiring lifecycle.
- [Test Suite for MCP Server Core Tools (Birth, Observe, Memory, State, Lifecycle)](./test-suite-for-mcp-server-core-tools-birth-observe-memory-state-lifecycle.md) — Integration tests for the core soul_protocol MCP server tools, covering soul lifecycle (birth, save, export), memory operations (remember, recall), emotional state (feel), observation, reflection, and multi-soul management (list, switch). Uses a registry reset helper and in-process FastMCP client to achieve full isolation between tests.
- [Test Suite for Memory Activation Functions (ACT-R Base-Level, Spreading, Emotional Boost)](./test-suite-for-memory-activation-functions-act-r-base-level-spreading-emotional-.md) — Validates the three activation components that determine how retrievable a memory is at query time: the ACT-R base-level activation formula (recency and frequency), spreading activation (query-content token overlap), and emotional boost (somatic marker arousal and valence). Together these tests pin the mathematical contracts that underlie memory retrieval scoring.
- [Test Suite for Memory Attention and Interaction Significance Scoring](./test-suite-for-memory-attention-and-interaction-significance-scoring.md) — Validates the attention system that decides whether an interaction is significant enough to trigger memory storage. Tests cover novelty scoring, emotional intensity detection, goal-relevance matching against core values, and the composite SignificanceScore type.
- [Test Suite for Memory Consolidation, Reflection, and Fact Supersession](./test-suite-for-memory-consolidation-reflection-and-fact-supersession.md) — Validates the consolidation pipeline that transforms raw episodic memories into structured semantic knowledge — converting summaries into semantic memories, grouping episodes under general-event themes, storing self-insight and emotional patterns, and superseding outdated facts. Also tests the reflect() entry point and the apply=False dry-run mode.
- [Test Suite for Memory Contradiction Detection (Heuristic and LLM Modes)](./test-suite-for-memory-contradiction-detection-heuristic-and-llm-modes.md) — Validates the contradiction detector that identifies when a new memory conflicts with existing ones — using both fast heuristic rules (negation patterns, entity-attribute conflicts) and LLM-powered analysis. Tests cover true-positive detection, false-positive prevention, superseded-memory exclusion, similarity thresholds, and graceful fallback when the LLM errors.
- [Test Suite for Memory Deduplication (Jaccard Similarity, Short-Token False Positives, and Containment Merge)](./test-suite-for-memory-deduplication-jaccard-similarity-short-token-false-positiv.md) — Validates the deduplication system that prevents near-identical facts from being stored multiple times, covering the tokenizer, Jaccard similarity computation, short-token false-positive prevention (go vs. js, ai vs. ml), fact reconciliation decisions (CREATE/SKIP/MERGE), and the containment-merge logic for enriched facts.
- [Test Suite for MemoryEntry, MemoryStore Protocol, and DictMemoryStore](./test-suite-for-memoryentry-memorystore-protocol-and-dictmemorystore.md) — This test suite validates the core in-memory building blocks of Soul Protocol's spec-level memory system: the `MemoryEntry` data model, the `MemoryStore` protocol interface, and the `DictMemoryStore` reference implementation. Tests cover entry creation, metadata handling, layer isolation, BM25-style search, delete semantics, and ordering guarantees.
- [Test Suite for Soul Error Handling](./test-suite-for-soul-error-handling.md) — Verifies that Soul's three main lifecycle operations — awaken(), export(), and retire() — raise specific, descriptive exception types rather than raw Python exceptions. Also tests exception class structure to ensure callers can extract actionable information like file paths and failure reasons.
- [Test Suite for Soul Eternal Storage Integration](./test-suite-for-soul-eternal-storage-integration.md) — Tests the integration between the Soul class and EternalStorageManager, covering Soul.archive(), the with_mocks() factory, the eternal= parameter on Soul.birth() and Soul.awaken(), and the export(archive=True) convenience flag.
- [Test Suite for Soul Evolution Manager](./test-suite-for-soul-evolution-manager.md) — Tests for `EvolutionManager`, which controls how a soul's DNA traits mutate over time across three modes: supervised, autonomous, and disabled. Covers the full mutation lifecycle — propose, approve, reject, apply — and guards against illegal mutations on immutable traits.
- [Test Suite for Soul Export and Pack/Unpack Operations](./test-suite-for-soul-export-and-packunpack-operations.md) — Tests for the `.soul` zip archive format, covering the full pack/unpack roundtrip, required file inventory inside the archive, and memory tier data inclusion. Ensures that `SoulConfig` and memory content survive serialisation to bytes and back without loss.
- [Test Suite for Soul File Encryption](./test-suite-for-soul-file-encryption.md) — Validates the full encryption stack for .soul files, from low-level blob crypto primitives through pack/unpack pipeline integration to the high-level Soul.export() and Soul.awaken() API. Covers round-trips, wrong-password errors, backward compatibility with unencrypted files, and manifest readability without a password.
- [Test Suite for Soul File Pack, Unpack, and Container Helpers](./test-suite-for-soul-file-pack-unpack-and-container-helpers.md) — This test suite validates the `.soul` archive format's core serialization functions: `pack_soul()`, `unpack_soul()`, and the `unpack_to_container()` convenience helper. It verifies identity and memory fidelity across the binary zip roundtrip, multi-layer preservation, empty-store edge cases, and robust error handling for malformed or incomplete archives.
- [Test Suite for Soul Format Auto-Detection](./test-suite-for-soul-format-auto-detection.md) — Tests for `detect_format()`, the function that inspects a filesystem path and returns the soul character format it represents — SoulSpec, TavernAI, Soul Protocol native, or unknown. Covers directory structures, JSON shapes, file extensions, PNG files, and error cases.
- [Test Suite for Soul Protocol Evaluation System](./test-suite-for-soul-protocol-evaluation-system.md) — Comprehensive tests for the heuristic evaluation engine, rubric models, and Evaluator class used to score AI companion responses across multiple quality dimensions. Covers model validation, scoring algorithms, domain auto-selection, history capping, and streak detection.
- [Test Suite for SoulProvider PocketPaw Integration](./test-suite-for-soulprovider-pocketpaw-integration.md) — Covers the `SoulProvider` reference integration class with 14 async pytest tests verifying system prompt generation, memory recall injection, interaction tracking, auto-save behavior, factory constructors, and low-energy behavioral adaptation. Tests are isolated using fresh Soul instances per fixture to prevent cross-test memory contamination.
- [Test Suite for SoulSpec Import and Export](./test-suite-for-soulspec-import-and-export.md) — Tests for the SoulSpec importer, which converts a human-authored directory of markdown files and soul.json into a Soul Protocol `SoulConfig`, and the corresponding exporter that writes those files back out. Covers OCEAN trait mapping, memory tier assignments, round-trip fidelity, and error handling.
- [Test Suite for StateManager: Mood Inertia, Label Mapping, Energy, and Auto-Regen](./test-suite-for-statemanager-mood-inertia-label-mapping-energy-and-auto-regen.md) — This test suite covers the `StateManager` component responsible for tracking a soul's emotional and physical state across interactions. It validates EMA-based mood inertia, label-to-mood mapping, the TIRED energy override, and the fully configurable `Biorhythms` parameter set including time-based auto-regeneration — with a particular focus on preventing mood instability and ensuring always-on agent deployments work correctly.
- [Test Suite for TavernAI Character Card Import and Export](./test-suite-for-tavernai-character-card-import-and-export.md) — Tests for the TavernAI importer, which converts Character Card V2 JSON (and PNG-embedded cards) into Soul Protocol `SoulConfig`, and the exporter that writes cards back. Covers field mapping to memory tiers, validation errors, and PNG tEXt chunk embedding.
- [Test Suite for Temporal Knowledge Graph](./test-suite-for-temporal-knowledge-graph.md) — This test suite validates the temporal knowledge graph (`KnowledgeGraph` and `TemporalEdge`) used by Soul Protocol to represent relationships between entities over time. It covers edge creation, time-range activation queries, relationship expiration, duplicate deduplication, point-in-time queries, evolution history, serialization roundtrips, and backward compatibility with older edge formats.
- [Test Suite for TF-IDF Embedder](./test-suite-for-tf-idf-embedder.md) — Validates the TFIDFEmbedder class, which converts text into fixed-length float vectors using term-frequency/inverse-document-frequency weighting. Tests cover the fit/embed lifecycle, semantic similarity behavior, edge cases for unfitted state, and batch consistency.
- [Test Suite for the A2A Agent Card Bridge](./test-suite-for-the-a2a-agent-card-bridge.md) — This test suite validates bidirectional conversion between Soul Protocol souls and Google A2A Agent Cards via `A2AAgentCardBridge`. It covers spec model serialization, full round-trip identity preservation (name, DID, OCEAN traits, skills), immutability guards on enrichment, and file-based CLI round-trips.
- [Test Suite for the Archival Memory Store](./test-suite-for-the-archival-memory-store.md) — This test suite validates `ArchivalMemoryStore` — the fifth memory tier that stores compressed conversation summaries as `ConversationArchive` objects. It covers creation, keyword search with overlap-based ranking, date-range overlap queries, count tracking, and get-by-id retrieval.
- [Test Suite for the Human-Soul Bond Model](./test-suite-for-the-human-soul-bond-model.md) — This test suite validates the `Bond` model that tracks the relationship strength between a soul and its bonded human, covering construction defaults, logarithmic growth on `strengthen()`, floor/ceiling clamping on `weaken()`, and Pydantic validation of out-of-range `bond_strength` values.
- [Test Suite for the Journal Engine](./test-suite-for-the-journal-engine.md) — Comprehensive test suite for the SQLite-backed journal engine — the event-sourcing backbone of soul-protocol. Covers append/query mechanics, every filter dimension, timezone enforcement, scope wildcards, monotonic timestamps, seq assignment, concurrent WAL writes, and schema migration from zero.
- [Test Suite for the Manifest Model](./test-suite-for-the-manifest-model.md) — This test suite validates the `Manifest` Pydantic model, which acts as the metadata header of a `.soul` archive. It covers default field values, explicit field assignment, and the full Pydantic serialization roundtrip to ensure no data is silently dropped during pack/unpack.
- [Test Suite for the Retrieval Spec: Credentials, Protocols, and Request Contracts](./test-suite-for-the-retrieval-spec-credentials-protocols-and-request-contracts.md) — This test suite validates the spec-level contracts defined in `soul_protocol.spec.retrieval` — covering `Credential` validation and expiry logic, structural Protocol conformance for sync and async adapters, `DataRef` promotion from dicts, and timezone-aware `RetrievalRequest` point-in-time fields. These tests intentionally contain no runtime machinery, serving as a conformance suite for any implementation of the retrieval standard.
- [Test Suite for the Spec Sub-Package](./test-suite-for-the-spec-sub-package.md) — The `tests/spec/__init__.py` file marks the `tests/spec/` directory as a Python package, enabling pytest to discover and correctly import the spec-level test modules it contains. This is a structural init with no executable code.
- [Test Suite for v0.3.4 Memory Model: MemoryCategory, Salience, and Temporal Edge Metadata](./test-suite-for-v034-memory-model-memorycategory-salience-and-temporal-edge-metad.md) — This test suite covers spec-level additions introduced in Soul Protocol v0.3.4: the `MemoryCategory` enum for classifying memory entries, `salience` as an additive activation boost on `MemoryEntry`, and `metadata` on `TemporalEdge` along with its surfacing through `KnowledgeGraph` query methods. The salience tests were rewritten from a multiplicative to an additive boost model to prevent low-importance memories from being penalized disproportionately.
- [Test Suite for Vector Similarity Functions](./test-suite-for-vector-similarity-functions.md) — Covers the three core similarity primitives — cosine similarity, Euclidean distance, and dot product — used by soul-protocol's embedding layer. Tests enforce correct mathematical behavior, zero-vector safety, empty-vector edge cases, and dimension-mismatch guards.
- [Test Suite for VectorSearchStrategy](./test-suite-for-vectorsearchstrategy.md) — Tests the VectorSearchStrategy class, which wraps an embedder to perform similarity-based memory search with configurable threshold filtering and pre-built index support. Covers both the deterministic HashEmbedder path and the semantic TFIDFEmbedder path.
- [Test Suite for Zip Recall Bug: Soul File Export/Awaken Roundtrip and Stale MCP Cache](./test-suite-for-zip-recall-bug-soul-file-exportawaken-roundtrip-and-stale-mcp-cac.md) — This regression test suite targets a production bug where `soul_recall` via the MCP server returned zero results for memories that existed in the `.soul` file on disk. The root cause was the MCP server loading souls at startup and never reloading when the file was updated externally. Tests cover semantic and episodic recall after zip roundtrip, bytes-based awakening, multi-memory preservation, content fidelity, and the stale-cache simulation.
- [Test Suite: Four Core Quality Validation Scenarios for Soul Protocol](./test-suite-four-core-quality-validation-scenarios-for-soul-protocol.md) — This test suite implements the four hardest quality probes for Soul Protocol — response quality uplift, personality consistency across OCEAN profiles, hard recall after 30 filler interactions, and emotional continuity via somatic markers — using paired LLM comparisons with a `ResponseJudge` to score soul-enriched versus baseline responses.
- [Test Suite: Graph-Augmented Recall Pipeline](./test-suite-graph-augmented-recall-pipeline.md) — Validates the integration of KnowledgeGraph with RecallEngine, covering graph traversal via progressive_context at multiple depth levels, graph-wired recall with entity matching, deduplication, and graceful degradation when no graph is provided.
- [Test Suite: Heuristic Sentiment Detection (Somatic Markers)](./test-suite-heuristic-sentiment-detection-somatic-markers.md) — Tests for the heuristic sentiment detector that produces Damasio-style somatic markers (valence, arousal, label) from raw text, covering neutral text, positive/negative words, intensifiers, negation, mixed emotions, and label accuracy.
- [Test Suite: Identity Spec — Construction, Auto-ID, Traits, and Serialization](./test-suite-identity-spec-construction-auto-id-traits-and-serialization.md) — Validates the core `Identity` primitive from `soul_protocol.spec`, covering minimal construction, deterministic auto-ID generation, uniqueness guarantees, arbitrary traits storage, and Pydantic `model_dump`/`model_validate` round-trip fidelity. Identity is the foundational spec model that all other containers reference.
- [Test Suite: JSON Schema Generation from Pydantic Models](./test-suite-json-schema-generation-from-pydantic-models.md) — Verifies that every Soul Protocol Pydantic model can produce a valid, JSON-serializable schema, that a sample `SoulConfig` dict validates against the generated schema, and that the `generate_schemas.py` script produces the expected on-disk files. This acts as a contract test for external consumers who depend on the published JSON schemas.
- [Test Suite: Known Sentiment Detector Gaps](./test-suite-known-sentiment-detector-gaps.md) — A bug-documenting test suite that captures known failure modes in the heuristic sentiment detector — missing vocabulary, joy/excitement confusion, sadness/frustration confusion, gratitude misclassification, and neutral false positives — intended to go green once the corresponding fixes land in sentiment.py.
- [Test Suite: LLM-Powered Memory Reranking and Smart Recall](./test-suite-llm-powered-memory-reranking-and-smart-recall.md) — Covers `rerank_memories()`, the `_parse_indices()` helper, and `Soul.smart_recall()` — the optional LLM-driven reranking layer on top of heuristic memory retrieval. The suite validates correct ordering, fallback behavior on engine failure or timeout, opt-in flag semantics, and prompt injection defenses.
- [Test Suite: Memory Compression -- Summarization, Deduplication, and Importance Pruning](./test-suite-memory-compression-summarization-deduplication-and-importance-pruning.md) — Tests for `MemoryCompressor`, which reduces the size of a soul's long-term memory store through four strategies: text summarization, near-duplicate removal, importance-based pruning, and export splitting. Covers edge cases like empty inputs, threshold tuning, and the rule that high-importance memories are never pruned regardless of age.
- [Test Suite: Memory Search Tokenizer and Synonym Expansion](./test-suite-memory-search-tokenizer-and-synonym-expansion.md) — Tests for two search quality improvements (issues #10 and #11): a tokenizer that splits on code-style separators like slashes and underscores, and a synonym expansion layer that maps programming terms to related aliases so queries for 'database' match memories containing 'postgresql'.
- [Test Suite: Memory Visibility Tiers](./test-suite-memory-visibility-tiers.md) — Tests for the three-tier memory visibility system (PUBLIC, BONDED, PRIVATE) that controls which memories are accessible to external requesters based on bond strength, preventing private memories from leaking while maintaining full system access for the soul itself.
- [Test Suite: MemoryManager Facade](./test-suite-memorymanager-facade.md) — Core test suite for the MemoryManager facade, covering all five memory stores (core, episodic, semantic, procedural, knowledge graph), cross-store recall, significance-gated observation, and the Bug #15 edit_core behavior fix.
- [Test Suite: Multi-Participant Interactions and Multi-Bond Identity](./test-suite-multi-participant-interactions-and-multi-bond-identity.md) — Tests for the multi-participant Interaction model (issue #95) and multi-bond Identity (issue #94), including full backward compatibility with the legacy user_input/agent_output API, serialization round-trips, and coverage of the spec-layer equivalents.
- [Test Suite: OCEAN Personality-Modulated Memory Retrieval](./test-suite-ocean-personality-modulated-memory-retrieval.md) — Comprehensive tests for the personality modulation layer that adjusts memory recall scores based on OCEAN (Big Five) personality traits, covering per-trait signal functions, the composite boost calculation, and end-to-end recall ranking changes.
- [Test Suite: Org Journal Spec — Actor, DataRef, and EventEntry Primitives](./test-suite-org-journal-spec-actor-dataref-and-evententry-primitives.md) — Validates the foundational primitives of Soul Protocol's organizational journal spec: `Actor` (the entity writing an event), `DataRef` (an external data reference with cache policy), and `EventEntry` (an immutable journal record). The suite enforces UTC-only datetimes, non-anonymous writes, required scopes, and payload union handling.
- [Test Suite Package Initializer for CLI Tests](./test-suite-package-initializer-for-cli-tests.md) — The `tests/test_cli/__init__.py` file marks the `tests/test_cli/` directory as a Python package, enabling pytest to discover and correctly namespace the CLI test modules it contains. It contains no executable code.
- [Test Suite: Phase 1 Ablation Fixes](./test-suite-phase-1-ablation-fixes.md) — Tests for five Phase 1 ablation fixes: the significance gate short-message penalty, mundane-interaction rejection, select_top_k attention selection, BM25Index scoring and the BM25SearchStrategy protocol, and logarithmic bond growth with diminishing returns.
- [Test Suite: Phase 2 Memory Architecture (Categories, Salience, Dedup)](./test-suite-phase-2-memory-architecture-categories-salience-dedup.md) — Tests for the Phase 2 memory architecture improvements: the MemoryCategory enum, generated abstracts, salience scoring, the deduplication reconcile_fact pipeline, TemporalEdge metadata in the knowledge graph, and the EpisodicStore entry-update API.
- [Test Suite: Pluggable Search Strategy Protocol](./test-suite-pluggable-search-strategy-protocol.md) — Tests for the SearchStrategy protocol and its propagation through the full memory stack — from Soul.birth() down through MemoryManager, RecallEngine, spreading_activation(), and compute_activation() — verifying that custom scoring functions can replace the default token-overlap approach at every layer.
- [Test Suite: Progressive Disclosure in Recall](./test-suite-progressive-disclosure-in-recall.md) — Tests for RecallEngine's progressive recall mode, which returns a primary set of full-content memories plus an overflow set of abstract-only summaries, allowing callers to receive more context without exceeding token budgets.
- [Test Suite: Retrieval Trace — Observability Receipt for Memory Recall](./test-suite-retrieval-trace-observability-receipt-for-memory-recall.md) — Locks the shape and runtime behavior of `RetrievalTrace`, the observability receipt emitted by `Soul.recall()` and `Soul.smart_recall()` after every memory lookup. The suite verifies that traces are populated even on empty results, are not serialized into `.soul` files, and correctly record actor, query, candidates, and rerank metadata.
- [Test Suite: Scope Tags and Hierarchical Memory Access Control](./test-suite-scope-tags-and-hierarchical-memory-access-control.md) — Tests the `match_scope` and `match_scope_strict` helpers that implement hierarchical RBAC/ABAC filtering for memory recall. The suite covers bidirectional glob containment, strict one-way matching, scope normalization, export/awaken round-trips for scoped memories, and `Soul.recall` scope-filter integration.
- [Test Suite: Self-Model Manager (Soul's Self-Concept)](./test-suite-self-model-manager-souls-self-concept.md) — Tests for SelfModelManager, which implements Klein's self-concept model — the soul's ongoing self-knowledge about what domains it is competent in, which roles it plays, and how confident it is in those self-images based on accumulated evidence.
- [Test Suite: Skills XP Progression, Level Cap, and Decay](./test-suite-skills-xp-progression-level-cap-and-decay.md) — Validates the Skills subsystem — a gamification layer that tracks a soul's proficiency across named abilities via an XP and level model. The suite covers default construction, XP accumulation with level-up triggers, overflow carry, the level 10 cap, significance-weighted XP grants, skill decay, and the `SkillRegistry` collection.
- [Test Suite: Smoke Tests for the Research Simulation Framework](./test-suite-smoke-tests-for-the-research-simulation-framework.md) — This test suite validates that all research framework components — agent generation, user generation, scenario generation, experiment configuration, metrics, statistical utilities, and memory conditions — work correctly at minimal scale without requiring any LLM API calls. It is fast enough for CI and guards against regressions in the framework before running expensive full ablations.
- [Test Suite: Soul Configuration — OCEAN, Communication, Biorhythms, Persona, and Config Files](./test-suite-soul-configuration-ocean-communication-biorhythms-persona-and-config-.md) — Validates the flexible birth configuration system for `Soul`, covering direct-kwarg birth with OCEAN personality traits, communication style, biorhythms, and persona text, as well as file-based birth from YAML and JSON configs via `Soul.birth_from_config()`. The suite also covers CLI `--config` and `--openness/--neuroticism` flags and verifies that all settings survive export → awaken round-trips.
- [Test Suite: Soul Reincarnation Lifecycle](./test-suite-soul-reincarnation-lifecycle.md) — Validates that `Soul.reincarnate()` correctly transitions a soul to a new identity while preserving accumulated state. The suite ensures that reincarnation is not a hard reset — personality, memories, values, archetype, and bond all carry forward, while the DID and incarnation counter change to mark the new life.
- [Test Suite: Soul Templates and Batch Spawning via SoulFactory](./test-suite-soul-templates-and-batch-spawning-via-soulfactory.md) — Validates `SoulTemplate` (reusable soul blueprints with personality profiles, memories, and skills) and `SoulFactory` (the factory that instantiates souls from templates, including batch spawning with controlled personality variance). The suite covers model validation, override semantics, variance clamping, reproducible seeding, and public API exports.
- [Test Suite: SoulContainer — Spec-Layer Soul Creation, Persistence, and Memory Access](./test-suite-soulcontainer-spec-layer-soul-creation-persistence-and-memory-access.md) — Validates the spec-layer `SoulContainer` — the lightweight soul abstraction for integrations that do not need the full runtime. The suite covers container construction, traits storage, `.soul` file save/open round-trips, multi-layer memory preservation, error handling for missing files, and the default `DictMemoryStore`.
- [Test Suite: test_spec Package Namespace Marker](./test-suite-testspec-package-namespace-marker.md) — An empty `__init__.py` that declares `tests/test_spec/` as a Python package. This file contains no executable code but is structurally required so that pytest and Python's import machinery can discover and run the test modules inside `test_spec/` as part of the broader test suite.
- [Tests Package Initializer](./tests-package-initializer.md) — The `tests/__init__.py` file marks the top-level `tests/` directory as a Python package, enabling pytest and import machinery to resolve test modules via standard dotted paths. It contains no executable code — its presence is purely structural.

### `text-retrieval` — 1 article(s)

- [BM25 and Token-Overlap Memory Search Engine](./bm25-and-token-overlap-memory-search-engine.md) — Implements the core text-matching primitives used by all three memory stores: a tokenizer that handles natural language and code identifiers, a BM25 index with IDF weighting, and a lightweight token-overlap relevance scorer kept as a fast fallback. Synonym expansion improves recall for common programming and technical terms.

### `tokenization` — 1 article(s)

- [Test Suite: Memory Search Tokenizer and Synonym Expansion](./test-suite-memory-search-tokenizer-and-synonym-expansion.md) — Tests for two search quality improvements (issues #10 and #11): a tokenizer that splits on code-style separators like slashes and underscores, and a synonym expansion layer that maps programming terms to related aliases so queries for 'database' match memories containing 'postgresql'.

### `tooling` — 1 article(s)

- [Schema Generator: JSON Schema Export for All Soul Protocol Pydantic Models](./schema-generator-json-schema-export-for-all-soul-protocol-pydantic-models.md) — A utility script that introspects every Pydantic model in `soul_protocol.runtime.types` and writes individual JSON Schema files plus a combined bundle to the `schemas/` directory, enabling cross-language clients (TypeScript, Rust, Go) to validate soul data structures without depending on the Python package.

### `tracing` — 1 article(s)

- [Retrieval Trace — Observability Receipt for Every Recall and Search Operation](./retrieval-trace-observability-receipt-for-every-recall-and-search-operation.md) — `RetrievalTrace` is a lightweight receipt written once per retrieval call, capturing the ranked candidate list, which candidates were actually used, latency, and a downstream reference. It feeds the paw-runtime JSONL sink, graduation policy, the "Why?" explainability drawer, and SoulBench fixture generation — all from a single portable shape.

### `traversal` — 1 article(s)

- [Test Suite for Knowledge Graph Traversal](./test-suite-for-knowledge-graph-traversal.md) — Tests for the `KnowledgeGraph` traversal API, covering BFS traversal, shortest-path finding, neighbourhood extraction, subgraph extraction, and progressive context rendering at three detail levels. Validates edge expiry, cycle handling, and node deduplication across a range of graph topologies.

### `ui` — 1 article(s)

- [Evaluation UI Package Marker](./evaluation-ui-package-marker.md) — Package initializer for the `research/eval_ui/` module, created in early March 2026 as a namespace boundary for a planned web-based evaluation results viewer. Contains no executable logic.

### `validation` — 2 article(s)

- [Research Package: Soul Protocol Validation Simulation Framework](./research-package-soul-protocol-validation-simulation-framework.md) — The `research/` package is a large-scale simulation framework designed to validate Soul Protocol's psychology-informed memory claims at publication grade, running 1,000 simulated agents across 5 memory conditions and 4 use cases for more than 20,000 experimental runs. It is structured for reproducibility and statistical rigor.
- [Tier 2 Validation Runner: 100-Agent Real LLM Benchmark](./tier-2-validation-runner-100-agent-real-llm-benchmark.md) — The Tier 2 runner validates Soul Protocol with real Anthropic API calls across 100 simulated agents, comparing heuristic-only cognitive processing against LLM-backed processing using `HaikuCognitiveEngine`. It collects a comprehensive set of metrics — memory, personality, recall, bond, emotional, and skill dimensions — and saves the results as JSON.

### `vector search` — 3 article(s)

- [EmbeddingProvider Protocol — Pluggable Embedding Backend Interface](./embeddingprovider-protocol-pluggable-embedding-backend-interface.md) — `EmbeddingProvider` is a runtime-checkable `Protocol` class that defines the three-method contract any embedding backend must satisfy. It decouples Soul Protocol's retrieval and search features from any specific ML framework by requiring only `dimensions`, `embed`, and `embed_batch`.
- [Embeddings Spec Package — Public API Surface](./embeddings-spec-package-public-api-surface.md) — The `spec/embeddings` package defines the protocol-level public interface for all embedding operations in Soul Protocol. It re-exports the `EmbeddingProvider` protocol and three vector math primitives so consumers import from a single stable namespace.
- [Vector Similarity Functions — Pure-Python Cosine, Euclidean, and Dot Product](./vector-similarity-functions-pure-python-cosine-euclidean-and-dot-product.md) — This module provides three zero-dependency vector similarity functions (`cosine_similarity`, `euclidean_distance`, `dot_product`) using only the Python standard library. All three include mismatch guards added in 2026-03-06 to catch incompatible vectors from different embedding backends before producing silent garbage results.

### `vector-search` — 1 article(s)

- [End-to-End Vector Search Test Suite — TF-IDF Semantic Memory Retrieval](./end-to-end-vector-search-test-suite-tf-idf-semantic-memory-retrieval.md) — End-to-end tests for soul-protocol's semantic memory search pipeline using `TFIDFEmbedder` and `VectorSearchStrategy`. Tests create memory entries on distinct topics, fit the embedder, and verify that topical queries return relevant results in the correct order.

### `visualization` — 1 article(s)

- [Whitepaper Chart Generator for Soul Protocol Research](./whitepaper-chart-generator-for-soul-protocol-research.md) — Generates five publication-quality PNG charts that visualize Soul Protocol's research validation results, covering multi-judge quality tests, component ablation, competitive comparisons (vs. Mem0), judge agreement heatmaps, and a waterfall plot of cumulative score improvements. Each chart is designed to standalone as a figure in the Soul Protocol whitepaper with accessible color coding and clean academic styling.

### `web application` — 1 article(s)

- [Human Evaluation Study Web Application](./human-evaluation-study-web-application.md) — A FastAPI web application that runs a blinded A/B human evaluation study, presenting participants with two AI agents — one soul-enabled, one a plain baseline — and collecting Likert survey responses after each. The app manages sessions, randomizes agent order to eliminate position bias, and exports results to JSON and CSV for statistical analysis.

### `whitepaper` — 1 article(s)

- [Whitepaper Chart Generator for Soul Protocol Research](./whitepaper-chart-generator-for-soul-protocol-research.md) — Generates five publication-quality PNG charts that visualize Soul Protocol's research validation results, covering multi-judge quality tests, component ablation, competitive comparisons (vs. Mem0), judge agreement heatmaps, and a waterfall plot of cumulative score improvements. Each chart is designed to standalone as a figure in the Soul Protocol whitepaper with accessible color coding and clean academic styling.

## Top Concepts (top 50 of 2198)

| Concept | Referenced by |
|---|---:|
| CognitiveEngine | 18 |
| SoulConfig | 18 |
| Soul.birth | 13 |
| package init | 13 |
| DID | 12 |
| MemoryEntry | 12 |
| Soul.awaken | 12 |
| eternal storage | 11 |
| pytest discovery | 11 |
| EternalStorageProvider | 10 |
| HeuristicEngine | 10 |
| Identity | 10 |
| __init__.py | 10 |
| bond strength | 10 |
| semantic memory | 10 |
| soul archive | 10 |
| spec layer | 10 |
| ContextNode | 9 |
| MemoryManager | 9 |
| OCEAN personality | 8 |
| OCEAN traits | 8 |
| Python package | 8 |
| backward compatibility | 8 |
| deduplication | 8 |
| memory recall | 8 |
| memory tiers | 8 |
| planted facts | 8 |
| procedural memory | 8 |
| somatic marker | 8 |
| ArchiveResult | 7 |
| BM25 | 7 |
| DSPy | 7 |
| EventEntry | 7 |
| RecoverySource | 7 |
| batch embedding | 7 |
| knowledge graph | 7 |
| pack_soul | 7 |
| protocol compliance | 7 |
| public API | 7 |
| recall precision | 7 |
| significance gate | 7 |
| Arweave | 6 |
| CLI entry point | 6 |
| ContextEngine | 6 |
| DataRef | 6 |
| DictMemoryStore | 6 |
| EmbeddingProvider | 6 |
| HashEmbedder | 6 |
| IPFS | 6 |
| LCM | 6 |

